# 前情提要：程式撰寫層面優化（程式碼層級設計原則建立）

## 這是什麼

上一輪對話建立了「程式撰寫設計原則」，準備對五個微服務進行「單檔案結構、函數粒度、重複代碼抽象」等程式碼層級的優化。本聊天室應接續並針對 **auth-service** 進行完整的稽核與優化。

---

## 背景脈絡

**前置完成**：
- 架構層優化（五個微服務 Phase 0-10）已完成，但未進行 Phase 11（commit/archive）
- 發現痛點：某些超過千行的腳本（如 `chat-service` 的 `conversationService.js` 1081 行）可讀性差，AI 改代碼時容易出錯

**本輪目標**：
建立「程式撰寫設計原則」（跨語言、跨層的通用準則），用來：
1. **稽核**現有代碼的程式碼品質
2. **優化**違反項（拆檔、重構函數、抽象重複）
3. **指導**後續新代碼的寫法

**決策形成過程**：
通過 `/grilling` 對話逐層深掘：
- 為什麼 AI 改代碼容易出錯？（可讀性差）
- 什麼情況下該拆檔？（按職責數量，不按行數）
- 怎樣判斷「該忍受重複」vs「該抽象」？（三次法則 + 成本評估）
- 目標優先度是什麼？（手冊 → 稽核 → 即時指引）

---

## 已建立的物件

### `程式撰寫設計原則.md`（根目錄）

**六大維度的完整原則文件**：

#### A. 單檔案結構（A1-A3）
- **A1. 適當的模組邊界**：職責數量決定拆檔（4+ 獨立職責 → 考慮拆）
  - 實務判斷：「一個檔案能否用一句話描述」→ 能 = 邊界清晰
  - 舉例：`conversationService.js` 1081 行、6 種職責（對話 CRUD、訊息 CRUD、摘要、建立狀態機、AI 生成狀態機、擁有權檢查）→ 應該拆

- **A2. 內部結構清晰性**：即使多職責也要用區段註解分割
  - 讀者應該 30 秒內理解「這個檔案有哪些職責」

- **A3. 獨立可修改性**：修改職責 A 不應被迫改職責 B
  - 測試：「改 deleteMessage() 是否必須同時改 sendMessage()」→ 否 = 邊界清晰

#### B. 函數粒度（B1-B3）
- **B1. 職責單一性**：一個函數只有一個「改變的理由」
- **B2. 函數長度**：< 30 行最佳，> 100 行肯定要拆
- **B3. 簽名清晰性**：參數、回傳值要明確（用 JSDoc/TypeScript）

#### C. 資料流與狀態（C1-C2）
- **C1. 副作用隔離**：純邏輯 vs 副作用函數分開
- **C2. 狀態流透明**：函數間資料傳遞明確可追蹤，不靠全域狀態

#### D. 錯誤處理（D1-D2）
- **D1. 錯誤集中化**：用 `ERROR_MAP` 一個地方定義、不散落在各 controller
- **D2. 邊界清晰**：JSDoc 清楚說明「何時拋異常」、「何時回 null」等

#### E. 重複代碼與抽象（E1-E2）
- **E1. 重複判斷**：不是「有重複就抽」，而是「三次法則 + 成本評估」
  - 重複 1-2 次 → 忍受
  - 重複 3 次 → 考慮抽象
  - 重複 4+ 次 → 肯定要抽
  - 但抽象成本 > 收益 → 可以接受重複（例如抽象後代碼更複雜）

- **E2. 抽象形式選擇**：從簡單到複雜（常數表 → 共用函數 → 策略物件 → 依賴注入）

#### F. 可讀性（F1-F2）
- **F1. 命名**：動詞 + 名詞，不用 handle/process/run
- **F2. 註解**：只寫「為什麼」不寫「做什麼」，public 函數必須有 JSDoc

---

## 核心立場（基於 grilling 結果）

### 關於拆檔
- **按職責數量，不按行數**
  - 800 行 + 2 個相關職責 = 不拆
  - 500 行 + 6 個獨立職責 = 該拆
- **不拆到「單一職責」那麼細碎**
  - 例如 Message CRUD + 訊息驗證 = 可同檔（高度相關）
  - 例如 CRUD + 摘要生成 + 狀態機 = 應考慮分檔（邏輯層次差異大）

### 關於重複與抽象
- **優先抽象，但不強制單一實作**
  - 三個地方用同樣邏輯 → 抽成共用函數
  - 第四個地方需要特化 → 自己寫，共用函數不動（不是改共用函數來適應）
  - 例子：`ERROR_MAP` 解決了前三個 controller 的重複 if/else，但如果第四個 controller 需要特殊錯誤處理，就自己寫，不硬改 ERROR_MAP

### 關於修改邊界
- **修改邏輯內容**：不動其他方法、不動依賴樹 ✓
- **修改函數簽名**：動到依賴樹是無可厚非的代價 ✓

### 關於原則使用優先度
1. **手冊**（已完成 ✓）：《程式撰寫設計原則.md》定義「好代碼長什麼樣」
2. **稽核清單**（接下來）：用手冊逐條掃描 auth-service，記錄違反項
3. **即時指引**（後續）：寫新代碼時參考原則、Code review 時引用

---

## auth-service 的優化流程（本聊天室任務）

### 預期的優化步驟
（參考《後端專案優化標準程序》Phase 0-11 的架構，改為程式碼層級）

**Phase 0-1：現況掃描**
- 用《程式撰寫設計原則.md》逐條掃描 auth-service 的所有程式碼
- 記錄所有違反項（檔案、行號、違反的原則、嚴重度）
- 製作成 `auth-service/mistake.md` 的「程式碼層面」版本

**Phase 2-10：優化與驗證**
- 按嚴重度優化（可能包括拆檔、重構函數、抽象重複、統一風格）
- 手動測試 + 確保行為不變

**Phase 11：收尾**
- git commit
- openspec change archive
- 清理備份

### 是否採 OpenSpec change 驅動？
前一輪架構優化用了 OpenSpec（change + proposal + design + specs + tasks），這輪程式碼優化建議：
- **A 方案**：同樣採 OpenSpec，保持一致流程
- **B 方案**：簡化為「記錄違反項 → 修復 → 驗證 → commit」，因為程式碼優化改動較小

**建議**：採 A 方案（OpenSpec），保持與前輪一致，方便後續對比與追蹤。

---

## 即將進行的稽核（auth-service）

### auth-service 現況摘要
- **路徑**：`auth-service/`
- **技術棧**：Node.js + Express
- **核心職責**：註冊/登入，簽發 JWT
- **檔案結構**（基本）：`src/app.js`、`src/controllers/`、`src/services/`、`src/repositories/`
- **已知情況**：架構優化時已做過一輪（移除死代碼、統一錯誤碼等），但未進行程式碼層級的細緻檢查

### 預期找到的違反點
（根據其他服務的經驗）
- [ ] 檔案超過某個長度閾值，職責不清晰？
- [ ] 函數簽名不明確（參數過多、無 JSDoc）？
- [ ] 重複的驗證邏輯散落在多個地方？
- [ ] 副作用混雜在業務邏輯中？
- [ ] 變數命名含糊（`data`, `result`, `tmp`）？

---

## 如何使用本前情提要

**給下一個聊天室的指示**：
1. 讀這份前情提要，理解背景與決策
2. 讀 `程式撰寫設計原則.md`（根目錄），熟悉六大維度
3. 使用《程式撰寫設計原則.md》的**稽核清單**逐一檢查 auth-service
4. 記錄所有違反項到 `auth-service/mistake.md`（程式碼層面版本）
5. 與使用者討論優先度與修復方案
6. 進行優化 + 驗證 + commit/archive

---

## 記住

- **前五個微服務的架構改動未 Phase 11**：如果需要參考前面的改動或決策，備份在根目錄 `.claude/` 各服務的 scratchpad
- **不要過度設計**：程式碼優化的目標是「可讀、易改、易測」，不是「最完美的設計」
- **尊重現有決策**：架構優化的決策（例如「JS 環境下 SOLID-DIP 不強制」）在程式碼優化中也適用
