# 前情提要：服務間通信架構重新設計

> 這份文件是要帶到另一個聊天室接續討論用的。記錄「為什麼要做這件事」「範圍是什麼」「已經確認的方向」「還沒決定的部分」，讓新的聊天室能直接銜接，不用重新從頭問一遍。

---

## 背景：這件事是怎麼冒出來的

在對 `ai-service` 做完《後端專案優化標準程序》第一輪優化（Phase 0–10，詳見《後端專案優化進度.md》）之後，處理了一個獨立的追加修正：`ai-service` 的 `CharacterInfo.gender` 被誤宣告成必填欄位，但 `character-service` 允許角色不填性別，導致沒填性別的角色無法聊天。修好後，執行《測試清單-進階整合測試.md》做端到端驗證（23 項案例全過），在最後**清除測試資料**的步驟中，嘗試透過 gateway 呼叫 `DELETE /api/users/:id` 刪除測試帳號，結果得到 `404 Cannot DELETE /usr_xxx`。

追查後發現一個**真實存在的既有 bug**（非本次操作失誤）：

### bug 的技術細節

`api-gateway/src/proxies/userProxy.js` 沒有設定 `pathRewrite`，但 `app.js` 是用 `app.use('/api/users', authMiddleware, userProxy)` 這種**前綴掛載**方式註冊路由。Express 的 `app.use()` 前綴掛載會把匹配到的前綴從 `req.url` 中 strip 掉，所以「打進 `userProxy` 時，`req.url` 已經不是 `/api/users/usr_123`，而是只剩 `/usr_123`」。

`userProxy.js` 的註解寫著「這裡沒有 pathRewrite，因為 Gateway 的 /users 和 user-service 的 /users 路徑剛好一樣」——這個假設是錯的，它沒有考慮到前綴會被 strip 掉。

對照組：`characterProxy.js`、`chatProxy.js` 都有寫 `pathRewrite: (path) => \`/api/v1/characters${path}\`` 這種「把前綴補回去」的邏輯，所以這兩條路由是正常的。只有 `userProxy` 漏寫，是四個服務級 proxy（auth／user／character／chat）裡**唯一沒補前綴**的一個。

`authProxy.js` 的情況不同：它用 `app.post('/api/auth/register', ...)` 精確路徑掛載（不是 `app.use()` 前綴掛載），Express 不會 strip 掉路徑，所以 `req.url` 進來時仍是完整的 `/api/auth/register`，靠 regex `pathRewrite: { '^/api/auth': '/api/v1/auth' }` 轉換。

### 為什麼一直沒被發現

`GET/DELETE /api/users/:id` 這條 gateway 路由在正常運作流程中**從未被實際呼叫過**：
- `auth-service` 建立使用者時是**直接**打 `user-service:4000`（server-to-server，不經 gateway）
- 沒有任何前端流程會透過 gateway 打這條路由
- 內部路由 `/internal/users`（同一個 `userProProxy`）理論上也有同樣問題，但目前沒有任何服務會經這條路徑查詢 user 資料

這次是第一次真的透過 gateway 呼叫這條路由（測試清理階段），才第一次踩到。

### 這個 bug 目前的狀態

**尚未修復**，已用「直接打 `user-service:4000`」繞過完成了測試資料清理，沒有動任何程式碼。是否要修、怎麼修，使用者決定要放到更大範圍的架構重新設計裡一起處理，而不是單獨修這一行。

---

## 為什麼在考慮做更大範圍的重新設計

使用者的原話是：「感覺這類 bug 可能不少」——直覺是這種「同一個機制在多處各自實作、容易漏掉某一處」的模式，可能不只 `userProxy` 這一個案例。與其逐一抓漏洞，使用者在考慮藉這個機會把 `api-gateway` 這個**所有前端唯一入口、也是全部服務間通信的中樞**，其認證機制、路由規則整個重新審視一次。

助手（Claude）當時提醒：這跟過去對單一微服務做的《後端專案優化標準程序》性質不同——那套 SOP 是「稽核單一服務、找違反設計原則之處、清理」，範圍限定在一個服務內部；這次若要做，會是**跨服務的架構重新設計**，會動到 `api-gateway` 這個服務本身的核心設計，且勢必牽動全部下游服務（auth/user/character/chat/ai-service）怎麼被呼叫。

**以下所有「範圍」「方向」都還沒定案，需要在新聊天室重新討論。** 上一輪對話中助手提出過一些選項、使用者也做過一些初步回答，但使用者要求整個重來，所以這裡不列出上次的答案，只列出「有哪些問題需要討論」。

---

## 需要在新聊天室討論的問題

### 1. 總範圍要多大
服務間通信涉及認證機制、路由規則、同步/非同步溝通方式——這次要處理其中哪些、不處理哪些，需要重新界定。

### 2. 認證機制現況（背景資訊，非決定）
目前的分工是：
- **外部**：前端 → gateway，gateway 集中驗 JWT，驗證通過後改寫成 `x-user-id` / `x-user-email` header 轉發給下游服務
- **內部**：服務對服務呼叫（例如 ai-service 呼叫 chat-service）一律經同一個 gateway 的 `/internal/*` 路由轉發，用 `internalAuthMiddleware`（帶 `x-internal-request: true` 之類的 header）放行，**不做任何身份驗證**——這個 header 目前只是一個名稱標記，沒有任何密鑰或簽章驗證，理論上任何請求都能加這個 header 偽裝成內部呼叫

這套分工要不要動、要怎麼動，都還沒討論。

### 3. 路由規則現況（背景資訊，非決定）
目前每個下游服務各自一支 `xxxProxy.js`（authProxy／userProxy／characterProxy／chatProxy／aiProxy），各自決定要不要寫 `pathRewrite`。`userProxy` 這次就是因為漏寫才出現 bug，其他幾支目前看起來是對的，但沒有做過系統性複查。

要不要統一成某種共用機制、或維持現況只修漏洞，都還沒討論。

### 4. 同步/非同步溝通模式現況（背景資訊，非決定）
目前現狀：
- 建立對話（`getOrCreateConversation`）、發送訊息（`sendMessageToConversation`）走「非同步 + 輪詢」模式，狀態存在各服務的**進程內記憶體 Map/dict**（chat-service 的 `creationJobs`/`aiGenerationStatus`、ai-service 的 `initialization_jobs`）——這是先前兩輪服務優化（chat-service、ai-service）都各自發現、但都選擇「本輪不處理，記錄為已知限制」的 12-Factor VI（無狀態）違反，牽涉是否要換成持久化儲存（DB 欄位或 Redis）的架構決策
- 其他操作（登入、建角色等）走傳統同步請求-回應

要不要納入這次的討論範圍，還沒決定。

### 5. 其他待討論項目
- 命名／流程定位（是否比照《後端專案優化標準程序》、或需要一套新流程）
- 執行方式的具體規劃（怎麼盤點現況、要不要先系統性複查其他 proxy 是否有類似 `userProxy` 的隱藏問題）
- 驗證方式（改動範圍若橫跨 gateway + 全部下游服務，測試該怎麼規劃）

---

## 已查證的現況（新聊天室已完成的第一輪盤點）

以下是實際讀過 `api-gateway` 五支 proxy、兩支 middleware、`app.js`、`config/services.js`，並對照 `user-service` 真實路由與全平台原始碼搜尋後確認的事實，取代前面「背景資訊、非決定」段落中未經查證的部分。**只查證了路徑層與內部認證這兩塊，character/chat/ai 下游服務的路由對照尚未完整覆核。**

### 1. 路徑處理其實有 4 種不同寫法，不是「一種模式漏一處」

逐一追蹤後，5 支 proxy 對「前綴掛載會被 Express strip 掉」這個問題，實際上用了 4 種不同手法：

| Gateway 路由 | 掛載方式 | pathRewrite | 下游實際期望 | 結果 |
|---|---|---|---|---|
| `POST /api/auth/register`／`/login` | `app.post()` 精確掛載 | regex `^/api/auth → /api/v1/auth` | auth-service `/api/v1/auth/*` | 正常 |
| `/api/users/*`、`/internal/users` | `app.use()` 前綴掛載 | **無** | user-service **`/users/*`**（裸路徑，非 `/api/v1/users`） | 壞：strip 後變 `/usr_x`，沒補回 → 404 |
| `/api/characters/*`、`/internal/characters` | `app.use()` 前綴掛載 | 函式補 `/api/v1/characters` | character-service `/api/v1/characters` | 正常 |
| `/api/conversations/*`、`/internal/conversations` | `app.use()` 前綴掛載 | 函式補 `/api/v1/conversations` | chat-service `/api/v1/conversations` | 正常（未逐行覆核 chat-service 路由定義） |
| `/internal/rag`、`/chat`、`/health` | `app.use()` 前綴掛載 | 函式依 `req.originalUrl` 分支改寫 | ai-service `/api/v1/rag`、`/api/v1/chat`、`/health` | 正常（未逐行覆核 ai-service 路由定義） |

**修 `userProxy` 的正確做法**：user-service 的路由（`user-service/src/app.js`）是掛在裸的 `/users`、`/users/:id`，**不是** `/api/v1/users`。所以正確 pathRewrite 是補回 `/users`（例如 `(path) => '/users' + path`），照抄 `characterProxy` 補 `/api/v1/users` 一樣會 404——這是原本的 bug 分析沒講清楚、容易在動手修的時候踩錯的細節。

### 2. 內部呼叫的 `x-internal-request` header：有消費者、沒生產者

全平台原始碼搜尋（含 node_modules 排除後的完整結果）確認：`x-internal-request` 這個 header **只有 `user-service/src/middlewares/authorizeSelf.js` 在讀取**（第 13 行 `if (req.headers['x-internal-request'] === 'true') return next()`），**沒有任何地方會設定它**——包括 gateway 的 `internalAuthMiddleware`、以及原本猜測會設定它的 ai-service/chat-service 呼叫端，都沒有寫入這個 header。

**影響**：即使把上面的路徑 bug 修好，`/internal/users/:id` 這條路仍會在 user-service 的 `authorizeSelf` 這關被 403 擋下——因為請求到達時既沒有 `x-internal-request`、也沒有能對上 `:id` 的 `x-user-id`（gateway 的 `internalAuthMiddleware` 對 `/internal/*` 路由完全不會注入 `x-user-id`）。也就是說 `/internal/users` 這條路徑實際上有**兩層**問題疊加，不是修好路徑就通了。

### 3. `internalAuthMiddleware` 的真實機制和原本認知不同

原本「認證機制現況」一節描述內部呼叫是「用 `x-internal-request: true` 之類的 header 放行」。實際讀 `api-gateway/src/middlewares/internalAuthMiddleware.js` 後確認：它做的是 **IP 白名單檢查**（`127.0.0.1`／`::1`／`192.168.*`／`10.*` 開頭），完全不檢查任何 header。「用 header 放行」的描述不準確，應更正為「IP-based，允許來源於本機或內網 IP 的請求，不做任何身份或來源真實性驗證」。

### 4. `api-gateway/CLAUDE.md` 過時的範圍比原本認知的更大

原本只知道 CLAUDE.md 對 `userProxy` 的描述（「路徑不變，無 rewrite」）是過時的。實際核對後，CLAUDE.md 裡的整張路由表都已過時：文件列的路由沒有 `/api` 前綴（例如寫 `/auth/register`、`/users/*`），但 `app.js` 現在全部路由都掛在 `/api/*` 下；且路由表完全沒有列出 `/api/conversations`（chat）、`/internal/*` 系列、`aiProxy` 這些後來新增的路由。重新設計時這份文件需要整份重寫，不只是修一行。

### 尚未查證、下一步可做的事

- character-service、chat-service、ai-service 的路由定義本身，這次只讀了 gateway 端的 pathRewrite 邏輯、沒有逐行核對三個下游服務各自 `app.js`／路由檔的真實掛載路徑（user-service 因為要修 bug 已核對過）。表格中標記「正常」的三列是基於程式碼邏輯推斷自洽，還不是端到端核對過的事實。
- `/internal/characters`、`/internal/conversations`、`/internal/rag`、`/internal/chat`、`/internal/health` 這幾條內部路由，下游服務端是否也有類似 user-service 的 `x-internal-request` 依賴、或其他形式的內部呼叫驗證邏輯，尚未查證。

---

## 相關檔案位置（供新聊天室快速定位）

- `api-gateway/src/app.js` — 路由掛載總表
- `api-gateway/src/proxies/*.js` — 5 支 proxy（authProxy／userProxy／characterProxy／chatProxy／aiProxy）
- `api-gateway/src/middlewares/authMiddleware.js` — 外部 JWT 驗證
- `api-gateway/src/middlewares/internalAuthMiddleware.js` — 內部服務間呼叫放行邏輯
- `api-gateway/CLAUDE.md` — 現況文件（記錄路由表、CORS 設定等，但目前對 userProxy 的描述「路徑不變（無 rewrite）」是過時/錯誤的，需要在這次重新設計時一併修正）
- 《後端專案優化標準程序.md》／《後端專案優化進度.md》— 先前對 auth/user/character/chat/ai-service 五個服務做的第一輪優化，可作為「單一服務怎麼被系統性稽核」的參考，但不直接適用於這次的跨服務範圍
- 《測試清單-persona-nexus-auth.md》《測試清單-persona-nexus-character.md》《測試清單-persona-nexus-lobby.md》《測試清單-persona-nexus-chat.md》《測試清單-進階整合測試.md》— 既有的端到端測試清單，這次架構改動後很可能需要重新跑過確認沒有破壞既有行為
- 《部署除錯測試.md》第 12 節 — 記錄了同源部署改造時「gateway 路由沒統一補 `/api` 前綴」的類似半成品遷移問題，是這次 userProxy bug 的同類前例
