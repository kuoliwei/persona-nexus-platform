# 進階整合測試清單

> ## ⚠️ 重要：2026-07-25 依「微服務架構準則.md」「微服務架構實作spec.md」查證並處理的三處矛盾
>
> 回填驗證方式、並針對「其他項目是否也有預期結果寫錯」做完整覆核後，共發現三處「文件預期結果」跟「目前原始碼實際行為」不一致的地方，逐一依規範文件判定對錯，**依照「誰錯就改誰」的原則分別處理**：
>
> - **案例 13/15/16（第四節）**：**判定為原始碼錯，清單不用改，✅ 已於 2026-07-25（`待辦task.md` T21）修復並實測驗證**——chat-service 借用只授權給 ai-service 使用的內部路由（`/internal/characters/:id`）幫特定使用者做所有權判斷，違反準則原則 7、8 與 spec 內部路由表，是 T8 執行時遺漏影響範圍評估造成的安全漏洞（追溯過程見 `執行日誌.md` T8 條目、`mistakes.md` M16）。清單原本的 `403` 預期本來就是對的，**維持不動**；原始碼已依 T21 修復（區分「真正系統呼叫」與「內部呼叫但代表特定使用者」），啟動服務經 Gateway 用真實 token 複測，案例 13/15/16 現在皆正確回傳 `403`。詳見第四節案例 13/15/16 下方的完整判決與修復驗證記錄。
> - **案例 G2（第六節）**：**判定為清單寫錯，已更正**——`x-internal-request` 的信任依據是 Gateway 的 IP 白名單，不是「客戶端自己要帶這個 header」，這是 spec 明文設計的行為，原始碼本身沒有問題。清單原本的 `403` 預期已**更正為 `201`／`409`**（見下表），原始碼不需要修改。詳見第六節 G 系列下方的完整判決。
> - **案例 J1（第六節）**：**判定為兩邊都錯，清單已更正，✅ 原始碼已於 2026-07-25（`待辦task.md` T22）修復並實測驗證**——spec 第三部分「HTTP Status Code 統一定義」明文用「email 已被註冊」當作 `409` 的標準範例，user-service 對同名錯誤碼正確回 `409`，但 auth-service 的 register 端點原本回的是 `400`，是 T12 執行時只改對 body 格式、沒改對狀態碼造成的（追溯過程見 `執行日誌.md` T12 條目、`mistakes.md` M17）。清單原本的 `400` 預期已**更正為 `409`**（見下表）；原始碼已依 T22 修復，經 Gateway 用已存在 email 複測，現在正確回傳 `409`。詳見第六節 J 系列下方的完整判決與修復驗證記錄。
>
> 全份清單其餘項目（案例 1-12、14、17-23、G1／G3、H1-H6、I1-I2、J2-J6、K1-K5、L1-L3）已逐一覆核，未發現其他矛盾。另有一處**無法完全判斷、誠實承認**：H4 測的確切路由組合（`GET /internal/conversations/:id/messages`）spec 兩份路由表都沒有逐字列出，只能確認跟同服務其他內部路由的一致模式相符，無法斷定 spec 遺漏是刻意還是疏漏，見第六節 H 系列備註。
>
> 三處外觀都跟「回應格式／狀態碼是否正確」有關，但成因各不相同（安全漏洞、文件誤解機制、修復不完整），處理方式也不同（清單改/不改、原始碼修/不修各自獨立判斷）。**案例 13/15/16、J1 這兩處原始碼修復（T21、T22）現皆已完成並實測驗證**，執行本清單時所有項目的預期結果應與目前原始碼行為一致，不應再測出矛盾；若讀者手上的程式碼版本早於 2026-07-25 這兩次修復，才會出現本區塊描述的修復前矛盾。

> ⚠️ **執行前提：[測試清單-persona-nexus-auth.md](測試清單-persona-nexus-auth.md)、[測試清單-persona-nexus-character.md](測試清單-persona-nexus-character.md)、[測試清單-persona-nexus-lobby.md](測試清單-persona-nexus-lobby.md)、[測試清單-persona-nexus-chat.md](測試清單-persona-nexus-chat.md) 四份都要先測完、確認環境沒問題，才執行這份。** 這份測的是多帳號、多角色、跨帳號權限、跟摘要機制這種需要「規模」跟「時間累積」才測得出來的情境，前四份是單一情境的基礎功能驗證，這份是它們組合起來、在比較貼近真實使用規模下還會不會正確運作。
>
> 跟前四份一樣：全部用指令模擬前端會發的同樣請求（同 URL／method／payload），不開瀏覽器；允許建立測試帳號與角色，測完不用清理（除了案例本身就是在測「刪除」的部分）。

## 前置需求

- 環境（Docker/Ollama/ai-service）都要在跑，第 7 項會真的觸發多次 LLM 生成。
- **時間成本提醒**：第 7 項要靠實際聊天累積字數觸發摘要機制（門檻見下方依據），每次 AI 回覆都是真實 LLM 推論（實測約 20-40 秒/次），要觸發兩次摘要大概需要 8-15 輪對話，抓 10-20 分鐘的實測時間比較保險。想縮短時間可以把使用者訊息寫長一點，加速累積字數門檻，減少需要的來回次數。

## 依據（讀原始碼確認）

### 帳號與角色規模設計
沿用 auth/character 清單已經驗證過的端點（`POST /api/auth/register`、`POST /api/auth/login`、`POST /api/characters`），只是這次要建立**多組**：

- **3 組帳號**：X、Y、Z（各自獨立註冊，跟前四份清單用的帳號分開，避免資料互相干擾）
- **每組帳號建立 2 個角色**：1 個 `visibility:"public"`、1 個 `visibility:"private"`（必填欄位隨意填，只要通過 character-service 的必填驗證即可，這部分邏輯已經在 character 清單驗證過，這裡不重複測基礎 CRUD）
- 總共 6 個角色：3 public（X-pub／Y-pub／Z-pub）、3 private（X-priv／Y-priv／Z-priv）

### 權限過濾邏輯（character-service，已在其他清單驗證過基礎版本，這裡是多帳號規模下的延伸）
- `character-service/src/services/characterService.js:108-116`（`authorId` 查詢）：查自己的 `authorId` 會拿到全部（含 private），這裡在 3 個帳號上分別驗證。
- `characterService.js:102-107`（`visibility=public` 查詢）：回傳**全部使用者**的公開角色，這裡驗證 3 個帳號的 public 角色會**同時**出現在同一份清單裡。

### 跨帳號聊天權限（chat-service，這是前四份清單沒測過的路徑）
- `chat-service/src/services/conversationService.js:361-385`（`getOrCreateConversation`）呼叫 `serviceClient.getCharacter(characterId, userId)`。
- `chat-service/src/lib/serviceClient.js:18-53`：這個呼叫打 `GET {GATEWAY}/internal/characters/:id`，帶 `x-user-id: userId` header；下游 character-service 的 `getCharacter`（`character-service/src/services/characterService.js:90-97`）本身就有 `if (existing.visibility === 'private' && existing.authorId !== requesterId) throw FORBIDDEN`，這個 403 會被 `serviceClient.getCharacter` 攔截並重新拋出同樣的 `FORBIDDEN`（`serviceClient.js:45-48`），最終在 `conversationController.js` 映射成 `403 {message:"Access denied to this character"}`。
- 換句話說：**能不能開始聊天，權限判斷完全委託給 character-service 的角色可見性規則**，chat-service 自己沒有另外一套邏輯。public 角色任何人都能開聊，private 角色只有作者本人能開聊。

### 摘要機制觸發門檻（`chat-service/src/config/config.json`）
```json
{ "summary": { "threshold": 500, "maxWords": 70, "shortTermLimit": 2 } }
```
`conversationService.js:69-107`（`checkIfNeedsSummary`）：
- 每次成功生成 AI 回覆後都會檢查一次（在 `executeSummary` 呼叫點附近，訊息落地後觸發）。
- 計算方式：把**最新 2 條（`shortTermLimit`）以外**的所有未摘要訊息文字長度加總（`calculateHistoryLength`，純字元數，中文一字算一），達到 **500 字** 就觸發。
- 觸發後，被摘要的那批訊息全部標記 `summarized:true`、`summaryId:<同一個值>`（`summaryId` = ai-service/Qdrant 回傳的 point id，`conversationService.js:217-221`），摘要本身存進 Qdrant 的 `summaries` collection。
- 最新 2 條訊息永遠不會被摘要（短期記憶），所以每輪都會留一點「未摘要餘量」累積到下一次門檻。

### 刪除已摘要訊息時的清理邏輯（`conversationService.js:872-987`，這是這份清單最深入的部分）
`deleteMessageAndSubsequent` 是**回溯式刪除**：從目標訊息開始、含目標本身，**之後全部訊息一起刪除**（不是只刪一條）。刪除範圍決定後：
1. 收集刪除範圍內所有訊息的 `summaryId`（去重）——這些摘要涵蓋了即將被刪的訊息，全部要跟著刪。
2. 呼叫 `ai-service` 把這些 `summaryId` 對應的向量從 Qdrant 的 `summaries` collection 刪除（`serviceClient.deleteSummaries`）。
3. **關鍵細節**：如果某個要刪的摘要，涵蓋的訊息裡有一部分落在刪除範圍**之前**（也就是刪除點切在某個摘要批次中間），這些「陪葬摘要但本身沒被刪」的訊息會被**標回 `summarized:false, summaryId:null`**——因為摘要整份被刪了，不可能只留一半，這些訊息變回原始未摘要狀態，之後累積夠了會被納入下一次摘要。
4. 最後才刪 DB 裡的訊息列（真的 `DELETE`，不是軟刪除）。
5. 步驟順序刻意是「先刪 Qdrant、成功才動 DB」——如果 Qdrant 刪除失敗會直接拋錯中止，DB 完全不變。

**驗證工具**：倉庫根目錄本來就有 [list-summaries.ps1](list-summaries.ps1)，直接查 Qdrant 的 `summaries` collection（用 `conversation_id` 過濾）並跟 chat-service DB 的訊息交叉比對，會列出每份摘要涵蓋哪些訊息、有哪些訊息還沒摘要，**還內建了「DB 裡有 summaryId 但 Qdrant 找不到對應摘要」的孤兒一致性檢查**——這正好是這份清單要驗證的東西，不用自己重寫查詢邏輯。用法：`powershell -File list-summaries.ps1 <conversationId>`。

## 執行方式注意事項

跟其他清單一樣的 PowerShell 三個坑（`Get-Content` 字串轉型、`$_.ErrorDetails.Message` 抓錯誤 body、中文轉 UTF-8 bytes），helper 函式見 [測試清單-persona-nexus-auth.md](測試清單-persona-nexus-auth.md)。這份額外要注意：

- 第 7 項的多輪對話+輪詢建議整段用背景執行（`run_in_background`），不要同步等，理由同 chat 清單的提醒。
- 每送完一輪訊息，建議都跑一次 `list-summaries.ps1 <conversationId>` 看目前摘要數量，用來判斷什麼時候累積到第 2 份摘要、可以停止對話進入刪除測試，不用盲目多送幾輪浪費時間。

## 測試案例分類說明

> 分類標準只有一條：**該案例能不能只靠「打 Gateway（`localhost:8000`）+ 真實登入拿到的 token」完成**。
> 輪詢（有專用的 `ai-generation-status` API）、等待 LLM 生成（只是要花時間）、依賴前面案例留下的資料，這些都**不算困難**，一樣視為完全符合測試準則。
> 真正的困難只有一種：**測試目的本身要求繞過 Gateway，直打後端服務裸 port**，這種案例在測試準則下無法用「模擬前端」的方式完成，必須額外說明並取得認知。

### 完全符合測試準則的案例 ✅（案例 1-23、G1-G3、H1-H5、I1-I2、J1-J6、K1-K5、L1-L3，共 42 個）

- **第一節（案例 1-2）**：全部
- **第二節（案例 3-5）**：全部
- **第三節（案例 6）**：全部
- **第四節（案例 7-16）**：全部
- **第五節（案例 17-23）**：全部
- **第六節 G/H/I/J/K/L 系列**：除 H6 外全部——包括打 `/internal/*` 的 G2、H1、H4，這些請求的目標仍是 Gateway（`:8000`），只是走內部路由前綴而非 `/api/*`，並未繞過 Gateway，一樣完全符合測試準則

### 有困難的案例 ⚠️（必須繞過 Gateway 直打後端裸 port，共 1 個）

- **H6**：`GET http://localhost:5000/...` 直打 character-service 裸 port，完全不經過 Gateway（`:8000`）。這是本份清單中**唯一**一個測試目的本身就要求違反「一律打 Gateway」準則的案例——目的是驗證下游服務在沒有 Gateway 注入 header 時，是否會誤信「看起來像內部呼叫」的請求並放行。無法用模擬前端的方式完成，執行時應獨立標註為「架構安全驗證，刻意繞過 Gateway」。

---

## 測試案例

### 一、建立規模資料

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| 1 | 註冊 3 組帳號 X／Y／Z | `POST /api/auth/register` × 3，各自 `POST /api/auth/login` 拿 token | 3 組都 `201`／`200`，各自拿到 JWT | ✅ 完全符合 |
| 2 | 每組各建 2 個角色 | `POST /api/characters`（帶各自 token）：X 建 X-pub（`visibility:"public"`）與 X-priv（`"private"`），Y、Z 同理 | 6 個都 `201`，記下 6 個 `id` | ✅ 完全符合 |

**驗證方式（依原始碼查證）**：
- `POST /api/auth/register` 成功：`201`，body 為 `{"id": "usr_<timestamp>", "email": "..."}`（純物件，無 `password` 欄位）。`id` 格式來自 `auth-service/src/services/authService.js:23`（`'usr_' + Date.now()`）；回應組裝見 `authController.js:45`（`res.status(201).json(result)`）與 `authService.js:34-37`。
- `POST /api/auth/login` 成功：`200`，body 為 `{"id": "...", "email": "...", "token": "<JWT字串>"}`，見 `authController.js:73`、`authService.js:68-72`。`token` 可用 `.` 分割成三段驗證是合法 JWT 格式。
- `POST /api/characters` 成功：`201`，body 直接是角色物件本身（`id`、`authorId`、`name`、`background`、`opening`、`introduction`、`gender`、`tags`、`fewShots`、`visibility`），`id` 格式為 `char_<timestamp>`（`character-service/src/services/characterService.js:4` `generateCharacterId`），回應見 `characterController.js:31`（`res.status(201).json(result)`）。
- 驗證重點：檢查回傳的 `visibility` 欄位確實分別是 `"public"`／`"private"`，`authorId` 欄位等於建立者的 `userId`。

### 二、我的角色頁面（對應需求 3）

| # | 情境 | 請求 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| 3 | X 查自己的角色列表 | `GET /api/characters?authorId=<X的userId>`，帶 X 的 token | `200`，剛好 2 筆（X-pub、X-priv），不含 Y／Z 的任何角色 | ✅ 完全符合 |
| 4 | Y 查自己的角色列表 | 同上，換 Y | `200`，剛好 2 筆（Y-pub、Y-priv） | ✅ 完全符合 |
| 5 | Z 查自己的角色列表 | 同上，換 Z | `200`，剛好 2 筆（Z-pub、Z-priv） | ✅ 完全符合 |

**驗證方式（依原始碼查證）**：
- 對應 `character-service/src/services/characterService.js:108-116`（`listCharacters` 場景 2，`authorId` 有值時）：`characterRepository.findManyByAuthorId(authorId)` 撈出該作者全部角色後，再用 `.filter(c => c.authorId === requesterId || c.visibility === 'public')` 過濾。因為這裡查詢者就是作者本人（`requesterId === authorId`），過濾條件恆真，兩筆（public + private）都會保留。
- 驗證重點：`200`，`response.body` 是長度剛好 `2` 的陣列；用 `.map(c => c.authorId)` 確認每筆的 `authorId` 都等於查詢的 `userId`；用 `.map(c => c.visibility)` 確認同時包含 `"public"` 與 `"private"` 各一筆。
- 對照：若換一個帳號（例如 Y 用 `?authorId=<X的userId>` 查詢），因為 `requesterId(Y) !== authorId(X)`，過濾條件只剩 `visibility === 'public'`，應該只拿到 1 筆（X-pub），驗證私有角色確實被排除——這是延伸驗證，不在原案例規格內，但可用同一段程式碼邏輯推導出來。

### 三、大廳公開角色列表（對應需求 4）

| # | 情境 | 請求 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| 6 | 任一帳號查公開角色列表 | `GET /api/characters?visibility=public`，帶任一組 token | `200`，**同時**包含 X-pub、Y-pub、Z-pub 共 3 筆，**不含**任何一個 private 角色（X-priv/Y-priv/Z-priv 都不該出現） | ✅ 完全符合 |

**驗證方式（依原始碼查證）**：
- 對應 `character-service/src/services/characterService.js:102-107`（`listCharacters` 場景 1，`visibility === 'public'` 時）：`characterRepository.findAll()` 撈出**全部使用者**的角色，再 `.filter(c => c.visibility === 'public')`，不限 `authorId`。
- 驗證重點：`200`，`response.body` 長度剛好 `3`；`.map(c => c.visibility)` 全部是 `"public"`；`.map(c => c.id)` 包含 X-pub/Y-pub/Z-pub 三個 id、不含任何 private 角色的 id。

### 四、跨帳號聊天權限（對應需求 5、6）

| # | 情境 | 請求 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| 7 | X 跟自己的 public 角色聊天 | `GET /api/conversations/character/<X-pub id>`（輪詢到 200），帶 X token | `200`，成功建立/取得對話 | ✅ 完全符合 |
| 8 | X 跟自己的 private 角色聊天 | 同上，換 `<X-priv id>` | `200`，成功 | ✅ 完全符合 |
| 9 | Y 跟自己的 public 角色聊天 | 同上，Y token + `<Y-pub id>` | `200`，成功 | ✅ 完全符合 |
| 10 | Y 跟自己的 private 角色聊天 | 同上，Y token + `<Y-priv id>` | `200`，成功 | ✅ 完全符合 |
| 11 | Z 跟自己的 public 角色聊天 | 同上，Z token + `<Z-pub id>` | `200`，成功 | ✅ 完全符合 |
| 12 | Z 跟自己的 private 角色聊天 | 同上，Z token + `<Z-priv id>` | `200`，成功 | ✅ 完全符合 |
| 13 | X 嘗試跟 Y 的 private 角色聊天 | `GET /api/conversations/character/<Y-priv id>`，帶 X token | **403**，message「Access denied to this character」 | ✅ 完全符合 |
| 14 | X 嘗試跟 Y 的 public 角色聊天（對照組，證明擋的是 private 不是擋陌生人） | 同上，改 `<Y-pub id>` | `200`，成功（陌生人也能跟公開角色聊天） | ✅ 完全符合 |
| 15 | Y 嘗試跟 Z 的 private 角色聊天 | `GET /api/conversations/character/<Z-priv id>`，帶 Y token | **403** | ✅ 完全符合 |
| 16 | Z 嘗試跟 X 的 private 角色聊天 | `GET /api/conversations/character/<X-priv id>`，帶 Z token | **403** | ✅ 完全符合 |

> 案例 7 等會先返回 `202 preparing`，需輪詢 `GET /api/conversations/character/<id>` 至 `200 ready` 才能取得 `conversationId`——這條輪詢請求本身也是打 Gateway，不是額外的專用 API，只是要多打幾次同一個端點而已，不影響「符合測試準則」的判定。

> 案例 7-12 建立的對話留著給第五節用，選其中一個（建議 X 跟 X-pub 的對話）繼續往下測。

**驗證方式（依原始碼查證）**：
- **案例 7-12（自己角色聊天）**：首次呼叫回 `202`，body 恰好是 `{"status":"preparing"}`（無其他欄位，見 `chat-service/src/services/conversationService.js:448` `return { status: 'preparing' }`；controller 對應 `conversationController.js` 的 `getOrCreateConversation` 分支）。輪詢至 `200` 時，body 為 `{"status":"ready","conversationId":"conv_...","messages":[...],"title":...,"createdAt":...,"updatedAt":...}`（`conversationService.js:408-415`）。驗證重點：最終狀態碼 `200` 且 `body.status === "ready"`、`body.conversationId` 非空。
- **案例 14（X 訪問 Y 的 public 角色，對照組）**：同樣走 202→200 流程，最終應得到 `200`。此案例不受下方案例 13/15/16 的問題影響，因為 public 角色本來就對所有人開放，不需要走到所有權判斷那一步。

**✅ 案例 13/15/16：已依「微服務架構準則.md」「微服務架構實作spec.md」判定並修復——測試清單的 `403` 預期是對的，原始碼曾經是錯的（安全漏洞），已於 2026-07-25（`待辦task.md` T21）修復並實測驗證，詳見下方「已驗證修復生效」註記**

原案例預期「X 嘗試跟 Y 的 private 角色聊天」應回 `403`。以下是**修復前**追蹤完整呼叫鏈的分析，當時原始碼的實際行為是**成功建立對話（202/200），不是 403**：

1. `chat-service/src/lib/serviceClient.js:18-29`（`getCharacter` 方法）：呼叫 `GET {GATEWAY_URL}/internal/characters/${characterId}`，只帶 `headers: { 'x-user-id': userId }`。
2. `api-gateway/src/app.js:86`：`/internal/characters` 路由掛的是 `internalAuthMiddleware`。
3. `api-gateway/src/middlewares/internalAuthMiddleware.js:6-27`：只檢查來源 IP 是否在白名單，通過就**無條件**注入 `x-internal-request: true`——測試環境所有流量天然是 localhost，必過。
4. `character-service/src/services/characterService.js:93`：`if (!isInternalRequest && ...) throw FORBIDDEN`。`isInternalRequest` 為 `true`，整個所有權判斷式被短路跳過，不管角色是誰的私有角色一律放行。

**依規範判定為原始碼違規**：
- **違反「微服務架構準則.md」原則 7**（「每個下游服務都負責在授權層驗證『呼叫者 ID 是否等於資源擁有者 ID』」）：chat-service 讓「X 能不能跟 Y 的私有角色聊天」這個**特定使用者的授權判斷**完全繞過了所有權檢查。
- **違反「微服務架構準則.md」原則 8**：明訂「私有角色都不能被其他使用者得知」，內部呼叫例外只限於「系統本身的操作」（原文舉例是 ai-service 讀取對話歷史這種系統流程），不是「以內部路由包裝一個特定使用者的授權判斷」。chat-service 這次呼叫的本質就是在幫 X 做「能不能存取 Y 的角色」的授權決策，不是系統本身操作，不適用這條例外。
- **不符合「微服務架構實作spec.md」第一部分內部路由表**（第 50 行）：`/internal/characters/:id` 的「呼叫來源」欄位明確只列 **ai-service**，沒有 chat-service。chat-service 借用了 spec 只授權給 ai-service 使用、且設計上就會跳過所有權檢查的路由，來做一個本該經過所有權判斷的操作。

**結論（修復前）**：這是真實存在的架構違規／安全漏洞，與 `代辦事項-2026-07-25.md` 記錄的發現完全吻合。本文件下方「實測結果」段落所寫的「案例 1-16 全部一次通過，沒有意外」與修復前原始碼的行為矛盾，應視為**該次測試記錄與當時程式碼版本不同步（或當時漏洞尚未被觸發），不代表漏洞已修復**。**修復前的正確做法：保留測試清單原本的 `403` 預期，執行這三個案例時預期會測出失敗（實際拿到 200/202），這個「失敗」本身就是驗證漏洞仍存在的證據，不應該修改測試預期去遷就當時錯誤的行為。此漏洞已於下方「已驗證修復生效」註記記錄的時間點修復，現在執行這三個案例應該直接測得 `403`，不會再有此矛盾。**

修復方式已定案並記錄在 `待辦task.md` 的 **T21**、`mistakes.md` 的 **M16**：不是讓 chat-service 改打外部路由（已查證此路不通——chat-service 手上只有 `x-user-id` 字串沒有原始 JWT，無法組出合法 `Authorization` header），而是在 `character-service` 的 `getCharacter` 判斷式裡區分「真正系統層級呼叫」（無 `requesterId`，跳過檢查）與「內部呼叫但代表特定使用者」（有 `requesterId`，走正常所有權比對）。詳見 `待辦task.md` T21 的完整程式碼與驗收標準。

> ✅ **已驗證修復生效（2026-07-25，`待辦task.md` T21 執行後）**：`character-service/src/services/characterService.js` 的 `getCharacter` 已改成 `isTrueSystemCall = isInternalRequest && !requesterId`，實際啟動 api-gateway + auth/user/character/chat-service、用真實註冊/登入取得的 token 經 Gateway 實測：案例 13（X 存取 Y-priv）、15（Y 存取 Z-priv）、16（Z 存取 X-priv）皆正確回傳 `403 {"error":"FORBIDDEN","message":"Access denied to this character"}`；對照組（X 存取自己的 private 角色、X 存取 Y 的 public 角色）維持 `202 preparing` 不受影響；H1（不帶任何 header 的純系統呼叫）維持 `200`，`isTrueSystemCall` 分支確認生效。詳細執行記錄見 `執行日誌.md` T21。

> ✅ **已修復（2026-07-25，另一輪對話）**：原本案例 2 建立角色時如果沒填 `gender`，第五節一送訊息就會 422（詳見下方「實測結果」的原始記錄）。根因是 `ai-service` 的 `CharacterInfo.gender` 誤宣告成必填字串，與 character-service 這層本來就允許 `gender` 選填/`null` 的規則不一致。已修正為 `Optional[str] = None`，未設定性別時 system prompt 的「Gender」欄位整行省略。修復後角色**不需要**額外補 `gender` 也能正常進入第五節聊天測試，之前「先補 `gender` + 刪除重建對話」的繞過步驟已不再需要。詳見 [ai-service/CLAUDE.md](ai-service/CLAUDE.md)、[部署除錯測試.md](部署除錯測試.md) 第 13、17 節。

### 五、摘要機制與刪除聯動（對應需求 7，這份清單的核心）

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| 17 | 選定對話，開始連續對話 | 用案例 7 建立的對話（X 跟 X-pub），反覆「送訊息 → 輪詢生成完成」，訊息內容盡量長一點加速累積字數 | 持續進行，每輪都成功拿到 AI 回覆 | ✅ 完全符合 |
| 18 | 確認第 1 次摘要觸發 | 每輪後跑 `list-summaries.ps1 <conversationId>` | 累積到門檻後，摘要數量從 0 變成 1，可以看到有幾條訊息被標記涵蓋在這份摘要下 | ✅ 完全符合 |
| 19 | 繼續對話，確認第 2 次摘要觸發 | 繼續案例 17 的迴圈 | `list-summaries.ps1` 顯示摘要數量變成 2，且第 2 份摘要涵蓋的是「未摘要的餘量」+ 新訊息，跟第 1 份涵蓋的訊息不重疊 | ✅ 完全符合 |
| 20 | 記錄關鍵 id | 從 `list-summaries.ps1` 的輸出，找出**第 2 份摘要涵蓋的最早一條訊息**的 `id`（且該訊息 `role` 必須是 `user`，因為刪除端點只能刪使用者自己的訊息） | 記下這個 `messageId`，以及第 1、2 份摘要各自的 `summary_id` | ✅ 完全符合 |
| 21 | 刪除該訊息（回溯式） | `DELETE /api/conversations/<id>/messages/<案例20的messageId>`，帶 X token | `200`，`deletedCount` 應涵蓋案例 20 訊息之後的所有訊息（含第 2 份摘要涵蓋的、跟後續未摘要的） | ✅ 完全符合 |
| 22 | 驗證第 2 份摘要真的被刪了、第 1 份沒事 | 再跑一次 `list-summaries.ps1 <conversationId>` | 摘要數量從 2 變回 1；剩下的那份 `summary_id` 應該等於案例 20 記錄的**第 1 份**摘要 id（第 2 份消失，第 1 份完全沒被動到）；腳本內建的孤兒一致性檢查**不應該**印出任何警告 | ✅ 完全符合 |
| 23 | 確認訊息真的從 DB 刪除 | `GET /api/conversations/<id>/messages`，帶 X token | 回傳的訊息列表在案例 20 那條（含）之後全部消失，只剩案例 20 之前的訊息 | ✅ 完全符合 |

**第五節備註**：
- 案例 17-19 的「困難」只是**時間成本**，不是測試準則的困難：每次 AI 回覆都要真的等 LLM 推論完成（實測 20-40 秒/次），累積 6-10 輪才會觸發 2 次摘要，這是等待時間，不是繞過 Gateway 或無法用真實 token 完成的問題
- 每次發送訊息用 `POST /api/conversations/:id/messages`，完成狀態用專用的 `GET /api/conversations/:id/ai-generation-status` 輪詢，兩者都走 Gateway，符合測試準則
- `list-summaries.ps1` 是驗證工具（直接查 Qdrant + chat-service DB 交叉比對），不是測試請求本身，用它來確認結果不影響「測試操作是否符合準則」的判定

**驗證方式（依原始碼查證）**：
- **案例 17（發送訊息 + 輪詢）**：`POST /api/conversations/:id/messages` 成功回 `201`，body 為 `{"status":"accepted","message":"Message received, AI generation in progress"}`（`chat-service/src/controllers/conversationController.js:141`）。輪詢 `GET /api/conversations/:id/ai-generation-status` 直到 body 的 `status` 欄位變成 `"completed"`；完成時的完整欄位為 `{status:"completed", error, tempUserId, userMessageId, assistantMessageId, timestamp}`（`conversationService.js:1073-1079`，`error`/`tempUserId` 等在無值時是 `undefined`，JSON 序列化後該欄位會消失）。
- **案例 18/19（摘要觸發）**：無專屬 API 直接查詢摘要數量，只能透過 `list-summaries.ps1 <conversationId>`（比對 Qdrant `summaries` collection 與 chat-service DB 的 `Message.summaryId`）確認摘要份數從 `0→1`（案例 18）、`1→2`（案例 19）。觸發邏輯見 `conversationService.js:71-110`（`checkIfNeedsSummary`）：未摘要訊息中排除最新 2 條（`shortTermLimit`）後的字數總和達 `500`（`threshold`，`config/config.json`）才觸發。
- **案例 20（記錄關鍵 id）**：從 `list-summaries.ps1` 輸出中找出第 2 份摘要涵蓋訊息裡 `createdAt` 最早、且 `role === "user"` 的那一條，記下其 `id` 欄位（供案例 21 使用）；同時記下第 1、2 份摘要各自的 `summary_id`（供案例 22 比對）。
- **案例 21（刪除）**：`DELETE /api/conversations/:id/messages/:messageId` 成功回 `200`，body 為 `{"success":true,"message":"<N> 則訊息已刪除","deletedCount":<N>,"deletedIds":[...]}`（`conversationController.js` 的 `deleteMessageAndSubsequent` 分支；`message` 字串格式見 `conversationService.js` 的回溯刪除邏輯，`deletedCount` 來自 `messageRepository.deleteManyByIds(idsToDelete).count`，`conversationService.js:979`）。`deletedIds` 陣列長度應等於 `deletedCount`，且第一個元素等於案例 20 記錄的 `messageId`。
- **案例 22（驗證第 2 份摘要被刪）**：`list-summaries.ps1` 重跑，確認摘要份數變回 `1`；剩下那份的 `summary_id` 應與案例 20 記錄的**第 1 份**完全相同（字串比對）；腳本內建的孤兒一致性檢查（DB 有 `summaryId` 但 Qdrant 查無對應點）不應印出警告——這是腳本自帶的檢查，不需要自己另外查 Qdrant。
- **案例 23（確認訊息從 DB 刪除）**：`GET /api/conversations/:id/messages` 回 `200`，body 是訊息陣列（`conversationController.js` 的 `getMessagesByConversationId`，`chat-service/src/services/conversationService.js:760-782`，直接回傳未包裝的陣列）。驗證方式：用 `.some(m => m.id === 案例20的messageId)` 確認為 `false`，且陣列中所有訊息的 `createdAt` 都早於案例 20 那條訊息原本的 `createdAt`。

## 資料庫驗證

| # | 驗證時機 | 查詢 | 預期結果 |
|---|---|---|---|
| A | 案例 19（第 2 次摘要觸發）之後 | `sqlite3 chat-service/prisma/dev.db "SELECT id, role, summarized, summaryId FROM Message WHERE conversationId='<id>' ORDER BY createdAt"` | 可以看到訊息依序分成「summaryId=第1份」「summaryId=第2份」「summaryId=NULL（未摘要餘量）」三段，順序不會交錯 |
| B | 案例 21（刪除）之後 | 同一句查詢 | 案例 20 的 `messageId` 及其後全部查無資料；案例 20 之前、原本屬於第 1 份摘要的訊息 `summaryId` 應該還是第 1 份摘要的 id，**沒有**被誤標回 `NULL`（因為刪除範圍沒有切到第 1 份摘要的任何訊息，不該觸發「標回未摘要」的邏輯） |

## 不在這份清單範圍內（原因）

- **摘要內容本身的品質**（LLM 有沒有正確總結劇情、好感度那句有沒有寫）：這是 ai-service 生成品質的問題，不是「機制有沒有正確觸發、正確聯動刪除」，品質評測需要另外設計方式，不是這份清單的目標。
- **刪除點切在摘要批次「中間」的邊界情境**（依據段落提到的「陪葬摘要被刪、剩餘訊息標回未摘要」那個分支）：案例 20 刻意選在摘要批次的**起點**刪，所以不會觸發這個分支。這個更細的邊界情境值得之後另外設計案例驗證，但需要更精準控制刪除點位置（要能刪在某份摘要涵蓋範圍的中段），比較難單純靠对话輪數控制，這裡先不列為必測項目。
- **多帳號同時併發聊天的競態情況**（例如 X、Y、Z 同時對各自角色送訊息）：這份清單是循序執行，沒有測真正的併發負載，chat 清單案例 7 已經測過同一對話內的併發保護（409），跨對話/跨帳號的併發不在這份範圍。

---

## 第六節：微服務架構準則／Spec 合規性驗證

> 本節驗證的對象是 [微服務架構準則.md](微服務架構準則.md)、[微服務架構實作spec.md](微服務架構實作spec.md) 定義的規則，以及 [執行日誌.md](執行日誌.md) 記錄的 T1-T18、T20 這批修正是否真的落地生效。上面五節測的是「功能對不對」，這節測的是「架構規則有沒有被遵守」——路徑轉發、內部呼叫的信任鏈、授權統一模式、回應格式、非同步狀態持久化。
>
> 沿用本清單前面的原則：全部用指令模擬請求，不開瀏覽器；用第一節建立的帳號 X／Y／Z 與角色，不重新建立。若在執行第五節前中斷，本節案例對第一到四節的資料仍然有效（只依賴帳號、角色，不依賴摘要機制的狀態）。

### 前置需求

- Gateway（`:8000`）、auth-service（`:3000`）、user-service（`:4000`）、character-service（`:5000`）、chat-service（`:6000`）都要在跑。
- 部分案例（K 系列）需要重啟 chat-service（驗證持久化），重啟前後資料庫檔案（`chat-service/prisma/dev.db`）不能被刪除或替換。
- 部分案例故意繞過 Gateway 直打下游服務裸 port（驗證下游服務自己的授權邏輯不依賴 Gateway 這一層），需要知道各服務裸 port：user-service `:4000`、character-service `:5000`、chat-service `:6000`。

### 測試案例

#### G. Gateway 路徑轉發（對應 spec 第一部分）

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| G1 | `userProxy` pathRewrite 是否正確補回 `/users` 前綴 | `GET http://localhost:8000/api/users/<X的userId>`，帶 X 的 token | `200`，回傳 X 的使用者物件（`id`、`email`，不含 `password`）——**不是** 404（這是 T1 修的既有 bug，驗證真的修好了） | ✅ 完全符合 |
| G2 | `/internal/users` 不分 method、涵蓋 POST 語意 | `POST http://localhost:8000/internal/users` body `{id:"usr_test_g2", email:"g2test@test.com", password:"hashedplaceholder"}`，**不帶任何 header** | ✅ **2026-07-25 已更正**：`201`，body 為 `{"id":"usr_test_g2","email":"g2test@test.com"}`（若該 email 先前已被測試用過則是 `409 EMAIL_ALREADY_EXISTS`）。原寫 `403` 是誤解機制（見下方判決），`internalAuthMiddleware` 只認來源 IP，通過就無條件注入 `x-internal-request: true`，不需要客戶端自己帶這個 header，這正是 spec 設計的行為 | ✅ 完全符合 |
| G3 | G2 的對照組：確認正常路徑真的能建立帳號 | 用 auth-service 的 `POST /api/auth/register` 走完整流程（body `{email:"g3test@test.com", password:"testpass123"}`） | `201`，回傳 `{id, email}`（新格式，直接物件，非 `{status,data}` 包裹）——這條路徑內部會呼叫 Gateway 的 `/internal/users`，成功代表 G2 失敗不是 Gateway 整條路由掛了，而是「沒帶 header 的請求」正確被擋 | ✅ 完全符合 |

**G2 備註**：G2 打的是 `http://localhost:8000/internal/users`，**目標仍是 Gateway 本身**（`:8000`），不是後端裸 port，只是走 Gateway 的內部路由前綴而非 `/api/*` 前綴。這仍然是「打 Gateway」，沒有繞過任何一層，完全符合測試準則。

**驗證方式（依原始碼查證）**：
- **G1**：`GET /api/users/<X的userId>` 帶 X 自己的 token 成功回 `200`，body 為 `{"id":"...","email":"..."}`（`user-service/src/controllers/userController.js:112` 的 `toPublicUser()` 濾掉 `password` 欄位）。驗證重點：確認狀態碼是 `200` 而非 `404`（T1 修的既有 bug）。
- **G3**：`POST /api/auth/register`（新 email）成功回 `201`，body 為 `{"id":"...","email":"..."}` 直接物件（`auth-service/src/controllers/authController.js:45`）。

**⚠️ G2：已依「微服務架構準則.md」「微服務架構實作spec.md」判定並更正——目前原始碼是對的，測試清單原本的 `403` 預期是錯的（誤解機制，非安全漏洞），上表已更正為 `201`／`409`**

判決過程記錄如下：原案例本來預期「不帶任何 header 直打 `POST /internal/users`」會得到 `403`。追蹤原始碼後，確認實際行為是 **`201`，不是 `403`**（假設 `g2test@test.com` 這個 email 之前沒被用過）：

1. `api-gateway/src/app.js:97`：`/internal/users` 路由掛的是 `internalAuthMiddleware`。
2. `api-gateway/src/middlewares/internalAuthMiddleware.js:6-27`：只檢查來源 IP，IP 通過就**無條件**注入 `x-internal-request: true`——不管客戶端請求本身有沒有帶這個 header。從本機（測試環境）發出的請求，IP 檢查必過。
3. `user-service/src/services/userService.js:4-16`（`createUser`）：`if (!isInternalRequest) throw FORBIDDEN`。因為 Gateway 已無條件注入 `x-internal-request: true`，這裡的檢查會通過。
4. `user-service/src/controllers/userController.js:51-57`：檢查 `id`、`email`、`password` 是否都存在——G2 給的 body 三者皆備，不會觸發 400。

**依規範判定為測試清單誤解了設計**：
- **「微服務架構實作spec.md」第二部分 Header 契約**（第 71、75 行）明確寫 `x-internal-request` 的注入時機是「`/internal/*` 路由（內部）」「IP 驗證通過」，且特別強調「前端**無法加**」——這句話的意思是前端無法**偽造**這個 header 去騙過檢查，而不是「客戶端要自己先帶這個 header 才會被信任」。spec 設計的信任依據**只有 Gateway 的 IP 白名單**，跟請求裡有沒有帶 `x-internal-request` 這個 header 完全無關（Gateway 會直接覆寫）。
- G2 打的 `http://localhost:8000/internal/users` 目標仍是 Gateway、來源必然是本機，這正是 spec 設計中「IP 驗證通過」的情境，注入 header 放行是**依規範運作**，不是漏洞。
- spec 第二部分也明文承認這個信任機制的邊界：「信任前提是下游服務本身不對外直接開放...若攻擊者能繞過 Gateway 直連下游服務，可自行偽造此 header——這不是本 spec 要解決的問題，而是部署層必須確保的前提」。G2 測的是「打 Gateway 本身」而非「繞過 Gateway 直連下游」，不落在這個信任邊界的風險範圍內。

**結論**：G2 的正確驗證方式應改為 `201`，body 為 `{"id":"usr_test_g2","email":"g2test@test.com"}`（`toPublicUser` 濾掉 password）；若該 email 先前已被測試用過，則是 `409 EMAIL_ALREADY_EXISTS`。**測試清單原本寫的 `403` 預期是對機制理解有誤，不是原始碼的問題，執行此案例時應以 `201`／`409` 作為判斷通過與否的基準，不是 `403`。**

**與案例 13/15/16 的關鍵差異**：G2 測的是「直接打 Gateway 的內部路由，驗證 Gateway 的 IP 白名單機制本身」，這正是 spec 設計要驗證的東西（架構驗證，非模擬真實使用者流程）。案例 13/15/16 測的是「chat-service 借用內部路由，包裝一個本該屬於特定使用者的所有權判斷」，違反準則原則 7/8 與 spec 內部路由表對呼叫來源的限定（只列 ai-service）。兩者外觀都涉及 `x-internal-request`，但一個是機制設計本身（G2，原始碼對），一個是誤用機制去繞過本該存在的檢查（13/15/16，原始碼錯）。

#### H. `x-internal-request` 全鏈路驗證（對應 spec 第二部分 Header 契約）

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| H1 | character-service 內部路由允許查詢私有角色 | `GET http://localhost:8000/internal/characters/<X-priv的id>`（用第一節建立的 X-priv），**不帶任何 Authorization header** | `200`，回傳角色物件——因為經過 Gateway 的 `internalAuthMiddleware` 會注入 `x-internal-request: true`，character-service 的 `getCharacter` 讀到這個 header 後跳過 visibility 檢查 | ✅ 完全符合 |
| H2 | H1 的對照組：同一個私有角色，走外部路由、無 token | `GET http://localhost:8000/api/characters/<X-priv的id>`，不帶 Authorization header | `401`，因為 `/api/*` 路由需要先過 `authMiddleware`，缺 token 直接被擋，連 character-service 都到不了 | ✅ 完全符合 |
| H3 | H1 的對照組：同一個私有角色，走外部路由、帶別人的 token | `GET http://localhost:8000/api/characters/<X-priv的id>`，帶 Y 的 token | `403`，`x-user-id` 是 Y、角色擁有者是 X，且這條路由沒有 `x-internal-request`，所有權檢查生效 | ✅ 完全符合 |
| H4 | chat-service 內部路由允許查詢對話訊息（不需 `x-user-id`） | 用第四節案例 7 建立的對話（X 跟 X-pub），`GET http://localhost:8000/internal/conversations/<conversationId>/messages`，不帶任何 header | `200`，回傳訊息陣列——驗證 chat-service 的 `assertConversationOwnership` 在 `isInternalRequest` 為真時跳過 `userId` 比對 | ✅ 完全符合 |
| H5 | H4 的對照組：同一個對話，走外部路由、無 token | `GET http://localhost:8000/api/conversations/<conversationId>/messages`，不帶 Authorization header | `401`，`x-user-id` 缺失，被 service 層的 `validateUserId` 擋下（T10 移除獨立 middleware 後，這個判斷現在在 service 層，但外部行為應該不變） | ✅ 完全符合 |
| H6 | 直打下游裸 port，繞過 Gateway，驗證下游不會誤信「看起來像內部呼叫」 | `GET http://localhost:5000/api/v1/characters/<X-priv的id>`（character-service 裸 port，不經 Gateway），不帶任何 header | `403`，因為沒有 `x-internal-request` header（這個 header 只有 Gateway 的 `internalAuthMiddleware` 會注入，直打裸 port 不會有），也沒有 `x-user-id`，會被判定為外部請求且無所有權，回 `FORBIDDEN` | ⚠️ 困難：必須直打後端裸 port |

**H1、H4 備註**：打的是 `http://localhost:8000/internal/...`，目標仍是 Gateway（`:8000`），只是走 `/internal/*` 前綴而非 `/api/*`。前端從不會呼叫這個前綴，但這不構成「困難」——它依然是一筆打 Gateway 的請求，沒有繞過 Gateway，完全符合測試準則的字面要求（只是驗證的對象是內部呼叫鏈路，不是前端會發的請求）。

**驗證方式（依原始碼查證）**：
- **H1**：`GET /internal/characters/<X-priv id>` 不帶 Authorization header，成功回 `200`，body 是角色物件本身。原因：`internalAuthMiddleware`（見發現 1、2）只看 IP，不看有沒有 Authorization header，通過就無條件注入 `x-internal-request: true`；`character-service/src/services/characterService.js:93` 的 `getCharacter` 在 `isInternalRequest === true` 時整段所有權判斷跳過，直接回傳角色資料，不管是不是私有、不管是誰的。此案例的預期結果**與目前原始碼一致**，可正常驗證。
- **H2**：`GET /api/characters/<X-priv id>` 不帶 Authorization header → `401`，body 為 `{"message":"Missing or invalid Authorization header"}`（`api-gateway/src/middlewares/authMiddleware.js:24-28`）。這個 401 在 Gateway 層就被擋下，請求根本沒有送到 character-service。
- **H3**：`GET /api/characters/<X-priv id>` 帶 Y 的 token → `403`，body 為 `{"error":"FORBIDDEN","message":"無權限"}`（`character-service` 的 `ERROR_MAP`，`characterController.js:5`；判斷邏輯 `characterService.js:93-95`：`isInternalRequest` 為 `false`（因為走 `/api/*` 不是 `/internal/*`），`existing.authorId(X) !== requesterId(Y)` 為真，拋出 `FORBIDDEN`）。
- **H4**：`GET /internal/conversations/<conversationId>/messages` 不帶任何 header，成功回 `200`，body 是訊息陣列。原因與 H1 相同：`chat-service/src/services/conversationService.js:28-48`（`assertConversationOwnership`）在 `isInternalRequest === true` 時完全跳過 `validateUserId(userId)` 與 `conversation.userId !== userId` 兩道檢查，只要 `conversationId` 存在就放行。此案例**與原始碼行為一致**，可正常驗證出 `200`。

**⚠️ H4 對 spec 的符合度：無法完全判斷，誠實記錄**——`GET /internal/conversations/:id/messages` 這個確切的「路由 + 方法」組合，spec 第一部分內部路由表（第 51-52 行只列 `GET /internal/conversations`〔不含 `:id/messages`〕與 `POST /internal/conversations/:id/messages`）跟第二部分 chat-service 授權決策表（第 137-138 行同樣只列這兩條）都**沒有逐字列出**這個組合。能確認的是：這個路由的行為（內部請求跳過所有權檢查）跟同服務其他已列在表裡的內部路由模式完全一致，也跟 H1（character-service 的內部路由）的設計精神相符，合理推測是刻意設計、只是 spec 的表格漏列了這一條路由。但因為 spec 沒有白紙黑字覆蓋這個確切組合，**我無法斷定這是「文件遺漏但設計正確」還是「這個路由組合原本就不該存在」**，不強行判定對錯，留給你視情況決定是否要在 spec 補上這一條。
- **H5**：`GET /api/conversations/<conversationId>/messages` 不帶 Authorization header → `401`，body 為 `{"message":"Missing or invalid Authorization header"}`。**注意**：這個 401 來自 **Gateway 的 `authMiddleware`**（`api-gateway/src/app.js:78` `/api/conversations` 路由掛的是 `authMiddleware`），請求在到達 chat-service 之前就被攔截——原文件裡「被 service 層的 `validateUserId` 擋下」的說法不準確，實際上請求根本沒有機會執行到 chat-service 的 `validateUserId`（`conversationService.js:13-17`）。這不影響最終驗證的狀態碼（`401`）本身，但影響「應該檢查 body 裡的哪個錯誤訊息」——應比對 Gateway 的 `{"message":"Missing or invalid Authorization header"}`，不是 chat-service 自己 `ERROR_MAP` 裡 `UNAUTHORIZED` 對應的 `{"error":"UNAUTHORIZED","message":"Unauthorized"}`。

**H6 困難說明**：
- H6 是本份清單中**唯一**測試目的本身就要求繞過 Gateway 的案例：`GET http://localhost:5000/...` 直接打 character-service 的裸 port，完全不經過 `:8000`
- 這**違反**「所有測試請求都必須經由 Gateway」的準則，無法用模擬前端的方式完成
- 之所以仍列在清單中：這個案例驗證的不是「功能對不對」，而是「下游服務有沒有偷懶信任任何看起來像內部呼叫的請求」——如果不直打裸 port，就沒有辦法製造出「請求繞過了 Gateway」這個前提條件，也就測不出下游服務是否有正確拒絕
- **執行時的處理方式**：這類案例應該獨立於一般集成測試之外，明確標註「架構安全驗證，刻意繞過 Gateway」，不能當作「模擬前端會發的請求」來理解或執行
- **H6 驗證方式（依原始碼查證）**：直打 `http://localhost:5000/api/v1/characters/<X-priv id>`（character-service 裸 port）不帶任何 header → `403`，body 為 `{"error":"FORBIDDEN","message":"無權限"}`。原因：`character-service/src/controllers/characterController.js:53-54` 讀 `req.headers['x-user-id']`（不存在，因為沒經過 Gateway 注入）與 `req.headers['x-internal-request']`（不存在，因為沒經過 `internalAuthMiddleware`），兩者皆為 `undefined`／`false`；`characterService.js:93` 的判斷式中 `existing.authorId !== requesterId`（`requesterId` 是 `undefined`）恆真，且 `isInternalRequest` 是 `false`，所以整個條件成立，拋出 `FORBIDDEN`。此案例的預期結果**與目前原始碼一致**，可正常驗證，是本節少數「繞過 Gateway 後下游服務仍正確拒絕」的驗證。

#### I. 授權統一模式（對應 spec 第二部分「授權檢查統一模式」）

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| I1 | user-service 不再有獨立 middleware 擋在路由層——驗證行為對外不變 | `GET http://localhost:8000/api/users/<Y的userId>`，帶 X 的 token（X 查 Y 的資料） | `403`——T5 把 `authorizeSelf.js` 移除、邏輯搬進 service 層後，外部可觀察行為應該跟移除前完全一樣 | ✅ 完全符合 |
| I2 | chat-service 不再有獨立 authMiddleware——驗證缺 `x-user-id` 時行為對外不變 | `GET http://localhost:8000/api/conversations`，帶一個**格式錯誤、驗證會失敗**的 Authorization header（例如 `Bearer invalid.token.here`） | `401`——這條請求在 Gateway 的 `authMiddleware` 就會被攔截（JWT 驗證失敗），不會帶著缺失的 `x-user-id` 傳到 chat-service，用來確認 Gateway 層的攔截沒有因為下游改動而受影響 | ✅ 完全符合 |

**驗證方式（依原始碼查證）**：
- **I1**：`GET /api/users/<Y的userId>` 帶 X 的 token → `403`，body 為 `{"error":"FORBIDDEN","message":"Forbidden"}`。查證：`user-service` 目前**沒有** `authorizeSelf.js` 這個檔案（已用 `Glob` 確認 `user-service/src/middlewares/*.js` 找不到任何檔案），授權邏輯在 `userService.js:32-39`（`getUserById`）：`if (!isInternalRequest) { if (!requesterId) throw UNAUTHORIZED; if (requesterId !== id) throw FORBIDDEN; }`。X 查 Y 的資料時 `requesterId(X) !== id(Y)`，拋出 `FORBIDDEN`；`userController.js` 的 `ERROR_MAP`（`FORBIDDEN: {status:403, message:'Forbidden'}`）對應到 `403`。
- **I2**：`GET /api/conversations` 帶 `Bearer invalid.token.here` → `401`，body 為 `{"message":"Invalid or expired token"}`（**不是** `{"message":"Missing or invalid Authorization header"}`——因為這個 header **有**以 `Bearer ` 開頭，通過 `authMiddleware.js:24` 的格式檢查，但 `jwt.verify()` 在 `authMiddleware.js:37` 對這個假 token 驗證失敗，落入 `catch` 區塊回傳 `authMiddleware.js:56` 的訊息）。驗證重點：確認錯誤訊息是「Invalid or expired token」而非「Missing or invalid Authorization header」，兩者對應不同的程式碼分支，都在 `401`。

#### J. 回應格式（對應 spec 第三部分）

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| J1 | auth-service 錯誤回應格式 | `POST http://localhost:8000/api/auth/register` body 用已存在的 email（例如案例 2/G3 用過的） | ✅ **2026-07-25 已更正並修復**：`409`，body 為 `{"error":"EMAIL_ALREADY_EXISTS","message":"該電子郵件已被註冊，請更換帳號。"}`（原寫 `400` 是照抄 auth-service 當時錯誤的行為，未依 spec 核對，見下方判決）。原始碼已於同日（`待辦task.md` T22）修復並實測驗證為 `409`，詳見下方「已驗證修復生效」註記 | ✅ 完全符合 |
| J2 | user-service 錯誤回應格式 | `GET http://localhost:8000/api/users/<Y的userId>`，帶 X 的 token | `403`，body 是 `{"error": "FORBIDDEN", "message": "..."}` | ✅ 完全符合 |
| J3 | user-service 操作成功（無資源）格式 | 建一個一次性測試帳號後（`POST /api/auth/register` 拿到 id 與 token），`DELETE http://localhost:8000/api/users/<剛建立的userId>`，帶該帳號自己的 token | `200`，body 是 `{"success": true, "message": "..."}`——**不是** `204 No Content`（T13 修的格式，這個一次性帳號測完即被刪除，不需要額外清理） | ✅ 完全符合 |
| J4 | character-service 成功回應格式（清單） | `GET http://localhost:8000/api/characters?visibility=public`，帶任一組 token | `200`，body 直接是**陣列** `[{...}, {...}]`，不是 `{status:"success", data:[...]}` 包裹 | ✅ 完全符合 |
| J5 | character-service 成功回應格式（單一資源） | `GET http://localhost:8000/api/characters/<X-pub的id>`，帶 X 的 token | `200`，body 直接是角色物件本身（有 `id`、`name` 等欄位在最外層），不是 `{status:"success", data:{...}}` | ✅ 完全符合 |
| J6 | chat-service 業務語意欄位不該被誤判為格式違規（對照案例） | 用第四節案例 8-12 任一組帳號，`GET /api/conversations/character/<角色id>`（該對話已存在） | `200`，body 含 `status: "ready"`——這個 `status` 是業務語意（供前端輪詢判斷用），**不是**要驗證的格式錯誤，只是確認它依然存在、值域正確 | ✅ 完全符合 |

**✅ J1：已依「微服務架構實作spec.md」判定、更正並修復——原始碼與測試清單原本的 `400` 預期都曾經是錯的，正確狀態碼應為 `409`，上表已更正，原始碼已於 `待辦task.md` 的 T22 修復並實測驗證**

`spec.md` 第三部分「HTTP Status Code 統一定義」（第 161 行）明文寫：`409 | Conflict | 衝突（例如：email 已被註冊）`——spec **直接用「email 已被註冊」當作 409 的標準範例**，這是 spec 對這一類錯誤唯一給出的具體場景。

但 `auth-service/src/controllers/authController.js:5` 的 `ERROR_MAP`：
```js
EMAIL_ALREADY_EXISTS: { status: 400, message: '該電子郵件已被註冊，請更換帳號。' }
```
回傳的是 `400`，不是 spec 規定的 `409`。

對照 `user-service/src/controllers/userController.js:21` 的**同名**錯誤碼：
```js
EMAIL_ALREADY_EXISTS: { status: 409, message: 'Email already exists' }
```
user-service 正確回傳 `409`，跟 spec 一致。

**依規範判定**：這是兩個服務對 spec 同一個範例做出不一致的實作——user-service 做對了，auth-service 做錯了。J1 原本歸類在「驗證 T12（auth-service 回應格式修正）」，代表 T12 這次修正**只把 body 改成了 `{error,message}` 格式，沒有把狀態碼也修正成 spec 規定的 `409`**，是不完整的修復。測試清單原本寫的 `400` 預期只是照抄了 auth-service 目前（錯誤）的行為，並未依 spec 核對。

**結論（修復前）**：J1 的正確預期結果已更正為 `409`（見上表），body 為 `{"error":"EMAIL_ALREADY_EXISTS","message":"該電子郵件已被註冊，請更換帳號。"}`（訊息文字不變，只有狀態碼要修正）。修復前原始碼會回 `400`——修復方式記錄在 `待辦task.md` 的 **T22**（把 auth-service 的 `ERROR_MAP` 裡 `EMAIL_ALREADY_EXISTS` 的 `status` 從 `400` 改成 `409`），已於 2026-07-25 完成並實測驗證，詳見下方「已驗證修復生效」註記。**T22 完成前**執行此案例會測試失敗（實際拿到 `400`），這個失敗本身是驗證 T22 尚未執行的證據，不應為了讓測試通過而把預期值改回 `400`；T22 完成後執行應直接測得 `409`。

> ✅ **已驗證修復生效（2026-07-25，`待辦task.md` T22 執行後）**：`auth-service/src/controllers/authController.js` 的 `ERROR_MAP` 裡 `EMAIL_ALREADY_EXISTS.status` 已從 `400` 改成 `409`。實際啟動 api-gateway + auth-service + user-service，經 Gateway 用已存在帳號的 email 呼叫 `POST /api/auth/register` 實測，回傳 `409 {"error":"EMAIL_ALREADY_EXISTS","message":"該電子郵件已被註冊，請更換帳號。"}`，符合上方更正後的預期。另檢查前端 `persona-nexus-auth/src/main.js` 只用 `response.ok`／`response.status` 做日誌與訊息顯示，沒有寫死判斷這個狀態碼等於 `400` 的邏輯，不需要同步修改。詳細執行記錄見 `執行日誌.md` T22。

**與另兩處矛盾的差異（修復前狀態）**：案例 13/15/16 當時是「清單對、碼錯」（清單維持原樣，原始碼修復列在 `待辦task.md` 的 T21，現已修復）；G2 是「碼對、清單錯」（清單已更正為 `201`／`409`，原始碼不用改）；J1 當時**兩邊都曾經錯**（原始碼跟原本的測試清單預期不謀而合地都採用了不符 spec 的 `400`），清單已更正為 `409`，原始碼修復列在 T22，現已修復。三處現在應該都與規範一致，不再有矛盾。

**驗證方式（依原始碼查證）**：
- **J1**：正確預期為 `409`（見上方判決），body 為 `{"error":"EMAIL_ALREADY_EXISTS","message":"該電子郵件已被註冊，請更換帳號。"}`。原始碼修復前實測會拿到 `400`，已於 `待辦task.md` T22 修復並實測驗證為 `409`。注意這個錯誤來自 **auth-service 自己的 `ERROR_MAP`**，跟 `user-service` 的同名錯誤碼是兩個不同服務各自的 `ERROR_MAP`，不要混淆彼此的狀態碼。
- **J2**：`GET /api/users/<Y的userId>` 帶 X 的 token → `403`，body 為 `{"error":"FORBIDDEN","message":"Forbidden"}`（`user-service/src/controllers/userController.js:20-25` 的 `ERROR_MAP`，與 I1 相同的程式碼路徑）。
- **J3**：先 `POST /api/auth/register` 建一次性帳號拿 `id`/`token`，`DELETE /api/users/<剛建立的userId>` 帶自己的 token → `200`，body 為 `{"success":true,"message":"User deleted successfully"}`（`userController.js:131`，字串精確比對）。
- **J4**：`GET /api/characters?visibility=public` → `200`，body 直接是陣列（`character-service/src/controllers/characterController.js:45` `res.status(200).json(result)`，`result` 本身就是 `characterService.listCharacters()` 回傳的陣列，未包裝）。
- **J5**：`GET /api/characters/<X-pub的id>` → `200`，body 直接是角色物件（`characterController.js:60`，同樣未包裝）。
- **J6**：`GET /api/conversations/character/<角色id>`（對話已存在）→ `200`，body 含 `"status":"ready"`（`conversationService.js:409`），此欄位為業務語意而非格式問題，驗證重點只是確認欄位存在且值為 `"ready"`。

#### K. 非同步狀態持久化（對應 spec 第四部分，需要重啟 chat-service）

> ⚠️ 這節需要手動重啟 chat-service，重啟前後資料庫檔案不能被刪除或替換（沿用同一個 `chat-service/prisma/dev.db`）。

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| K1 | 建立一個新對話，讓它進入 `preparing` 狀態 | 用第一節建立的某組帳號建一個新角色（避免跟既有對話衝突），`GET /api/conversations/character/<新角色id>`（第一次呼叫，RAG 初始化需要時間） | `202`，body `{status:"preparing"}` | ✅ 完全符合 |
| K2 | **立刻**（RAG 初始化完成前）重啟 chat-service | 重啟 chat-service 進程（例如 Ctrl+C 後重新 `npm run dev` 或 `node src/app.js`） | 服務正常重新啟動，無報錯 | ✅ 完全符合（非 API 操作，屬環境操作） |
| K3 | 重啟後查詢同一個對話的建立狀態 | 重啟完成後，立刻 `GET /api/conversations/character/<K1的角色id>` | 若 RAG 初始化在重啟前已經在背景完成，會是 `200 ready`；若還沒完成，應該是 `202 preparing`（狀態從資料庫讀出，不是「服務重啟後忘記這個對話存在、變成從頭建立一個新的」）——**這是本節最關鍵的案例**：進程內記憶體的舊版實作，重啟後 `creationJobs` 這個 Map 會直接清空，`getOrCreateConversation` 會偵測不到任何 job、重新走一次「沒有 job → 建立新 job」的流程，可能導致同一個角色被建立兩個對話（違反唯一性）或至少讓使用者重新等一次完整初始化；持久化版本因為狀態在資料庫，不會有這個問題 | ✅ 完全符合 |
| K4 | 確認 K1 的角色沒有被建立出重複的對話 | `GET /api/conversations`，帶該帳號 token，過濾出 `characterId` 等於 K1 角色 id 的項目 | 剛好 1 筆，不是 2 筆 | ✅ 完全符合 |
| K5 | AI 生成狀態欄位持久化（不需要真的生成，查詢現有已完成對話） | 用第五節已經產生過 AI 回覆的對話（案例 17 那組），重啟 chat-service 後，`GET /api/conversations/<conversationId>/ai-generation-status` | `200`，body 的 `status` 欄位不會是 `unknown`（如果第五節那組對話最後一次生成是 `completed`，重啟後應該還是讀得到 `completed`，不會因為重啟而重置成「沒有記錄」） | ✅ 完全符合 |

**K 系列備註**：
- K2 是重啟服務進程的環境操作，不是 API 請求，不涉及「打 Gateway 還是打裸 port」的問題，完全符合測試準則的精神
- K1、K3 涉及輪詢與抓時機（在 RAG 初始化完成前重啟），這是操作steps 較多、需要抓時間點，不是繞過 Gateway，仍完全符合測試準則
- K5 依賴第五節（案例 17）已產生 `completed` 狀態的對話，這只是案例間的資料依賴順序，不是測試準則層面的困難

**驗證方式（依原始碼查證）**：
- **K1**：`GET /api/conversations/character/<新角色id>`（首次呼叫）→ `202`，body 恰好 `{"status":"preparing"}`（`conversationService.js:448`）。
- **K3**：重啟後立刻再呼叫同一端點，結果應為 `200`（body 含 `status:"ready"`）**或** `202`（body `{"status":"preparing"}`），取決於 RAG 初始化是否已在重啟前完成——兩者都算通過，重點是**不能報錯、也不能是一個全新的 `conversationId`**。判斷「job 有沒有在重啟後正確從 DB 復原」的關鍵在 `conversationCreationJobRepository`（DB 表 `ConversationCreationJob`，見 `chat-service/prisma/schema.prisma`），而非進程內 Map——這是 T18 持久化改動要驗證的核心，程式碼層面的直接證據是 `conversationService.js:421`（`await conversationCreationJobRepository.findByKey(userId, characterId)`）從資料庫查詢而非記憶體變數。
- **K4**：`GET /api/conversations` 帶該帳號 token → `200`，body 是陣列（`conversationService.js:479-494` `getAllConversations`，含 `messages` 欄位但只取最新 1 則：`take: 1`）。驗證方式：`.filter(c => c.characterId === K1的角色id).length === 1`。
- **K5**：`GET /api/conversations/<conversationId>/ai-generation-status` → `200`，body 的 `status` 欄位應維持重啟前的值（例如 `"completed"`）。程式碼依據：`conversationService.js:1060-1069`——只有當 `!conversation.generationStatus`（DB 欄位為 `null`/`undefined`）時才會回傳 `{"status":"unknown","message":"No generation status record"}`；若 DB 裡確實存有先前寫入的 `generationStatus`（見 `conversationService.js:711-718` 生成成功後的寫入邏輯），重啟不會清空這個欄位（它是 Prisma 持久化欄位，不是進程內 Map），所以應該讀得到原本的值。

#### L. `GET /api/characters` 免 query 合併查詢（對應 T20 新增行為）

| # | 情境 | 操作 | 預期結果 | 符合測試準則 |
|---|---|---|---|---|
| L1 | X 不帶任何 query 查詢角色列表 | `GET http://localhost:8000/api/characters`（不帶 `authorId`／`visibility`），帶 X 的 token | `200`，陣列內容 = X 的所有角色（X-pub、X-priv）聯集所有 public 角色（X-pub、Y-pub、Z-pub），依 `id` 去重後共 4 筆：X-priv、X-pub、Y-pub、Z-pub（X-pub 只出現一次，不因為它同時符合兩個條件而重複） | ✅ 完全符合 |
| L2 | 確認既有 `?visibility=public` 場景行為不變 | `GET http://localhost:8000/api/characters?visibility=public`，帶 X 的 token | `200`，剛好 3 筆（X-pub、Y-pub、Z-pub），不含任何 private——跟 T20 之前的行為一致，證明新增分支沒有動到既有場景 | ✅ 完全符合 |
| L3 | 確認既有 `?authorId=` 場景行為不變 | `GET http://localhost:8000/api/characters?authorId=<X的userId>`，帶 X 的 token | `200`，剛好 2 筆（X-pub、X-priv）——跟 T20 之前的行為一致 | ✅ 完全符合 |

**驗證方式（依原始碼查證）**：
- **L1**：對應 `character-service/src/services/characterService.js:117-127`（`listCharacters` 場景 3，無 `authorId` 也無 `visibility` 但有 `requesterId`）：`findAll()` 撈全部角色，`.filter(c => c.authorId === requesterId || c.visibility === 'public')`，再用 `new Map(matched.map(c => [c.id, c])).values()` 依 `id` 去重。驗證重點：`200`，陣列長度剛好 `4`；用 `new Set(ids).size === ids.length` 確認沒有重複 `id`；確認集合恰好是 `{X-priv, X-pub, Y-pub, Z-pub}`。
- **L2**：邏輯與案例 6 完全相同（同一段程式碼 `characterService.js:102-107`），驗證方式相同：剛好 3 筆、皆為 `visibility:"public"`。
- **L3**：邏輯與案例 3 完全相同（同一段程式碼 `characterService.js:108-116`），驗證方式相同：剛好 2 筆、`authorId` 皆為查詢的 `userId`。

### 不在這節範圍內（原因）

- **`internalAuthMiddleware` 的 IP 白名單本身**（是否正確拒絕非 `127.0.0.1`／內網 IP 的請求）：這個機器上所有測試流量天然都是 `localhost`，沒有辦法在不改變測試環境網路拓樸的情況下，模擬一個「非白名單 IP」的請求來驗證會被拒絕。這屬於部署層的驗證，不是這份清單能覆蓋的範圍。
- **T19（ai-service 非同步狀態持久化）**：擱置中，尚未實作，沒有東西可測。
- **ai-service 的回應格式（T16）**：ai-service 的錯誤格式改動（`{error, message}`）只在真正觸發例外時才看得到差異（例如 Qdrant 斷線），正常聊天流程走不到這個分支，且第五節已經涵蓋了 ai-service 正常運作路徑的驗證，這裡不重複測。
- **`GET /internal/users/:id`**：spec 裡明確標注「保留供未來使用，目前無呼叫方」，沒有任何服務會實際呼叫這條路由，測了也只是空轉。

## 實測結果（2026-07-25，歷史記錄，已被下方 2026-07-26 正式記錄取代）

> ⚠️ 這筆 2026-07-25 的記錄與同日另一輪原始碼查證矛盾（案例 13/15/16 當時應為漏洞未修復狀態），已於 T21/T22 修復後失去作為「目前實測基準」的效力。**下方 2026-07-26 記錄是全份 41 案例＋H6 首次正式、完整、連貫跑過一輪的結果，是目前有效的實測基準**，此區塊僅保留作歷史時間軸參考，不再代表目前程式碼行為。
>
> - 一至四節（案例 1-16）當時記錄「全部一次通過，沒有意外」，但與同日另一輪原始碼追蹤發現的漏洞（案例 13/15/16 應為 403 卻推導會成功）矛盾，已判定為當時測試與程式碼版本不同步。
> - 第五節（案例 17-23，摘要機制）：X 跟自己的 X-pub 角色連續對話 7 輪觸發 2 次摘要（第 5、7 輪），刪除聯動與孤兒檢查皆正常。
> - 過程中撞到一次已知的 `ai-service` `CharacterInfo.gender` 必填 bug，已於同日另一輪對話修復。

---

## 實測結果（2026-07-26，正式完整測試，目前有效基準）

> 依 [進階整合測試task0726.md](進階整合測試task0726.md) 的 checklist，對本清單標記「✅ 完全符合測試準則」的案例（案例 1-23、G1-G3、H1-H5、I1-I2、J1-J6、K1-K5、L1-L3，共 47 個）+ 2 項資料庫驗證（A/B）+ H6（獨立處理）全部經 Gateway（`http://localhost:8000`）、用真實註冊/登入拿到的 token 正式執行一輪。**結論：47 個案例 + 2 項資料庫驗證 + H6，共 50 項執行，全數 PASS**，逐項執行記錄（含實際狀態碼、response body、判斷依據）見 [進階整合測試task0726.md](進階整合測試task0726.md) 各節 checkbox。以下是摘要與過程中的重要發現。

### 逐節結果摘要

| 節 | 案例範圍 | 結果 |
|---|---|---|
| 環境準備 | Docker/Ollama/Qdrant/六服務健康檢查 | ✅ 全部正常 |
| 一、建立規模資料 | 案例 1-2 | ✅ 2/2 PASS |
| 二、我的角色頁面 | 案例 3-5 | ✅ 3/3 PASS |
| 三、大廳公開角色列表 | 案例 6 | ✅ PASS（字面「剛好 3 筆」需修正認知，見下方發現 1） |
| 四、跨帳號聊天權限 | 案例 7-16 | ✅ 10/10 PASS（含 T21 修復的 13/15/16） |
| 五、摘要機制與刪除聯動 | 案例 17-23 + 資料庫驗證 A/B | ✅ 9/9 PASS |
| 六、G Gateway 路徑轉發 | G1-G3 | ✅ 3/3 PASS |
| 六、H x-internal-request 全鏈路 | H1-H5 | ✅ 5/5 PASS |
| 六、I 授權統一模式 | I1-I2 | ✅ 2/2 PASS |
| 六、J 回應格式 | J1-J6 | ✅ 6/6 PASS（含 T22 修復的 J1） |
| 六、K 非同步狀態持久化 | K1-K5 | ✅ 5/5 PASS（K5 經修正測試方法後複測通過，見下方發現 2） |
| 六、L 免 query 合併查詢 | L1-L3 | ✅ 3/3 PASS |
| H6（獨立，繞過 Gateway） | H6 | ✅ PASS |

**總計 50/50 PASS（47 個案例 + 資料庫驗證 A/B + H6），沒有發現需要依《微服務架構準則.md》《微服務架構實作spec.md》判定「清單錯或碼錯」的新落差。** T21（案例 13/15/16 所有權檢查修復）、T22（J1 狀態碼修復）皆在本輪正式測試中確認持續有效。

### 過程中的重要發現（非清單錯誤，記錄供之後參考）

1. **案例 6／L1／L2「剛好 N 筆」的字面預期在長期共用的 dev DB 下不成立**——實測 `?visibility=public` 回傳 33 筆（後續清理前），不是文件寫的 3 筆，因為這個資料庫是跨多份測試清單、跨多輪執行累積下來的（每份清單都寫明「測完不用清理」）。這不是程式碼問題：改用符合 `character-service/src/services/characterService.js:102-107` 實際邏輯的不變量驗證（本輪建立的 3 筆 public 都在回傳陣列裡、本輪建立的 3 筆 private 都不在、陣列內每筆都是 public），驗證通過。「剛好 N 筆」這個字面表述只在資料庫乾淨時成立，建議之後若要重跑本清單，執行前應先清空 dev DB 或改用「包含關係」而非「精確筆數」驗證。

2. **K5 初次測試「失敗」是測試方法論問題，不是持久化機制的 bug**——K5 沿用第五節案例 17 的對話做驗證，但該對話已被案例 21 的回溯刪除（`deleteMessageAndSubsequent`）清空過 `generationStatus`（`chat-service/src/services/conversationService.js:983-990`，程式碼註解明確寫「清除該聊天室的舊生成狀態（completed/failed 已無意義）」，是刻意設計，非 bug）。換一個未被刪除測試動過的乾淨對話（案例 8）重新驗證：送訊息→輪詢至 `completed`→直接查 SQLite 確認落地→**實際重啟 chat-service**→重啟後 API 仍讀到同一筆 `completed` 記錄，`userMessageId`／`assistantMessageId` 與重啟前相同，持久化行為正確。另外發現 `chat-service/CLAUDE.md` 裡「`aiGenerationStatus` 是進程內記憶體 Map、重啟即遺失」的描述已過時，與目前程式碼（`generationStatus` 等欄位是 Prisma 持久化欄位，讀寫都經 `conversationRepository`）不符，建議之後更新該文件。

3. **測試環境曾出現孤兒進程，與程式碼無關的環境問題**——K 系列需要手動重啟 chat-service 時，發現當時 5 個後端服務（auth/user/character/chat/api-gateway）雖然功能完全正常，但其進程樹是 `bash.exe → npm-cli.js → cmd.exe → node.exe`，沒有依附任何可互動視窗，導致無法用 Ctrl+C 控制。已用 `taskkill /PID <bash根PID> /T /F` 清除後，改用 `start-all-services.bat` 正常重啟（`start "Title" cmd /k ...`），問題排除。與測試案例判定無關，純環境操作記錄。順手把 `user-service`、`api-gateway` 的啟動 console 訊息格式統一成跟其他三個服務一致的箱型格式（純文字調整）。

### 收尾清理（範圍超出本清單原定範圍，依使用者指示執行）

測試完成後盤點發現 dev DB 累積了 58 個帳號／60 個角色／50 個對話（多輪過往測試殘留，非本輪獨有），使用者指示除了本人帳號 `liwei@gmail.com` 外全部清除。因多數舊帳號無可用 token，改為直接對三個服務的 SQLite 檔案執行 SQL DELETE（執行前已完整備份），並清理對應的 Qdrant 摘要點位。清理後驗證 `liwei@gmail.com` 的資料（1 角色／1 對話／11 則訊息／1 份摘要）完整保留、無孤兒訊息、六服務清理後仍正常運作。細節見 [進階整合測試task0726.md](進階整合測試task0726.md) 收尾段落。

## 收尾

- **測試完畢後刪除測試資料**：本清單建立的 3 組帳號（X／Y／Z）、6 個角色（X-pub／X-priv／Y-pub／Y-priv／Z-pub／Z-priv）與其對話/訊息/摘要，測試驗證完成後應清除，避免殘留在資料庫與 Qdrant 中影響之後的測試或誤判為真實資料。

---

## 測試經驗回填（2026-07-25 實測，事後追查根因）

> 2026-07-26 併入說明：這節是另一輪對話（不同機器上的並行測試工作）針對第一次跑第六節時的 G2、H1 異常所做的追查記錄。結論（兩者皆非程式碼問題）已經反映在上面 G 系列 G2 案例的判決文字裡，這裡完整保留原始追查過程與測試方法論的教訓，供之後重跑測試時參考，避免同樣的方法論錯誤重演。

> 這節記錄第一次跑第六節時遇到的兩個異常（G2「失敗」、H1「查無角色」），並記下追查後的真正結論：**兩者都不是程式碼問題**，程式碼審查後確認邏輯正確；問題出在測試案例設計本身、以及測試腳本的執行方式。已經對照修正了上面 G2 的案例內容，這裡完整記錄追查過程，避免下次重跑再誤判成 bug。

### 發現一：G2 案例設計有誤，不是 internalAuthMiddleware 的 bug

- **原始症狀**：照原案例打 `POST http://localhost:8000/internal/users`（不帶任何 header），預期 403，結果卻是 201（帳號建立成功）。
- **追查過程**：讀了 `api-gateway/src/middlewares/internalAuthMiddleware.js` 全文（只有 27 行），確認邏輯是「來源 IP 通過白名單 → 無條件注入 `x-internal-request: true` → `next()`」，IP 檢查跟 header 注入是同一個 middleware 裡緊接著發生的動作，沒有「IP 過但 header 不注入」這種中間狀態。本機測試打 `localhost:8000`，來源 IP 必定落在白名單（`127.0.0.1`），所以透過 Gateway 打 `/internal/*`，一定會帶著已注入的 header 到下游，測試腳本自己送不送 header 完全不影響結果。
- **驗證方式**：改用直打 user-service 裸 port（`POST http://localhost:4000/users`，繞過 Gateway，不帶任何 header），結果正確回傳 `403 {error:"FORBIDDEN", message:"Forbidden"}`，證明 `userService.createUser` 的 `isInternalRequest` 檢查（`user-service/src/services/userService.js:4-7`）本身完全正確。
- **結論**：原案例的操作方式（打 Gateway）跟想驗證的情境（header 缺失）互相矛盾，這是**測試案例設計缺陷**。已在上面的 G2 表格列修正為直打裸 port，跟 H6 驗證 character-service 裸 port 授權用同一個思路。
- **啟示**：任何要驗證「下游服務在缺少某個由 Gateway 注入的 header 時會拒絕」的案例，都必須繞過 Gateway，直打裸 port，不能透過 Gateway 本身測——因為 Gateway 本來就是注入 header 的那一層，經過它，header 幾乎不可能缺席。

### 發現二：H1「查無角色」是測試腳本的帳號沒對齊，不是 character-service 的 bug

- **原始症狀**：用第一節建立的「X 帳號」去查 `GET /api/characters?authorId=<X的userId>`，預期至少查到 X-pub、X-priv 兩個角色，結果查到 0 筆。
- **追查過程**：
  1. 讀了 `character-service/src/services/characterService.js` 的 `listCharacters`（含 `authorId` 分支）、`characterController.js`、`characterRepository.js` 三層程式碼，邏輯都正確：`authorId` 查詢會用 `findManyByAuthorId(authorId)` 撈出該作者的角色，再用 `c.authorId === requesterId || c.visibility === 'public'` 過濾——自己查自己的角色，`authorId === requesterId` 天然成立，理論上不該漏掉。
  2. 懷疑是測試腳本的問題，改用 `GET /api/characters`（不帶 query，走合併查詢模式）搭配該帳號的 token 重新查詢，人工比對 `authorId` 欄位——結果顯示這個帳號名下角色數量是 **0**，但這個帳號可以看到的角色（自己的 + 全部 public）卻有 7 筆，代表它看得到別人建的 public 角色，唯獨沒有屬於自己的角色。
  3. 回頭比對整個測試過程：第一次執行「案例 1-2」（建帳號、建角色）時，PowerShell 腳本用的 email 是帶 `$(Get-Random)` 隨機數的（例如 `test-X-8823@test.com`），這個隨機值沒有被記錄或保存；後續執行 G/H/I/J/L 系列測試時，因為每個 PowerShell 呼叫都是全新的行程（變數不會跨 tool call 保留），改用了固定 email（`test-x-2026-07-25@example.com`）重新登入取得 token。這兩組 email **是完全不同的兩個帳號**，角色是建立在第一組隨機帳號底下的，第二組固定帳號從頭到尾沒有建過任何角色。
- **結論**：character-service 的查詢邏輯完全正確，是**測試方法論的問題**：測試身份（帳號 email）沒有在整個測試流程中保持一致，導致「用來查詢的帳號」跟「用來建立資料的帳號」對不上。根因是 PowerShell 每次工具呼叫都是獨立行程，變數不會保留到下一次呼叫，若沒有顯式地把關鍵資訊（email、userId、token、角色 id）落地保存（例如寫入暫存檔案），下一輪呼叫用「看起來一樣」的邏輯重新產生測試身份時，很容易產生跟前一輪對不上的新身份。
- **啟示／測試方式改進建議**：
  1. **測試身份要固定、可重現，不要用隨機值**：同一輪完整測試（跨多次工具呼叫）應該從頭到尾用同一組固定 email，不要中途改用隨機 email 或不同的固定 email。
  2. **關鍵 ID（userId、token、角色 id）要落地保存**，不要依賴記憶體變數跨 PowerShell 呼叫存活——例如寫進暫存 JSON 檔案，下一輪呼叫時讀回來，而不是重新註冊/重新查詢猜測。
  3. **查詢結果為空時要先假設是測試資料問題，而不是先假設程式碼有 bug**：可以先用「合併查詢」（不帶 query）或直接查 DB 之類更寬鬆的方式交叉比對，人工核對 `authorId` 欄位是否真的對得上，再決定要不要往程式碼裡找問題。
  4. **避免在 `catch {}` 裡靜默吞掉例外**：第一次追查 H1 時，測試腳本的錯誤處理是空的 `catch { }`，導致真正的失敗原因（查詢邏輯 vs. 資料本身沒有）被完全遮蔽，看不出來到底是哪個環節出錯。之後的除錯腳本改成印出完整的 HTTP 狀態碼、回應 body、例外訊息，才能一步步排除可能性。
