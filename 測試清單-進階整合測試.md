# 進階整合測試清單

> ⚠️ **執行前提：[測試清單-persona-nexus-auth.md](測試清單-persona-nexus-auth.md)、[測試清單-persona-nexus-character.md](測試清單-persona-nexus-character.md)、[測試清單-persona-nexus-lobby.md](測試清單-persona-nexus-lobby.md)、[測試清單-persona-nexus-chat.md](測試清單-persona-nexus-chat.md) 四份都要先測完、確認環境沒問題，才執行這份。** 這份測的是多帳號、多角色、跨帳號權限、跟摘要機制這種需要「規模」跟「時間累積」才測得出來的情境，前四份是單一情境的基礎功能驗證，這份是它們組合起來、在比較貼近真實使用規模下還會不會正確運作。
>
> 跟前四份一樣：全部用指令模擬前端會發的同樣請求（同 URL／method／payload），不開瀏覽器；允許建立測試帳號與角色，測完不用清理（除了案例本身就是在測「刪除」的部分）。

## 前置需求

- 環境（Docker/Ollama/ai-service）都要在跑，第 7 項會真的觸發多次 LLM 生成。
- **時間成本提醒**：第 7 項要靠實際聊天累積字數觸發摘要機制（門檻見下方依據），每次 AI 回覆都是真實 LLM 推論（實測約 20-40 秒/次），要觸發兩次摘要大概需要 8-15 輪對話，抓 10-20 分鐘的實測時間比較保險。想縮短時間可以把使用者訊息寫長一點，加速累積字數門檻，減少需要的來回次數。

## 依據（讀原始碼確認）

### 帳號與角色規模設計
沿用 auth/character 清單已經驗證過的端點（`POST /api/auth/register`、`POST /api/auth/login`、`POST /api/characters`），只是這次要建立**多組**：

- **3 組帳號**：X、Y、Z（各自獨立註冊，跟前四份清單用的帳號分開，避免資料互相干擾）
- **每組帳號建立 2 個角色**：1 個 `visibility:"public"`、1 個 `visibility:"private"`（必填欄位隨意填，只要通過 character-service 的必填驗證即可，這部分邏輯已經在 character 清單驗證過，這裡不重複測基礎 CRUD）
- 總共 6 個角色：3 public（X-pub／Y-pub／Z-pub）、3 private（X-priv／Y-priv／Z-priv）

### 權限過濾邏輯（character-service，已在其他清單驗證過基礎版本，這裡是多帳號規模下的延伸）
- `character-service/src/services/characterService.js:108-116`（`authorId` 查詢）：查自己的 `authorId` 會拿到全部（含 private），這裡在 3 個帳號上分別驗證。
- `characterService.js:102-107`（`visibility=public` 查詢）：回傳**全部使用者**的公開角色，這裡驗證 3 個帳號的 public 角色會**同時**出現在同一份清單裡。

### 跨帳號聊天權限（chat-service，這是前四份清單沒測過的路徑）
- `chat-service/src/services/conversationService.js:361-385`（`getOrCreateConversation`）呼叫 `serviceClient.getCharacter(characterId, userId)`。
- `chat-service/src/lib/serviceClient.js:18-53`：這個呼叫打 `GET {GATEWAY}/internal/characters/:id`，帶 `x-user-id: userId` header；下游 character-service 的 `getCharacter`（`character-service/src/services/characterService.js:90-97`）本身就有 `if (existing.visibility === 'private' && existing.authorId !== requesterId) throw FORBIDDEN`，這個 403 會被 `serviceClient.getCharacter` 攔截並重新拋出同樣的 `FORBIDDEN`（`serviceClient.js:45-48`），最終在 `conversationController.js` 映射成 `403 {message:"Access denied to this character"}`。
- 換句話說：**能不能開始聊天，權限判斷完全委託給 character-service 的角色可見性規則**，chat-service 自己沒有另外一套邏輯。public 角色任何人都能開聊，private 角色只有作者本人能開聊。

### 摘要機制觸發門檻（`chat-service/src/config/config.json`）
```json
{ "summary": { "threshold": 500, "maxWords": 70, "shortTermLimit": 2 } }
```
`conversationService.js:69-107`（`checkIfNeedsSummary`）：
- 每次成功生成 AI 回覆後都會檢查一次（在 `executeSummary` 呼叫點附近，訊息落地後觸發）。
- 計算方式：把**最新 2 條（`shortTermLimit`）以外**的所有未摘要訊息文字長度加總（`calculateHistoryLength`，純字元數，中文一字算一），達到 **500 字** 就觸發。
- 觸發後，被摘要的那批訊息全部標記 `summarized:true`、`summaryId:<同一個值>`（`summaryId` = ai-service/Qdrant 回傳的 point id，`conversationService.js:217-221`），摘要本身存進 Qdrant 的 `summaries` collection。
- 最新 2 條訊息永遠不會被摘要（短期記憶），所以每輪都會留一點「未摘要餘量」累積到下一次門檻。

### 刪除已摘要訊息時的清理邏輯（`conversationService.js:872-987`，這是這份清單最深入的部分）
`deleteMessageAndSubsequent` 是**回溯式刪除**：從目標訊息開始、含目標本身，**之後全部訊息一起刪除**（不是只刪一條）。刪除範圍決定後：
1. 收集刪除範圍內所有訊息的 `summaryId`（去重）——這些摘要涵蓋了即將被刪的訊息，全部要跟著刪。
2. 呼叫 `ai-service` 把這些 `summaryId` 對應的向量從 Qdrant 的 `summaries` collection 刪除（`serviceClient.deleteSummaries`）。
3. **關鍵細節**：如果某個要刪的摘要，涵蓋的訊息裡有一部分落在刪除範圍**之前**（也就是刪除點切在某個摘要批次中間），這些「陪葬摘要但本身沒被刪」的訊息會被**標回 `summarized:false, summaryId:null`**——因為摘要整份被刪了，不可能只留一半，這些訊息變回原始未摘要狀態，之後累積夠了會被納入下一次摘要。
4. 最後才刪 DB 裡的訊息列（真的 `DELETE`，不是軟刪除）。
5. 步驟順序刻意是「先刪 Qdrant、成功才動 DB」——如果 Qdrant 刪除失敗會直接拋錯中止，DB 完全不變。

**驗證工具**：倉庫根目錄本來就有 [list-summaries.ps1](list-summaries.ps1)，直接查 Qdrant 的 `summaries` collection（用 `conversation_id` 過濾）並跟 chat-service DB 的訊息交叉比對，會列出每份摘要涵蓋哪些訊息、有哪些訊息還沒摘要，**還內建了「DB 裡有 summaryId 但 Qdrant 找不到對應摘要」的孤兒一致性檢查**——這正好是這份清單要驗證的東西，不用自己重寫查詢邏輯。用法：`powershell -File list-summaries.ps1 <conversationId>`。

## 執行方式注意事項

跟其他清單一樣的 PowerShell 三個坑（`Get-Content` 字串轉型、`$_.ErrorDetails.Message` 抓錯誤 body、中文轉 UTF-8 bytes），helper 函式見 [測試清單-persona-nexus-auth.md](測試清單-persona-nexus-auth.md)。這份額外要注意：

- 第 7 項的多輪對話+輪詢建議整段用背景執行（`run_in_background`），不要同步等，理由同 chat 清單的提醒。
- 每送完一輪訊息，建議都跑一次 `list-summaries.ps1 <conversationId>` 看目前摘要數量，用來判斷什麼時候累積到第 2 份摘要、可以停止對話進入刪除測試，不用盲目多送幾輪浪費時間。

## 測試案例

### 一、建立規模資料

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| 1 | 註冊 3 組帳號 X／Y／Z | `POST /api/auth/register` × 3，各自 `POST /api/auth/login` 拿 token | 3 組都 `201`／`200`，各自拿到 JWT |
| 2 | 每組各建 2 個角色 | `POST /api/characters`（帶各自 token）：X 建 X-pub（`visibility:"public"`）與 X-priv（`"private"`），Y、Z 同理 | 6 個都 `201`，記下 6 個 `id` |

### 二、我的角色頁面（對應需求 3）

| # | 情境 | 請求 | 預期結果 |
|---|---|---|---|
| 3 | X 查自己的角色列表 | `GET /api/characters?authorId=<X的userId>`，帶 X 的 token | `200`，剛好 2 筆（X-pub、X-priv），不含 Y／Z 的任何角色 |
| 4 | Y 查自己的角色列表 | 同上，換 Y | `200`，剛好 2 筆（Y-pub、Y-priv） |
| 5 | Z 查自己的角色列表 | 同上，換 Z | `200`，剛好 2 筆（Z-pub、Z-priv） |

### 三、大廳公開角色列表（對應需求 4）

| # | 情境 | 請求 | 預期結果 |
|---|---|---|---|
| 6 | 任一帳號查公開角色列表 | `GET /api/characters?visibility=public`，帶任一組 token | `200`，**同時**包含 X-pub、Y-pub、Z-pub 共 3 筆，**不含**任何一個 private 角色（X-priv/Y-priv/Z-priv 都不該出現） |

### 四、跨帳號聊天權限（對應需求 5、6）

| # | 情境 | 請求 | 預期結果 |
|---|---|---|---|
| 7 | X 跟自己的 public 角色聊天 | `GET /api/conversations/character/<X-pub id>`（輪詢到 200），帶 X token | `200`，成功建立/取得對話 |
| 8 | X 跟自己的 private 角色聊天 | 同上，換 `<X-priv id>` | `200`，成功 |
| 9 | Y 跟自己的 public 角色聊天 | 同上，Y token + `<Y-pub id>` | `200`，成功 |
| 10 | Y 跟自己的 private 角色聊天 | 同上，Y token + `<Y-priv id>` | `200`，成功 |
| 11 | Z 跟自己的 public 角色聊天 | 同上，Z token + `<Z-pub id>` | `200`，成功 |
| 12 | Z 跟自己的 private 角色聊天 | 同上，Z token + `<Z-priv id>` | `200`，成功 |
| 13 | X 嘗試跟 Y 的 private 角色聊天 | `GET /api/conversations/character/<Y-priv id>`，帶 X token | **403**，message「Access denied to this character」 |
| 14 | X 嘗試跟 Y 的 public 角色聊天（對照組，證明擋的是 private 不是擋陌生人） | 同上，改 `<Y-pub id>` | `200`，成功（陌生人也能跟公開角色聊天） |
| 15 | Y 嘗試跟 Z 的 private 角色聊天 | `GET /api/conversations/character/<Z-priv id>`，帶 Y token | **403** |
| 16 | Z 嘗試跟 X 的 private 角色聊天 | `GET /api/conversations/character/<X-priv id>`，帶 Z token | **403** |

> 案例 7-12 建立的對話留著給第五節用，選其中一個（建議 X 跟 X-pub 的對話）繼續往下測。

> ✅ **已修復（2026-07-25，另一輪對話）**：原本案例 2 建立角色時如果沒填 `gender`，第五節一送訊息就會 422（詳見下方「實測結果」的原始記錄）。根因是 `ai-service` 的 `CharacterInfo.gender` 誤宣告成必填字串，與 character-service 這層本來就允許 `gender` 選填/`null` 的規則不一致。已修正為 `Optional[str] = None`，未設定性別時 system prompt 的「Gender」欄位整行省略。修復後角色**不需要**額外補 `gender` 也能正常進入第五節聊天測試，之前「先補 `gender` + 刪除重建對話」的繞過步驟已不再需要。詳見 [ai-service/CLAUDE.md](ai-service/CLAUDE.md)、[部署除錯測試.md](部署除錯測試.md) 第 13、17 節。

### 五、摘要機制與刪除聯動（對應需求 7，這份清單的核心）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| 17 | 選定對話，開始連續對話 | 用案例 7 建立的對話（X 跟 X-pub），反覆「送訊息 → 輪詢生成完成」，訊息內容盡量長一點加速累積字數 | 持續進行，每輪都成功拿到 AI 回覆 |
| 18 | 確認第 1 次摘要觸發 | 每輪後跑 `list-summaries.ps1 <conversationId>` | 累積到門檻後，摘要數量從 0 變成 1，可以看到有幾條訊息被標記涵蓋在這份摘要下 |
| 19 | 繼續對話，確認第 2 次摘要觸發 | 繼續案例 17 的迴圈 | `list-summaries.ps1` 顯示摘要數量變成 2，且第 2 份摘要涵蓋的是「未摘要的餘量」+ 新訊息，跟第 1 份涵蓋的訊息不重疊 |
| 20 | 記錄關鍵 id | 從 `list-summaries.ps1` 的輸出，找出**第 2 份摘要涵蓋的最早一條訊息**的 `id`（且該訊息 `role` 必須是 `user`，因為刪除端點只能刪使用者自己的訊息） | 記下這個 `messageId`，以及第 1、2 份摘要各自的 `summary_id` |
| 21 | 刪除該訊息（回溯式） | `DELETE /api/conversations/<id>/messages/<案例20的messageId>`，帶 X token | `200`，`deletedCount` 應涵蓋案例 20 訊息之後的所有訊息（含第 2 份摘要涵蓋的、跟後續未摘要的） |
| 22 | 驗證第 2 份摘要真的被刪了、第 1 份沒事 | 再跑一次 `list-summaries.ps1 <conversationId>` | 摘要數量從 2 變回 1；剩下的那份 `summary_id` 應該等於案例 20 記錄的**第 1 份**摘要 id（第 2 份消失，第 1 份完全沒被動到）；腳本內建的孤兒一致性檢查**不應該**印出任何警告 |
| 23 | 確認訊息真的從 DB 刪除 | `GET /api/conversations/<id>/messages`，帶 X token | 回傳的訊息列表在案例 20 那條（含）之後全部消失，只剩案例 20 之前的訊息 |

## 資料庫驗證

| # | 驗證時機 | 查詢 | 預期結果 |
|---|---|---|---|
| A | 案例 19（第 2 次摘要觸發）之後 | `sqlite3 chat-service/prisma/dev.db "SELECT id, role, summarized, summaryId FROM Message WHERE conversationId='<id>' ORDER BY createdAt"` | 可以看到訊息依序分成「summaryId=第1份」「summaryId=第2份」「summaryId=NULL（未摘要餘量）」三段，順序不會交錯 |
| B | 案例 21（刪除）之後 | 同一句查詢 | 案例 20 的 `messageId` 及其後全部查無資料；案例 20 之前、原本屬於第 1 份摘要的訊息 `summaryId` 應該還是第 1 份摘要的 id，**沒有**被誤標回 `NULL`（因為刪除範圍沒有切到第 1 份摘要的任何訊息，不該觸發「標回未摘要」的邏輯） |

## 不在這份清單範圍內（原因）

- **摘要內容本身的品質**（LLM 有沒有正確總結劇情、好感度那句有沒有寫）：這是 ai-service 生成品質的問題，不是「機制有沒有正確觸發、正確聯動刪除」，品質評測需要另外設計方式，不是這份清單的目標。
- **刪除點切在摘要批次「中間」的邊界情境**（依據段落提到的「陪葬摘要被刪、剩餘訊息標回未摘要」那個分支）：案例 20 刻意選在摘要批次的**起點**刪，所以不會觸發這個分支。這個更細的邊界情境值得之後另外設計案例驗證，但需要更精準控制刪除點位置（要能刪在某份摘要涵蓋範圍的中段），比較難單純靠对话輪數控制，這裡先不列為必測項目。
- **多帳號同時併發聊天的競態情況**（例如 X、Y、Z 同時對各自角色送訊息）：這份清單是循序執行，沒有測真正的併發負載，chat 清單案例 7 已經測過同一對話內的併發保護（409），跨對話/跨帳號的併發不在這份範圍。

---

## 第六節：微服務架構準則／Spec 合規性驗證

> 本節驗證的對象是 [微服務架構準則.md](微服務架構準則.md)、[微服務架構實作spec.md](微服務架構實作spec.md) 定義的規則，以及 [執行日誌.md](執行日誌.md) 記錄的 T1-T18、T20 這批修正是否真的落地生效。上面五節測的是「功能對不對」，這節測的是「架構規則有沒有被遵守」——路徑轉發、內部呼叫的信任鏈、授權統一模式、回應格式、非同步狀態持久化。
>
> 沿用本清單前面的原則：全部用指令模擬請求，不開瀏覽器；用第一節建立的帳號 X／Y／Z 與角色，不重新建立。若在執行第五節前中斷，本節案例對第一到四節的資料仍然有效（只依賴帳號、角色，不依賴摘要機制的狀態）。

### 前置需求

- Gateway（`:8000`）、auth-service（`:3000`）、user-service（`:4000`）、character-service（`:5000`）、chat-service（`:6000`）都要在跑。
- 部分案例（K 系列）需要重啟 chat-service（驗證持久化），重啟前後資料庫檔案（`chat-service/prisma/dev.db`）不能被刪除或替換。
- 部分案例故意繞過 Gateway 直打下游服務裸 port（驗證下游服務自己的授權邏輯不依賴 Gateway 這一層），需要知道各服務裸 port：user-service `:4000`、character-service `:5000`、chat-service `:6000`。

### 測試案例

#### G. Gateway 路徑轉發（對應 spec 第一部分）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| G1 | `userProxy` pathRewrite 是否正確補回 `/users` 前綴 | `GET http://localhost:8000/api/users/<X的userId>`，帶 X 的 token | `200`，回傳 X 的使用者物件（`id`、`email`，不含 `password`）——**不是** 404（這是 T1 修的既有 bug，驗證真的修好了） |
| G2 | `/internal/users` 不分 method、涵蓋 POST 語意 | `POST http://localhost:8000/internal/users` body `{id:"usr_test_g2", email:"g2test@test.com", password:"hashedplaceholder"}`，**不帶任何 header** | `403`，因為 `internalAuthMiddleware` 只認來源 IP，`localhost` 呼叫本身會通過 IP 檢查，但 user-service 的 `createUser` 還要求 `x-internal-request: true`——這裡驗證的是即使 IP 通過，若 Gateway 沒有正確注入 header，請求仍會被下游擋下（見下方 G3 對照組） |
| G3 | G2 的對照組：確認正常路徑真的能建立帳號 | 用 auth-service 的 `POST /api/auth/register` 走完整流程（body `{email:"g3test@test.com", password:"testpass123"}`） | `201`，回傳 `{id, email}`（新格式，直接物件，非 `{status,data}` 包裹）——這條路徑內部會呼叫 Gateway 的 `/internal/users`，成功代表 G2 失敗不是 Gateway 整條路由掛了，而是「沒帶 header 的請求」正確被擋 |

#### H. `x-internal-request` 全鏈路驗證（對應 spec 第二部分 Header 契約）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| H1 | character-service 內部路由允許查詢私有角色 | `GET http://localhost:8000/internal/characters/<X-priv的id>`（用第一節建立的 X-priv），**不帶任何 Authorization header** | `200`，回傳角色物件——因為經過 Gateway 的 `internalAuthMiddleware` 會注入 `x-internal-request: true`，character-service 的 `getCharacter` 讀到這個 header 後跳過 visibility 檢查 |
| H2 | H1 的對照組：同一個私有角色，走外部路由、無 token | `GET http://localhost:8000/api/characters/<X-priv的id>`，不帶 Authorization header | `401`，因為 `/api/*` 路由需要先過 `authMiddleware`，缺 token 直接被擋，連 character-service 都到不了 |
| H3 | H1 的對照組：同一個私有角色，走外部路由、帶別人的 token | `GET http://localhost:8000/api/characters/<X-priv的id>`，帶 Y 的 token | `403`，`x-user-id` 是 Y、角色擁有者是 X，且這條路由沒有 `x-internal-request`，所有權檢查生效 |
| H4 | chat-service 內部路由允許查詢對話訊息（不需 `x-user-id`） | 用第四節案例 7 建立的對話（X 跟 X-pub），`GET http://localhost:8000/internal/conversations/<conversationId>/messages`，不帶任何 header | `200`，回傳訊息陣列——驗證 chat-service 的 `assertConversationOwnership` 在 `isInternalRequest` 為真時跳過 `userId` 比對 |
| H5 | H4 的對照組：同一個對話，走外部路由、無 token | `GET http://localhost:8000/api/conversations/<conversationId>/messages`，不帶 Authorization header | `401`，`x-user-id` 缺失，被 service 層的 `validateUserId` 擋下（T10 移除獨立 middleware 後，這個判斷現在在 service 層，但外部行為應該不變） |
| H6 | 直打下游裸 port，繞過 Gateway，驗證下游不會誤信「看起來像內部呼叫」 | `GET http://localhost:5000/api/v1/characters/<X-priv的id>`（character-service 裸 port，不經 Gateway），不帶任何 header | `403`，因為沒有 `x-internal-request` header（這個 header 只有 Gateway 的 `internalAuthMiddleware` 會注入，直打裸 port 不會有），也沒有 `x-user-id`，會被判定為外部請求且無所有權，回 `FORBIDDEN` |

#### I. 授權統一模式（對應 spec 第二部分「授權檢查統一模式」）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| I1 | user-service 不再有獨立 middleware 擋在路由層——驗證行為對外不變 | `GET http://localhost:8000/api/users/<Y的userId>`，帶 X 的 token（X 查 Y 的資料） | `403`——T5 把 `authorizeSelf.js` 移除、邏輯搬進 service 層後，外部可觀察行為應該跟移除前完全一樣 |
| I2 | chat-service 不再有獨立 authMiddleware——驗證缺 `x-user-id` 時行為對外不變 | `GET http://localhost:8000/api/conversations`，帶一個**格式錯誤、驗證會失敗**的 Authorization header（例如 `Bearer invalid.token.here`） | `401`——這條請求在 Gateway 的 `authMiddleware` 就會被攔截（JWT 驗證失敗），不會帶著缺失的 `x-user-id` 傳到 chat-service，用來確認 Gateway 層的攔截沒有因為下游改動而受影響 |

#### J. 回應格式（對應 spec 第三部分）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| J1 | auth-service 錯誤回應格式 | `POST http://localhost:8000/api/auth/register` body 用已存在的 email（例如案例 2/G3 用過的） | `400`，body 是 `{"error": "EMAIL_ALREADY_EXISTS", "message": "..."}`——確認有 `error` 欄位、且是英文錯誤碼常數，不是 `{status:"error", message}` 舊格式 |
| J2 | user-service 錯誤回應格式 | `GET http://localhost:8000/api/users/<Y的userId>`，帶 X 的 token | `403`，body 是 `{"error": "FORBIDDEN", "message": "..."}` |
| J3 | user-service 操作成功（無資源）格式 | 建一個一次性測試帳號後（`POST /api/auth/register` 拿到 id 與 token），`DELETE http://localhost:8000/api/users/<剛建立的userId>`，帶該帳號自己的 token | `200`，body 是 `{"success": true, "message": "..."}`——**不是** `204 No Content`（T13 修的格式，這個一次性帳號測完即被刪除，不需要額外清理） |
| J4 | character-service 成功回應格式（清單） | `GET http://localhost:8000/api/characters?visibility=public`，帶任一組 token | `200`，body 直接是**陣列** `[{...}, {...}]`，不是 `{status:"success", data:[...]}` 包裹 |
| J5 | character-service 成功回應格式（單一資源） | `GET http://localhost:8000/api/characters/<X-pub的id>`，帶 X 的 token | `200`，body 直接是角色物件本身（有 `id`、`name` 等欄位在最外層），不是 `{status:"success", data:{...}}` |
| J6 | chat-service 業務語意欄位不該被誤判為格式違規（對照案例） | 用第四節案例 8-12 任一組帳號，`GET /api/conversations/character/<角色id>`（該對話已存在） | `200`，body 含 `status: "ready"`——這個 `status` 是業務語意（供前端輪詢判斷用），**不是**要驗證的格式錯誤，只是確認它依然存在、值域正確 |

#### K. 非同步狀態持久化（對應 spec 第四部分，需要重啟 chat-service）

> ⚠️ 這節需要手動重啟 chat-service，重啟前後資料庫檔案不能被刪除或替換（沿用同一個 `chat-service/prisma/dev.db`）。

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| K1 | 建立一個新對話，讓它進入 `preparing` 狀態 | 用第一節建立的某組帳號建一個新角色（避免跟既有對話衝突），`GET /api/conversations/character/<新角色id>`（第一次呼叫，RAG 初始化需要時間） | `202`，body `{status:"preparing"}` |
| K2 | **立刻**（RAG 初始化完成前）重啟 chat-service | 重啟 chat-service 進程（例如 Ctrl+C 後重新 `npm run dev` 或 `node src/app.js`） | 服務正常重新啟動，無報錯 |
| K3 | 重啟後查詢同一個對話的建立狀態 | 重啟完成後，立刻 `GET /api/conversations/character/<K1的角色id>` | 若 RAG 初始化在重啟前已經在背景完成，會是 `200 ready`；若還沒完成，應該是 `202 preparing`（狀態從資料庫讀出，不是「服務重啟後忘記這個對話存在、變成從頭建立一個新的」）——**這是本節最關鍵的案例**：進程內記憶體的舊版實作，重啟後 `creationJobs` 這個 Map 會直接清空，`getOrCreateConversation` 會偵測不到任何 job、重新走一次「沒有 job → 建立新 job」的流程，可能導致同一個角色被建立兩個對話（違反唯一性）或至少讓使用者重新等一次完整初始化；持久化版本因為狀態在資料庫，不會有這個問題 |
| K4 | 確認 K1 的角色沒有被建立出重複的對話 | `GET /api/conversations`，帶該帳號 token，過濾出 `characterId` 等於 K1 角色 id 的項目 | 剛好 1 筆，不是 2 筆 |
| K5 | AI 生成狀態欄位持久化（不需要真的生成，查詢現有已完成對話） | 用第五節已經產生過 AI 回覆的對話（案例 17 那組），重啟 chat-service 後，`GET /api/conversations/<conversationId>/ai-generation-status` | `200`，body 的 `status` 欄位不會是 `unknown`（如果第五節那組對話最後一次生成是 `completed`，重啟後應該還是讀得到 `completed`，不會因為重啟而重置成「沒有記錄」） |

#### L. `GET /api/characters` 免 query 合併查詢（對應 T20 新增行為）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| L1 | X 不帶任何 query 查詢角色列表 | `GET http://localhost:8000/api/characters`（不帶 `authorId`／`visibility`），帶 X 的 token | `200`，陣列內容 = X 的所有角色（X-pub、X-priv）聯集所有 public 角色（X-pub、Y-pub、Z-pub），依 `id` 去重後共 4 筆：X-priv、X-pub、Y-pub、Z-pub（X-pub 只出現一次，不因為它同時符合兩個條件而重複） |
| L2 | 確認既有 `?visibility=public` 場景行為不變 | `GET http://localhost:8000/api/characters?visibility=public`，帶 X 的 token | `200`，剛好 3 筆（X-pub、Y-pub、Z-pub），不含任何 private——跟 T20 之前的行為一致，證明新增分支沒有動到既有場景 |
| L3 | 確認既有 `?authorId=` 場景行為不變 | `GET http://localhost:8000/api/characters?authorId=<X的userId>`，帶 X 的 token | `200`，剛好 2 筆（X-pub、X-priv）——跟 T20 之前的行為一致 |

### 不在這節範圍內（原因）

- **`internalAuthMiddleware` 的 IP 白名單本身**（是否正確拒絕非 `127.0.0.1`／內網 IP 的請求）：這個機器上所有測試流量天然都是 `localhost`，沒有辦法在不改變測試環境網路拓樸的情況下，模擬一個「非白名單 IP」的請求來驗證會被拒絕。這屬於部署層的驗證，不是這份清單能覆蓋的範圍。
- **T19（ai-service 非同步狀態持久化）**：擱置中，尚未實作，沒有東西可測。
- **ai-service 的回應格式（T16）**：ai-service 的錯誤格式改動（`{error, message}`）只在真正觸發例外時才看得到差異（例如 Qdrant 斷線），正常聊天流程走不到這個分支，且第五節已經涵蓋了 ai-service 正常運作路徑的驗證，這裡不重複測。
- **`GET /internal/users/:id`**：spec 裡明確標注「保留供未來使用，目前無呼叫方」，沒有任何服務會實際呼叫這條路由，測了也只是空轉。

## 實測結果（2026-07-25）

**五節（案例 1-23）全部通過，第六節案例待執行。**

- 一至四節（案例 1-16，帳號/角色規模、我的角色頁面、大廳公開列表、跨帳號聊天權限）全部一次通過，沒有意外。
- 第五節（案例 17-23，摘要機制）：X 跟自己的 X-pub 角色連續對話 **7 輪**觸發了 **2 次摘要**（第 5 輪、第 7 輪）。刪除第二份摘要涵蓋範圍最早一條使用者訊息後，`deletedCount=8`（第二份摘要的 4 條 + 之後未摘要的 4 條），`list-summaries.ps1` 確認第二份摘要從 Qdrant 消失、第一份摘要的 `summary_id`／內容／涵蓋訊息**完全沒變**，沒有孤兒警告；DB 裡剩下的 7 條訊息全部一致指向第一份摘要，沒有任何訊息被誤標回未摘要——證實案例 20 刻意選在摘要批次起點刪除，沒有觸發「陪葬摘要、部分退回未摘要」那個更複雜的分支（見「不在範圍內」段落）。
- 過程中撞到一次已知的 `ai-service` `CharacterInfo.gender` 必填 bug（跟本次搬機器無關，先前已發現過、裁示不動），用編輯角色補 `gender` + 刪除重建對話的方式繞過，沒有改動任何程式碼，詳情見上方警告框與 [部署除錯測試.md](部署除錯測試.md)。此 bug 已於 2026-07-25（另一輪對話）正式修復，見上方警告框更新內容——之後重跑本清單不需要再繞過。

## 收尾

- **測試完畢後刪除測試資料**：本清單建立的 3 組帳號（X／Y／Z）、6 個角色（X-pub／X-priv／Y-pub／Y-priv／Z-pub／Z-priv）與其對話/訊息/摘要，測試驗證完成後應清除，避免殘留在資料庫與 Qdrant 中影響之後的測試或誤判為真實資料。

---

# 第六節：微服務架構準則／Spec 合規性驗證

> 本節驗證的對象是 [微服務架構準則.md](微服務架構準則.md)、[微服務架構實作spec.md](微服務架構實作spec.md) 定義的規則，以及 [執行日誌.md](執行日誌.md) 記錄的 T1-T18、T20 這批修正是否真的落地生效。上面五節測的是「功能對不對」，這節測的是「架構規則有沒有被遵守」——路徑轉發、內部呼叫的信任鏈、授權統一模式、回應格式、非同步狀態持久化。
>
> 沿用本清單前面的原則：全部用指令模擬請求，不開瀏覽器；用第一節建立的帳號 X／Y／Z 與角色，不重新建立。若在執行第五節前中斷，本節案例對第一到四節的資料仍然有效（只依賴帳號、角色，不依賴摘要機制的狀態）。

## 前置需求

- Gateway（`:8000`）、auth-service（`:3000`）、user-service（`:4000`）、character-service（`:5000`）、chat-service（`:6000`）都要在跑。
- 部分案例（G4、G5、H 系列）需要重啟 chat-service（驗證持久化），重啟前後資料庫檔案（`chat-service/prisma/dev.db`）不能被刪除或替換。
- 部分案例故意繞過 Gateway 直打下游服務裸 port（驗證下游服務自己的授權邏輯不依賴 Gateway 這一層），需要知道各服務裸 port：user-service `:4000`、character-service `:5000`、chat-service `:6000`。

## 依據（讀原始碼／文件確認）

### 為什麼要測「直打下游裸 port」
[微服務架構實作spec.md](微服務架構實作spec.md) 的「信任前提」明確寫：`x-internal-request: true` 之所以能被下游服務信任，前提是「下游服務本身不對外直接開放」，如果攻擊者能繞過 Gateway 直連下游，可以自己偽造這個 header。這個前提**不是**程式碼層面能驗證的（那是部署層防火牆規則的責任），但下游服務在「沒有這個 header」時的授權邏輯**是**可以驗證的——直打裸 port、不帶任何內部 header，應該要跟一般外部請求一樣被要求 `x-user-id` 或直接拒絕，不能因為「看起來像內部呼叫」就放行。

### `x-internal-request` 全鏈路驗證方式
[執行日誌.md](執行日誌.md) T2 記錄 Gateway 的 `internalAuthMiddleware` 現在會在 IP 驗證通過後注入這個 header；T8、T9+T10 記錄 character-service、chat-service 的 service 層現在會讀這個 header 放行。要驗證「全鏈路真的接通」，不能只測 Gateway 端（那只證明 Gateway 有注入），也不能只測下游服務端（那只證明下游服務邏輯本身正確），必須**經過 Gateway 的 `/internal/*` 路由**實際打一次，才能證明「注入」與「讀取」兩端真的對得上——這正是執行日誌裡多次提到「未啟動服務實測」的缺口，這節要補上。

### 回應格式驗證方式
[微服務架構實作spec.md](微服務架構實作spec.md) 第三部分定義的格式：錯誤 `{error, message}`、成功單一資源直接返回物件、成功清單直接返回陣列、操作成功（無資源）`{success, message}`。[執行日誌.md](執行日誌.md) T11-T16 記錄 5 個服務都已改動，但也記錄了刻意不動的業務語意欄位（例如 `getOrCreateConversation` 的 `status: 'preparing'/'ready'/'failed'`）——這節的測試案例要能區分「這個回應該符合新格式」跟「這個欄位是業務語意、不該被誤判成格式違規」。

### 非同步狀態持久化驗證方式
[執行日誌.md](執行日誌.md) T18 記錄 `creationJobs`、`aiGenerationStatus` 已經從進程內 Map 改成 `Conversation` 表欄位與 `ConversationCreationJob` 表。這個改動的**唯一**可觀察差異是「服務重啟後狀態還在不在」——這是進程內記憶體與持久化儲存的根本差異，沒有重啟這個步驟就測不出兩者的差別，其他行為（API 回應格式、授權邏輯）跟改動前應該完全一致。

## 測試案例

### G. Gateway 路徑轉發（對應 spec 第一部分）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| G1 | `userProxy` pathRewrite 是否正確補回 `/users` 前綴 | `GET http://localhost:8000/api/users/<X的userId>`，帶 X 的 token | `200`，回傳 X 的使用者物件（`id`、`email`，不含 `password`）——**不是** 404（這是 T1 修的既有 bug，驗證真的修好了） |
| G2 | `/internal/users` 不分 method、涵蓋 POST 語意 | `POST http://localhost:8000/internal/users` body `{id:"usr_test_g2", email:"g2test@test.com", password:"hashedplaceholder"}`，**不帶任何 header** | `403`，因為 `internalAuthMiddleware` 只認來源 IP，`localhost` 呼叫本身會通過 IP 檢查，但 user-service 的 `createUser` 還要求 `x-internal-request: true`——這裡驗證的是即使 IP 通過，若 Gateway 沒有正確注入 header，請求仍會被下游擋下（見下方 G3 對照組） |
| G3 | G2 的對照組：確認正常路徑真的能建立帳號 | 用 auth-service 的 `POST /api/auth/register` 走完整流程（body `{email:"g3test@test.com", password:"testpass123"}`） | `201`，回傳 `{id, email}`（新格式，直接物件，非 `{status,data}` 包裹）——這條路徑內部會呼叫 Gateway 的 `/internal/users`，成功代表 G2 失敗不是 Gateway 整條路由掛了，而是「沒帶 header 的請求」正確被擋 |

### H. `x-internal-request` 全鏈路驗證（對應 spec 第二部分 Header 契約）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| H1 | character-service 內部路由允許查詢私有角色 | `GET http://localhost:8000/internal/characters/<X-priv的id>`（用第一節建立的 X-priv），**不帶任何 Authorization header** | `200`，回傳角色物件——因為經過 Gateway 的 `internalAuthMiddleware` 會注入 `x-internal-request: true`，character-service 的 `getCharacter` 讀到這個 header 後跳過 visibility 檢查 |
| H2 | H1 的對照組：同一個私有角色，走外部路由、無 token | `GET http://localhost:8000/api/characters/<X-priv的id>`，不帶 Authorization header | `401`，因為 `/api/*` 路由需要先過 `authMiddleware`，缺 token 直接被擋，連 character-service 都到不了 |
| H3 | H1 的對照組：同一個私有角色，走外部路由、帶別人的 token | `GET http://localhost:8000/api/characters/<X-priv的id>`，帶 Y 的 token | `403`，`x-user-id` 是 Y、角色擁有者是 X，且這條路由沒有 `x-internal-request`，所有權檢查生效 |
| H4 | chat-service 內部路由允許查詢對話訊息（不需 `x-user-id`） | 用第四節案例 7 建立的對話（X 跟 X-pub），`GET http://localhost:8000/internal/conversations/<conversationId>/messages`，不帶任何 header | `200`，回傳訊息陣列——驗證 chat-service 的 `assertConversationOwnership` 在 `isInternalRequest` 為真時跳過 `userId` 比對 |
| H5 | H4 的對照組：同一個對話，走外部路由、無 token | `GET http://localhost:8000/api/conversations/<conversationId>/messages`，不帶 Authorization header | `401`，`x-user-id` 缺失，被 service 層的 `validateUserId` 擋下（T10 移除獨立 middleware 後，這個判斷現在在 service 層，但外部行為應該不變） |
| H6 | 直打下游裸 port，繞過 Gateway，驗證下游不會誤信「看起來像內部呼叫」 | `GET http://localhost:5000/api/v1/characters/<X-priv的id>`（character-service 裸 port，不經 Gateway），不帶任何 header | `403`，因為沒有 `x-internal-request` header（這個 header 只有 Gateway 的 `internalAuthMiddleware` 會注入，直打裸 port 不會有），也沒有 `x-user-id`，會被判定為外部請求且無所有權，回 `FORBIDDEN` |

### I. 授權統一模式（對應 spec 第二部分「授權檢查統一模式」）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| I1 | user-service 不再有獨立 middleware 擋在路由層——驗證行為對外不變 | `GET http://localhost:8000/api/users/<Y的userId>`，帶 X 的 token（X 查 Y 的資料） | `403`——T5 把 `authorizeSelf.js` 移除、邏輯搬進 service 層後，外部可觀察行為應該跟移除前完全一樣 |
| I2 | chat-service 不再有獨立 authMiddleware——驗證缺 `x-user-id` 時行為對外不變 | `GET http://localhost:8000/api/conversations`，帶一個**格式錯誤、驗證會失敗**的 Authorization header（例如 `Bearer invalid.token.here`） | `401`——這條請求在 Gateway 的 `authMiddleware` 就會被攔截（JWT 驗證失敗），不會帶著缺失的 `x-user-id` 傳到 chat-service，用來確認 Gateway 層的攔截沒有因為下游改動而受影響 |

### J. 回應格式（對應 spec 第三部分）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| J1 | auth-service 錯誤回應格式 | `POST http://localhost:8000/api/auth/register` body 用已存在的 email（例如案例 2/G3 用過的） | `400`，body 是 `{"error": "EMAIL_ALREADY_EXISTS", "message": "..."}`——確認有 `error` 欄位、且是英文錯誤碼常數，不是 `{status:"error", message}` 舊格式 |
| J2 | user-service 錯誤回應格式 | `GET http://localhost:8000/api/users/<Y的userId>`，帶 X 的 token | `403`，body 是 `{"error": "FORBIDDEN", "message": "..."}` |
| J3 | user-service 操作成功（無資源）格式 | 建一個一次性測試帳號後（`POST /api/auth/register` 拿到 id 與 token），`DELETE http://localhost:8000/api/users/<剛建立的userId>`，帶該帳號自己的 token | `200`，body 是 `{"success": true, "message": "..."}`——**不是** `204 No Content`（T13 修的格式，這個一次性帳號測完即被刪除，不需要額外清理） |
| J4 | character-service 成功回應格式（清單） | `GET http://localhost:8000/api/characters?visibility=public`，帶任一組 token | `200`，body 直接是**陣列** `[{...}, {...}]`，不是 `{status:"success", data:[...]}` 包裹 |
| J5 | character-service 成功回應格式（單一資源） | `GET http://localhost:8000/api/characters/<X-pub的id>`，帶 X 的 token | `200`，body 直接是角色物件本身（有 `id`、`name` 等欄位在最外層），不是 `{status:"success", data:{...}}` |
| J6 | chat-service 業務語意欄位不該被誤判為格式違規（對照案例） | 用第四節案例 8-12 任一組帳號，`GET /api/conversations/character/<角色id>`（該對話已存在） | `200`，body 含 `status: "ready"`——這個 `status` 是業務語意（供前端輪詢判斷用），**不是**要驗證的格式錯誤，只是確認它依然存在、值域正確 |

### K. 非同步狀態持久化（對應 spec 第四部分，需要重啟 chat-service）

> ⚠️ 這節需要手動重啟 chat-service，重啟前後資料庫檔案不能被刪除或替換（沿用同一個 `chat-service/prisma/dev.db`）。

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| K1 | 建立一個新對話，讓它進入 `preparing` 狀態 | 用第一節建立的某組帳號建一個新角色（避免跟既有對話衝突），`GET /api/conversations/character/<新角色id>`（第一次呼叫，RAG 初始化需要時間） | `202`，body `{status:"preparing"}` |
| K2 | **立刻**（RAG 初始化完成前）重啟 chat-service | 重啟 chat-service 進程（例如 Ctrl+C 後重新 `npm run dev` 或 `node src/app.js`） | 服務正常重新啟動，無報錯 |
| K3 | 重啟後查詢同一個對話的建立狀態 | 重啟完成後，立刻 `GET /api/conversations/character/<K1的角色id>` | 若 RAG 初始化在重啟前已經在背景完成，會是 `200 ready`；若還沒完成，應該是 `202 preparing`（狀態從資料庫讀出，不是「服務重啟後忘記這個對話存在、變成從頭建立一個新的」）——**這是本節最關鍵的案例**：進程內記憶體的舊版實作，重啟後 `creationJobs` 這個 Map 會直接清空，`getOrCreateConversation` 會偵測不到任何 job、重新走一次「沒有 job → 建立新 job」的流程，可能導致同一個角色被建立兩個對話（違反唯一性）或至少讓使用者重新等一次完整初始化；持久化版本因為狀態在資料庫，不會有這個問題 |
| K4 | 確認 K1 的角色沒有被建立出重複的對話 | `GET /api/conversations`，帶該帳號 token，過濾出 `characterId` 等於 K1 角色 id 的項目 | 剛好 1 筆，不是 2 筆 |
| K5 | AI 生成狀態欄位持久化（不需要真的生成，查詢現有已完成對話） | 用第五節已經產生過 AI 回覆的對話（案例 17 那組），重啟 chat-service 後，`GET /api/conversations/<conversationId>/ai-generation-status` | `200`，body 的 `status` 欄位不會是 `unknown`（如果第五節那組對話最後一次生成是 `completed`，重啟後應該還是讀得到 `completed`，不會因為重啟而重置成「沒有記錄」） |

### L. `GET /api/characters` 免 query 合併查詢（對應 T20 新增行為）

| # | 情境 | 操作 | 預期結果 |
|---|---|---|---|
| L1 | X 不帶任何 query 查詢角色列表 | `GET http://localhost:8000/api/characters`（不帶 `authorId`／`visibility`），帶 X 的 token | `200`，陣列內容 = X 的所有角色（X-pub、X-priv）聯集所有 public 角色（X-pub、Y-pub、Z-pub），依 `id` 去重後共 4 筆：X-priv、X-pub、Y-pub、Z-pub（X-pub 只出現一次，不因為它同時符合兩個條件而重複） |
| L2 | 確認既有 `?visibility=public` 場景行為不變 | `GET http://localhost:8000/api/characters?visibility=public`，帶 X 的 token | `200`，剛好 3 筆（X-pub、Y-pub、Z-pub），不含任何 private——跟 T20 之前的行為一致，證明新增分支沒有動到既有場景 |
| L3 | 確認既有 `?authorId=` 場景行為不變 | `GET http://localhost:8000/api/characters?authorId=<X的userId>`，帶 X 的 token | `200`，剛好 2 筆（X-pub、X-priv）——跟 T20 之前的行為一致 |

## 不在這節範圍內（原因）

- **`internalAuthMiddleware` 的 IP 白名單本身**（是否正確拒絕非 `127.0.0.1`／內網 IP 的請求）：這個機器上所有測試流量天然都是 `localhost`，沒有辦法在不改變測試環境網路拓樸的情況下，模擬一個「非白名單 IP」的請求來驗證會被拒絕。這屬於部署層的驗證，不是這份清單能覆蓋的範圍。
- **T19（ai-service 非同步狀態持久化）**：擱置中，尚未實作，沒有東西可測。
- **ai-service 的回應格式（T16）**：ai-service 的錯誤格式改動（`{error, message}`）只在真正觸發例外時才看得到差異（例如 Qdrant 斷線），正常聊天流程走不到這個分支，且第五節已經涵蓋了 ai-service 正常運作路徑的驗證，這裡不重複測。
- **`GET /internal/users/:id`**：spec 裡明確標注「保留供未來使用，目前無呼叫方」，沒有任何服務會實際呼叫這條路由，測了也只是空轉。
