# 佈署便利性改進清單

> 2026-07-24 從公司電腦搬到家用電腦、從零建置時踩到的坑（見 [部署除錯測試.md](部署除錯測試.md)），整理成待改進項目。每項附現況、問題、與具體修法建議。狀態依使用者裁示標記。

狀態圖示：☐ 待做　⏸ 暫緩（先不做）　👀 之後再檢查（非現在的動作項目）　✅ 已完成

---

## ✅ 1. 4 個前端 `vite.config.js` 統一設定

**現況／問題**：`persona-nexus-lobby`、`persona-nexus-chat` 有 `server.host: true` + `allowedHosts: true`（讓 Docker 裡的 Caddy 能透過 `host.docker.internal` 連進來），但 `persona-nexus-auth`、`persona-nexus-character` 當初漏改，這次才發現造成 `/login/`、`/character/` 502。4 個是**各自獨立的 git 倉庫**，沒有共用設定檔，複製貼上很容易再度漂移。

**限制**：4 個是獨立 repo（非 monorepo），無法用單一共用檔案 import——沒有現成的「跨 repo 共用設定」機制，除非額外發一個 npm 套件（對這個規模的專案太重）。

**建議修法**：
1. 把 4 個 `vite.config.js` 的 `server` 區塊統一成完全一致的寫法（除了 `port` 不同之外，其餘 key 順序、註解、值都要求逐字一致），現在就趁這次順手對齊。
2. 在根目錄新增一支輕量檢查腳本 `check-vite-configs.ps1`：掃描 4 個前端的 `vite.config.js`，確認都包含 `host: true` 和 `allowedHosts: true`，缺了就印警告。之後 `setup.ps1`（見第 6 項）跑完可以順便呼叫這支腳本做 sanity check，新增第 5 個前端時也能手動跑一次確認沒漏改。
3. 把這個 pattern 寫進 [ARCHITECTURE.md](ARCHITECTURE.md) 或 `deploy/README.md`，註明「新增前端專案時，`vite.config.js` 必須包含這兩個設定，否則 Caddy 容器連不進來」，避免下次又是靠事後除錯才發現。

**完成紀錄（2026-07-24）**：

- 順帶抓到一個比原本描述更嚴重的問題：`persona-nexus-auth`、`persona-nexus-character` 不只缺 `host`/`allowedHosts`，還**完全沒設定 `base`**，導致資產路徑（JS/CSS）用絕對路徑 `/src/...` 請求，被 Caddy 導到大廳的 catch-all，載入到錯的前端資產（跟 `deploy/README.md` troubleshooting 段落描述的故障模式一致）。已補上 `base: '/login/'`、`base: '/character/'`，並實測資產路徑確認 200。
- 4 個 `vite.config.js` 的 `server` 區塊（含縮排、註解、key 順序）已統一，`base` 的說明註解也統一寫法。
- 新增 [check-vite-configs.ps1](check-vite-configs.ps1)：檢查 `host`/`allowedHosts`/`base` 是否都符合預期，實測通過（4 個全部 `[OK]`）。
- Pattern 記進 [deploy/README.md](deploy/README.md) 新增的「Every frontend's `vite.config.js` must set three things」段落，並提及要跑 `check-vite-configs.ps1`。

---

## 👀 2. `deploy/README.md` 的 port 編號（3001-3005）與實際腳本（3000-6001）對不上

**現況**：`deploy/README.md`（same-origin-deployment 那份規格文件）寫的是 auth 3001／user 3002／character 3003／chat 3004／ai 3005，但 `start-all-services.bat` 和目前實際在跑的 `.env` 都是 3000／4000／5000／6000／6001。兩份文件互相矛盾。

**裁示**：現在不動，等哪天真的要跑 `deploy/` 那條 docker-compose 全容器化路徑時，再回頭核對這個 port 編號到底該用哪一套、兩份文件哪份要更新。

**追蹤方式**：先記在這裡，等第 1 項（Dockerfile 補齊）真的要做的時候一併處理，屆時應該同時解決 port 對不上的問題。

---

## ⏸ 3. `deploy/` docker-compose 全容器化路徑缺 Dockerfile

**現況**：`deploy/README.md` 自己都寫明「每個後端專案需要在根目錄有 `Dockerfile`」，但目前 6 個後端都沒有。`docker-compose up --build` 會直接失敗。

**裁示**：專案還在開發階段，不需要現在做全容器化部署。**暫緩，不在這次處理範圍內**。等專案進入要交付/正式部署的階段再回來做。

---

## ✅ 4. Ollama 模型名稱不寫死，只補文件

**現況**：`ai-service/src/config/config.json` 目前 commit 進 git 的 `ollama.model` 是 `gemma-26b:latest`，這是舊機器上自己重新命名過的本地模型，換機器完全查無此模型。

**裁示**：**不打算把模型綁死在 config 裡**，本來就希望每個人可以自己決定要用哪個模型。不改程式碼邏輯，只需要補一份清楚的 config 範例，明確告訴使用者這欄位要自己決定。

**建議修法**（下次要做時）：
1. 仿照 `.env` / `.env.example` 的模式，把 `ai-service/src/config/config.json` 加進 `ai-service/.gitignore`，改成只 commit `ai-service/src/config/config.example.json`。這樣以後每台新機器 clone 下來都不會繼承到別人選的模型，會被強迫走一次「自己決定」的步驟，從根本解決「換機器查無此模型」的問題。
2. `config.example.json` 裡 `ollama.model` 欄位用明確的佔位字串加註解性質的 key，例如：
   ```json
   {
     "ollama": {
       "model": "<choose-a-model-you-have-pulled-via-ollama-pull>",
       "_comment_model": "任何你自己 `ollama pull` 過的模型都可以，依你的 GPU VRAM 決定大小，不要求跟其他人一致"
     }
   }
   ```
3. `ai-service` 啟動時若偵測不到 `config.json`（只有 `config.example.json`），印出清楚的錯誤訊息並中止，而不是靜默用不存在的模型名稱噴一堆難懂的錯誤——這部分要看 `config_loader.py` 現在的行為，可能需要補一個明確的啟動檢查。

**注意**：這一項目前標成暫緩，因為只是「補文件/改 gitignore 結構」，不急迫；但邏輯本身（不綁死模型）已經是既定方向，不需要再討論，之後找時間做就好。

**完成紀錄（2026-07-25）**：三點都做完了。

1. 新增 [ai-service/src/config/config.example.json](ai-service/src/config/config.example.json)，內容跟 `config.json` 一致，只有 `ollama.model` 換成佔位字串 `<choose-a-model-you-have-pulled-via-ollama-pull>`，並加一個 `_comment_model` 說明欄位解釋怎麼用（JSON 沒有原生註解語法，用多一個 key 代替）。
2. `ai-service/.gitignore` 原本有一段註解特別寫「不要忽略 `config.json`，這是應該版本控制的配置模板」——這正是問題本身（把機器專屬的模型選擇當成共用模板進版控），已經拿掉，改成忽略 `src/config/config.json`，並用 `git rm --cached` 把它從 git 索引移除（只影響追蹤狀態，本機檔案完全沒動，`ai-service` 服務不用重啟）。
3. 第 3 點（找不到 `config.json` 時要印清楚錯誤並中止）**不用額外寫**——檢查 `ai-service/src/config/config_loader.py` 發現它本來就有 `if not config_path.exists(): raise FileNotFoundError(...)`，啟動時會印清楚的中文錯誤訊息（`❌ [config_loader] 加載配置失敗: 找不到配置檔案: ...`）並中止，已經符合需求。
4. [setup.ps1](setup.ps1) 原本第 3 步預留的「存在才處理、不存在就跳過」向前相容邏輯，補上實際的 bootstrap：`config.json` 已存在就跳過、不存在但 `config.example.json` 存在就複製過去並印警告提醒去改 `ollama.model`、兩個都沒有才跳過。兩種情境都手動模擬測過：(a) `config.json` 已存在時輸出 `[SKIP] ...已存在`；(b) 暫時把本機 `config.json` 改名成 `.bak` 模擬全新環境，跑過確認會正確從範本複製並印警告，再把原檔改回來，過程沒有動到正在跑的 `ai-service`（設定在啟動時就讀進記憶體了，跟磁碟上的檔案改名無關）。

**還沒做**：`ai-service` 這邊的改動（`.gitignore`、新增 `config.example.json`、`git rm --cached`）目前都還沒 commit，需要另外找時間進 `ai-service` 資料夾 commit + push。

---

## ✅ 5. `.bat` 腳本裡寫死單一開發者的機器路徑

**現況**：`start-backend-services.bat`、`start-all-services.bat` 裡的 `CONDA_ACTIVATE` 這次已經手動改成這台機器的路徑（`C:\ProgramData\anaconda3\Scripts\activate.bat`），但這只是治標——下次換機器又要重改一次。`start-all-services.bat` 裡的 ngrok 路徑（`C:\Users\MSI3090\ngrok`）也是一樣的問題，還沒修。

**建議修法**：
1. **conda 路徑改成自動偵測**：用 `where conda` 找到 `conda.exe` 的位置，往上推導出 `Scripts\activate.bat`，找不到才印錯誤訊息提示使用者手動設定，而不是預設寫死某個人的路徑。批次寫法大致是：
   ```bat
   for /f "delims=" %%i in ('where conda 2^>nul') do set CONDA_EXE=%%i
   if defined CONDA_EXE (
       for %%i in ("%CONDA_EXE%") do set CONDA_ACTIVATE=%%~dpiactivate.bat
   ) else (
       echo [錯誤] 找不到 conda，請確認已安裝並在 PATH 中，或手動設定 CONDA_ACTIVATE。
       pause & exit /b 1
   )
   ```
2. **機器專屬設定（ngrok 路徑等無法自動偵測的）獨立成一份 gitignored 的 local 設定檔**，例如 `start-config.local.bat`，每台機器自己建立一次（內容只有像 `set NGROK_PATH=C:\Users\xxx\ngrok` 這種本機才知道的值）。主腳本開頭用 `if exist start-config.local.bat call start-config.local.bat` 載入，沒有這個檔案就跳過 ngrok 相關區塊（而不是硬噴錯誤）。這樣機器差異就不會混進主腳本、不會被 commit，也不用每次換機器都改 tracked 的檔案。
3. 記得把 `start-config.local.bat`（或類似名稱）加進根目錄的 `.gitignore`。

**完成紀錄（2026-07-24）**：三點都照建議做完，並實測驗證過。

- 兩個 `.bat` 腳本開頭都改成用 `where conda` 自動偵測（取第一筆結果，避免多筆時互相覆蓋），偵測失敗或路徑不存在會清楚印錯誤訊息並中止，不會靜默用錯路徑繼續跑。實測：這台機器 `where conda` 抓到的是 `condabin\conda.bat`（跟 Bash 環境下的順序不同），推導出 `condabin\activate.bat`，另外驗證了這個路徑也能正確啟用 `ai-service` 環境（不是只有先前手動用的 `Scripts\activate.bat` 才行）。
- 新增 [start-config.local.bat.example](start-config.local.bat.example)（英文寫成，因為原本兩支 `.bat` 都是純 ASCII，中文註解在 cp950 下有機率跟 batch 特殊字元衝突，不值得冒險），文件裡的機器專屬變數目前是 `CONDA_ACTIVATE`（自動偵測失敗時的手動覆寫）和 `NGROK_PATH`。
- 兩個 `.bat` 開頭都會 `if exist start-config.local.bat call start-config.local.bat`，`start-config.local.bat` 本身已加進 `.gitignore`。
- `start-all-services.bat` 的 ngrok 區塊改成看 `NGROK_PATH` 是否有設定，沒設定就印提示訊息跳過，不會硬噴錯誤或用錯的人路徑。
- 實測：用一個暫時的 `start-config.local.bat`（手動設 `CONDA_ACTIVATE`／`NGROK_PATH`）驗證覆寫機制生效、且會跳過自動偵測，測完已清掉暫時檔案。

**追加修正（2026-07-25）**：使用者實際雙擊 `start-all-services.bat` 跑的時候，`ai-service` 那個視窗噴出 `BATCH RECURSION exceeds STACK limits`——問題出在 `condabin\activate.bat` 內部會再委派呼叫 `conda.bat`，在 `start-all-services.bat` 這種「`start cmd /k` 開新視窗、裡面又疊好幾層 `call`」的巢狀情境下，容易觸發 conda 在 Windows 上已知的批次檔遞迴 bug。這是我當初改自動偵測邏輯時只驗證過「能不能正確啟用環境」，沒驗證過「在這種巢狀 `start cmd /k` 情境下呼叫會不會出事」，屬於没測完整就當作修好的疏漏。

修法：**完全不用 `activate.bat`／`conda.bat`**，改成解析 `conda env list` 直接拿到 `ai-service` 環境的 `python.exe` 完整路徑，之後直接呼叫這個 `python.exe` 執行 `main.py`（跳過整套 activation 機制——單純跑一支腳本本來就不需要 activation，那只是為了讓 `python` 指令能在 PATH 裡解析到對的環境，直接給完整路徑效果一樣）。變數也從 `CONDA_ACTIVATE` 改名成 `AI_PYTHON_EXE`，`start-config.local.bat.example` 同步更新。實測：路徑含空格（`C:\Users\Guo Li-Wei\...`）解析正確，直接呼叫該 `python.exe` 也確認能正常 import `fastapi`/`langchain`/`qdrant_client`/`ollama`。

---

## ✅ 6. 一鍵 bootstrap 腳本 `setup.ps1`

**現況**：這次從零建置的整個過程——clone 11 個 repo、猜 `.env` 內容、裝 npm 依賴、處理 `npm approve-scripts`、跑 Prisma migration、建 conda 環境——全部是這次對話裡手動一步步做的，沒有任何腳本化，換一台機器或找人一起開發，得再重新走一次一模一樣的偵探過程。

**建議修法**：新增根目錄 `setup.ps1`，比照現有 `.bat` 腳本「重複執行安全」的原則設計成**冪等（idempotent）**——已存在的東西會跳過，不會重複出錯。建議流程：

1. **前置檢查**：確認 `git`、`node`／`npm`、`conda`、`docker`、`ollama` 都在 PATH 上，缺什麼就列出來並中止（而不是跑到一半才用難懂的錯誤訊息失敗）。
2. **Clone 11 個子專案**：讀 [MICROSERVICES.md](MICROSERVICES.md) 裡的 remote 清單（或未來把這份清單抽成腳本內的資料結構），資料夾已存在就跳過。
3. **產生 `.env`**：
   - 若 `auth-service/.env` 不存在，生成隨機 `JWT_SECRET`，同步寫進 `auth-service/.env` 與 `api-gateway/.env`。
   - 其餘欄位（`PORT`、`DATABASE_URL`、各種 `*_SERVICE_URL`）用這次確認過的固定值直接寫入 5 個後端服務的 `.env`（已存在則跳過，不覆蓋使用者手動改過的值）。
   - 若第 4 項的 `config.example.json` 機制已經做了，這裡也順便檢查 `ai-service/src/config/config.json` 是否存在，不存在就從 example 複製一份並提醒使用者要自己填模型名稱。
4. **`npm install`**：對 9 個 JS 專案跑，並自動處理 `npm approve-scripts --all`（這次踩到的 bcrypt/prisma 腳本被擋的坑，腳本化後就不會再忘記這一步）。
5. **Prisma**：對 `user-service`／`character-service`／`chat-service` 跑 `prisma migrate deploy` + `prisma generate`。
6. **ai-service conda 環境**：`conda env list` 檢查 `ai-service` 環境是否存在，不存在才 `conda create`，接著 `pip install -r requirements.txt`。
7. **收尾提示**：印出「還需要你自己做」的事項，例如 `ollama pull` 選一個模型、Docker/WSL2 若還沒裝要先裝好，並附上 `start-all-services.bat` 的使用說明。

**語言選擇**：用 PowerShell（`.ps1`）而不是延續現有 `.bat` 風格，因為這次的操作（JSON 讀寫、亂數生成、迴圈跑多個資料夾）用 PowerShell 明顯比 batch 好寫、好維護。這會讓專案裡同時存在 `.bat`（啟動用）和 `.ps1`（建置用）兩種腳本語言，是刻意的取捨——啟動腳本維持現狀不強求統一，避免不必要的重寫風險。

**完成紀錄（2026-07-24）**：[setup.ps1](setup.ps1) 已寫完，內容跟建議流程一致（前置檢查 → clone → `.env` → npm install/approve-scripts → Prisma → conda）。第 3 步的 `config.example.json` 部分因為第 4 項還沒做，先寫成「存在才處理，不存在就跳過」，向前相容。

實測跑了 3 次（在這台已經全部設定好的機器上，等於是最嚴格的冪等測試），過程中修掉兩個實測才發現的 bug：

1. **Windows PowerShell 5.1 對原生指令用 `2>&1` 會整個腳本中斷**：`npm`/`prisma`/`conda` 只要往 stderr 印任何一行（即使成功也會印，例如 Prisma 的 `Loaded Prisma config...`），`2>&1 | Out-Null` 會把這行包成 `NativeCommandError` 丟進 pipeline，腳本第一次跑到 `prisma migrate deploy` 就直接中斷退出。改成不對原生指令用 `2>&1`（只用 `| Out-Null` 讓 stdout 安靜，stderr 照原生方式印出，不會觸發這個問題）。
2. **抓 conda 環境路徑時，用空白字元切字串會被 Windows 使用者帳號名稱裡的空格（`Guo Li-Wei`）弄斷**：原本寫 `-split '\s+'` 取最後一段當路徑，遇到路徑本身含空格就抓錯。改成用正規表示式抓「環境名稱之後的全部內容」而不是「最後一個空白分隔的片段」。同時發現 conda 環境實際路徑是使用者層級的 `~\.conda\envs\ai-service`，不是 conda 安裝目錄下的 `envs\`——腳本原本假設的路徑結構本身也是錯的，一併修正為直接解析 `conda env list` 的輸出，不再用組合路徑用猜的。

另外也順手把 Prisma 那步的成功判定改成真的檢查 exit code：`chat-service` 因為它的 dev server 這次測試時剛好在跑，Windows 鎖住了 Prisma query engine 的 DLL 檔案導致 `prisma generate` 失敗（EPERM），腳本現在會正確印出 `[WARN]` 並提示「先關掉該服務的 dev server 再重跑」，而不是不管三七二十一都印 `[OK]`。這不是腳本的 bug，是 Windows 檔案鎖的正常限制，但至少現在不會謊報成功。

三次實測後，5 個 `.env`、10 個 repo、9 個 npm install 全部正確判斷「已存在→跳過」，conda 環境與 pip 依賴的偵測與安裝也驗證過完整跑一次是成功的。跑完之後重新確認 5 條 Caddy 路由仍全部 200，證實這些操作不會弄壞正在運作中的服務。

---

## ✅ 7. gateway 路由沒有統一補 `/api` 前綴，跟 Caddy 的同源代理對不上

**現況／問題**：這是 2026-07-24 同一天稍晚，追問「除了 ai-service，其他服務會不會有麻煩」時才挖出來的**新發現**，跟第 2 項（`deploy/README.md` 的 port 編號矛盾）是完全不同的兩個 bug，只是剛好都跟 Caddy 有關——第 2 項是講**還沒在用**的 docker-compose 全容器化路徑，這一項是講**現在正在跑**的 dev Caddyfile 路徑。

實測驗證（非猜測）：
- `localhost:8080/api/characters` → **404**（Caddy 把 `/api/*` 原樣轉給 gateway:8000，但 gateway 只定義了 `/characters`，沒有 `/api/characters`）
- `localhost:8000/characters`（bare，無前綴）→ 401 Unauthorized（路由本身沒問題，只是沒帶 token）

根因：gateway（`api-gateway/src/app.js`）的路由裡，只有 `/api/config` 這一個端點寫死帶了 `/api` 前綴，其餘 `/auth/register`、`/auth/login`、`/users`、`/characters`、`/conversations` 都是裸路徑，跟 Caddy「`/api/*` 不 strip prefix」的行為對不上。看起來是「同源部署」改造時只改了 `/api/config` 這個新端點，其餘既有路由沒有跟著改，是半成品遷移，不是這次搬機器造成的。

**目前受影響範圍**：
- `persona-nexus-lobby`、`persona-nexus-chat`：用相對路徑 `/api/...`，經 `localhost:8080` 存取時，除了 `/api/config` 之外**所有功能性 API 呼叫都 404**（角色列表、對話紀錄、傳訊息全部壞）。
- `persona-nexus-auth`、`persona-nexus-character`：目前是**直接寫死打 `http://localhost:8000`**，繞過 Caddy，本機測試不會發現這個問題，但也代表沒有真的走同源架構——換成用 ngrok 公開给同事看的情境（只轉發 8080）會壞。
- 登入成功後的導頁網址（`LOBBY_APP_URL`）目前也是吃 `/api/config` 回傳的裸 port 預設值（`http://localhost:5175`），同樣會把使用者導出 Caddy 代理範圍。

**裁示**：採用方向 1——gateway 路由全面補 `/api` 前綴，讓所有路由行為一致，符合 `deploy/README.md` 原本「`/api/*` 全部經由 gateway」的設計意圖。**現在就要修**，範圍：

1. `api-gateway/src/app.js`：4 個 proxy 路由掛載改成 `/api/auth/register`、`/api/auth/login`、`/api/users`、`/api/characters`、`/api/conversations`（`/internal/*` 系列是服務間內部呼叫，不經過 Caddy，不用改）。
2. `api-gateway/src/app.js` 的 `/api/config` 回傳預設值：`API_GATEWAY_URL`、`FRONTEND_WEB_URL`、`FRONTEND_CHARACTER_URL`、`FRONTEND_LOBBY_URL`、`FRONTEND_CHAT_URL` 的 fallback 全部改成基於 `http://localhost:8080`（而非裸 Vite/gateway port），讓 `persona-nexus-auth` 自動吃到正確網址、登入後導頁也會留在 Caddy 代理範圍內。
3. `persona-nexus-character/src/api.js` 的 `BASE_URL = 'http://localhost:8000'` 是唯一一個寫死常數、不會自動吃到 `/api/config` 修正的地方，要單獨改掉（改用相對路徑或動態讀 config）。
4. `persona-nexus-auth` 不用改，它已經是動態讀 `config.services.gateway`，第 2 點修好後自動對齊。

**注意**：`api-gateway`、`persona-nexus-character` 是各自獨立 git 倉庫，改完要分別進資料夾 commit + push 到各自 remote，不能只存在工作目錄——[MICROSERVICES.md](MICROSERVICES.md) 提醒過這點，很容易漏掉其中一個 repo 忘記 push。

**完成紀錄（2026-07-24）**：已照方向 1 修完並實測驗證通過。

- `api-gateway/src/app.js`：`/auth/register`、`/auth/login`、`/users`、`/characters`、`/conversations` 五個掛載點都加上 `/api` 前綴；`/api/config` 的預設值全部改成基於 `http://localhost:8080`。
- `api-gateway/src/proxies/authProxy.js`：`pathRewrite` 正規表示式從 `^/auth` 改成 `^/api/auth`（因為 `publicAuthProxy` 是用 `app.post(精確路徑)` 掛載，Express 不會 strip prefix，所以要手動同步）。`userProxy`／`characterProxy`／`chatProxy` 因為是用 `app.use(前綴, ...)` 掛載，Express 本來就會自動 strip 前綴，這三個檔案**不需要改**。
- `persona-nexus-character/src/api.js`：`BASE_URL` 從寫死的 `http://localhost:8000` 改成相對路徑 `/api`。
- `persona-nexus-auth` 未改動，驗證後確認它會自動吃到 `/api/config` 修正後的新網址。

實測結果（透過 `localhost:8080` 和直打 `:8000` 都測過）：`/api/characters`、`/api/users` 從 404 變成 401（路由對上了，只是沒帶 token，行為正確）；`/api/auth/login` 從 404 變成 400（有打到 auth-service 的驗證邏輯）；`/`、`/login/`、`/character/`、`/chat/`、`/api/config` 五條 Caddy 路由全部仍是 200，沒有連帶弄壞其他東西。

**還沒做**：`api-gateway`、`persona-nexus-character` 這兩個獨立倉庫目前只改在工作目錄，還沒 commit + push。

---

## 執行順序建議

0. ~~第 7 項（gateway `/api` 前綴）~~ ✅ 已修完並實測通過（2026-07-24），還差 `api-gateway`／`persona-nexus-character` 兩個倉庫的 commit + push。
1. ~~第 1 項（vite.config.js 對齊）＋ 第 5 項（`.bat` 路徑動態化）~~ ✅ 都已完成（2026-07-24）。
2. ~~第 6 項（`setup.ps1`）~~ ✅ 已完成並實測 3 輪冪等測試（2026-07-24），過程中連帶修掉兩個實測才發現的 PowerShell/conda 路徑 bug（見上方完成紀錄）。
3. ~~第 4 項（`config.example.json`）~~ ✅ 已完成並實測兩種情境（config.json 已存在時跳過／不存在時從範本複製）都正確（2026-07-25）。

**目前狀態（2026-07-25）**：第 1、4、5、6、7 項都做完了。剩下：

- 第 2、3 項：維持現狀（暫緩／之後檢查），等各自的觸發時機到了再處理。
- 未 commit + push 的異動：`api-gateway`（第 7 項）、`persona-nexus-character`（第 7 項）、`persona-nexus-auth`（第 1 項的 `base` 修正）、`persona-nexus-lobby`（第 1 項的註解統一）、`persona-nexus-chat`（第 1 項的註解統一）、`ai-service`（第 4 項的 `.gitignore` 修正、新增 `config.example.json`、`git rm --cached config.json`）——這 6 個獨立倉庫的修改目前都只在工作目錄，根倉庫這邊新增/修改的 [佈署便利性改進清單.md](佈署便利性改進清單.md)、[check-vite-configs.ps1](check-vite-configs.ps1)、[setup.ps1](setup.ps1)、[start-config.local.bat.example](start-config.local.bat.example)、`.gitignore`、`start-*.bat`、`deploy/README.md` 也還沒 commit。
