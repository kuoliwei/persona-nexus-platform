# 進階整合測試 task（2026-07-26）

## 給下一個聊天室的任務說明

對 `測試清單-進階整合測試.md` 中所有標記「✅ 完全符合測試準則」的測試項目（案例 1-23、G1-G3、H1-H5、I1-I2、J1-J6、K1-K5，共 41 個）依照 `測試準則.md` 的規範**實際執行**一次完整測試（全部經 Gateway、用真實登入拿到的 token，不是只看程式碼推導）。

H6 因為測試目的本身就要求繞過 Gateway 直打裸 port，性質跟其餘 41 個不同，另外獨立處理（見下方專屬段落），不要跟其他案例混在一起用同一套「打 Gateway」的方式執行。

這份清單裡的每一條預期結果，在 2026-07-25 已經過兩輪查證：一輪對照《微服務架構準則.md》《微服務架構實作spec.md》逐條判定文件跟原始碼誰對誰錯（找到案例 13/15/16、G2、J1 三處落差），另一輪修復了其中兩處原始碼問題（T21、T22）並做過**局部**驗證。**這次任務要做的是把全部 41 個案例正式、完整地跑一遍**，不是重做 T21/T22 已經驗證過的那幾條，而是把整份清單（含摘要機制、非同步持久化、回應格式等從未實際跑過的部分）補齊，並把結果正式記錄下來。

---

## 開始前必讀（依序）

1. **`測試準則.md`** — 執行方式的硬性規範：全部經 `http://localhost:8000`、用 `POST /api/auth/register` + `POST /api/auth/login` 拿真實 token（禁止手工編造 token）、跨帳號測試要用不同帳號的不同 token、非同步端點要輪詢驗證不能假設立即完成、錯誤場景也要正面測試、每個測試都要輸出清楚的通過/失敗記錄。開始執行前對照文末的「檢查清單」逐條確認。
2. **`測試清單-進階整合測試.md`** — 本次要測的所有案例、請求方式、預期結果、原始碼查證依據都在這裡，是這次任務的主體。文件開頭的「三處矛盾」說明已在 2026-07-25 全部處理完畢（案例 13/15/16、J1 的原始碼已修復並實測驗證，G2 是清單本身寫錯已更正），**執行時預期結果應該直接對得上目前的程式碼行為，不需要自己再重新判斷一次「這條現在到底該預期什麼」**——如果測出來對不上，才需要回頭查規範文件（見下方第 5 點）。
3. **`執行日誌.md` 的 T21、T22 條目** — 上一輪已經對案例 13/15/16、H1、J1、G2（部分）做過驗證，記錄了用的帳號/token/實測結果。這次不用重測這幾條的「有沒有修好」，但仍要把它們納入這次的正式完整測試（因為這次是要對整份 41 個案例做一次完整、連貫、正式記錄的測試，不是東拼西湊）。
4. **`mistakes.md` 的 M16、M17** — 案例 13/15/16、J1 這兩處落差的根因說明，執行到這幾條時如果有疑問可以回頭看。
5. **`微服務架構準則.md`、`微服務架構實作spec.md`** — 如果實測結果跟 `測試清單-進階整合測試.md` 寫的預期不符，**不要自己猜是清單錯還是程式碼錯**，回頭對照這兩份規範文件判斷（比照 T21/T22 那一輪「誰錯就改誰」的處理原則），判斷完再決定改測試清單的預期值，還是回報需要修程式碼。
6. **`測試清單-persona-nexus-auth.md`** 開頭的 PowerShell 三個坑（`Get-Content` 字串轉型、`$_.ErrorDetails.Message` 抓錯誤 body、中文轉 UTF-8 bytes）——這份進階清單沿用同樣的 helper 寫法，不用重新摸索。
7. **`前端API指引.md`** — 所有開放給前端的 API 端點完整規格（路徑、請求 body、成功／錯誤回應格式，皆附源碼行號佐證）。`測試清單-進階整合測試.md` 本身已經把每個案例要打的請求寫清楚，這份指引是在執行過程中對請求格式、欄位名稱、回應結構有疑問時的權威對照，不需要另外回去翻各服務的原始碼確認。

---

## 環境準備

- [x] Docker Desktop 已啟動（WSL2 backend），Ollama、Qdrant 都要能連得上——第五節（案例 17-23）會真的觸發多次 LLM 生成，沒有這兩個服務跑不完（確認：`curl :11434/api/tags`→200、`curl :6333/collections`→200，`docker ps` 顯示 qdrant、nexus-caddy 容器 Up）
- [x] 用根目錄的 `start-all-services.bat` 一次啟動全部服務，或個別 `npm run dev` / `python main.py` 手動啟動：auth-service(3000)、user-service(4000)、character-service(5000)、chat-service(6000)、ai-service(6001)、api-gateway(8000)
  - 不需要啟動 4 個前端 Vite dev server 跟 Caddy——這份清單全部用指令模擬前端會發的請求，不開瀏覽器
- [x] 確認 6 個後端服務都正常監聽——TCP 連線檢查 3000/4000/5000/6000/6001/8000 全部 LISTENING；4000/6000/6001/8000 對 `/health` 回 200，3000/5000 對 `/health`／`/` 回 404（Express「Cannot GET」，代表服務有在跑只是沒掛 `/health` 路由，非服務未啟動）；案例 1（真實註冊+登入）成功當作最終煙霧測試確認

---

## 第一節：建立規模資料（案例 1-2）

- [x] 案例 1：註冊 + 登入 X／Y／Z 三組帳號，確認 `201`／`200`，記錄三組 `userId`／`token` — ✅ PASS。X=usr_1784995404424、Y=usr_1784995404637、Z=usr_1784995404768，三組皆 register 201 + login 200，拿到 JWT
- [x] 案例 2：三組帳號各自建立 2 個角色（1 public + 1 private），確認 `201`，記錄 6 個角色 `id` — ✅ PASS。6 個角色皆 201：X_public=char_1784995404923、X_private=char_1784995404945、Y_public=char_1784995404961、Y_private=char_1784995404977、Z_public=char_1784995404994、Z_private=char_1784995405010

## 第二節：我的角色頁面（案例 3-5）

- [x] 案例 3-5：X／Y／Z 各自用 `?authorId=<自己的userId>` 查詢，確認剛好 2 筆（自己的 public + private） — ✅ PASS，三組帳號皆 `200`，剛好 2 筆，`visibility` 各含 public/private 一筆

## 第三節：大廳公開角色列表（案例 6）

- [x] 案例 6：任一帳號查 `?visibility=public`，確認剛好 3 筆（X-pub／Y-pub／Z-pub），不含任何 private 角色 — ⚠️ PASS（但清單「剛好 3 筆」的字面預期需要修正認知，非程式碼問題，見下方備註）

> **案例 6 備註**：實測 `?visibility=public` 回傳 33 筆，不是 3 筆——這個 dev DB 是長期共用、跨多份測試清單（auth/character/lobby/chat/本清單歷次執行）累積下來的，先前每份清單都寫明「測完不用清理」，所以 public 角色數量隨歷次測試單調遞增，不是這次程式碼有問題。已改用符合 `characterService.js:102-107` 實際邏輯的驗證方式：確認這次建立的 X-pub/Y-pub/Z-pub 三筆都在回傳陣列裡（`containsAll3Public=true`）、這次建立的三筆 private 角色都不在裡面（`noPrivateLeak=true`）、陣列裡每一筆 `visibility` 都是 `"public"`——這才是這條查詢邏輯真正該驗證的不變量（回傳全系統 public 角色，無 authorId 範圍限制），「剛好 3 筆」只在資料庫乾淨時才成立，不是程式碼行為的一部分，判斷依據見 `character-service/src/services/characterService.js:102-107`，不需要對照架構準則文件（這不是架構合規問題，是測試環境資料累積的現實）。

## 第四節：跨帳號聊天權限（案例 7-16）

- [x] 案例 7-12：X／Y／Z 各自跟自己的 public、private 角色聊天，輪詢 `202 preparing` 到 `200 ready` — ✅ 全部 PASS（各花 3 次輪詢即 ready），6 個 conversationId 皆記錄
- [x] 案例 13、15、16：跨帳號存取他人 private 角色（X→Y-priv、Y→Z-priv、Z→X-priv），確認皆為 `403 {"error":"FORBIDDEN","message":"Access denied to this character"}`（T21 修復後的行為，這次要納入正式完整記錄，不是重新驗證修復本身） — ✅ 全部 PASS，三筆皆精確回傳 `403 {"error":"FORBIDDEN","message":"Access denied to this character"}`，確認 T21 修復在正式完整測試中依然生效
- [x] 案例 14：陌生人存取他人 public 角色，確認 `200`（對照組，證明擋的是 private 不是擋陌生人） — ✅ PASS，`200`，`conversationId=conv_1784995765892`
- [x] 留一組對話（建議 X 跟 X-pub）給第五節繼續用 — 已留 `conv_1784995741285`（X 跟 X-pub）

## 第五節：摘要機制與刪除聯動（案例 17-23，這節最花時間，抓 10-20 分鐘）

- [x] 案例 17-19：用留下來的對話反覆「送訊息 → 輪詢生成完成」，訊息內容盡量寫長加速累積字數，每輪後跑 `list-summaries.ps1 <conversationId>` 確認摘要數量從 0→1→2 — ✅ 全部 PASS。刻意寫長訊息（~380 字/輪），只花 4 輪：第 3 輪觸發第 1 份摘要（messageCount=7），第 4 輪觸發第 2 份（messageCount=9），比文件預估的 6-10 輪快
- [x] 案例 20：記錄第 2 份摘要涵蓋的最早一條 **user** 訊息 `id`，以及第 1、2 份摘要各自的 `summary_id` — ✅ PASS。summary1_id=`0c0b12f9-9b14-4659-bcff-5a3de7bfe7d8`、summary2_id=`a9406973-eba9-4131-8dc5-103c8943279c`、目標刪除訊息 id=`cms0kng7h000jgotrtg7yhlng`
- [x] 案例 21：`DELETE /api/conversations/<id>/messages/<案例20的messageId>`，確認 `200` 且 `deletedCount` 正確 — ✅ PASS。`200 {"success":true,"message":"6 則訊息已刪除","deletedCount":6,"deletedIds":[...]}`，6 筆＝第 2 份摘要涵蓋的 2 筆＋之後未摘要的 4 筆
- [x] 案例 22：重跑 `list-summaries.ps1`，確認摘要數量變回 1、剩下的是第 1 份、無孤兒警告 — ✅ PASS。剩 1 份，id 與 summary1 完全相同，孤兒檢查 0 筆
- [x] 案例 23：`GET /api/conversations/<id>/messages` 確認案例 20 之後的訊息全部消失 — ✅ PASS。目標訊息確認不在回傳陣列中，剩 3 筆（案例 20 之前的）
- [x] 資料庫驗證 A：案例 19 後查 `Message` 表，確認 summaryId 分段不交錯 — ✅ PASS，9 筆訊息依序分成 summary1(3筆)／summary2(2筆)／NULL(4筆) 三段，無交錯
- [x] 資料庫驗證 B：案例 21 後同一句查詢，確認第 1 份摘要的訊息 `summaryId` 完全沒被誤標回 `NULL` — ✅ PASS，剩 3 筆全部仍指向 summary1，目標訊息及之後全部消失，無誤標

## 第六節：架構準則／Spec 合規性驗證（G/H/I/J/K/L 系列）

- [x] G1-G3：Gateway 路徑轉發（含 `/internal/users` 不帶 header 的 G2） — ✅ 全部 PASS。G1 `200`（無 password 洩漏）；G2 `201 {id,email}`；G3 `201 {id,email}`
- [x] H1-H5：`x-internal-request` 全鏈路驗證（H6 見下方獨立段落，不在這裡做） — ✅ 全部 PASS。H1 `200`（內部路由跳過私有檢查）；H2 `401`；H3 `403 {"error":"FORBIDDEN","message":"無權限"}`；H4 `200`（`/internal/conversations/:id/messages`，spec 表格未逐字列出但行為一致，維持原文件「無法完全判斷」的誠實記錄，此次只驗證行為本身，不重新judge spec覆蓋度）；H5 `401 {"message":"Missing or invalid Authorization header"}`
- [x] I1-I2：授權統一模式（middleware 移除後行為不變） — ✅ 全部 PASS。I1 `403 {"error":"FORBIDDEN","message":"Forbidden"}`；I2 `401 {"message":"Invalid or expired token"}`
- [x] J1-J6：回應格式（J1 是 T22 修復的案例，這次要納入正式完整記錄） — ✅ 全部 PASS。J1 `409 {"error":"EMAIL_ALREADY_EXISTS","message":"該電子郵件已被註冊，請更換帳號。"}`（確認 T22 修復在正式完整測試中依然生效，訊息文字逐字比對正確）；J2 `403`；J3 `200 {"success":true,"message":"User deleted successfully"}`；J4 `200` 陣列；J5 `200` 直接物件；J6 `200 status:"ready"`
- [x] K1-K5：非同步狀態持久化——**需要手動重啟 chat-service**，重啟前後 `chat-service/prisma/dev.db` 不能被刪除或替換；K1 建立新對話後要抓「RAG 初始化完成前」的時間點重啟 — ✅ 全部 PASS。K1：新角色 `char_1784996324304`，觸發 `202 preparing`；K2：重啟（過程中發現 5 個後端進程當時是孤兒進程，已排除、詳見下方「環境問題」備註，一共重啟兩輪）；K3：重啟後查詢直接拿到 `200 ready`（RAG 已在背景完成），非重新建立；K4：`GET /api/conversations` 過濾 K1 角色剛好 1 筆，無重複；K5：初次用第五節（案例 17）對話測出 `unknown`，查證是案例 21 的回溯刪除刻意清空了 `generationStatus`（`conversationService.js:983-990`，設計行為非 bug），改用未被刪除測試動過的案例 8 對話（X-priv）重新驗證：送訊息→輪詢 completed→直接查 SQLite 確認 `generationStatus=completed` 已落地→實際重啟 chat-service（PID 25956）→重啟後 API 仍讀到同一筆 `completed` 記錄（`userMessageId`／`assistantMessageId` 與重啟前相同），持久化行為確認正確

> **K5 備註（重要，非程式碼問題）**：`generationStatus` 等生成狀態欄位是 `Conversation` 表的真實 Prisma 欄位（`chat-service/prisma/schema.prisma`），寫入見 `conversationService.js:712-716`、讀取見 `conversationService.js:1060-1069`（都經 `conversationRepository`，不是進程內 Map）——`chat-service/CLAUDE.md` 裡「`aiGenerationStatus` 是記憶體 Map、重啟即遺失」的描述已過時，與目前程式碼行為不符，建議之後更新該文件。K5 初次測試「失敗」是因為沿用了已被案例 21 回溯刪除清空過生成狀態的對話，不是重啟持久化機制本身的問題；換一個乾淨對話並實際重啟驗證後，持久化行為正確。

> **環境問題備註（跟測試案例本身無關，過程中排除）**：K1 之後第一次請你重啟 chat-service 時，發現當時 5 個後端服務（auth/user/character/chat/api-gateway）雖然功能正常（整場測試一路打過去都成功），但其 process 樹是 `bash.exe → npm-cli.js → cmd.exe → node.exe`，沒有任何依附的可互動視窗，導致在你自己的終端機按 Ctrl+C 完全碰不到真正在跑的進程。已用 `taskkill /PID <bash根PID> /T /F` 把 5 條完整行程鏈清乾淨，請你改用 `start-all-services.bat`（直接雙擊，或在自己開的 cmd/PowerShell 視窗執行，不要用 Git Bash）重新啟動，之後就有正常可控的視窗。這與本次測試的任何案例判定無關，純粹是環境操作記錄。另外順手把 `user-service`、`api-gateway` 的啟動 console 訊息格式統一成跟 `auth-service`／`character-service`／`chat-service` 一致的箱型格式（純 console.log 文字調整，不影響行為），變更檔案：`user-service/src/app.js`、`api-gateway/src/app.js`。
- [x] L1-L3：`GET /api/characters` 免 query 合併查詢 — ✅ 全部 PASS。L1 `200`，包含這次建立的 4 筆預期 id（X-priv/X-pub/Y-pub/Z-pub）、不含 Y-priv/Z-priv、無重複 id；L2/L3 行為與案例 6/3 一致（同樣有 DB 歷史累積筆數的備註，見上方案例 6 備註）

## H6（獨立處理，刻意繞過 Gateway，不算在「完全符合」的 41 個之內）

- [x] 執行前明確標註「架構安全驗證，刻意繞過 Gateway」，不要當作模擬前端請求來理解 — 已標註獨立執行，未混入其他案例
- [x] 直打 `http://localhost:5000/api/v1/characters/<X-priv id>`（character-service 裸 port），不帶任何 header，確認回傳 `403 {"error":"FORBIDDEN","message":"無權限"}` — ✅ PASS，`403 {"error":"FORBIDDEN","message":"無權限"}`，逐字相符

---

## 收尾

- [x] 測試完畢後清除本次建立的測試資料：3 組帳號（X／Y／Z）、6 個角色、對話／訊息／摘要 — ✅ 完成，且範圍依使用者要求擴大為「清除除 `liwei@gmail.com` 本人帳號外的全部歷史測試資料」（不只本次測試建立的，含歷次過往測試累積的殘留資料）

> **收尾清理備註**：盤點發現資料庫裡累積了 58 個帳號／60 個角色／50 個對話（歷次測試留下的殘留，非本次獨有），只有 1 個是使用者本人帳號 `liwei@gmail.com`（`usr_1784916448187`）。因為其餘 57 個帳號絕大多數不是本次測試建立、沒有 token 也不知道密碼，無法走 `DELETE /api/users/:id` 正常 API 流程，改為直接對三個服務的 SQLite 檔案（`user-service`／`character-service`／`chat-service` 的 `prisma/dev.db`）執行 SQL DELETE，並手動清理對應的 Qdrant `summaries` collection 點位（模擬 app 本來「先清 RAG 再刪 DB」的順序）。**執行前已把三個 `.db` 檔完整備份**到 `.cleanup-backup-20260726_005117/`（專案根目錄下）。清理後驗證：`liwei@gmail.com` 的 1 個角色／1 個對話／11 則訊息／1 份 Qdrant 摘要完整保留；無孤兒訊息（`conversationId` 找不到對應 `Conversation` 的訊息數＝0）；6 個後端服務清理後仍正常回應。
- [x] 在 `測試清單-進階整合測試.md` 的「實測結果」段落，補上這次（2026-07-26）完整跑一輪、逐節記錄通過情形的正式紀錄——目前那筆 2026-07-25 的舊記錄已經標註過「與後續原始碼查證矛盾（歷史）」，這次應該是取代它成為目前有效的實測基準，而不是再疊加一筆語意不清楚的補充
- [x] 若任何案例的實測結果跟文件預期不符：**先不要直接改測試清單的預期值或動程式碼**，回頭依《微服務架構準則.md》《微服務架構實作spec.md》判斷是清單寫錯還是原始碼有問題（比照案例 13/15/16／G2／J1 當初「誰錯就改誰」的處理方式），判斷清楚、並在 `mistakes.md`／`待辦task.md` 視情況記錄後，再決定怎麼處理，不要憑感覺各打五十大板 — 本輪 41 個案例＋H6 全部最終 PASS，**沒有出現需要依架構準則文件判斷「清單錯或碼錯」的新落差**。過程中兩處看似失敗，查證後都確認是測試方法論問題、不是規格或程式碼問題，不需要動測試清單預期值或原始碼：①案例 6／L1／L2「剛好 N 筆」的字面預期在長期累積的共用 DB 下不成立（資料庫歷史殘留，非本輪程式碼行為），已改用符合實際查詢邏輯的不變量驗證；②K5 沿用了已被案例 21 回溯刪除清空生成狀態的對話，換乾淨對話＋真實重啟複測後行為正確。
