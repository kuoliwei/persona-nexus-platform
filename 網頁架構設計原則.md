# 網頁架構設計原則

> **這份文件是什麼**
> 定義 Persona Nexus **整個平台的網頁架構**——四個前端應用如何組合成使用者眼中的「一個網站」：
> 誰是外殼、誰是子頁、路徑怎麼分、身分怎麼傳、共用元件的契約是什麼。
>
> **與《前端系統設計原則.md》的分工**
>
> | | 《前端系統設計原則》 | 本文件 |
> |---|---|---|
> | 範圍 | **單一前端專案內部**的系統設計 | **跨前端專案**的組合方式 |
> | 回答的問題 | 「這個專案的程式碼寫得好不好？」 | 「這四個專案拼起來是不是一個合理的網站？」 |
> | 典型議題 | 模組邊界、DOM 生命週期、效能、可及性 | 外殼與子頁契約、路由劃分、身分傳遞、跨專案一致性 |
>
> 兩份文件互為補充：一個違反可能同時觸犯兩者（例如「訊息框元件各專案各寫一套」
> 在本文件是「跨應用一致性」違反，在《前端系統設計原則》是 DRY 違反）。
>
> **怎麼用**
> 與《前端系統設計原則》相同的兩步工作流——先立準則（本文件），再拿準則稽核程式碼。
> 每條原則附「**如何檢查違反**」作為稽核鉤子。文末〈附錄：現況對照〉記錄 2026-07-28
> 全面盤點的實際狀態，供對照使用。

---

## 零、架構基線（不可變動的前提）

> 這一節不是「原則」，是**既定的架構決策**。以下兩點是平台的骨架，
> 後續所有原則都建立在這個前提上；要變動必須是明確的架構決策，不能在日常修改中悄悄改掉。

### 基線 1：登入頁是獨立的完整頁面

`persona-nexus-auth` 掛在 `/login`，是一個**獨立的整頁應用**，不被任何外殼包覆。
使用者未登入時被導向此頁，**登入成功後才跳轉**進入平台主體。

**為什麼**：登入是「進入系統之前」的動作，此時使用者還沒有身分，
不該先載入需要身分才能運作的外殼（側邊欄要顯示聊天歷史、使用者資訊）。
讓登入頁獨立也使它成為唯一的身分入口，便於集中管理。

### 基線 2：大廳是外殼，其餘功能頁是嵌入其中的子頁

`persona-nexus-lobby` 掛在根路徑 `/`，是平台的**外殼（shell）**。它提供：

- **左側邊欄**——貫穿全站常駐，含首頁／創建角色／聊天歷史／登出
- **右側內容區**——唯一會被抽換的區域

聊天室、角色編輯、角色創建等功能頁，都是**被載入右側內容區的子頁**，
不是各自獨立的整頁應用。使用者在這些功能之間切換時，外殼不重載。

```
┌─────────────────────────────────────────┐
│ 外殼（lobby，路徑 /）                    │
│ ┌──────────┬──────────────────────────┐ │
│ │          │  內容區（唯一可抽換）      │ │
│ │ 側邊欄    │  ├ 首頁                   │ │
│ │ （常駐）  │  ├ 我的角色               │ │
│ │          │  ├ 聊天室    ← 子應用      │ │
│ │          │  └ 角色編輯／創建 ← 子應用  │ │
│ └──────────┴──────────────────────────┘ │
└─────────────────────────────────────────┘
       ↑ 未登入時導向 → /login（獨立整頁）
```

**為什麼**：側邊欄的聊天歷史需要跨功能持續存在；若每個功能都是獨立整頁，
切換功能就要重載側邊欄與其資料，體驗與效能都差。

---

## 一、單一來源（Single Origin）

### 原則 1.1：整個平台對外只有一個來源

所有前端與 API 都透過**同一個網域與埠**對外提供服務（由反向代理統一收口），
以**路徑**區分不同應用，而非以埠或子網域區分。

- **為什麼**：同源之下沒有 CORS 問題、`localStorage` 天然共用、Cookie 可直接共享，
  iframe 嵌入不受跨源限制。任何一項改成跨源，這四件事會同時變成需要額外處理的問題。
- **為什麼歸類在此**：這是所有其他架構決策的地基——身分傳遞、子頁嵌入、
  相對路徑 API 呼叫，全都依賴同源這個前提。
- **如何檢查違反**：程式碼裡是否出現寫死的 `http://localhost:<port>` 或完整網域的
  API／前端網址？前端呼叫 API 是否一律使用相對路徑？

### 原則 1.2：路徑前綴是應用的唯一識別，且必須全域互斥

每個前端應用擁有一段**專屬的路徑前綴**，由反向代理據此分流。
前綴一旦分配即為該應用獨佔，**其他應用的內部路由不得使用該前綴**。

- **為什麼**：反向代理依前綴分流，是在請求抵達任何前端程式碼**之前**就決定的。
  若外殼的內部路由撞上其他應用的前綴，平常用 `pushState` 切換不會出事
  （不發請求），但使用者一按重新整理，請求就會被代理送往錯誤的應用。
  這類 bug 只在重新整理／貼網址／開新分頁時發作，日常點擊測試抓不到。
- **如何檢查違反**：把反向代理設定裡的所有路徑前綴列成清單，
  再檢查外殼路由表中的每一條路徑，是否有任何一條落在別人的前綴底下？
- **配套要求**：新增路由前必須先查這份前綴清單；前綴清單應在代理設定與外殼路由
  兩處**互相引用註解**，讓改動任一邊的人看得到另一邊的存在。

### 原則 1.3：建置設定的資產路徑必須與代理路徑對齊

各應用的建置工具 `base` 設定，必須與反向代理分配給它的路徑前綴一致。

- **為什麼**：`base` 決定了建置產物中 JS／CSS 的引用路徑。若不一致，
  資產會以錯誤的絕對路徑請求，被代理的預設規則接走，載入到**另一個應用的資產**——
  症狀是頁面空白或行為詭異，而非明確的 404，極難診斷。
- **如何檢查違反**：逐一比對每個應用的 `base` 與代理設定中對應的路徑前綴是否字面相符
  （含結尾斜線）。

---

## 二、外殼與子頁的契約

> 這一節定義基線 2 中「外殼」與「子頁」的權責邊界。
> 邊界模糊是跨應用架構最容易出問題的地方——雙方都以為對方會處理，結果沒人處理。

### 原則 2.1：外殼擁有導覽與身分，子頁擁有功能

職責劃分必須明確且不重疊：

| 職責 | 歸屬 | 說明 |
|---|---|---|
| 路由與網址列狀態 | **外殼** | 子頁不得自行操作瀏覽器歷史 |
| 身分驗證與導向登入 | **外殼** | 子頁假定「能被載入就代表已登入」 |
| 全站導覽（側邊欄） | **外殼** | 子頁不得自行渲染導覽元素 |
| 功能本身的畫面與邏輯 | **子頁** | 外殼不得干涉子頁內部 |
| 功能內的錯誤與提示 | **子頁** | 顯示在子頁自己的範圍內 |
| 跨功能的錯誤與提示 | **外殼** | 例如側邊欄操作失敗 |

- **為什麼**：職責重疊會導致同一件事被做兩次（兩套路由互相覆蓋）
  或都沒做（雙方都以為對方會顯示錯誤）。
- **如何檢查違反**：子頁的程式碼裡是否出現 `history.pushState`／`replaceState`？
  是否自行檢查登入狀態並導向登入頁？是否渲染了導覽列？

### 原則 2.2：子頁回到外殼必須透過明確的通道，不得直接操作外殼

子頁需要讓外殼做事（例如「儲存完成，請帶我回列表頁」）時，
必須透過**雙方約定的通道**，不得直接改寫外殼的網址或 DOM。

- **為什麼**：子頁直接操作外殼，等於子頁必須知道外殼的內部細節
  （外殼的路由長什麼樣、DOM 結構如何）。這是最典型的模組邊界外洩：
  外殼日後調整路由，所有子頁一起壞掉，而且壞在別的專案裡，很難聯想。
- **如何檢查違反**：子頁程式碼中是否出現 `window.parent`／`window.top`
  的直接賦值（尤其是改 `location`）？是否有子頁寫死了外殼的路由字串？
- **建議的通道**：跨文件情境用 `postMessage` 定義明確的訊息型別；
  同文件情境用外殼提供的回呼函式。無論哪種，**通道的訊息格式就是契約**，
  應與 API 契約一樣被記錄下來。

### 原則 2.3：嵌入方式應與職責相稱

子頁的嵌入方式（同文件動態載入 vs. `iframe`）應依實際隔離需求選擇，
且**同一類子頁應採用一致的方式**。

- **為什麼**：`iframe` 提供強隔離（獨立的 CSS、JS 執行環境、歷史堆疊），
  代價是溝通變困難（跨文件）、樣式無法共享、載入成本較高。
  同文件載入則相反。混用兩種方式而無明確標準，會讓「這個子頁能不能用共用元件」
  這種基本問題每次都要重新確認。
- **如何檢查違反**：列出所有子頁與其嵌入方式，能否說明每個選擇的理由？
  同性質的子頁是否採用了不同方式？

---

## 三、身分與狀態的跨應用傳遞

### 原則 3.1：身分憑證有唯一的儲存位置與存取介面

整個平台對「使用者憑證存在哪裡、用什麼鍵名、如何讀寫」只能有一套答案，
且每個應用都透過**相同的存取介面**使用它，而非各自直接操作儲存層。

- **為什麼**：這是 SSOT 在跨應用層級的體現。若憑證的鍵名、格式、
  過期處理散落在各應用各寫一份，任何調整（換儲存方式、加過期檢查、
  改鍵名）都必須同時改動所有應用，漏改一個就產生「某個功能突然要求重新登入」
  這類難以追查的問題。
- **如何檢查違反**：搜尋所有應用中直接存取儲存層的程式碼（如 `localStorage.getItem`），
  是否有多個應用各自實作了讀寫憑證的邏輯？各應用使用的鍵名是否一致？

### 原則 3.2：同源之下不應以網址傳遞憑證

當所有應用同源時，憑證已經天然共享，**不應再透過網址查詢字串傳遞**。

- **為什麼**：網址中的憑證會被寫進瀏覽器歷史、被 `Referer` 標頭帶往外部、
  出現在伺服器與代理的存取日誌、被使用者複製貼上時意外外洩。
  這在跨源架構下是不得已的權衡，但同源之下**完全沒有必要**，
  是純粹的風險而無對應收益。
- **如何檢查違反**：跨應用跳轉或嵌入子頁時，網址是否帶有憑證參數？
  若有，該傳遞是在同源前提下發生的嗎？
- **注意**：這類寫法常是**跨源時期的遺留**。架構改為同源後若未一併清除，
  就會留下這種「已無必要但仍在運作」的傳遞路徑，因為它不會報錯，
  只會靜靜地把憑證灑進日誌。

### 原則 3.3：登入狀態的判定集中在外殼

「使用者是否已登入」的判定與「未登入時導向何處」的決策，
應由外殼在單一位置完成，子頁不重複實作。

- **為什麼**：若每個子頁各自檢查登入狀態，判定邏輯（憑證是否存在、
  是否過期、格式是否正確）會有多份實作，容易不一致——
  出現「外殼認為已登入、子頁認為未登入」的矛盾狀態。
- **如何檢查違反**：子頁是否有自己的「無憑證則導向登入頁」邏輯？

---

## 四、跨應用的一致性

> 這一節處理的是**多專案並存**特有的問題：同一件事在四個地方各做一次，
> 而且每次都做得不太一樣。

### 原則 4.1：使用者可感知的共用元件必須有統一契約

在多個應用中都會出現的 UI 元件（訊息提示、確認對話框、載入指示、
選單定位），必須有**統一的行為契約**：相同的觸發時機、相同的視覺語彙、
相同的自動消失規則、相同的可及性屬性。

- **為什麼**：使用者眼中這是「一個網站」，不會知道畫面背後換了應用。
  同一種錯誤在 A 功能停留 3 秒自動消失、在 B 功能永久停留、
  在 C 功能是浮動提示，會讓使用者無法建立穩定預期。
- **如何檢查違反**：列出各應用的提示類元件，比較它們的
  （a）元素識別名稱（b）呈現方式（常態排版 vs 浮動）（c）自動消失規則
  （d）可及性屬性——是否存在無法用「情境不同」解釋的差異？
- **落實方式的取捨**：理想是抽成共用套件由各應用引用；
  若專案結構不允許（如各自獨立的版控倉庫且無共用發布機制），
  **至少要有一份書面契約**明訂行為規範，讓各自實作能對齊。
  「各寫各的、也沒有契約」是唯一不可接受的狀態。

### 原則 4.2：跨應用重複的程式碼必須有明確歸屬

同一份邏輯出現在多個應用時，必須明確決定它的歸屬：抽成共用套件、
或指定一份為權威來源並記錄同步方式。

- **為什麼**：跨倉庫的重複程式碼比單一專案內的重複更危險——
  單一專案內至少搜尋得到全部副本，跨倉庫則連「有幾份副本」都不容易知道，
  修了一份而其他份還帶著 bug 的情況幾乎必然發生。
- **如何檢查違反**：跨應用比對同名檔案的內容（可用雜湊值快速比對），
  是否存在完全相同或高度相似的檔案？若有，是否有任何機制保證它們保持同步？

### 原則 4.3：跨應用的命名慣例必須一致

元素識別名稱、儲存鍵名、路由命名等跨應用可見的識別字，
應遵循同一套命名慣例。

- **為什麼**：命名不一致本身不會造成故障，但會讓「這兩個是不是同一個東西」
  這個判斷變得需要逐一確認，稽核與重構成本因此升高。
  它也是「各做各的」最明顯的表面症狀，通常伴隨更深層的契約缺失。
- **如何檢查違反**：跨應用蒐集同類識別字，檢查大小寫風格是否統一
  （如全部 `kebab-case` 或全部 `camelCase`）。

---

## 五、可診斷性

### 原則 5.1：跨應用的失敗必須指出是哪一環

當請求跨越多個環節（瀏覽器 → 反向代理 → 前端應用 → API 閘道 → 服務）時，
失敗訊息應能讓人判斷斷在哪一環，而非只顯示籠統的「載入失敗」。

- **為什麼**：這類架構的故障排除成本主要花在「定位是哪一層壞了」。
  代理設定錯誤、應用未啟動、API 不可用這三種情況，
  在使用者端可能呈現完全相同的畫面，但修法完全不同。
- **如何檢查違反**：把各層依序關掉，觀察使用者看到的畫面是否可區分？

### 原則 5.2：架構層的關鍵約束必須就地註記

路徑前綴互斥、`base` 對齊、憑證傳遞方式這類**跨檔案、跨專案**的約束，
必須在相關的每個位置留下註解，指向另一端。

- **為什麼**：這類約束的特性是「單看任何一個檔案都看不出問題」。
  沒有就地註記，下一個人只能靠踩雷來重新發現，
  而這些雷通常在重新整理、正式建置等非日常路徑上才會炸。
- **如何檢查違反**：代理設定、各應用建置設定、外殼路由表——
  三處是否互相提及對方的存在與約束？

---

## 附錄：現況對照（2026-07-28 全面盤點）

> 以下為撰寫本文件時實地讀取程式碼的盤點結果，**非推測**，
> 每項均附可覆核的檔案位置。此附錄只描述現況，**不代表已決定要修**。

### 符合的部分

| 原則 | 現況 |
|---|---|
| 1.1 單一來源 | ✅ 全平台經 Caddy 收口於 `:8080`，前端 API 呼叫皆為相對路徑 `/api/*` |
| 1.2 前綴互斥 | ✅ `/login`／`/character`／`/chat` 三個前綴已分配，外殼刻意用 `/rooms/` 而非 `/chat/` 迴避衝突（`persona-nexus-lobby/src/main.js` 有註解說明） |
| 1.3 base 對齊 | ✅ 四個應用的 `vite.config.js` `base` 與 Caddy 前綴逐一相符 |
| 3.3 登入判定集中 | ✅ 外殼 `main.js` 統一判定，未登入導向 `/login/` |
| 5.2 就地註記 | ✅ Caddyfile、各 `vite.config.js`、`main.js` 三處皆有交叉註解 |

### 落差項目

**落差 1｜同源之下仍以網址傳遞憑證（原則 3.2）**

- 登入成功後：`persona-nexus-auth/src/main.js:167`
  `window.location.href = \`${LOBBY_PATH}?token=${...}\``
- 嵌入子頁時：`persona-nexus-lobby/src/iframe-loader.js` 將憑證組進查詢字串
- 子頁接收：`persona-nexus-chat/src/main.js:13`、
  `persona-nexus-character/src/create.js:32`／`edit.js:35` 皆從網址讀取後寫入儲存層

四個應用同源（皆經 `:8080`），儲存層本已共享；外殼 `main.js` 亦已有
「讀取後從網址移除」的清理邏輯，佐證此為跨源時期遺留。

**落差 2｜憑證存取邏輯四處各自實作（原則 3.1）**

鍵名一致（皆為 `token`），但存取介面各自為政：

| 應用 | 位置 |
|---|---|
| lobby | `src/api.js` — `getToken`／`setToken`／`clearToken` |
| chat | `src/session.js` — 另一套同名函式 |
| character | `src/api.js`、`create.js:35`、`edit.js:35` — 直接呼叫 `localStorage` |
| auth | 未封裝 |

**落差 3｜提示元件四套實作、行為各異（原則 4.1、4.3）**

| 應用 | 機制 | 元素識別名 | 呈現方式 |
|---|---|---|---|
| lobby | `src/message-utils.js` | `message-box` | 常態排版區塊 |
| character | `create.js`／`edit.js` 各自直接操作 | `message-box` | 常態排版區塊 |
| auth | `main.js` 內嵌 `showMessage` | **`messageBox`** ← 命名不一致 | 常態排版區塊 |
| chat | `src/toast.js` | — | 浮動提示 |

自動消失規則亦不一致，同一應用內的不同呼叫點都有差異
（詳見《前端網頁debug_task_checklist.md》代辦 A／B 的補充調查）。

**落差 4｜跨倉庫的完全重複檔案（原則 4.2）**

`persona-nexus-lobby/src/menuPosition.js` 與 `persona-nexus-chat/src/menuPosition.js`
**內容位元組完全相同**（MD5 皆為 `630a70038ef906682725c9aa5178d28a`），
分屬兩個獨立版控倉庫，無任何同步機制。

**落差 5｜子頁直接操作外殼網址（原則 2.2）**

`persona-nexus-character/src/create.js:59`、`edit.js:81`、`edit.js:104`
三處使用 `window.parent.location.href = MY_CHARACTERS_URL`，
子頁因此寫死了外殼的路由字串。原始碼註解顯示這是刻意選擇
（「避免在 iframe 內跳轉」），但未走任何約定通道。

**落差 6｜子頁嵌入方式與職責的對應未成文（原則 2.3）**

外殼的內容區同時存在兩種子頁：

- **同文件載入**：首頁、我的角色（`fetch` HTML 片段後填入內容區）
- **`iframe` 嵌入**：聊天室、角色編輯／創建（`chat.html`／`character-edit.html`
  各僅 5 行，內容為單一 `<iframe>`）

差異的成因可理解（後者屬不同應用），但未有成文標準說明分界，
且此差異直接影響共用元件可用性——`iframe` 內的子頁無法使用外殼的提示元件。

---

## Sources

- Michael Geers — [Micro Frontends](https://micro-frontends.org/)（微前端組合模式、
  團隊自治與整合方式的分類）
- Cam Jackson, Martin Fowler's site — [Micro Frontends](https://martinfowler.com/articles/micro-frontends.html)
  （組合方式取捨：建置期整合 vs 執行期整合 vs iframe）
- MDN Web Docs — [Same-origin policy](https://developer.mozilla.org/docs/Web/Security/Same-origin_policy)、
  [Window.postMessage()](https://developer.mozilla.org/docs/Web/API/Window/postMessage)
- OWASP — [Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)
  （憑證不應置於 URL 的理由）
- David Parnas（1972）— Information Hiding（模組邊界，套用至跨應用邊界）
- Hunt & Thomas,《The Pragmatic Programmer》— DRY（套用至跨倉庫重複）
- Jakob Nielsen, Nielsen Norman Group — [Consistency and Standards](https://www.nngroup.com/articles/ten-usability-heuristics/)
