# 程式撰寫設計原則

> **這份文件是什麼**
> 一套**通用的**程式撰寫層面設計原則，涵蓋程式碼粒度（檔案/函數/重複）、資料流、錯誤處理、可讀性等面向。
> 整理自業界公認文獻，**不綁定特定專案**，但會用 Persona Nexus 的具體案例說明。
>
> **適用對象**：Node.js + Python 混合技術棧（JavaScript/TypeScript + FastAPI）。
> 原則本身跨語言，但「何時拆檔」「函數簽名」等實務判斷會針對這兩個語言的慣例調整。
>
> **怎麼用**
> 1. **稽核現有代碼**：逐條檢查各服務的程式碼，找出違反點
> 2. **寫新代碼時參考**：邊寫邊對照原則，避免事後補救
> 3. **Code Review 時引用**：「這裡違反了 B2（函數職責）原則」
>
> **來源誠實聲明**：以下原則名稱、定義、發明人歸屬皆取自公認文獻（見文末 Sources）。
> 本文不附精確頁碼——需要學術級引用請另查原始論文。

---

## A. 單檔案結構原則

### A1. 適當的模組邊界 — Module Cohesion & Granularity

**定義**：
一個檔案應該對應一個清晰的職責邊界。單一檔案可以有多個相關職責（例如「訊息 CRUD」包括建立/讀取/更新/刪除），
但不應該是「這個檔案有 6 種完全不相關的職責」（例如「訊息 CRUD + 摘要生成 + 狀態機管理 + 所有權檢查 + 暫存狀態追蹤」全混在一起）。

**為什麼**：
- 檔案太大 → 難以導航 → 改一個地方時容易遺漏相關代碼
- 職責混雜 → 難以測試 → 難以獨立修改一個邏輯
- 新人上手慢 → AI 分析成本高 → 改錯機率高

**出處**：
Robert C. Martin 的 SRP（Single Responsibility Principle）在模組層級的應用。
Steve McConnell《Code Complete》第 5 章「設計構造」。

**實務判斷準則**（Persona Nexus 的認知）：

| 應該拆檔 | 不一定拆 |
|---------|---------|
| 一個檔案有 **4 個或以上完全獨立的職責** | 2-3 個高度相關的職責（例如 Message CRUD） |
| 檔案長度超過 **800 行**，且職責不明確 | 檔案長度 500-800 行，但職責清晰 |
| 修改「職責 A」經常被迫同時改「職責 B」 | 職責間有緊密的資料共享（難以拆開） |
| **包山包海的「service」** 檔案：做 CRUD、做狀態機、做驗證、做轉發 | **相聚的職責**：例如 `messageService.js` 做訊息 CRUD + 訊息驗證 |

**實作指南**：

1. **列出檔案內所有職責**（這是拆檔的第一步）
   ```
   例：conversationService.js 的職責清單
   - 職責 1：對話 CRUD（建立/讀取/列表/刪除）
   - 職責 2：訊息 CRUD（建立/讀取/刪除）
   - 職責 3：摘要機制（觸發/存取摘要）
   - 職責 4：對話建立狀態機（preparing/failed 狀態管理）
   - 職責 5：AI 生成狀態機（generating/completed/failed 狀態管理）
   - 職責 6：所有權檢查（驗證 userId 擁有權）
   ```

2. **判斷「相關性」**
   - 職責 1 + 2：相關（都是 CRUD，數據模型緊密）→ 可同檔
   - 職責 3 + 4 + 5：相關（都是狀態追蹤，但與職責 1/2 邏輯不同） → 考慮獨立
   - 職責 6：完全獨立 → 應該提取為 middleware 或 helper

3. **拆檔的時機**
   - 如果「修改職責 A 完全不會碰職責 B」 → 強烈考慮拆檔
   - 如果「修改職責 A 經常得同時改職責 B」 → 先觀察，如果頻繁則拆檔
   - 如果「職責共享大量工具函數」 → 先試試提取共用模組，再決定是否必須同檔

**如何檢查違反**（稽核清單）：

- [ ] 這個檔案能否用一句話描述（例如「訊息 CRUD 與摘要管理」）？如果超過兩個名詞（CRUD、摘要、狀態機、驗證），可能過度混雜
- [ ] 檔案超過 800 行，但職責不清晰（沒有區段註解分割不同職責）？
- [ ] 改「檔案 A 中的方法 B」時，常常得改同檔其他 3+ 個方法？
- [ ] 有沒有「只被某一個職責使用的 helper 函數」（例如 `_validateConversationOwnership` 只被 service 層用，卻在 controller 層也有調用痕跡）？

**可自動化檢測**：
- ⚠️ 檔案長度超過 800 行 → 警告（需人工判斷是否應拆）
- ✅ 無內部區段註解的 800+ 行檔案 → 強烈警告
- ⚠️ 方法數超過 20 個 → 警告（可能該拆）
- ❌ 無法自動化：是否「職責相關」（需領域知識）

---

### A2. 內部結構清晰性 — Code Organization

**定義**：
即使一個檔案內有多個職責，也應該用明確的邊界（區段、註解、子模組）讓讀者快速導航。
讀者應該能在 30 秒內理解「這個檔案有哪些職責」和「每個職責在哪裡」。

**為什麼**：
- 長檔案一開始看時難以掌握全貌
- AI 分析時容易「只看到前面、漏掉後面」的相關邏輯
- 維護時容易改漏相關代碼

**實務判斷準則**：

| 好的組織 | 需要改進 |
|---------|---------|
| 檔案開頭有「職責列表」註解 | 沒有說明，直接開始寫函數 |
| 職責之間用 `// ========== 職責 X ==========` 分割 | 代碼一團，找相關邏輯得逐行掃 |
| 相關方法聚在一起（例如所有 CRUD 方法連續）| 邏輯分散，A 方法在行 50、相關方法在行 800 |
| 職責內的方法遵循某種邏輯順序（create → read → update → delete） | 方法排列無規律 |

**實作指南**（以 `conversationService.js` 為例）：

```javascript
// ========== conversationService.js ==========
// 職責清單：
// 1. 對話 CRUD（建立/讀取/列表/刪除）
// 2. 訊息 CRUD（建立/讀取/刪除）
// 3. 摘要機制（觸發/存取摘要）
// 4. 對話建立狀態機（準備/失敗）
// 5. AI 生成狀態機（生成中/完成/失敗）
// 6. 所有權檢查（驗證擁有權）

// ========== 職責 1：對話 CRUD ==========

async function getOrCreateConversation(userId, characterId) { ... }
async function getConversation(userId, conversationId) { ... }
async function listConversations(userId) { ... }
async function deleteConversation(userId, conversationId) { ... }

// ========== 職責 2：訊息 CRUD ==========

async function sendMessage(userId, characterId, text) { ... }
async function getMessage(messageId) { ... }
async function deleteMessage(conversationId, messageId) { ... }

// ========== 職責 3：摘要機制 ==========

async function summarizeConversation(conversationId) { ... }
async function getSummaryContext(conversationId) { ... }

// ========== 職責 4-5：狀態機管理 ==========

async function _prepareAndCreateConversation(userId, characterId) { ... }
async function _generateAIResponseAsync(conversationId, messageId) { ... }

// ========== 職責 6：所有權檢查 ==========

async function assertConversationOwnership(userId, conversationId) { ... }

// ========== Helper Functions ==========

function _validateUserId(userId) { ... }
```

**如何檢查違反**：
- [ ] 檔案超過 500 行，但沒有區段註解分割職責？
- [ ] 相同職責的方法分散在不同位置（需要 Ctrl+F 才能找全）？
- [ ] 沒有一個地方能快速看出「這個檔案做什麼」？

**可自動化檢測**：
- ⚠️ 檔案超過 500 行但無區段註解 → 警告
- ❌ 無法自動化檢查「職責順序合理性」

---

### A3. 獨立可修改性 — Modification Isolation

**定義**：
修改某個職責的邏輯時，盡量不要被迫修改同檔其他職責的代碼。
如果常常出現「改了 deleteMessage，不得不同時改 sendMessage」的情況，檔案結構可能有問題。

**為什麼**：
- 修改範圍廣 → 副作用難預測 → bug 機率高
- AI 改代碼時容易「改了 A 卻漏了 B」

**實務判斷準則**：

| 應該獨立 | 能共享狀態 |
|---------|-----------|
| 訊息刪除邏輯只改 `deleteMessage()`，不動其他方法 | `deleteMessage()` 改時，需要同步更新 `summarizationState`（兩者共用狀態） |
| `summarizeConversation()` 改不動建立邏輯 | `summarizeConversation()` 改時，需要檢查摘要指標是否對現有對話仍有效 |

**實作指南**：

1. **識別「共用狀態」**
   ```javascript
   // 壞例子：多個方法各自讀寫同一個狀態，難以獨立修改
   class ConversationService {
     _summarizationState = {}  // 多個方法共享
     _creationJobs = {}        // 多個方法共享
     
     async sendMessage(conversationId) {
       // 改這裡時，可能要考慮 _summarizationState 和 _creationJobs
     }
     
     async deleteMessage(conversationId) {
       // 改這裡時，可能要考慮 _summarizationState 和 _creationJobs
     }
   }
   
   // 好例子：封裝共用狀態到獨立的倉庫層
   class ConversationService {
     constructor(conversationRepository, messageRepository, summarizationRepository) {
       this.conversationRepository = conversationRepository
       this.messageRepository = messageRepository
       this.summarizationRepository = summarizationRepository
     }
     
     async sendMessage(conversationId) {
       // 只直接改 messageRepository
       // 摘要狀態改動委派給 summarizationRepository
     }
   }
   ```

2. **簽名變更時的影響範圍**（這是能被迫改的、無可厚非的）
   ```javascript
   // 如果改了 deleteMessage() 的簽名：
   // function deleteMessage(conversationId, messageId)
   // → function deleteMessage(conversationId, messageId, userId)
   
   // 那麼呼叫方（controller、其他 service）也要改，這是正常的
   // 重點是：同檔的 _其他邏輯_（例如 sendMessage、summarize）不應該被迫改
   ```

**如何檢查違反**：
- [ ] 修改某個職責時，同檔其他 3+ 個方法也需要改？
- [ ] 有沒有「看起來不相關、實際上互相依賴」的邏輯？
- [ ] 共用狀態（全域 map、共用 helper）是否過度？

**可自動化檢測**：
- ❌ 無法自動化（需領域知識）

---

## B. 函數粒度原則

### B1. 函數職責單一性 — Function SRP

**定義**：
一個函數應該只有一個改變的理由。當你說「因為 A 需求改了」而改函數，應該只改這個函數，
不應該因為「A 改」和「B 改」同時改一個函數。

**為什麼**：
- 函數做多個事 → 難測 → 難改 → 易出 bug
- AI 改函數時容易「改了主邏輯卻漏了邊界情況」

**實務判斷準則**：

| 單一職責 | 職責過多 |
|---------|---------|
| `function sendMessage(conversationId, text)` — 只負責發送訊息這一件事 | `function sendAndSummarizeMessage(conversationId, text)` — 做兩件事 |
| `function validateUserId(userId)` — 只驗證格式 | `function validateAndFetchUser(userId)` — 驗證 + 調用 DB |
| `function mapErrorToStatus(error)` — 只做錯誤碼映射 | `function handleError(error)` — 映射、記日誌、發告警、回應客户端 |

**實作指南**：

1. **函數命名就能透露職責**
   ```javascript
   // ❌ 名字模糊，職責不明確
   function process(data) { ... }
   function handle(input) { ... }
   function execute(params) { ... }
   
   // ✅ 名字清楚，一眼看出做什麼
   function sendMessageToConversation(conversationId, userId, text) { ... }
   function validateMessageText(text) { ... }
   function fetchConversationHistory(conversationId, limit) { ... }
   ```

2. **「一個改變的理由」測試**
   ```javascript
   // 問自己：「這個函數為什麼需要改」？
   
   async function sendMessage(conversationId, text) {
     // 假設日後需要改：
     // - 修改「訊息發送邏輯」 → 改這裡 ✓
     // - 修改「摘要觸發時機」 → 應該不在這裡改（職責分離）
     // - 修改「訊息驗證規則」 → 應該不在這裡改（委派給 validateMessage）
   }
   ```

3. **超過 1 個原因就考慮拆**
   ```javascript
   // ❌ 做太多事
   async function processUserMessage(conversationId, userId, text) {
     // 1. 驗證訊息
     if (!text || text.length === 0) throw new Error('Empty message')
     
     // 2. 保存訊息
     const message = await messageRepository.create(conversationId, text)
     
     // 3. 觸發摘要
     const summaryNeeded = await checkIfSummaryNeeded(conversationId)
     if (summaryNeeded) {
       await summarizeConversation(conversationId)
     }
     
     // 4. 生成 AI 回覆
     await generateAIResponse(conversationId, message.id)
     
     return message
   }
   
   // ✅ 拆成清晰的責任鏈
   async function sendMessage(conversationId, userId, text) {
     validateMessageText(text)  // 委派給驗證函數
     const message = await messageRepository.create(conversationId, text)
     return message
   }
   
   // 背景工作由上層協調
   async function _generateAIResponseAsync(conversationId, messageId) {
     await generateAIResponse(conversationId, messageId)
   }
   
   async function _triggerSummaryIfNeeded(conversationId) {
     if (await checkIfSummaryNeeded(conversationId)) {
       await summarizeConversation(conversationId)
     }
   }
   ```

**如何檢查違反**：
- [ ] 函數內有多個 `if/else` 分支，每個分支都在做不同的事？
- [ ] 函數的註解需要說「做 A，然後做 B，然後做 C」？
- [ ] 函數名字不能準確描述它做什麼（例如 `handleRequest` 其實做了 5 件事）？

**可自動化檢測**：
- ⚠️ 函數超過 50 行 → 警告（可能職責過多，需人工判斷）
- ⚠️ 函數有 4+ 個 `if/else` 分支 → 警告
- ✅ 函數參數超過 4 個 → 警告（可能在做太多不同的事）

---

### B2. 函數長度 — Function Size

**定義**：
一個函數應該短到「能在一個螢幕上看完」（大約 30-50 行），最多不超過 100 行。

**為什麼**：
- 短函數 → 易於理解 → 易於測試 → 易於改
- 長函數 → 認知負擔高 → AI 改錯機率高

**實務判斷準則**：

| 好的長度 | 考慮拆分 | 肯定要拆 |
|---------|---------|---------|
| < 30 行 | 30-50 行 | > 100 行 |
| 一眼能看出邏輯流 | 邏輯清，但有點長 | 需要上下翻頁才能理解 |

**實作指南**：

1. **「一眼能看出邏輯」的檢驗**
   ```javascript
   // ✅ 好例子（20 行，邏輯清晰）
   async function sendMessage(conversationId, userId, text) {
     await assertConversationOwnership(userId, conversationId)
     validateMessageText(text)
     const message = await messageRepository.create(conversationId, {
       role: 'user',
       text,
     })
     return message
   }
   
   // ❌ 需要拆分（60 行，多個層級邏輯）
   async function sendMessage(conversationId, userId, text) {
     // 驗證 — 層級 1
     if (!conversationId) throw new Error('...')
     if (!userId) throw new Error('...')
     if (!text) throw new Error('...')
     
     // 擁有權檢查 — 層級 2
     const conversation = await conversationRepository.get(conversationId)
     if (conversation.userId !== userId) throw new Error('...')
     
     // 保存訊息 — 層級 3
     const message = await messageRepository.create(conversationId, {
       role: 'user',
       text,
       createdAt: new Date(),
     })
     
     // 檢查摘要 — 層級 4
     const messageCount = await messageRepository.countByConversation(conversationId)
     if (messageCount > 10) {
       const summary = await generateSummary(conversationId)
       await summaryRepository.save(summary)
     }
     
     return message
   }
   
   // ✅ 拆成多個清晰的函數
   async function sendMessage(conversationId, userId, text) {
     await assertConversationOwnership(userId, conversationId)
     validateMessageText(text)
     
     const message = await messageRepository.create(conversationId, {
       role: 'user',
       text,
     })
     
     await triggerSummaryIfNeeded(conversationId)
     return message
   }
   
   async function triggerSummaryIfNeeded(conversationId) {
     const messageCount = await messageRepository.countByConversation(conversationId)
     if (messageCount > 10) {
       const summary = await generateSummary(conversationId)
       await summaryRepository.save(summary)
     }
   }
   ```

2. **「垂直切片」方法——按邏輯層級拆**
   ```javascript
   // 一個長函數可能有多個層級
   // 層級 1：輸入驗證 → 層級 2：業務邏輯 → 層級 3：副作用
   
   // 可以拆成：validateInput(), executeBusinessLogic(), executeSideEffects()
   ```

**如何檢查違反**：
- [ ] 函數超過 50 行，但沒有垂直切片（多個層級混在一起）？
- [ ] 需要上下翻頁才能看完函數邏輯？
- [ ] 函數內層層嵌套（3+ 層 if/for/try）？

**可自動化檢測**：
- ⚠️ 函數超過 50 行 → 警告
- ⚠️ 函數超過 100 行 → 強烈警告（肯定要拆）
- ✅ 嵌套深度超過 3 層 → 警告

---

### B3. 函數簽名清晰性 — Function Signature

**定義**：
函數的參數列表和回傳值應該清楚地說明「需要什麼」和「會回傳什麼」。

**為什麼**：
- 簽名清楚 → 呼叫方不會誤用 → 不容易改錯
- 簽名含糊 → 呼叫方猜測 → 容易傳錯參數 → AI 改時容易改漏下游

**實務判斷準則**：

| 清晰簽名 | 含糊簽名 |
|---------|---------|
| `async function sendMessage(conversationId: string, userId: string, text: string): Promise<Message>` | `function send(data): any` |
| `function validateMessage(text: string): boolean` | `function validate(input)` |
| `async function deleteConversation(userId: string, conversationId: string, options?: { hardDelete: boolean }): Promise<void>` | `function delete(id, flag)` |

**實作指南**（JavaScript/TypeScript 混合環境）：

1. **帶型別資訊（TypeScript 或 JSDoc）**
   ```javascript
   // ❌ 含糊——光看名字不知道參數是什麼、回傳什麼
   function sendMessage(id, text) {
     // ...
   }
   
   // ✅ 清楚——即使 JS 也要用 JSDoc
   /**
    * 發送訊息到指定對話
    * @param {string} conversationId - 對話 ID
    * @param {string} userId - 使用者 ID（用於擁有權檢查）
    * @param {string} text - 訊息內容
    * @returns {Promise<Message>} 已保存的訊息物件
    * @throws {Error} 若對話不存在或無權限
    */
   async function sendMessage(conversationId, userId, text) {
     // ...
   }
   ```

2. **參數個數**（超過 3 個考慮封裝）
   ```javascript
   // ❌ 參數太多，呼叫方容易傳錯
   function createConversation(userId, characterId, title, description, tags, language, isPublic, metadata) {
     // ...
   }
   
   // ✅ 用物件參數或分層
   function createConversation(userId, characterId, options) {
     const { title, description, tags, language, isPublic, metadata } = options
     // ...
   }
   ```

3. **回傳值明確**
   ```javascript
   // ❌ 不清楚回傳什麼
   async function getConversation(id) {
     return await repository.query(id)  // 回傳的是什麼？只有欄位嗎？有計算值嗎？
   }
   
   // ✅ 清楚
   /**
    * @returns {Promise<{id, userId, characterId, title, messages: Message[]}>}
    */
   async function getConversation(id) {
     const conv = await repository.get(id)
     return {
       id: conv.id,
       userId: conv.userId,
       characterId: conv.characterId,
       title: conv.title,
       messages: await messageRepository.findByConversation(id),
     }
   }
   ```

**如何檢查違反**：
- [ ] 函數名字和簽名對不上（例如 `deleteMessage` 但其實刪了對話）？
- [ ] 參數超過 4 個，且沒有說明？
- [ ] 回傳值類型不確定（可能是物件、可能是陣列、可能是 null）？
- [ ] 沒有 JSDoc/TypeScript 型別說明？

**可自動化檢測**：
- ⚠️ 參數超過 4 個 → 警告
- ⚠️ 無 TypeScript 型別、無 JSDoc → 警告（尤其對 > 50 行函數）
- ✅ 回傳值不明確（沒有註解、沒有型別） → 警告

---

## C. 資料流與狀態管理原則

### C1. 副作用隔離 — Side Effect Isolation

**定義**：
把「純邏輯」（輸入→輸出，無外部影響）和「副作用」（修改外部狀態、調用 API、寫 DB）分開。
函數要麼是「純函數」，要麼是「明確的副作用容器」，不要混著寫。

**為什麼**：
- 副作用混雜 → 難以測試 → 難以追蹤 bug
- AI 改代碼時容易「改了邏輯，卻不知道有 DB 副作用會被改」

**實務判斷準則**：

| 好的分離 | 混雜 |
|---------|-----|
| `validateMessage(text)` — 純邏輯，只檢查格式 | `validateAndSaveMessage(text)` — 既驗證又存 DB |
| `async function sendMessage(...) { ... /* 副作用 */ }` — 副作用函數明確 | `function processMessage(text) { ... /* 混著驗證、存 DB、呼叫 API */ }` |
| 副作用集中在 repository 層 | 副作用散落在 service、controller、helper 各處 |

**實作指南**：

1. **純函數集中區**
   ```javascript
   // ========== 純函數區（utilities/validators.js）==========
   function validateMessageText(text) {
     if (!text) throw new Error('Message cannot be empty')
     if (text.length > 10000) throw new Error('Message too long')
     return true
   }
   
   function calculateMessageHash(text) {
     return sha256(text)
   }
   
   function truncateForPreview(text, length) {
     return text.substring(0, length) + '...'
   }
   ```

2. **副作用容器（service 層）**
   ```javascript
   // ========== 副作用區（service 層 ）==========
   async function sendMessage(conversationId, userId, text) {
     // 先用純邏輯驗證
     validateMessageText(text)
     
     // 再做副作用（DB 寫入、API 呼叫）
     const message = await messageRepository.create(conversationId, { text })
     
     // 觸發非同步副作用
     await _generateAIResponseAsync(conversationId, message.id)
     
     return message
   }
   
   // 背景副作用用 async 函數明確區分
   async function _generateAIResponseAsync(conversationId, messageId) {
     // 這個函數名字的 `_` 和 `Async` 都在說「這是副作用」
     // 內部是呼叫外部 API、修改 DB 的工作
   }
   ```

**如何檢查違反**：
- [ ] 有一個函數既驗證、既保存、既回應客户端？
- [ ] 副作用散落在 4+ 層（controller、service、repository、helper 都有 DB 呼叫）？
- [ ] 純邏輯函數被迫依賴全域狀態或 mock 物件？

**可自動化檢測**：
- ⚠️ 一個函數內同時有驗證、DB 呼叫、API 呼叫 → 警告
- ❌ 無法完全自動化（需要追蹤所有副作用標記）

---

### C2. 狀態傳遞透明性 — State Flow Clarity

**定義**：
函數間的資料傳遞應該明確可追蹤。調用方清楚知道「修改了什麼狀態」，被呼叫方清楚知道「會收到什麼、會改什麼」。

**為什麼**：
- 狀態流含糊 → 難以預測副作用 → 改一個地方時容易改漏下游
- AI 改代碼時容易「改了 A，不知道 B 也依賴 A」

**實務判斷準則**：

| 透明狀態流 | 含糊狀態流 |
|-----------|----------|
| 函數接收明確參數，修改 repository 層 | 函數修改全域 map，呼叫方不知道何時生效 |
| `await messageRepository.create(...)` — 明確說「要存訊息」 | `this._messages.push(...)` — 隱性修改內部狀態 |
| 回傳值明確（成功的物件、失敗的異常） | 回傳 undefined，靠全域狀態判斷成敗 |

**實作指南**：

1. **參數作為「輸入承諾」**
   ```javascript
   // ❌ 隱性依賴全域狀態
   class ConversationService {
     currentUserId  // 全域狀態
     
     async deleteMessage(messageId) {
       // 依賴 this.currentUserId，呼叫方不知道需要先設定它
       return await messageRepository.delete(messageId, this.currentUserId)
     }
   }
   
   // ✅ 明確參數
   async function deleteMessage(messageId, userId) {
     // 呼叫方一眼看出需要提供 userId
     return await messageRepository.delete(messageId, userId)
   }
   ```

2. **回傳值作為「輸出保證」**
   ```javascript
   // ❌ 隱性靠全域狀態判斷
   async function deleteMessage(messageId) {
     try {
       await db.delete(messageId)
       // 成功或失敗不明確，呼叫方得查全域的 this._lastError
     } catch (e) {
       this._lastError = e
     }
   }
   
   // ✅ 明確回傳值
   async function deleteMessage(messageId) {
     // 成功時回傳物件，失敗時拋異常
     const result = await db.delete(messageId)
     return result
   }
   ```

3. **記憶體 Map 的處理**（平台常見的複雜狀態）
   ```javascript
   // ❌ 隱性狀態，修改難以追蹤
   const creationJobs = {}  // 誰在讀？誰在寫？什麼時候會清理？
   
   async function getOrCreateConversation(userId, characterId) {
     const key = `${userId}:${characterId}`
     if (creationJobs[key]) {
       return creationJobs[key].status  // 誰在修改 creationJobs[key]?
     }
     // ...
   }
   
   // ✅ 用 Repository 封裝，明確介面
   class ConversationCreationJobRepository {
     async get(userId, characterId) { ... }
     async set(userId, characterId, status) { ... }
     async delete(userId, characterId) { ... }
   }
   
   async function getOrCreateConversation(userId, characterId) {
     const job = await jobRepository.get(userId, characterId)
     if (job?.status === 'preparing') return job.status
     // ...
   }
   ```

**如何檢查違反**：
- [ ] 有全域 map/物件被多個函數讀寫，無清晰的 setter/getter？
- [ ] 呼叫某個函數，結果莫名其妙，因為它修改了你不知道的全域狀態？
- [ ] 追蹤某個狀態被誰修改，需要 grep 整個檔案？

**可自動化檢測**：
- ⚠️ 全域變數被多個函數寫入 → 警告
- ⚠️ 函數修改 `this` 的屬性，但沒有名字標記（例如方法名未包含 "set"/"update"） → 警告
- ❌ 無法自動化檢查「狀態流邏輯」

---

## D. 錯誤處理與邊界情況原則

### D1. 錯誤處理集中化 — Centralized Error Handling

**定義**：
錯誤的分類、錯誤碼、HTTP 狀態碼的對應關係應該集中在一個地方（例如 `ERROR_MAP`），
不應該在每個 controller 方法裡各寫一套 if/else。

**為什麼**：
- 分散的錯誤邏輯 → 新增錯誤碼時改 10 個地方 → 容易漏改
- 錯誤碼對應不一致 → 前端無法準確判斷錯誤

**實務判斷準則**：

| 集中化 | 分散 |
|--------|-----|
| `ERROR_MAP = { "UNAUTHORIZED": 401, "NOT_FOUND": 404, ... }` | 每個 controller 方法各寫 `if (error.message === 'XXX') return 400` |
| 新增錯誤碼時只改一個地方 | 新增錯誤碼時改 10 個 controller 方法 |
| Service 層拋「業務錯誤」，controller 統一轉 HTTP 碼 | Service 層已經轉成 HTTP 碼，controller 直接轉發 |

**實作指南**：

1. **建立 `ERROR_MAP`（一個中央設定檔）**
   ```javascript
   // errors/errorMap.js
   const ERROR_MAP = {
     UNAUTHORIZED: { status: 401, message: 'Unauthorized' },
     FORBIDDEN: { status: 403, message: 'Forbidden' },
     NOT_FOUND: { status: 404, message: 'Not found' },
     CONFLICT: { status: 409, message: 'Conflict' },
     INVALID_INPUT: { status: 422, message: 'Invalid input' },
     INTERNAL_ERROR: { status: 500, message: 'Internal server error' },
   }
   
   module.exports = ERROR_MAP
   ```

2. **Service 層拋「業務錯誤」**
   ```javascript
   // services/conversationService.js
   async function deleteConversation(userId, conversationId) {
     const conversation = await conversationRepository.get(conversationId)
     
     if (!conversation) {
       throw new Error('NOT_FOUND')  // 拋業務錯誤，不要拋 HTTP 碼
     }
     
     if (conversation.userId !== userId) {
       throw new Error('FORBIDDEN')
     }
     
     return await conversationRepository.delete(conversationId)
   }
   ```

3. **Controller 統一轉成 HTTP 碼**
   ```javascript
   // controllers/conversationController.js
   async function deleteConversation(req, res) {
     try {
       const { conversationId } = req.params
       const userId = req.headers['x-user-id']
       
       await conversationService.deleteConversation(userId, conversationId)
       
       res.status(204).send()
     } catch (error) {
       const errorCode = error.message  // 假設 error.message 是業務錯誤碼
       const httpConfig = ERROR_MAP[errorCode] || ERROR_MAP.INTERNAL_ERROR
       
       res.status(httpConfig.status).json({
         error: errorCode,
         message: httpConfig.message,
       })
     }
   }
   ```

**如何檢查違反**：
- [ ] Controller 方法中有多個 `if (error.message === 'XXX') return YYY`？
- [ ] 同一個業務錯誤在 A controller 回 400，在 B controller 回 422？
- [ ] 新增錯誤碼時需要改多個檔案？

**可自動化檢測**：
- ⚠️ Controller 方法內有 3+ 個 `if` 判斷 error.message → 警告
- ✅ 搜尋「ERROR_MAP」使用次數，如果偏低 → 警告

---

### D2. 邊界情況處理 — Edge Case Handling

**定義**：
每個函數應該清楚定義「什麼情況下會拋異常」「什麼情況下會回 null」「什麼情況下會回空陣列」。

**為什麼**：
- 邊界情況未定義 → 呼叫方猜測 → 容易改漏邊界
- AI 改代碼時常常改掉邊界情況處理

**實務判斷準則**：

| 清晰邊界 | 含糊邊界 |
|---------|---------|
| `getMessage(messageId)` — 不存在時拋 `NOT_FOUND` | `getMessage(messageId)` — 不存在時回 null？回空物件？拋異常？ |
| `listMessages(conversationId, limit)` — 空結果回 `[]` | `listMessages(conversationId)` — 空時回什麼不明確 |
| JSDoc 清楚寫出所有拋異常情況 | 沒有文件說明邊界 |

**實作指南**：

1. **用 JSDoc 明確邊界**
   ```javascript
   /**
    * 取得單一訊息
    * @param {string} messageId
    * @returns {Promise<Message>} 訊息物件
    * @throws {Error} 'NOT_FOUND' 若訊息不存在
    * @throws {Error} 'UNAUTHORIZED' 若無權限讀取
    */
   async function getMessage(messageId, userId) {
     const message = await messageRepository.get(messageId)
     if (!message) throw new Error('NOT_FOUND')
     // ...
   }
   
   /**
    * 列出某對話的訊息
    * @param {string} conversationId
    * @returns {Promise<Message[]>} 訊息陣列，空結果回 []
    */
   async function listMessages(conversationId) {
     return await messageRepository.find({ conversationId })
     // 回傳 [] 而不是 null
   }
   ```

2. **統一「空值」的處理**
   ```javascript
   // ❌ 不一致：有時回 null，有時拋異常，有時回 []
   async function getUser(userId) {
     const user = await db.findUser(userId)
     return user || null  // 不存在回 null
   }
   
   async function getMessage(messageId) {
     const msg = await db.findMessage(messageId)
     if (!msg) throw new Error('NOT_FOUND')  // 不存在拋異常
   }
   
   async function listMessages(conversationId) {
     return await db.find({ conversationId })  // 不存在回 []
   }
   
   // ✅ 一致：資源不存在拋異常
   async function getUser(userId) {
     const user = await db.findUser(userId)
     if (!user) throw new Error('NOT_FOUND')
     return user
   }
   
   async function getMessage(messageId) {
     const msg = await db.findMessage(messageId)
     if (!msg) throw new Error('NOT_FOUND')
     return msg
   }
   
   async function listMessages(conversationId) {
     return await db.find({ conversationId }) || []  // 列表空回 []
   }
   ```

3. **事前驗證 vs 事後拋異常**
   ```javascript
   // ❌ 混雜：有些驗證在開始，有些在中間
   async function sendMessage(conversationId, text) {
     if (!text) throw new Error('INVALID_INPUT')  // 驗證 1
     
     const conversation = await db.find(conversationId)
     if (!conversation) throw new Error('NOT_FOUND')  // 邏輯檢查
     
     // ... 一堆邏輯 ...
     
     if (text.length > 10000) throw new Error('INVALID_INPUT')  // 驗證 2（太晚）
   }
   
   // ✅ 清楚：先驗證輸入，再做邏輯
   async function sendMessage(conversationId, text) {
     // 事前驗證
     if (!text) throw new Error('INVALID_INPUT')
     if (text.length > 10000) throw new Error('INVALID_INPUT')
     
     // 邏輯檢查
     const conversation = await db.find(conversationId)
     if (!conversation) throw new Error('NOT_FOUND')
     
     // ... 邏輯...
   }
   ```

**如何檢查違反**：
- [ ] 函數沒有 JSDoc 說明「什麼情況下拋異常」？
- [ ] 相同情況下，有時回 null，有時拋異常，有時回 undefined？
- [ ] 列表查詢有時回 null，有時回 []，呼叫方得加 if 判斷？

**可自動化檢測**：
- ⚠️ 函數無 JSDoc `@throws` 標記，但內部有 throw 語句 → 警告
- ⚠️ 函數回傳可能是 null、[]、undefined 的混雜 → 警告（需人工判斷）
- ❌ 無法自動化檢查「邊界情況是否完整」

---

## E. 重複代碼與抽象原則

### E1. 重複判斷準則 — DRY Decision Model

**定義**：
不是「只要有重複就抽象」，而是「判斷抽象的成本 vs 重複的成本」。

**為什麼**：
- 過度抽象 → 可讀性反而下降 → 維護成本高
- 重複太多 → 改一個地方漏掉其他 → bug 機率高
- 需要找到平衡點

**實務判斷準則**（基於你的 Q5 回答）：

| 應該抽象 | 可以忍受重複 |
|---------|-----------|
| **完全相同** 的邏輯重複 3+ 次 | 相似度 < 70% 的邏輯（抽象成本太高） |
| 新增功能時常需要改同一個邏輯 2+ 個地方 | 預計未來不會再改（一次性代碼） |
| 抽象後**比原代碼還簡短** | 抽象後代碼反而變長（例如需要 wrapper） |
| 共用邏輯涉及「業務規則」（改規則會改多個地方） | 共用邏輯涉及「工程細節」（改細節只改一個地方） |

**實作指南**：

1. **「三次法則」**
   ```javascript
   // 第一次：只有一個地方用，不抽
   function mapErrorToStatus(error) {
     if (error.message === 'NOT_FOUND') return 404
     return 500
   }
   
   // 第二次：又有地方要用，還是不抽（可能只是巧合）
   function mapErrorToStatus(error) { ... }  // 同樣邏輯寫兩遍可以接受
   
   // 第三次：第三個地方也需要，現在抽成共用函數
   ERROR_MAP = { 'NOT_FOUND': 404, ... }
   ```

2. **成本-收益評估**
   ```javascript
   // ❌ 抽象成本太高（為了抽 3 行而寫 15 行 wrapper）
   // 原代碼（三個地方各一份）
   if (error.message === 'NOT_FOUND') res.status(404)
   if (error.message === 'NOT_FOUND') res.status(404)
   if (error.message === 'NOT_FOUND') res.status(404)
   
   // 抽象成本太高的嘗試
   class ErrorMapper {
     static map(error) { ... }
     static statusCode(error) { ... }
     static message(error) { ... }
   }
   // 需要加 class、加方法、加測試、加文件 → 超出 3 行代碼的回報
   
   // ✅ 抽象成本低（簡單的常數表）
   const ERROR_MAP = {
     'NOT_FOUND': 404,
     'UNAUTHORIZED': 401,
   }
   
   const status = ERROR_MAP[error.message] || 500
   // 只多了一個小常數表，清晰且維護成本低
   ```

3. **判斷「是否真的相同」**
   ```javascript
   // ❌ 看起來重複，實際上略有差異，硬抽會出問題
   // Controller A
   if (error.message === 'NOT_FOUND') {
     return res.status(404).json({ error: 'Not found' })
   }
   
   // Controller B
   if (error.message === 'NOT_FOUND') {
     return res.status(404).json({ error: 'Resource not found', details: error })
   }
   
   // 硬抽成共用函數會遺漏 details，B 就得特化
   
   // ✅ 相同就抽，不同就各自處理
   // 共用邏輯
   const status = ERROR_MAP[error.message] || 500
   
   // 個性化邏輯保留在各 controller
   res.status(status).json({
     error: error.message,
     details: error.details,  // A 不需要，B 需要
   })
   ```

**如何檢查違反**：
- [ ] 有「相同的邏輯重複 5+ 次」卻沒有抽象？
- [ ] 有「抽象出來的函數只被用一次」？
- [ ] 有「為了抽象而建的複雜 wrapper，反而降低可讀性」？

**可自動化檢測**：
- ⚠️ 完全相同的代碼段重複 4+ 次 → 警告
- ✅ 搜尋「只被呼叫一次的函數」→ 標記為候選刪除
- ❌ 無法自動化判斷「相似度多少時該抽象」

---

### E2. 抽象的形式與層級 — Abstraction Forms

**定義**：
抽象有多種形式（常數表、共用函數、策略物件、依賴注入），應該選擇最簡單的形式。

**為什麼**：
- 過度設計 → 引入不必要的複雜性
- 選擇最簡單的形式 → 易於維護、易於理解

**實務判斷準則**：

| 邏輯類型 | 最佳形式 | 說明 |
|---------|--------|------|
| 常數映射（錯誤碼、狀態碼） | `const ERROR_MAP = {...}` | 簡單常數表即可 |
| 重複邏輯 | 共用函數 | `function mapErrorToStatus(error) {}` |
| 條件分支過多（if/else） | 策略物件或表驅動 | 用表驅動而非分支 |
| 高層邏輯依賴具體實作 | 依賴注入 | 傳入 interface 而非具體類別 |

**實作指南**：

1. **從簡單到複雜**
   ```javascript
   // 第一級：常數表（最簡單）
   const STATUS_CODES = {
     'NOT_FOUND': 404,
     'UNAUTHORIZED': 401,
   }
   
   // 第二級：共用函數（中等複雜）
   function mapStatusCode(error) {
     return STATUS_CODES[error.code] || 500
   }
   
   // 第三級：策略物件（較複雜）
   const errorHandlers = {
     'NOT_FOUND': (error) => ({ status: 404, message: 'Not found' }),
     'UNAUTHORIZED': (error) => ({ status: 401, message: 'Unauthorized' }),
   }
   
   function handle(error) {
     const handler = errorHandlers[error.code]
     return handler ? handler(error) : { status: 500, message: 'Error' }
   }
   
   // 第四級：依賴注入（最複雜）
   class ErrorHandler {
     constructor(strategies) {
       this.strategies = strategies
     }
     
     handle(error) {
       const strategy = this.strategies[error.code]
       return strategy ? strategy(error) : { status: 500 }
     }
   }
   ```

2. **表驅動 vs 分支**
   ```javascript
   // ❌ 分支過多，難以新增
   if (action === 'create') {
     // ...
   } else if (action === 'read') {
     // ...
   } else if (action === 'update') {
     // ...
   } else if (action === 'delete') {
     // ...
   }
   
   // ✅ 表驅動，易於新增
   const actions = {
     'create': (params) => { ... },
     'read': (params) => { ... },
     'update': (params) => { ... },
     'delete': (params) => { ... },
   }
   
   const handler = actions[action]
   if (handler) return handler(params)
   ```

**如何檢查違反**：
- [ ] 為了一個共用的常數新建了一個 class？
- [ ] 有一堆 if/else 分支，可以改成表驅動？
- [ ] 依賴注入引入了不必要的複雜性（只有一個實作時）？

**可自動化檢測**：
- ⚠️ 4+ 層 if/else 嵌套 → 建議改表驅動
- ❌ 無法自動化判斷「該用哪種抽象形式」

---

## F. 可讀性規則

### F1. 命名規範 — Naming Conventions

**定義**：
變數、函數、類別名字應該清楚地說明「這是什麼」。

**為什麼**：
- 名字清楚 → 不需要深入閱讀實作就能理解用途
- 名字含糊 → 讀者得猜測 → 容易改錯

**實務判斷準則**：

| 好名字 | 壞名字 |
|--------|--------|
| `validateMessageText()` | `validate()` |
| `conversationRepository` | `repo` |
| `isConversationOwner` | `check` |
| `MAX_MESSAGE_LENGTH` | `LIMIT` |
| `sendMessage()` | `process()` |

**實作指南**：

1. **動詞 + 名詞**
   ```javascript
   // ❌ 模糊
   function handle(data) { }
   function execute(params) { }
   function run(input) { }
   
   // ✅ 清楚
   function validateUserInput(data) { }
   function generateAIResponse(params) { }
   function fetchConversationHistory(input) { }
   ```

2. **布林變數 + is/has/should**
   ```javascript
   // ❌
   const valid = true
   const permission = false
   const processing = true
   
   // ✅
   const isValid = true
   const hasPermission = false
   const isProcessing = true
   ```

3. **常數 + 大寫**
   ```javascript
   // ❌
   const timeout = 30000
   const retries = 3
   
   // ✅
   const REQUEST_TIMEOUT_MS = 30000
   const MAX_RETRIES = 3
   ```

**如何檢查違反**：
- [ ] 有變數名為單字母（`x`, `d`, `tmp`）？
- [ ] 有函數名無法說明做什麼（`handle`, `process`, `run`）？
- [ ] 有變數名與實際用途相差太遠？

**可自動化檢測**：
- ✅ 變數名為單字母（除了迴圈變數） → 警告
- ✅ 函數名為 `handle`, `process`, `run` 等泛稱 → 警告
- ⚠️ 布林變數未用 is/has/should 開頭 → 警告

---

### F2. 註解與文件 — Comments & Documentation

**定義**：
程式碼應該自說自明，註解只在「為什麼」不明顯時才寫。

**為什麼**：
- 過多註解 → 反而降低可讀性 → 維護時註解容易過時
- 缺少必要的 JSDoc → 下游呼叫方猜測 → 容易改錯

**實務判斷準則**：

| 好的註解 | 不必要的註解 |
|---------|-----------|
| `// 檢查是否需要摘要（訊息數超過 100）以控制 token 成本` | `// 設定 x = 1` |
| JSDoc 說明參數、回傳值、例外 | 逐行註解說明代碼做什麼 |
| 註解說「為什麼」而非「做什麼」 | 註解重複代碼已清楚表達的事 |

**實作指南**：

1. **只寫「為什麼」不寫「做什麼」**
   ```javascript
   // ❌ 多餘：代碼已清楚表達
   const user = await userRepository.get(userId)  // 取得使用者
   if (!user) throw new Error('NOT_FOUND')  // 若無使用者拋異常
   
   // ✅ 必要：說明為什麼要檢查
   if (!user) {
     // 使用者必須存在，因為認證層已驗證過 JWT（見 auth.md）
     throw new Error('NOT_FOUND')
   }
   ```

2. **JSDoc for Public APIs**
   ```javascript
   // ❌ 無文件，下游不知道用法
   async function sendMessage(conversationId, text) { }
   
   // ✅ 有文件，下游一眼看出
   /**
    * 發送訊息到指定對話
    * @param {string} conversationId - 對話 ID
    * @param {string} text - 訊息內容（1-10000 字元）
    * @returns {Promise<Message>} 已保存的訊息
    * @throws {Error} 'NOT_FOUND' 對話不存在
    * @throws {Error} 'FORBIDDEN' 無權限
    * @example
    * const msg = await sendMessage('conv-123', 'Hello')
    */
   async function sendMessage(conversationId, text) { }
   ```

**如何檢查違反**：
- [ ] public 函數無 JSDoc，卻有內部詳細註解？
- [ ] 有大量「做什麼」型註解（代碼已清楚表達）？
- [ ] 函數無 JSDoc，新人不知道怎麼用？

**可自動化檢測**：
- ⚠️ 公開函數無 JSDoc → 警告
- ⚠️ 函數無 `@throws` 標記，但內部有 throw → 警告
- ❌ 無法自動化檢查「註解品質」

---

## 原則檢查清單（稽核用）

針對每個服務的程式碼，逐項檢查：

### A. 單檔案結構
- [ ] 檔案超過 800 行？職責是否明確？
- [ ] 超過 4 個獨立職責？
- [ ] 職責間無區段註解分割？
- [ ] 改一個職責時常被迫改其他職責？

### B. 函數粒度
- [ ] 函數超過 100 行？
- [ ] 函數有 4+ 個參數且無說明？
- [ ] 函數名字（handle, process, run）無法說明做什麼？
- [ ] 函數做多於一個「改變的理由」的事？

### C. 資料流與狀態管理
- [ ] 副作用混雜在業務邏輯中？
- [ ] 有全域狀態被多個函數修改？
- [ ] 函數間的資料傳遞隱性（靠全域狀態）？

### D. 錯誤處理
- [ ] 錯誤碼映射分散在多個 controller 方法？
- [ ] 同一業務錯誤在不同地方回不同 HTTP 碼？
- [ ] 函數無 JSDoc 說明「何時拋異常」？

### E. 重複代碼
- [ ] 有「完全相同的邏輯重複 4+ 次」？
- [ ] 有「為了抽象而建的複雜 wrapper」？

### F. 可讀性
- [ ] 變數名為單字母（非迴圈變數）？
- [ ] 函數名無法說明做什麼？
- [ ] Public 函數無 JSDoc？

---

## Sources

- Robert C. Martin. *Clean Code*. Prentice Hall, 2008.
- Steve McConnell. *Code Complete* (2nd ed). Microsoft Press, 2004.
- Hunt & Thomas. *The Pragmatic Programmer*. Addison-Wesley, 1999.
- Frederick P. Brooks Jr. *The Mythical Man-Month*. Addison-Wesley, 1975.

---

## 後續使用流程

1. **現況稽核**：用上方清單逐一檢查五個微服務的程式碼
2. **記錄違反項**：建立各服務的「程式撰寫優化」change，記錄所有違反項
3. **優化實作**：按優先度逐項修正
4. **驗證**：測試、code review、更新文件
5. **存檔**：commit、archive change
