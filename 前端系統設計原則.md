# 前端系統設計原則

> **這份文件是什麼**
> 一套**通用的**前端系統設計原則，整理自業界公認、有名有姓的文獻，**不綁定任何特定專案或服務**。
> 適用範圍：本平台的前端技術棧（**Vite + 原生 JavaScript 的多頁應用**，無 UI 框架），
> 因此以下原則刻意排除了 React/Vue 等框架特有的準則（如 hooks 規則、虛擬 DOM），
> 聚焦在「原生 JS 模組化」「瀏覽器原生能力」「建置工具慣例」這個技術棧實際會遇到的面向。
>
> **怎麼用**
> 它是凌駕於各前端服務規格之上的「元規格（meta-spec）」。工作流程分兩步：
> 1. **立準則**（本文件）——定義「一個好的前端服務應該長什麼樣」。
> 2. **拿準則稽核程式碼**——逐條檢查現有服務，凡不符者即為優化點。
>
> 每條原則都附「**如何檢查違反**」，就是給第二步稽核用的對照鉤子。
>
> **來源誠實聲明**：以下原則名稱、定義、發明人歸屬皆取自公認文獻（見文末 Sources）。
> 本文**不附精確頁碼或逐字引言**——需要學術級引用請另查原始論文。
>
> **與《後端系統設計原則》的關係**：兩份文件的「A. 通用軟體設計原則」內容相同（KISS/DRY/YAGNI/SSOT
> 是跨語言、跨層的通用準則，前後端都適用），其餘章節各自替換成該層真正相關的準則
> （後端是 SOLID + 12-Factor + Design by Contract；前端是模組化/關注點分離、效能與資源管理、
> 可及性與體驗一致性）。

---

## A. 通用軟體設計原則

> 與《後端系統設計原則》A 節相同——這四條不分前後端，是所有程式碼共用的底層準則。

### KISS — Keep It Simple
- **定義**：在能解決問題的前提下，選擇最簡單的設計。可讀、易懂優於聰明、炫技。
- **為什麼**：簡單的程式碼易於除錯與交接；複雜度是缺陷的溫床。
- **如何檢查違反**：一段 DOM 操作或表單邏輯是否可以用更少的分支、更少的抽象達成同樣效果？是否有為了「將來可能用到」而存在的複雜結構（例如只有一頁卻先搭一套路由框架）？

### DRY — Don't Repeat Yourself
- **定義**：系統中每一項知識都只有**單一、權威、明確**的表述。
- **為什麼**：重複的知識一旦要改，就得到處改；漏改一處即產生 bug。
- **出處**：Hunt & Thomas,《The Pragmatic Programmer》。
- **如何檢查違反**：同一段 fetch 封裝、同一份 API base URL、同一套表單驗證規則，是否散落在多個檔案或多個前端專案各寫一份？

### YAGNI — You Aren't Gonna Need It
- **定義**：只實作當前需求明確要求的東西，不做臆測性、預留性的功能。
- **為什麼**：臆測性的通用化增加了要維護與測試的程式碼，卻沒有當下的效益。
- **出處**：極限編程（XP）實務。
- **如何檢查違反**：是否留著 Vite 鷹架產生的預設範例檔案（如 `counter.js`）、沒人呼叫的元件、為了「以後可能要用」而留的 UI 分支？

### 單一真相 — Single Source of Truth (SSOT)
- **定義**：每一項邏輯或資料，在系統中都有唯一的權威來源。（DRY 在資料/狀態面的推論）
- **為什麼**：多個來源會不一致；不一致的真相無法信任。
- **如何檢查違反**：使用者登入狀態（token）、當前使用者 ID 這類跨頁共用的狀態，是否有多處各自讀寫（例如同時有 `localStorage` 和記憶體變數兩份、或多個前端各自實作一套讀取邏輯而未共用）？

---

## B. 前端架構與模組化原則

### 關注點分離 — Separation of Concerns (SoC)
- **定義**：把一個問題切成彼此獨立、可各自理解與修改的部分。在網頁前端的傳統體現是
  **結構（HTML）／表現（CSS）／行為（JavaScript）三者分離**。
- **為什麼**：能只改一個關注點而不牽動其他（改樣式不必碰邏輯，改文案不必碰互動）；也是可維護性的根基。
- **出處**：Edsger W. Dijkstra（1974）提出通用概念；HTML/CSS/JS 三分離是 Web 標準社群長期實務
  （W3C／MDN 均以此為基礎教材架構）。
- **如何檢查違反**：JS 檔案裡是否直接寫死大量樣式（inline style 取代 CSS class）？HTML 裡是否嵌了大段商業邏輯（inline `onclick`、內聯 script 夾雜 API 呼叫）？一支 JS 檔案是否同時處理「畫面渲染」「API 溝通」「表單驗證」多種職責？

### 漸進增強 — Progressive Enhancement
- **定義**：先確保最基本的 HTML／表單能運作（核心功能），再用 CSS／JS 疊加更好的體驗；
  JS 失敗或未載入時，頁面不應該完全無法使用。
- **為什麼**：網路環境與瀏覽器能力不可控，核心功能不該完全依賴最脆弱的一層（JS 執行）。
- **出處**：Steven Champeon（2003）提出概念；Aaron Gustafson《Adaptive Web Design》系統化整理。
- **如何檢查違反**：表單是否設了原生 HTML 驗證屬性（`required`、`type="email"`）當作第一道防線，還是完全依賴 JS 才會擋錯？關鍵互動是否綁在會被 JS 錯誤整個中斷的同一個執行流程裡（一個例外炸掉整頁互動）？

### 最低能力原則 — Rule of Least Power
- **定義**：解決問題時，優先選擇「能力最弱、但足夠達成目的」的語言或機制（例如能用 CSS 做的動畫不用 JS、能用原生表單驗證不用手刻 JS 驗證）。
- **為什麼**：能力越弱的機制越可預測、越容易被瀏覽器優化、越不容易出錯或被誤用。
- **出處**：Tim Berners-Lee，W3C TAG（"The Rule of Least Power"，2006）。
- **如何檢查違反**：是否用 JS 重新實作了瀏覽器原生就有的能力（如原生 `<form>` 驗證、原生 `<dialog>`）？

### 模組邊界與資訊隱藏 — Module Boundaries / Information Hiding
- **定義**：每個 ES module 對外只透過明確的 `export` 暴露「做什麼」，內部實作細節（DOM 結構假設、私有狀態）不外流。
- **為什麼**：呼叫方不該依賴模組的內部實作，否則實作稍微調整就會波及所有呼叫方。
- **出處**：David Parnas（1972），套用到 ES Modules 的現代實務。
- **如何檢查違反**：模組之間是否透過**共用的全域變數**或直接扒對方的 DOM 結構來傳遞資料，而非透過 `export`/`import` 或明確的函式參數？

### API 層與 UI 層分離
- **定義**：所有對後端（api-gateway）的請求，集中在專責的模組（如 `api.js`）；UI 互動邏輯（DOM 操作、事件綁定）不直接內嵌 `fetch()` 呼叫細節。
- **為什麼**：API 端點、header 組裝、錯誤格式若集中一處，替換後端契約或加上共用邏輯（如附加 Authorization header）時只需改一個地方；也讓 UI 程式碼專注在畫面。
- **為什麼歸類在此**：是 SoC 原則在「前後端整合」場景下的具體套用。
- **如何檢查違反**：`fetch()` 呼叫、API URL 組裝、Authorization header 組裝，是否散落在多支處理 DOM 事件的檔案裡各寫一份，而非集中在單一 API 模組？

### 狀態管理的單一入口
- **定義**：跨頁共用的狀態（登入 token、當前使用者資訊）應有單一、明確的讀寫入口（如集中在一個模組的 `getToken()`/`setToken()`），而非各檔案直接散呼叫 `localStorage.getItem/setItem`。
- **為什麼**：狀態的儲存位置、鍵名、序列化格式若要調整（例如 token 改存 sessionStorage、改用 cookie），只需改一處；也讓「這個狀態合法的讀寫時機」有地方集中把關。
- **為什麼歸類在此**：SSOT 原則在「前端跨頁狀態」場景下的具體套用。
- **如何檢查違反**：`localStorage`/`sessionStorage` 的 key（如 `'token'`）是否在多個檔案各自直接呼叫讀寫，而非透過共用的存取函式？

---

## C. 效能與資源管理

> **出處**：Google Web Vitals（web.dev，2020 起）為業界公認的前端效能量測標準；
> Vite 官方文件（vitejs.dev）為建置工具層面的最佳實務來源。

### 核心網頁指標 — Core Web Vitals
- **定義**：Google 定義的三項使用者體感效能指標——
  **LCP**（Largest Contentful Paint，最大內容繪製時間）、
  **INP**（Interaction to Next Paint，互動反應時間，2024 取代 FID）、
  **CLS**（Cumulative Layout Shift，累積版面位移）。
- **為什麼**：這些是少數有大規模使用者研究佐證、與「使用者感受到的流暢度」直接相關的指標，而非工程師憑感覺判斷「快不快」。
- **如何檢查違反**：頁面關鍵內容（如表單、角色列表）是否被非必要的大型資源（未壓縮圖片、同步阻塞的 script）擋在前面才能渲染？圖片是否缺少明確的 `width`/`height`（會造成載入時版面跳動，即 CLS）？

### 建置產物精簡 — Bundle Hygiene
- **定義**：只把實際會用到的程式碼與資源打包進正式產物；未使用的依賴、範例檔案、重複資源不該進 `dist/`。
- **為什麼**：多餘的 bytes 直接拖慢下載與解析時間，尤其影響行動網路使用者。
- **出處**：Vite 官方文件關於 `build` 與 tree-shaking 的說明（基於 Rollup）。
- **如何檢查違反**：專案裡是否有從未被任何 `import`/`<script>`/`<link>` 引用的檔案（鷹架殘留的範例元件、重複的樣式檔）？`package.json` 的 dependencies 是否有已經沒人 `import` 的套件？

### 資源快取與版本化
- **定義**：靜態資源（JS/CSS/圖片）應能被瀏覽器與 CDN 長期快取，內容變更時透過檔名雜湊（content hash）自然讓快取失效，而非仰賴使用者手動清快取。
- **為什麼**：正確的快取策略是前端效能最高槓桿的手段之一；Vite build 預設就會替產物加上 content hash 檔名，前提是不要手動繞過這個機制。
- **出處**：Vite 官方文件（`build.rollupOptions.output` / 預設檔名雜湊行為）；HTTP 快取語意見 MDN。
- **如何檢查違反**：是否有自建的、繞過 Vite 建置管線的靜態檔案（直接手動複製到 `dist/` 或以絕對路徑引用未經處理的資源）？

### 環境設定外部化（12-Factor Config 在前端的對應）
- **定義**：不同環境（開發／正式）的 API 位址、功能開關等設定，透過 Vite 的 `import.meta.env` 環境變數機制注入，而非寫死在程式碼裡。
- **為什麼**：與《後端系統設計原則》C 節的 12-Factor Config 同一道理——寫死的網址在換環境時得改程式碼、重新建置，容易漏改。
- **出處**：Adam Wiggins，The Twelve-Factor App（Factor III: Config）；Vite 官方文件 `.env` 機制。
- **如何檢查違反**：API base URL、後端 port 等是否直接寫死在 `.js` 檔案裡，而非讀取 `import.meta.env.VITE_*` 或執行期設定端點？

---

## D. 可及性與使用者體驗一致性

### 網頁內容可及性 — WCAG（Web Content Accessibility Guidelines）
- **定義**：W3C 制定的網頁可及性標準，涵蓋「可感知、可操作、可理解、穩健」四大原則
  （POUR: Perceivable, Operable, Understandable, Robust）。
- **為什麼**：確保使用輔助科技（螢幕報讀器、鍵盤導覽）的使用者也能完成核心流程；也是多數地區的法規要求基準。
- **出處**：W3C Web Accessibility Initiative，WCAG 2.1/2.2。
- **如何檢查違反**：表單欄位是否都有對應的 `<label>`（而非只靠 placeholder）？純粹用顏色（如紅字）傳達的錯誤訊息，是否有文字或圖示做為額外提示？互動元素（tab 切換、按鈕）是否能用鍵盤（Tab / Enter）操作，而不是只在滑鼠點擊時綁事件？

### 一致性與標準 — Consistency and Standards（Nielsen 十大可用性原則之一）
- **定義**：介面的用詞、視覺樣式、互動模式在同一產品（甚至跨產品家族）內應保持一致，讓使用者不必為每個頁面重新學習。
- **為什麼**：不一致會增加認知負擔，使用者得重新猜測「這裡的按鈕是不是跟剛剛那個一樣的意思」。
- **出處**：Jakob Nielsen，"10 Usability Heuristics for User Interface Design"（Nielsen Norman Group，1994）。
- **如何檢查違反**：同一組件（如錯誤訊息框、載入中按鈕文字）在不同前端專案裡的樣式、文案風格、互動時機是否各自為政？例如 A 專案的按鈕在送出中顯示「處理中...」，B 專案卻用不同用詞或完全沒有載入態提示？

### 錯誤預防與明確回饋（Nielsen 十大可用性原則之一）
- **定義**：系統應在使用者犯錯前就先預防（如格式限制、明確標示必填），犯錯後要給清楚、非技術性的錯誤說明，並提供修正路徑。
- **為什麼**：使用者不該收到後端內部訊息或技術堆疊資訊，也不該在出錯後不知道下一步能做什麼。
- **出處**：Jakob Nielsen，同上（"Error Prevention" 與 "Help Users Recognize, Diagnose, and Recover from Errors"）。
- **如何檢查違反**：錯誤訊息是否直接把後端回傳的原始技術訊息（stack trace、SQL 錯誤、內部代碼）原封不動顯示給使用者？使用者是否曾在錯誤後找不到任何後續動作（表單被清空又沒有提示重填）？

---

## 附：稽核使用方式

第二階段稽核某個服務時，逐條走過上表，對每條問「**這個服務有沒有違反？違反在哪個檔案？**」。
把找到的每個違反點，轉成一筆規格變更（OpenSpec change），由修改後的規格驅動程式碼優化——
這就是「**設計準則定義『對』，稽核找出『錯』，優化把『錯』改成『對』**」的完整閉環。

---

## Sources

- Hunt & Thomas,《The Pragmatic Programmer》（DRY）
- 極限編程（Extreme Programming）實務（YAGNI）
- Edsger W. Dijkstra（1974）— Separation of Concerns
- David Parnas（1972）— Information Hiding
- Steven Champeon（2003）／Aaron Gustafson,《Adaptive Web Design》— Progressive Enhancement
- Tim Berners-Lee, W3C TAG — [The Rule of Least Power](https://www.w3.org/2001/tag/doc/leastPower.html)
- Google / web.dev — [Core Web Vitals](https://web.dev/articles/vitals)
- [Vite 官方文件](https://vitejs.dev/guide/) — Build、Env Variables、Static Asset Handling
- W3C Web Accessibility Initiative — [WCAG 2.2](https://www.w3.org/WAI/standards-guidelines/wcag/)
- Jakob Nielsen, Nielsen Norman Group — [10 Usability Heuristics for User Interface Design](https://www.nngroup.com/articles/ten-usability-heuristics/)
- Adam Wiggins — [The Twelve-Factor App](https://12factor.net/)（Factor III: Config，前端環境設定對應）
