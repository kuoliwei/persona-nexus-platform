# 微服務架構準則

> 定義 Persona Nexus 微服務間的通訊、驗證、授權、資料隱私規則。

## 原則

### 1. 所有服務間通訊都須通過 API Gateway

前端、後端服務之間的所有通訊都經 Gateway 轉發，沒有例外。

### 2. 前端驗證策略

除了註冊（`POST /api/auth/register`）與登入（`POST /api/auth/login`）以外，所有路由都需要 JWT token 驗證。

後端服務之間的通訊不需要驗證機制。

### 3. 前端與 Gateway 的驗證流程

- 前端在登入時獲得 JWT token，存放於本機（localStorage）
- 前端每次請求都在 `Authorization: Bearer <token>` header 中帶上 token
- Gateway 驗證 token 的簽名和過期時間，驗證失敗則回 401

### 4. Gateway 與下游服務的信任機制

- Gateway 驗完 token 後，**不轉發 token 給下游服務**
- 改為從 token 提取使用者身份（user ID），注入 `x-user-id` header 轉發
- 下游服務信任 Gateway 已驗證，直接讀 `x-user-id` header 得知呼叫者身份
- **下游服務不驗 JWT**，只做授權檢查

### 5. 為什麼不傳 token 給下游

- token 是前端與 Gateway 間的驗證工具，下游服務只需知道「呼叫者是誰」
- 下游服務用 `x-user-id` header 足以判斷授權（例如：該使用者是否有權查詢這筆資料）
- 減少 token 在內部傳遞的風險

### 6. 所有資料存取都需認證

從資料庫取得任何資訊都需要先確認呼叫者的身份。token 的合法性只由 Gateway 驗證一次；下游服務不重驗 token，只依 Gateway 注入的 `x-user-id` header 確認身份。

### 7. 資源所有權檢查

除了明確標記為「公開」的資訊外，其他屬於某個使用者的所有資訊都不會被其他使用者取得。

每個下游服務都負責在授權層驗證「呼叫者 ID 是否等於資源擁有者 ID」。

### 8. 公開資訊的範圍

使用者創建的「公開角色」可被任何已登入的使用者查詢，不受所有權限制。除此以外，沒有其他公開資訊。

使用者的聊天記錄、帳號資訊、私有角色都不能被其他使用者得知。

服務間內部呼叫（帶 `x-internal-request` 標記）視為系統本身的操作，不受此條「其他使用者」的限制——內部呼叫本來就代表系統流程需要跨服務取用資料（例如 ai-service 讀取對話歷史），不是使用者身份意義上的「其他使用者」。

### 9. 路徑隱藏

Gateway 對外定義統一的 API 路徑，隱藏下游服務的內部路由結構。

前端只需認識 Gateway 的路徑契約，下游服務如何調整內部路由不影響前端。

各下游服務的 proxy 負責做路徑轉換，使下游服務可以獨立演進而無需變更前端呼叫方式。
