# 執行日誌：修正《微服務架構準則》《微服務架構實作 Spec》落差

> 對照 `待辦task.md` 逐項執行時的紀錄。每完成一個 T 補一則，格式固定：**改了哪些檔案、驗收結果、有沒有偏離原計劃**。

---

## 模板

```
### TN. 任務標題

- **日期**：
- **改了哪些檔案**：
- **驗收結果**：
- **是否偏離原計劃**：（沒有 / 有——說明原因和調整內容）
```

---

## 紀錄

### T1. 修 `userProxy.js` 補上 pathRewrite

- **日期**：2026-07-25
- **改了哪些檔案**：`api-gateway/src/proxies/userProxy.js`（新增函式式 `pathRewrite: (path) => `/users${path}``，取代原本完全沒有 pathRewrite 的空白）
- **驗收結果**：邏輯核對通過——`/api/users/usr_123` 經 `app.use('/api/users', ...)` 前綴掛載後 `path` 剩 `/usr_123`，pathRewrite 後變成 `/users/usr_123`，符合 user-service 實際路由（`/users/:id`）。同一支 proxy 也用在 `/internal/users`，一併修正。未啟動完整服務做端到端測試，僅靜態核對轉換邏輯。
- **是否偏離原計劃**：沒有

### T2. `internalAuthMiddleware` 注入 `x-internal-request: true`

- **日期**：2026-07-25
- **改了哪些檔案**：`api-gateway/src/middlewares/internalAuthMiddleware.js`（IP 驗證通過後，加一行 `req.headers['x-internal-request'] = 'true'`，在 `next()` 之前）
- **驗收結果**：邏輯核對通過——所有掛載 `internalAuthMiddleware` 的 6 條 `/internal/*` 路由（characters、conversations、users、rag、chat、health）現在都會在 IP 驗證通過後帶上這個 header 轉發給下游。未啟動服務實測。
- **是否偏離原計劃**：沒有

### T3. 新增 `POST /internal/users` 路由（Gateway 端）

- **日期**：2026-07-25
- **改了哪些檔案**：`api-gateway/src/app.js`（`/internal/users` 掛載處補上註解，明確標示涵蓋 GET/POST 兩種語意）
- **驗收結果**：原本 `app.use('/internal/users', ...)` 就是不分方法的前綴掛載，本來就會放行 POST，只是缺乏明確定義且 T1 之前 pathRewrite 是壞的。T1 修好 pathRewrite 後，這個既有掛載對 `POST /internal/users` 已經是可運作的：轉發到 user-service 的 `POST /users`。沒有新增獨立的 `app.post()` 路由，因為現有前綴掛載已經涵蓋，重複定義反而會造成路由衝突。
- **是否偏離原計劃**：有——原計劃是「新增路由」，實際上判斷不需要新增獨立路由定義，只需確保既有前綴掛載搭配已修好的 pathRewrite 就能滿足需求，改成補強註解讓意圖明確。這不影響行為結果，只是實作手法比原計劃更精簡。

### T4. user-service 新增 `POST /users` 的 `x-internal-request` 檢查

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `user-service/src/services/userService.js`：`createUser` 加上 `{ isInternalRequest }` 參數，非內部呼叫直接 throw `FORBIDDEN`
  - `user-service/src/controllers/userController.js`：`createUser` 讀 `x-internal-request` header 傳給 service；`ERROR_MAP` 補上 `FORBIDDEN`（403）、`UNAUTHORIZED`（401）
- **驗收結果**：邏輯核對通過——沒有 `x-internal-request: true` header 的請求呼叫 `POST /users` 會被 service 層擋下並回 403；帶正確 header（由 T2 的 `internalAuthMiddleware` 注入）才能建帳號。未啟動服務實測。
- **是否偏離原計劃**：沒有

### T5. 移除 `authorizeSelf.js`，授權邏輯搬到 service 層

- **日期**：2026-07-25
- **改了哪些檔案**：
  - 刪除 `user-service/src/middlewares/authorizeSelf.js`（及變空的 `middlewares/` 目錄）
  - `user-service/src/services/userService.js`：`getUserById`、`deleteUser` 加上 `{ requesterId, isInternalRequest }` 參數，判斷順序改為「`isInternalRequest` 為真則放行，否則檢查 `requesterId` 存在且等於目標 `id`」
  - `user-service/src/controllers/userController.js`：兩個方法改讀 `x-user-id`、`x-internal-request` header 傳給 service
  - `user-service/src/app.js`：移除 `authorizeSelf` 的 import 與掛載
- **驗收結果**：邏輯核對通過——GET/DELETE `/users/:id` 的授權判斷順序與原本 middleware 一致（`x-internal-request === 'true'` 放行；否則比對 `x-user-id === :id`，不符則 403，缺 header 則 401）。grep 確認全專案 `.js` 檔已無任何 `authorizeSelf` 引用，僅存文件與 openspec 歷史記錄提及（不影響執行，未列入本次任務範圍，未更動）。未啟動服務實測。
- **是否偏離原計劃**：沒有——但與 T4 合併在同一輪修改中完成，因為兩者改動同一批檔案（`userController.js`、`userService.js`），分開做會產生中間不一致的狀態，故依序做完 T4 後緊接著做 T5，日誌分開記錄以對應 task 編號。

### T6. auth-service 改連 Gateway `/internal/users`，不再直連 user-service

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `auth-service/src/repositories/userRepository.js`：`USER_SERVICE_URL`（直連 user-service:4000）改成 `GATEWAY_URL`（打 Gateway:8000），`BACKEND_GET_USER_BY_EMAIL_URL`／`BACKEND_REGISTER_URL` 改指向 `${GATEWAY_URL}/internal/users?email=`／`${GATEWAY_URL}/internal/users`
  - `auth-service/.env`、`.env.example`：`USER_SERVICE_URL=http://localhost:4000` 改成 `GATEWAY_URL=http://localhost:8000`
- **驗收結果**：邏輯核對通過——註冊、登入查詢流程改走 Gateway 的 `/internal/users` 路由，該路由已在 T1-T3 修好（pathRewrite 正確、經 `internalAuthMiddleware` 做 IP 驗證並注入 `x-internal-request`）。grep 確認 `.js` 檔已無殘留的 `USER_SERVICE_URL` 引用。未啟動服務實測。
- **是否偏離原計劃**：有一處中途修正——第一次編輯時誤在 `userRepository.js` 裡自己组一組 `x-internal-request: 'true'` header 準備附加到 fetch 請求上，隨即發現這樣寫違反信任鏈設計（`x-internal-request` 應該只能由 Gateway 依來源 IP 注入，若呼叫端自己能加這個 header，等於任何服務都能偽造「我是內部請求」，這正是準則裡「前端無法加」這個信任假設要防的事）。已改回不在 auth-service 端自行注入該 header，改成單純呼叫 Gateway、讓 Gateway 的 `internalAuthMiddleware` 負責注入。`GET /internal/users?email=` 這條路由本身在 user-service 端的授權補強（目前 `getUserByEmail` 尚未檢查 `x-internal-request`）留給 T7 處理，不在本次範圍內混著改。

### T7. 修掉或收斂 `GET /users?email=` 這個暗門路由

- **日期**：2026-07-25
- **採用方案**：方案 A（併入 `/internal/users` 系列）。理由：T6 已經讓 auth-service 改走 Gateway 的 `/internal/users`，而 Gateway 端 `app.js:97` 的 `/internal/users` 掛載本來就不分方法、也不分 query，`GET /internal/users?email=` 自然被涵蓋，不需要另開路由，只差 user-service 端補上檢查。
- **改了哪些檔案**：
  - `user-service/src/services/userService.js`：`getUserByEmail` 加上 `{ isInternalRequest }` 參數，非內部呼叫 throw `FORBIDDEN`
  - `user-service/src/controllers/userController.js`：`getUserByEmail` 讀 `x-internal-request` header 傳給 service；更新兩處過時註解（原本寫「不對外開放、auth-service 直連」，改成「內部請求限定、經 Gateway /internal/users?email= 轉發」）
- **驗收結果**：邏輯核對通過——現在 `POST /users`、`GET /users?email=`、`GET /users/:id`、`DELETE /users/:id` 四個路由全部要求 `x-internal-request: true` 或（後兩者）`x-user-id` 比對，不再有無條件放行的暗門路由。密碼雜湊只會回給帶正確 header 的內部呼叫。未啟動服務實測。
- **是否偏離原計劃**：沒有——task 文件列的兩個方案裡選了方案 A，且此方案在 T6 完成後已幾乎是自然結果，只需補 user-service 這一端。

---

**批次 2（user-service 收尾）到此全部完成（T4-T7）。**

### T8. character-service 新增 `/internal/characters/:id` 路由與 `x-internal-request` 放行邏輯

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `character-service/src/services/characterService.js`：`getCharacter`、`updateCharacter`、`deleteCharacter` 加上 `{ isInternalRequest }` 參數（預設 `{}`），判斷順序改為「`isInternalRequest` 為真則跳過所有權/可見性檢查，否則走原本邏輯」
  - `character-service/src/controllers/characterController.js`：三個對應方法讀 `x-internal-request` header 傳給 service
- **驗收結果**：邏輯核對通過。Gateway 端 `/internal/characters` 掛載本來就沿用 `characterProxy` 既有的 pathRewrite（補 `/api/v1/characters` 前綴），會命中 character-service 現有的 `/api/v1/characters/:id` 路由——不需要新增獨立路由，跟 T3 的判斷一致。內部呼叫（帶 `x-internal-request: true`）現在能查詢私有角色詳情、編輯、刪除，不會被誤擋 `FORBIDDEN`。`createCharacter` 沒有動，因為 spec 授權決策表只對 GET/PUT/DELETE 列了 `x-internal-request` 例外，建立角色不在此列。未啟動服務實測。
- **是否偏離原計劃**：沒有——task 描述原本預期「新增路由」，但跟 T3 一樣，判斷既有前綴掛載＋pathRewrite 已經涵蓋，不需要另外新增，只需補 service 層邏輯。

- **⚠️ 2026-07-25（另一輪對話，回填 `測試清單-進階整合測試.md` 驗證方式時發現）追加記錄：本次改動的「驗收結果」有覆蓋盲點，已間接造成一個安全漏洞（案例 13/15/16）**
  - 本任務的驗收標準（`待辦task.md` T8）與驗收結果都**只鎖定 ai-service** 作為 `/internal/characters/:id` 的呼叫者（與 spec 內部路由表一致），驗證方式也只確認「內部呼叫能不能查到私有角色」，**沒有檢查這個新加上去的 `isInternalRequest` 繞過邏輯，會不會被其他既有呼叫者意外觸發**。
  - `mistakes.md` 的 M7 記錄了 T8 修復前的現況：`characterService.js` 全文 grep `x-internal-request` 零匹配——也就是說，**T8 之前，這個判斷式完全不存在，`getCharacter` 一律走一般所有權判斷**。
  - 但 `chat-service/src/lib/serviceClient.js` 的 `getCharacter()` 方法（本輪從未被任何 T 任務修改過）本來就會呼叫 `GET {GATEWAY}/internal/characters/:id`，目的是幫 `conversationService.getOrCreateConversation` 驗證角色是否存在——**T8 之前，這個呼叫因為沒有繞過邏輯，會正常回傳 403 擋下跨帳號私有角色訪問；T8 之後，同一個呼叫因為 `isInternalRequest` 被 T2 無條件注入為 `true`，所有權檢查被整段跳過，任何登入者都能透過 chat-service 開啟與他人私有角色的對話**。
  - 這代表 T8 本身的程式碼邏輯（讓 ai-service 內部呼叫繞過所有權檢查）完全正確、符合 spec，**但驗收時沒有評估「這個新能力的影響範圍（blast radius）」，波及了一個原本無害、且不在 T8 任務範圍內的既有呼叫者**。
  - 詳細判決見 `測試清單-進階整合測試.md` 第四節案例 13/15/16 下方的完整分析（已依準則原則 7/8 與 spec 內部路由表判定為原始碼違規，需修復）。修復不應動 T8 本身的邏輯（那對 ai-service 是正確的），應該是讓 `chat-service` 改走 `/api/characters/:id` 外部路由並帶使用者身份，走一般所有權檢查。

### T9 + T10（合併執行）. chat-service 補齊 `/internal/conversations` 放行邏輯，並拆除獨立 authMiddleware

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `chat-service/src/services/conversationService.js`：
    - `assertConversationOwnership` 加上 `{ isInternalRequest }` 參數（預設 `{}`），為真時跳過 `validateUserId` 與 `conversation.userId !== userId` 兩處檢查
    - `sendMessageToConversation`、`getMessagesByConversationId` 這兩個 spec 指定要支援內部放行的方法，加上 `{ isInternalRequest }` 參數並傳給 `assertConversationOwnership`
    - 其餘呼叫 `assertConversationOwnership` 的方法（`deleteConversation`、`getProtagonist`、`updateProtagonist`、`deleteMessageAndSubsequent`、`getMessageById`、`getAIGenerationStatus`、`clearAIGenerationStatus`）未傳第三參數，`isInternalRequest` 為 `undefined`，行為與修改前完全一致
    - 清掉一句過時註解（原本說「authMiddleware 已保證它存在」，現在改由 service 層自己的 `validateUserId` 承擔）
  - `chat-service/src/controllers/conversationController.js`：`sendMessageToConversation`、`getMessagesByConversationId` 讀取 `x-internal-request` header 傳給 service
  - `chat-service/src/app.js`：移除 `authMiddleware` 的 import 與所有路由上的掛載（16 條路由全部拿掉）
  - 刪除 `chat-service/src/middlewares/authMiddleware.js`（及變空的 `middlewares/` 目錄）
- **驗收結果**：
  - 邏輯核對通過——沒有呼叫 `assertConversationOwnership` 的方法（`getOrCreateConversation`、`getAllConversations`、`getConversationsSummary`、`sendMessage`、`getMessages`、`deleteConversationsByCharacter`、`retryConversationCreation`）各自都在方法開頭呼叫 `validateUserId(userId)`，缺 `x-user-id` 時仍會被 service 層擋下 401，與拆 middleware 前行為一致
  - 確認 Gateway 端 `chatProxy` 的 pathRewrite 是 `(path) => '/api/v1/conversations' + path`，`/internal/conversations/:id/messages` 轉發後會命中與外部 `/api/conversations/:id/messages` 完全相同的路徑，不需要在 `app.js` 新增獨立路由定義
  - grep 確認 `.js` 檔已無 `authMiddleware` 的 import/掛載殘留，只剩一句已核實無害的舊註解文字提及（已一併清除）
  - 未啟動服務實測
- **是否偏離原計劃**：有——task 文件把 T9、T10 列成先後兩個獨立任務（T10 依賴 T9），但實際動手時發現兩者有循環依賴：T9 若不先拆掉 `authMiddleware`，內部呼叫（沒有 `x-user-id`）連 controller 都進不去、根本測不出 `x-internal-request` 放行邏輯有沒有生效；反過來 T10 拆 middleware 時，若 `assertConversationOwnership` 還沒有 `isInternalRequest` 判斷，外部行為的驗收基準也無從對照。因此把兩者合併在同一輪修改內完成，日誌合併記錄。過程中也先誤在 `app.js` 新增了一段獨立的 `/internal/conversations` 路由定義，隨即發現會與既有外部路由（相同路徑）衝突、Express 只會匹配先註冊的那條，新路由永遠執行不到，已撤回；正確作法是讓現有路由的 controller 同時服務兩種呼叫來源。

---

**批次 3（character-service、chat-service 補齊 `/internal/*` 與授權邏輯）到此全部完成（T8、T9+T10）。**

### T11. 決定並記錄最終回應格式規格

- **日期**：2026-07-25
- **決策**：改成 spec 原定格式，5 個服務都要改過去。理由：spec 定義的格式簡潔、和多數 REST API 慣例一致；反過來讓 spec 遷就 `{status, message, data}` 的話，這個格式本身資訊重複（`data` 外面又包一層 `status`/`message`），不是常見慣例，且部分服務（`user-service` 的錯誤回應、`chat-service` 的部分成功回應）連這個「現況格式」自己都沒有寫齊全（缺 `error` 欄位、鍵名 `status` vs `success` 不一致），沒有一個乾淨的現況可言，不如直接對齊 spec。
- **驗收結果**：決策已確認，成為 T12-T16 的依據：
  - 錯誤回應：`{ error: "<CODE>", message: "<human readable>" }`
  - 成功回應（單一資源）：直接返回物件本身
  - 成功回應（資源清單）：直接返回陣列本身
  - 成功回應（操作成功、無回傳內容）：`{ success: true, message: "..." }`
- **是否偏離原計劃**：沒有——task 描述本來就是「先做決策」，決策已由使用者選定「改成 spec 原定格式」。

### T12. auth-service 回應格式對齊

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `auth-service/src/controllers/authController.js`：
    - `respondError`：改成 `{ error: error.message, message: mapped.message }`（`error.message` 本身就是英文錯誤碼常數，如 `EMAIL_ALREADY_EXISTS`），未命中 `ERROR_MAP` 時回 `{ error: 'INTERNAL_SERVER_ERROR', message: ... }`
    - `register`、`login`：成功回應從 `{status:'success', message, data:result}` 改成直接 `res.json(result)`
  - `persona-nexus-auth/src/main.js`：前端讀取結果的路徑同步調整，`result.data.id` → `result.id`、`result.data.token` → `result.token`（`result.message` 因為在錯誤分支讀取、格式本來就沒變，不用改）
- **驗收結果**：邏輯核對通過——`authService.js` 的 `register()` 回傳 `{id, email}`、`login()` 回傳 `{id, email, token}`，都是扁平物件，符合 spec「單一資源直接返回物件」的定義，controller 直接 `res.json(result)` 不需要額外轉換。前端讀取路徑已同步更新，登入/註冊訊息顯示與 token 導轉邏輯不受影響。未啟動服務實測。
- **是否偏離原計劃**：有——task 只列了「auth-service 回應格式對齊」，實際執行時發現前端 `persona-nexus-auth/src/main.js` 直接依賴舊格式的巢狀路徑（`result.data.id`、`result.data.token`），若不同步改前端，回應格式改完後登入/註冊會直接顯示 `undefined`、token 導轉會帶入 `undefined` 字串。因此把前端同步修正一併納入這次改動，不算脫離任務精神（T11 決策本來就標注「這批會動到前端」），但具體改到哪個檔案是任務文件沒寫清楚的細節，補充記錄於此。

- **⚠️ 2026-07-25（另一輪對話，回填 `測試清單-進階整合測試.md` 驗證方式時發現）追加記錄：本次改動範圍認定過窄，遺漏了狀態碼本身也需要對照 spec**
  - 本任務（及對應的 `mistakes.md` M11）把「回應格式對齊」理解成**只需要修正 body 的鍵名**（從 `{status,message}` 改成 `{error,message}`），從頭到尾沒有檢查**狀態碼**是否也符合 spec。
  - spec.md 第三部分「HTTP Status Code 統一定義」明文寫：`409 | Conflict | 衝突（例如：email 已被註冊）`——**用「email 已被註冊」當作 409 的唯一具體範例**。但 `authController.js` 的 `ERROR_MAP` 裡 `EMAIL_ALREADY_EXISTS` 的 `status` 從改動前到改動後都是 `400`，T12 完全沒有動過這個數字。
  - 對照 `user-service`（T13）的同名錯誤碼 `EMAIL_ALREADY_EXISTS`，`status` 正確是 `409`——代表這不是「spec 本身模糊」，是 T12 執行時漏查了 spec 的狀態碼表，只做了 body 格式這一半。
  - 詳細判決見 `測試清單-進階整合測試.md` 第六節 J1 下方的完整分析。修復方式：把 `auth-service/src/controllers/authController.js` 的 `ERROR_MAP` 裡 `EMAIL_ALREADY_EXISTS` 的 `status` 從 `400` 改成 `409`，訊息文字不變。

### T13. user-service 回應格式對齊

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `user-service/src/controllers/userController.js`：
    - `respondError`：補上 `error` 欄位（未命中 `ERROR_MAP` 時回 `error: 'INTERNAL_SERVER_ERROR'`）
    - `createUser`、`getUserByEmail` 的就地 400 檢查（缺欄位）補上 `error: 'REQUIRED_FIELDS_MISSING'`
    - `deleteUser` 成功回應從 `res.status(204).send()`（無 body）改成 `res.status(200).json({ success: true, message: '...' })`
- **驗收結果**：邏輯核對通過——成功回應（`createUser`、`getUserByEmail`、`getUserById` 皆為 `res.json(user物件)`）本來就符合 spec「直接返回物件」，這次不用改；只需補齊錯誤格式的 `error` 欄位、修正 `deleteUser` 的無回傳內容格式。grep 確認前端 4 個 `persona-nexus-*` 專案沒有任何地方依賴 `DELETE /users/:id` 回 204 的行為，改動安全。未啟動服務實測。
- **是否偏離原計劃**：沒有

### T14. character-service 回應格式對齊

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `character-service/src/controllers/characterController.js`：`respondWithError` 補上 `error` 欄位；5 個方法（`createCharacter`、`listCharacters`、`getCharacter`、`updateCharacter`、`deleteCharacter`）的成功回應全部從 `{status, message, data}` 改成直接 `res.json(result)`
  - `persona-nexus-character/src/create.js`：`createCharacter` 的 `api.js` 封裝本來就是「失敗即 throw、成功回傳結果本身」的模式，`if (result.status === 'success')` 這個判斷從來就是死路徑（失敗早在 `api.js` 就 throw 了，走到這裡必然成功）。移除該判斷與對應的 `else` 錯誤分支，直接讀 `result.id`
  - `persona-nexus-character/src/edit.js`：`loadCharacter()`、表單送出、刪除按鈕三處，同樣移除死路徑判斷，直接使用 `getCharacter`/`updateCharacter`/`deleteCharacter` 的回傳值（`getCharacter` 回傳值直接當角色物件 `c` 使用）
  - `persona-nexus-lobby/src/home.js`：`initHomePage()` 改成 `const characters = await response.json()`，不再讀 `result.data`
  - `persona-nexus-lobby/src/my-character.js`：`loadMyCharacterPage()` 同樣移除 `result.status !== 'success'` 判斷（`listMyCharacters()` 的 `api.js` 封裝已經是 throw-or-return-result 模式），直接用回傳值渲染
- **驗收結果**：
  - 讀過 `persona-nexus-character/src/api.js`、`persona-nexus-lobby/src/api.js` 的 `getCharacter`/`updateCharacter`/`createCharacter`/`deleteCharacter`/`listMyCharacters` 這幾個封裝函式，全部都是「`!response.ok` 就 `throw new Error(...)`，成功則 `return response.json()`」的固定模式——這代表呼叫端拿到的 `result` 必然是成功回應本體，原本 `if (result.status === 'success')` 這種判斷邏輯本來就是死碼（不可能為 false，因為 false 分支的資料格式根本不會被 `api.js` 傳到這一層），這次順便清掉了
  - `home.js` 是直接 `fetch` 沒有經過 `api.js` 封裝，但邏輯一致：`!response.ok` 已先 throw，走到 `characters = await response.json()` 時必為成功回應（陣列本身）
  - 未啟動服務或前端實測，僅靜態核對呼叫鏈與回傳格式是否對得上
- **是否偏離原計劃**：有——task 只寫「character-service 回應格式對齊」，範圍原本預期只動後端；但這次前端影響面比 T12（auth-service）更廣，一次牽動兩個前端專案（`persona-nexus-character`、`persona-nexus-lobby`）共 4 個檔案。過程中意外發現這些前端原本的 `if (result.status !== 'success')` 判斷邏輯，因為 `api.js` 已經用 `throw` 處理了失敗情況，這些判斷從一開始就是永遠不會進入的死路徑（不是這次格式修改才產生的問題，是既有程式碼的邏輯冗餘），順手一併清理，不算擴大任務範圍，而是修正回應格式時「順便」讓程式碼更誠實地反映實際會發生的分支。

### T15. chat-service 回應格式對齊

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `chat-service/src/controllers/conversationController.js`：15 個方法逐一核對，錯誤回應統一補上 `error` 欄位（`ERROR_MAP` 查表的用 `error.message` 本身當錯誤碼；各種特殊分支如 `FORBIDDEN`、`AI_SERVICE_UNAVAILABLE`、`AI_GENERATION_IN_PROGRESS`、`SERVICE_ERROR` 都補上對應碼；500 fallback 統一用 `error: 'INTERNAL_SERVER_ERROR'`）；`deleteConversation`、`deleteConversationsByCharacter`、`deleteMessageAndSubsequent`、`clearAIGenerationStatus` 這幾個「操作成功、無實質資源」的端點，成功回應從 `{status: 'success'/'cleared', ...}` 改成 `{success: true, ...}`；`updateProtagonist` 從 `{status:'success', ...result}` 改成直接返回 `result`（它本身是資源物件，不該包裹）
  - `chat-service/src/services/conversationService.js`：`retryConversationCreation` 的回傳值從 `{status: 'cleared', message}` 改成 `{success: true, message}`（controller 只是原樣轉發，實際格式定義在 service 層，改動要一起做）
- **驗收結果**：
  - **刻意不動**：`getOrCreateConversation` 回傳的 `status: 'preparing'/'failed'/'ready'` 與 `getAIGenerationStatus` 回傳的 `status: 'generating'/'completed'/'failed'/'unknown'`，這兩處的 `status` 是業務語意欄位（供前端輪詢判斷用），不是「操作成功/失敗」的格式包裹，維持原樣。
  - grep 確認 `persona-nexus-chat` 前端沒有依賴任何被改動的欄位（`res.status` 是 HTTP status code、不是 body 欄位；`generationStatus.status` 讀的是保留不動的業務語意欄位）；也確認前端目前沒有呼叫 `retry`/`deleteConversation`/`deleteMessageAndSubsequent`/`clearAIGenerationStatus`/`updateProtagonist` 這幾個端點，不受影響。
  - 未啟動服務實測。
- **是否偏離原計劃**：沒有——但這次改動量是 5 個服務裡最大的一支（單一 controller 檔案 15 個方法全部要核對），且發現一個需要謹慎判斷的細節：不能盲目把所有 `{status, ...}` 都當成「格式包裹」拿掉，`getOrCreateConversation`／`getAIGenerationStatus` 的 `status` 欄位是給前端輪詢用的業務語意，誤刪會直接弄壞輪詢邏輯，這點在動手前先花時間分辨清楚才下手。

### T16. ai-service 回應格式對齊

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `ai-service/app.py`：新增全域 `@app.exception_handler(HTTPException)`，把所有端點既有的 `raise HTTPException(status_code=..., detail="...")` 統一轉換成 `{error: 'AI_SERVICE_ERROR', message: <原本的 detail>}` 格式；`raise HTTPException(...)` 呼叫點本身完全不用改
  - `chat-service/src/lib/serviceClient.js`：4 處讀取 ai-service 錯誤回應 `error.response?.data?.detail` 的地方，改讀 `error.response?.data?.message`（因為 ai-service 錯誤回應的鍵名從 `detail` 改成 `message`）
- **驗收結果**：
  - **刻意不動**：`rag_service.py`／`chat_service.py` 所有成功回應裡的 `status` 欄位（`accepted`/`pending`/`ready`/`failed`/`success`/`ok`/`error`/`unknown`）都是業務語意，不是格式包裹，維持原樣。`serviceClient.js` 裡讀 `response.data.status === 'success'` 的判斷邏輯（`initializeRAG`、`cleanupRAG`、`generateResponse`、`generateSummary`、`addSummary`、`deleteSummaries`、`updateProtagonistRAG`）全部不受影響
  - 核對 `chat_service.py::generate_summary` 回傳 `{status:'success', data:{summary}}`，與 `serviceClient.js::generateSummary` 讀取的 `response.data.data.summary` 對得上，這個結構我沒有改動，維持一致
  - 未啟動服務實測
- **是否偏離原計劃**：有——task 只寫「ai-service 回應格式對齊」，範圍只指向 ai-service 本身。實際執行時發現兩個更深的相依問題：
  1. `serviceClient.js` 有 4 處直接讀 ai-service 錯誤回應的 `detail` 欄位，若不同步改，改完格式後這些地方會讀到 `undefined`，錯誤訊息退化成 axios 的籠統訊息（不會整個壞掉，但除錯資訊會變差）——這是 ai-service 格式改動的直接連鎖，理應在本輪一起處理
  2. 過程中額外發現 `serviceClient.js` 的 `getCharacter`／`getUser` 兩個方法（呼叫 character-service／user-service，不是 ai-service）還在讀 `response.data.status === 'success'` 和 `response.data.data`，這是 **T14／T13 遺留的相依性**——那兩個服務的回應格式在先前批次已經改成直接返回物件，但 `serviceClient.js` 這兩處呼叫端當時沒有同步發現並修正。這次做 T16 時逐一核對 `serviceClient.js` 全檔才補上，屬於補一個先前批次的漏網之魚，不算 T16 本身範圍內的東西，但既然發現了就一併修掉，避免留下一個已知會壞掉的呼叫路徑。

---

**批次 4（回應格式統一）到此全部完成（T11-T16）。**

### T17. 決定持久化技術選型

- **日期**：2026-07-25
- **決策**：DB 欄位，沿用現有 Prisma/SQLite。理由：不用新增基礎設施依賴、部署維持單服務單資料庫的現況，符合學習型專案的規模；`aiGenerationStatus` 兼職的並行鎖需要用 DB 的唯一性約束或明確狀態機取代，選型時一併設計。
- **驗收結果**：決策已確認，成為 T18、T19 的依據。
- **是否偏離原計劃**：沒有——task 描述本來就是「先做決策」。

### T18. chat-service 的 `creationJobs`、`aiGenerationStatus` 改為持久化

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `chat-service/prisma/schema.prisma`：
    - `Conversation` 表新增 6 個欄位：`generationStatus`（`'generating'|'completed'|'failed'|null`）、`generationError`、`generationTempUserId`、`generationUserMessageId`、`generationAssistantMessageId`、`generationUpdatedAt`
    - 新增 `ConversationCreationJob` 表，複合主鍵 `(userId, characterId)`，欄位 `status`（`'preparing'|'failed'`）、`conversationId`、`error`、時間戳
  - `chat-service/src/repositories/conversationRepository.js`：新增 `conversationCreationJobRepository`（`findByKey`／`upsert`／`delete`）
  - `chat-service/src/services/conversationService.js`：
    - 移除 `const aiGenerationStatus = new Map()`、`const creationJobs = new Map()` 兩個模組層級變數
    - `_prepareAndCreateConversation`：成功時改成刪除 job 記錄（不再標記 `ready`——這個狀態值原本就從未被讀取，DB 有 `Conversation` 記錄本身就代表就緒，符合原本註解「聊天室一旦成功寫入 DB，就從 Map 移除」的既有設計意圖），失敗時 `upsert` 寫入 `failed` 狀態
    - `getOrCreateConversation`、`retryConversationCreation`：改呼叫 `conversationCreationJobRepository`
    - `sendMessageToConversation` 的並行鎖：原本靠 in-memory Map 天然的同步區塊原子性（檢查後立刻上鎖、中間無 `await`），改成 `prisma.conversation.updateMany()` 搭配 `where` 條件做資料庫層級的「檢查後更新」——`where` 涵蓋「目前不是 generating」「從未生成過（null）」「殭屍鎖已過期」三種情況，`updateMany` 回傳的 `count === 0` 代表這三個條件都不成立（別人持有有效鎖），視為搶鎖失敗
    - `_generateAIResponseAsync`、`deleteMessageAndSubsequent`、`getAIGenerationStatus`、`clearAIGenerationStatus`：改讀寫 `Conversation` 表的 `generation*` 欄位
    - `deleteConversation`、`deleteConversationsByCharacter`：移除原本「額外清除 Map 記錄」的程式碼——因為 `generationStatus` 現在是 `Conversation` 表本身的欄位，整筆記錄被刪除時自然一併清除，不需要分開處理
    - `deleteMessageAndSubsequent` 順便優化：改用 `assertConversationOwnership` 已回傳的 `conversation` 物件讀 `generationStatus`，不用再多查一次 DB
  - 新增 migration `20260725092955_add_persistent_generation_and_creation_job_state`，已在本機 `dev.db` 執行 `npx prisma migrate dev` 套用，並重新產生 Prisma Client
- **驗收結果**：
  - 逐一 grep 確認 `conversationService.js` 已無任何 `aiGenerationStatus`／`creationJobs` 的實際使用（僅剩一段本來就被停用的實驗性註解程式碼，內容也一併同步更新避免未來被取消註解時撞到已刪除的變數）
  - `node --check` 確認 `conversationService.js`、`conversationRepository.js`、`conversationController.js` 三個檔案語法正確
  - controller／前端相容性核對：`getAIGenerationStatus` 的回傳格式（`{status, error, tempUserId, userMessageId, assistantMessageId, timestamp}`）與原本 Map 存的內容欄位名一致，`persona-nexus-chat/src/chat.js` 讀取的欄位（`generationStatus.status`、`.error`、`.assistantMessageId`、`.tempUserId`、`.userMessageId`）全部對得上，不需要改前端
  - `migrate dev` 實際執行成功、`prisma generate` 成功，schema 變更已生效
  - 未啟動服務做端到端聊天流程實測（未實際跑過一次完整的建立聊天室 + 發訊息 + 輪詢流程）
- **是否偏離原計劃**：有兩處值得記錄：
  1. 並行鎖設計是這次改動裡風險最高的部分——task 文件的驗收標準只寫「狀態要能在服務重啟後保留、且能被同一服務的任意實例讀取」，沒有明確規範鎖的實作細節，但既有程式碼的鎖語意（拒絕並行生成 + 殭屍鎖超時保險）必須完整保留，否則會引入新的併發 bug。花了額外時間確認 `updateMany` 的 `where` 條件在 SQLite 對 `NULL` 值的比較語意，明確加了 `{ generationStatus: null }` 這個條件，不能只依賴 `{ not: 'generating' }` 涵蓋所有情況
  2. 為了做原子性條件更新，`sendMessageToConversation` 直接呼叫 `prisma.conversation.updateMany`，跳過了 `conversationRepository` 這層抽象，打破了「service 層不碰 Prisma、只透過 repository」的既有慣例。這是刻意的取捨：這種帶條件的原子更新語意很特殊，硬包成通用 repository 方法（例如 `tryAcquireGenerationLock`）也是可以，但這次判斷直接寫更清楚地表達「這是一個資料庫層面的樂觀鎖」，沒有特別抽象化。

### T19. ai-service 的 `initialization_jobs` 改為持久化 —— 擱置

- **日期**：2026-07-25
- **狀態**：**未執行，擱置待後續決策**
- **原因**：T17 決定的「DB 欄位」方案，前提是服務本身已有關聯式資料庫可以加欄位。chat-service（T18）符合這個前提（既有 Prisma/SQLite），但 ai-service 目前完全沒有關聯式資料庫——CLAUDE.md 明確記錄「無 ORM／關聯式資料庫，只存 RAG 用的向量切片」，只有 Qdrant 這個向量資料庫連線。要嘛引入一個新的關聯式 DB（例如 SQLite + SQLAlchemy），要嘛借用 Qdrant 當 key-value 儲存（但這會偏離「DB 欄位」的原意、且與 chat-service 的做法不一致）。這是一個比「換儲存介質」更大的技術棧決策，詢問後決定先擱置，不在本輪動工。
- **目前狀態**：`ai-service/src/services/rag_service.py` 的 `initialization_jobs` 維持原本的進程內記憶體 dict，未改動。`mistakes.md` 的 M14 落差依然存在，未解決。
- **是否偏離原計劃**：是——`待辦task.md` 原本把 T19 列為與 T18 同批次、可並行執行的任務，實際執行時因為技術前提不成立而擱置，只完成了 T18。

---

**批次 5（非同步狀態持久化）到此結束：T17 決策完成、T18 完成、T19 擱置。**

### T20. 確認 `GET /api/characters` 的查詢方式是否為預期設計

- **日期**：2026-07-25
- **決策過程**：起初以為要合併「自己的角色」與「別人的 public 角色」兩個集合，但這個框架本身有問題——`public` 角色對所有人可見，「別人的」這個限定詞是多餘的，會誤導成需要排除自己的 public 角色。經過確認，正確邏輯應該是「自己的所有角色（含 private）」聯集「所有 public 角色（不限作者，含自己的）」，兩集合天然可能重疊（自己的 public 角色），需要去重。
- **改法**：選擇「新增第三種查詢模式，不帶任何 query 時觸發合併邏輯」，不動現有的 `?visibility=public`（大廳）、`?authorId=xxx`（我的角色頁）兩個既有場景，前端呼叫方式不受影響。
- **改了哪些檔案**：
  - `character-service/src/services/characterService.js`：`listCharacters` 新增 `else if (requesterId)` 分支，查詢邏輯為 `authorId === requesterId OR visibility === 'public'`，用 `Map`（依 `id` 為 key）去重後回傳
  - `微服務架構實作spec.md`：修正 `GET /api/characters` 那一列的邏輯描述，把「登入者自己的角色 + 標記為 public 的其他角色」改成精確描述三種查詢模式（不帶 query 的合併模式、`?visibility=public`、`?authorId=xxx`），並明確寫出去重規則
- **驗收結果**：`node --check` 語法確認通過。現有兩個場景（`home.js` 用 `?visibility=public`、`my-character.js` 用 `?authorId=`）完全沒被改動，行為不受影響。新場景（不帶 query）目前沒有前端呼叫方，是為未來可能的「合併視圖」需求預先支援，不影響現況。未啟動服務實測。
- **是否偏離原計劃**：有——過程中我第一次對「自己+別人 public」的理解是錯的（以為要排除自己的 public 角色），經過來回確認才釐清正確語意是「所有 public 角色（含自己的）」與「自己的所有角色（含 private）」取聯集。這個澄清過程比預期多花了幾輪確認，但避免了實作出一個語意錯誤的查詢邏輯。

---

## 批次 7：T1-T20 執行後新發現的落差（M16、M17）

### T21. 修復 chat-service 呼叫 character-service 時繞過私有角色所有權檢查的漏洞

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `character-service/src/services/characterService.js`：`getCharacter` 方法的判斷式新增 `isTrueSystemCall = isInternalRequest && !requesterId`，取代原本無條件信任 `isInternalRequest` 的寫法。`isInternalRequest` 為真但同時帶有 `requesterId`（chat-service 現在的用法：帶 `x-user-id` 走 `/internal/*`）時，不再跳過所有權檢查，走正常的 `visibility`/`authorId` 比對；只有真正不帶任何使用者身份的系統呼叫才會跳過
  - 未動 `chat-service` 任何檔案（`serviceClient.js` 的 `getCharacter` 呼叫方式不需要改），也未動 Gateway 或 `internalAuthMiddleware`——`characterController.js` 原本就已經把 `x-user-id` header 讀進 `requesterId` 並傳給 service 層（不論是否為內部請求），所以只改 service 層這一處判斷式就足夠
- **驗收結果**：啟動 api-gateway（8000）、auth-service（3000）、user-service（4000）、character-service（5000）、chat-service（6000）（未啟動 ai-service/Ollama/Qdrant/Caddy——`getOrCreateConversation` 在呼叫 ai-service 之前就會先做角色所有權驗證，FORBIDDEN 會在到達 ai-service 前就短路拋出，故不需要這些依賴），全程經 Gateway、用真實註冊/登入拿到的 token 實測：
  - 案例 13（X 用真實 token 存取 Y 的 private 角色開始對話）：`403 {"error":"FORBIDDEN","message":"Access denied to this character"}` ✅
  - 案例 15（Y 存取 Z 的 private 角色）：`403` ✅
  - 案例 16（Z 存取 X 的 private 角色）：`403` ✅
  - 對照組：X 存取自己的 private 角色（`202 preparing`）、X 存取 Y 的 public 角色（`202 preparing`）皆維持原本行為，不受影響 ✅
  - H1（`GET /internal/characters/<id>` 完全不帶任何 header，模擬未來 ai-service 的純系統呼叫）：`200`，回傳角色物件，符合 `isTrueSystemCall` 條件、行為不受影響 ✅
  - 額外核對 G2（`POST /internal/users` 不帶 header）仍是 `201`，確認這次改動沒有意外波及其他內部路由的既有行為
- **是否偏離原計劃**：沒有——完全依照 `待辦task.md` T21 給定的程式碼與驗收標準執行，未採用「讓 chat-service 改打外部路由」的替代方案（已查證該方案有實作障礙：chat-service 手上只有 `x-user-id` 字串沒有原始 JWT，無法組出合法 `Authorization` header）。

### T22. 修正 auth-service 的 `EMAIL_ALREADY_EXISTS` 狀態碼

- **日期**：2026-07-25
- **改了哪些檔案**：
  - `auth-service/src/controllers/authController.js`：`ERROR_MAP` 的 `EMAIL_ALREADY_EXISTS.status` 從 `400` 改成 `409`，訊息文字不變
  - 檢查 `persona-nexus-auth/src/main.js`：確認前端只用 `response.ok`／`response.status` 做日誌與通用錯誤訊息顯示（`showMessage('error', ...result.message)`），沒有任何寫死判斷「這個狀態碼等於 400」的分支邏輯，不需要同步修改前端
- **驗收結果**：啟動 api-gateway + auth-service（+ user-service，供 auth-service 內部呼叫 `/internal/users`），全程經 Gateway、用已存在帳號的 email 實測 `POST /api/auth/register`：回傳 `409 {"error":"EMAIL_ALREADY_EXISTS","message":"該電子郵件已被註冊，請更換帳號。"}`（對應第六節 J1），符合 spec 第三部分 HTTP Status Code 表。
- **是否偏離原計劃**：沒有——只改了一行狀態碼，前端核對後確認不需要同步修改。

---

**批次 7（T1-T20 執行後新發現的落差）到此全部完成（T21、T22）。**
