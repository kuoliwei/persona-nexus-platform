# 前端專案優化進度

> 沿用《後端專案優化標準程序.md》的 Phase 0–11 流程骨架對各前端微服務逐一做「規格驅動的架構優化」，
> 只有兩處因應前端技術棧調整（見文末「與後端 SOP 的差異」），不另外重寫一份完整的前端專用 SOP。
> 設計準則見《前端系統設計原則.md》（跨服務共用，已建立，不需重寫）。
> 本檔是**進度總覽**，供在別的聊天室接續下一個服務時快速取得上下文。

---

## 前置作業：建立跨服務共用文件

- **《前端系統設計原則.md》**（2026-07-26 建立）：對齊《後端系統設計原則.md》的結構——
  A 節（KISS/DRY/YAGNI/SSOT）與後端共用；B/C/D 節換成前端相關準則：
  B 前端架構與模組化（關注點分離、漸進增強、最低能力原則、模組邊界、API 層與 UI 層分離、
  狀態管理單一入口）、C 效能與資源管理（Core Web Vitals、Bundle Hygiene、快取版本化、
  環境設定外部化）、D 可及性與使用者體驗一致性（WCAG、Nielsen 十大可用性原則）。
  每條皆附業界文獻出處與稽核鉤子，不自行杜撰。

---

## 已完成（Phase 0–10，Phase 11 收尾視情況執行）

### 1. persona-nexus-auth

- **Phase 0**：選定為示範服務（四個前端裡檔案最少、功能最單純，比照後端當初選 auth-service 的邏輯）。
- **Phase 1**：讀完全部程式碼，驗證並修正 `CLAUDE.md` 現況宣稱。發現並修正兩處嚴重過時的錯誤說法：
  - 「登入成功後沒存 token、沒跳轉」——**已查證為假**：`main.js` 會把 token 帶在 URL query string
    導向大廳，`persona-nexus-lobby/src/main.js` 會接手寫入 `localStorage`，路徑是通的。
    這個錯誤說法同時也存在於 `auth-service/CLAUDE.md`，已一併修正。
  - 「正式環境會用 Nginx 做同源部署」被誤寫成未來規劃——**實際上 Caddy 同源部署已是現況**
    （`vite.config.js` 的 `base: '/login/'`、`host: true`、`allowedHosts: true` 皆為此設計）。
- **Phase 2**：`openspec init` + 現況基準線 spec（`openspec/specs/auth-ui/spec.md`），`validate --strict` 通過。
- **Phase 4**：回溯地基文件 `changes/auth-ui-foundation/`（proposal + design + delta spec），
  因本專案是第一輪 openspec 記錄、尚未經過任何優化 change，delta spec 內容與 main spec 一致
  （provenance，非冗餘）。
- **Phase 5**：稽核產出 `mistake.md`，找到 7 項候選違反點 + 1 項非原則類的功能性缺陷，按把握度排序：
  1. **測試壞掉（高把握，已實跑驗證）**：`npx jest` 2/2 全部失敗——`main.js` 加了 `import` 陳述式後，
     `main.test.js` 用 `eval()` 載入原始碼字串的方式無法處理 ES module 語法。
  2. **YAGNI（高把握）**：5 個 Vite 鷹架殘留死檔案（`counter.js`／`javascript.svg`／`vite.svg`／
     `hero.png`／`style copy.css`），已用 `npm run build` 驗證這些檔案本來就不會進正式產物
     （純粹是原始碼庫死重量，最初曾誤判為 Bundle Hygiene 違反，稽核時已自我訂正）。
  3. **API 層與 UI 層分離（高把握）**：`fetch()` 直接寫在表單 submit handler 內，無獨立 `api.js`
     （同平台的 lobby、chat 都有）。
  4. **DRY（中把握，跨服務）**：`config-loader.js` 與 `persona-nexus-character` 幾乎逐字重複。
  5. **SSOT／架構方向一致性（中把握，跨服務）**：`persona-nexus-lobby` 已改用「同源部署後不需要
     從 `/api/config` 取得其他前端網址」的新模式（相對路徑寫死常數），本專案（與 character）
     仍在用舊的「執行期取得絕對網址」模式。
  6. **WCAG 4.1.3（中把握）**：`messageBox` 動態訊息缺少 `aria-live`，螢幕報讀器不會被告知結果。
  7. **關注點分離 SoC（中把握）**：`main.js` 身兼多重角色，與第 3 點同根因。
  - 做得好、不該動的部分：漸進增強（原生表單驗證）、config-loader 的模組邊界設計、環境設定
    外部化、錯誤訊息不暴露內部細節、資源快取版本化（已用 build 實測）、表單 `<label>` 正確關聯。
- **使用者核對結果**：全部 7 項納入處理，包含兩項跨服務項目（第 4、5 點）。第 4 項
  （DRY，與 `persona-nexus-character` 重複）的範圍界定為：本專案改採 `persona-nexus-lobby`
  已驗證的新模式，`persona-nexus-character` 那份維持現狀，留待該服務自己的稽核輪次處理
  （改兩個獨立 repo 才能真正消除重複，非本輪單一服務範圍）。
- **Phase 6**：建立優化 change `simplify-auth-ui`（proposal + design + delta spec + tasks），
  `validate --strict` 通過。
- **Phase 7**：備份改動/刪除的檔案至 scratchpad 外的暫存資料夾。
- **Phase 8 實作**：
  - 刪除 5 個 YAGNI 死檔案
  - 新增 `src/api.js` 封裝 `register()`/`login()`，解決 API 層與 UI 層混雜（第 3、7 項）
  - 簡化 `config-loader.js` 為僅探測 `/api/config` 可達性（比照 lobby），`main.js`／`api.js`
    改用寫死的相對路徑常數（第 5 項）
  - `main.js` 的 top-level `await loadConfig()` 改包進 `async function init()`，為測試修復鋪路
  - `index.html` 的 `#messageBox` 加上 `role="status" aria-live="polite"`（第 6 項）
  - `main.test.js` 改用 `jest.resetModules()` + `require()` 取代 `eval()`，補上 `/api/config`
    探測請求的 mock（第 1 項）
- **Phase 9 驗證**：`npx jest` 2/2 全過；`npm run build` 正常（7 modules transformed，`dist/`
  僅 3 個檔案，`aria-live` 屬性確認存在於建置後的 `index.html`）；`grep` 確認無殘留對已刪
  檔案／`getConfig` 的引用；`openspec validate --all --strict` 全綠。手動瀏覽器走查（實際
  透過 Caddy + 完整後端流程驗證註冊/登入/導向/錯誤畫面）留給使用者自行確認。
- **Phase 10**：main spec（`openspec/specs/auth-ui/spec.md`）、`CLAUDE.md`、`mistake.md`
  三份文件均已同步回與程式碼一致的現況。
- **狀態**：程式碼改動未 commit、openspec change 未 archive、Phase 7 備份仍在暫存資料夾——
  Phase 11 尚未執行，需要使用者明確指示才進行。

---

### 2. persona-nexus-character

- **Phase 0–10**（2026-07-26 完成）：完整走完 Phase 0–10，Phase 11 尚未執行。
- **Phase 1**：讀碼、驗證 CLAUDE.md，發現現況與代碼一致。
- **Phase 5**：稽核找到 7 項候選違反點（YAGNI、DRY、SSOT、WCAG、Bundle Hygiene、環境設定、遺留依賴）。
- **Phase 6**：建立優化 change `character-simplify`（proposal/design/spec/tasks）。
- **Phase 8–9**：實作並驗證全部改動：
  - 刪除 `src/counter.js`
  - 簡化 `src/config-loader.js`（只探測可達性，與 auth 新版一致）
  - 改寫 `create.js`/`edit.js`/`index.html` 用相對路徑常數（`LOGIN_APP_URL = '/login/'`、`LOBBY_APP_URL = '/'`）
  - `creator-create.html`/`creator-edit.html` 加上 `aria-live="polite" role="status"`
  - 移除 `package.json` 的 `cors` 依賴
  - `npm run build` 成功，產物 1.32 KB JS，無 dead code
- **Phase 10**：建立 openspec main spec、更新 CLAUDE.md、mistake.md 完成標記。
- **Phase 11**：尚未執行，使用者決定暫不做。

### 3. persona-nexus-chat

- **Phase 0–10**（2026-07-26 完成）：Phase 11（commit/archive）尚未執行，使用者決定暫不做。
- **Phase 1**：發現 `CLAUDE.md` 嚴重過時（宣稱有 `config-loader.js`——實際沒有；描述的登入
  守門機制與實際不符；宣稱聊天是「模擬」——實際已對接真實後端），已修正。
- **Phase 5**：稽核找到 7 項候選違反點（WCAG、API/UI 分離、YAGNI 死代碼、DRY、狀態管理
  單一入口、SoC、錯誤呈現一致性），使用者核對後**全部納入處理**。
- **Phase 6–8 實作**：
  - 重寫 `src/api.js` 為真正的 API 層（原本是零呼叫點的死代碼 `getCurrentUserId()`），
    集中封裝 9 個後端端點與 `Authorization` header 組裝
  - 新增 `src/session.js`（token/conversationId 存取單一入口）
  - 把 886 行的 `chat.js` 拆分出 `toast.js`／`messageMenu.js`／`protagonistModal.js`
    三個模組，`chat.js` 瘦身為協調層
  - 新增 `makeFailureMessage()`/`replaceOrPushMessage()` 輔助函式消除重複建構邏輯
  - 補齊可及性：toast/初始化狀態加 `aria-live`，彈窗加 `role="dialog"`/`aria-modal`/
    焦點管理/Escape 關閉，圖示按鈕加 `aria-label`
  - 重啟失敗的錯誤呈現從 `alert()` 改為 toast（統一錯誤呈現機制，刻意保留失敗氣泡）
- **Phase 9 驗證**：`npm run build` 成功（13 modules，JS 產物大小與優化前幾乎相同）、
  `grep` 確認無殘留死代碼引用/直接 storage 存取/`alert()`；手動瀏覽器走查留給使用者
  （需完整 Caddy + gateway + 後端服務）。
- **Phase 10**：main spec（`openspec/specs/chat-ui/spec.md`）、`CLAUDE.md`、`mistake.md`
  三份文件均已同步回與程式碼一致的現況。
- **特殊狀況**：本專案從一開始就是獨立 git repo（`.git`、遠端 `origin/main`），不同於
  auth/character；且工作目錄本來就有兩筆與本輪優化無關的既存未 commit 修改（`chat.js`
  欄位路徑修正、`vite.config.js` 註解），本輪未觸碰。
- **狀態**：Phase 11（commit/archive）尚未執行，需要使用者明確指示才進行。

### 4. persona-nexus-lobby

- **Phase 0–10**（2026-07-26 完成）：Phase 11（commit/archive）尚未執行。
- **Phase 1**：讀碼時 Glob 工具漏抓 `public/src/` 目錄下的 5 個 HTML 樣板檔案
  （`sidebar.html`/`home.html`/`my-character.html`/`character-edit.html`/`chat.html`）與
  `character-tooltip.js`/`menuPosition.js` 兩個模組，二次驗證時用 PowerShell 直接列目錄才發現，
  補讀後確認不影響稽核結論。同時發現 `CLAUDE.md` 嚴重過時（宣稱「主內容區暫時空白」——實際已是
  完整的角色發現首頁；宣稱「創建角色按鈕直接跳創建頁」——實際導向的是「我的角色」清單頁；
  用舊的 `localhost:port` 絕對網址描述登入流程——實際是同源部署相對路徑），已修正回與現況一致。
- **Phase 5**：稽核找到 6 項候選違反點（DRY、SoC、模組邊界、狀態管理單一入口、WCAG、一致性），
  使用者核對後**全部納入處理**，包含跨檔案的 localStorage 存取修正、alert() 統一改為 toast。
  另外在 Phase 6 文件撰寫時發現一處失真：舉例用了程式碼裡實際不存在的 `success` 訊息類型
  （`style.css` 沒有 `.success` class），已依 YAGNI 修正為只涵蓋現有的 `info`/`error`。
- **Phase 6–8 實作**：
  - 新增 `src/message-utils.js`（統一 `showMessage(type, text, autoHideMs?)`/`clearMessage()`，
    確保 `#message-box` 具備 `aria-live`；`autoHideMs` 為選填，保留 home.js 原有 3 秒自動隱藏、
    my-character.js 原本沒有自動隱藏的既有差異，未強制統一掉）
  - 新增 `src/conversation-history.js`（聊天歷史載入/刪除從 sidebar.js 抽出，含菜單焦點管理）
  - 新增 `src/iframe-loader.js`（統一三個 iframe 頁面的 src 組裝與 token 參數化）
  - `api.js` 補齊 `setToken`/`clearToken`/`deleteConversation`，`getToken` 補上 `export`
  - 所有模組的 token 讀寫收斂到只透過 `api.js`，移除 `alert()`，補齊 `aria-label`
  - 實作中臨時追加 4 項未在原始 tasks.md 規劃內的細節（皆已記錄原因於 tasks.md）：
    `showMessage` 補 `autoHideMs` 參數、`loadChatHistoryButtons` 補 `onNavigate` 回調參數、
    補做菜單焦點自動聚焦（spec.md 已承諾但第一輪漏做）、sidebar.html 的「創建角色」按鈕
    aria-label 訂正為「查看我的角色」
- **Phase 9 驗證**：`npm run build` 成功（19 modules）、grep 確認無殘留 `alert()`／散落的
  `localStorage` 存取、`openspec validate --all --strict` 全綠；手動瀏覽器整合測試
  （需完整 Caddy + gateway + 後端服務）留給使用者自行確認。
- **Phase 10**：main spec（`openspec/specs/lobby-ui/spec.md`）、`CLAUDE.md`、`mistake.md`
  三份文件均已同步回與程式碼一致的現況。
- **狀態**：Phase 11（commit/archive）尚未執行，需要使用者明確指示才進行。

## 尚未開始的候選服務

四個前端服務（auth、character、chat、lobby）皆已完成 Phase 0–10，目前沒有尚未開始的候選服務。
四個服務的 Phase 11（commit/archive）皆尚未執行，之後若要收尾可個別回來處理。

---

## 與後端 SOP 的差異（沿用同一份《後端專案優化標準程序.md》，僅這兩處需因應前端技術棧調整）

- **Phase 0 檔案範圍排除清單**：後端排除 `node_modules`／Prisma `generated`／`__pycache__`／`venv`；
  前端排除 `node_modules`／`dist`。
- **Phase 9 驗證方式**：後端預設用 `test.http`／curl 測路由層，且明講「跳過 gateway、不觸發
  CORS，不等於端到端前端測試」；前端剛好相反——CORS、瀏覽器實際行為、UI 互動才是前端要驗證
  的核心，不能只跑 curl。前端 Phase 9 應改為：先跑 Jest 單元測試，再開瀏覽器手動走過表單流程，
  並檢查 Network 分頁確認實際打的網址與 CORS 行為。

其餘 Phase 0–11 的骨架、心法（規格是單一真相、記錄≠評判、準則要有依據、誠實不灌水、關鍵節點
停下核對等）直接沿用，不重寫。

---

## ⚠️ 重要提醒：優化完成後必須回來更新本檔

**每當完成一個前端服務的優化（無論是否執行 Phase 11 commit/archive）：**

1. **立即回到本檔（`前端專案優化進度.md`）**，在「已完成」段落新增該服務的摘要
   - 列出完成的 Phase 編號（例如「Phase 0–10」或「Phase 0–11」）
   - 簡述該輪的重點改動（3–5 個重點項目）
   - 記錄日期
2. **同時更新《前情提要-前端專案優化.md》**，把該服務從「接下來要做什麼」移到「已經做完的事」
3. **更新「尚未開始的候選服務」段落**
   - 移除已完成的服務
   - 對剩餘服務補充新發現的「已知特殊狀況」或「注意事項」

**不更新的後果**：
- 下一個接續者無法快速了解進度
- 跨服務的 DRY/SSOT 問題可能無法被統一發現
- 重複踩坑（例如又發現某個服務有同樣的舊版 config-loader）

---

## 給下一個聊天室的開場指令範例

> 「請閱讀『前端專案優化進度.md』與『後端專案優化標準程序.md』，我要核對 persona-nexus-auth
> 的 mistake.md 稽核結果，決定 Phase 6 的處理範圍。」

或

> 「請閱讀『前端專案優化進度.md』與『後端專案優化標準程序.md』，接續優化 <新服務名>。
> 優化完成後記得回來更新這兩份進度檔。」
