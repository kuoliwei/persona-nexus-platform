# 如何批量同步所有服務倉庫

## 概述

Persona Nexus 平台由多個**獨立的 git 倉庫**組成，而非 monorepo。開發者需要手動將每個服務倉庫複製到本地工作資料夾，並定期同步最新代碼。本文說明如何批量操作，避免逐一進入各資料夾手動 pull。

---

## 當前架構

根據 `ARCHITECTURE.md`，平台包含：

### 後端服務（5個）
- **api-gateway** — API 閘道
- **auth-service** — 認證服務
- **user-service** — 使用者管理
- **character-service** — 角色管理
- **chat-service** — 對話管理
- **ai-service** — AI 生成 + RAG

### 前端（4個 Vite 應用）
- **persona-nexus-auth** — 登入頁
- **persona-nexus-character** — 角色編輯頁
- **persona-nexus-lobby** — 首頁 / 角色大廳
- **persona-nexus-chat** — 聊天室

---

## 方案一：手動逐一 Pull（簡單但繁瑣）

### 步驟

1. **進入根目錄**
   ```powershell
   cd c:\Users\liweikuo\persona-nexus-platform
   ```

2. **進入每個服務資料夾，執行 `git pull`**
   ```powershell
   cd api-gateway
   git pull origin master
   cd ..

   cd auth-service
   git pull origin master
   cd ..

   # 依此類推...
   ```

### 缺點
- 需要手動進入每個資料夾
- 容易遺漏某個服務
- 對開發效率影響大

---

## 方案二：批次腳本（推薦 ⭐）

### 批次檔版本（Windows）

在根目錄建立 `sync-all-repos.bat`：

```batch
@echo off
REM 批量同步所有服務倉庫
REM 使用方法：double-click 或在 PowerShell/cmd 執行 .\sync-all-repos.bat

setlocal enabledelayedexpansion

set BASE_DIR=%cd%

REM 定義所有服務名稱（與資料夾名稱相同）
set SERVICES=^
  api-gateway ^
  auth-service ^
  user-service ^
  character-service ^
  chat-service ^
  ai-service ^
  persona-nexus-auth ^
  persona-nexus-character ^
  persona-nexus-lobby ^
  persona-nexus-chat

echo.
echo ========================================
echo 開始批量同步所有服務倉庫
echo ========================================
echo.

set SYNC_COUNT=0
set SKIP_COUNT=0
set ERROR_COUNT=0

for %%S in (%SERVICES%) do (
  if exist "!BASE_DIR!\%%S\.git" (
    echo.
    echo [!SYNC_COUNT!] 正在同步 %%S ...
    cd "!BASE_DIR!\%%S"
    
    REM 先檢查是否有未commit的更改
    git status --porcelain > nul 2>&1
    if !ERRORLEVEL! equ 0 (
      REM 執行 pull
      git pull origin master 2>&1
      if !ERRORLEVEL! equ 0 (
        echo ✓ %%S 同步成功
        set /a SYNC_COUNT+=1
      ) else (
        echo ✗ %%S 同步失敗（可能有衝突，請手動處理）
        set /a ERROR_COUNT+=1
      )
    ) else (
      echo ✗ %%S 無法存取 git，跳過
      set /a ERROR_COUNT+=1
    )
  ) else (
    echo [略] %%S 資料夾不存在，跳過
    set /a SKIP_COUNT+=1
  )
)

echo.
echo ========================================
echo 同步完成
echo ========================================
echo 成功: !SYNC_COUNT! 個
echo 跳過: !SKIP_COUNT! 個
echo 失敗: !ERROR_COUNT! 個
echo.
pause
```

### 使用方法

1. **保存上述內容** 到 `c:\Users\liweikuo\persona-nexus-platform\sync-all-repos.bat`
2. **執行腳本**
   - 方式 A：在資源管理員雙擊 `sync-all-repos.bat`
   - 方式 B：在 PowerShell 執行
     ```powershell
     cd c:\Users\liweikuo\persona-nexus-platform
     .\sync-all-repos.bat
     ```

3. **查看結果** — 腳本會顯示每個服務的同步狀態

---

## 方案三：PowerShell 版本（更靈活）

在根目錄建立 `sync-all-repos.ps1`：

```powershell
# 批量同步所有服務倉庫 (PowerShell 版)
# 使用方法：.\sync-all-repos.ps1

param(
  [switch]$SkipAiService = $false,  # 跳過 ai-service（如果環境問題）
  [switch]$Verbose = $false         # 詳細輸出
)

$baseDir = Get-Location
$services = @(
  "api-gateway",
  "auth-service",
  "user-service",
  "character-service",
  "chat-service",
  "ai-service",
  "persona-nexus-auth",
  "persona-nexus-character",
  "persona-nexus-lobby",
  "persona-nexus-chat"
)

$syncCount = 0
$skipCount = 0
$errorCount = 0

Write-Host "`n========================================" -ForegroundColor Cyan
Write-Host "開始批量同步所有服務倉庫" -ForegroundColor Cyan
Write-Host "========================================`n" -ForegroundColor Cyan

foreach ($service in $services) {
  $servicePath = Join-Path $baseDir $service
  
  # 跳過 ai-service（可選）
  if ($service -eq "ai-service" -and $SkipAiService) {
    Write-Host "⊘ $service - 已略過（--SkipAiService）" -ForegroundColor Yellow
    $skipCount++
    continue
  }
  
  # 檢查資料夾是否存在
  if (-not (Test-Path $servicePath)) {
    Write-Host "⊘ $service - 資料夾不存在" -ForegroundColor Gray
    $skipCount++
    continue
  }
  
  # 檢查是否為 git 倉庫
  if (-not (Test-Path (Join-Path $servicePath ".git"))) {
    Write-Host "⊘ $service - 不是 git 倉庫" -ForegroundColor Gray
    $skipCount++
    continue
  }
  
  Write-Host "→ $service..." -ForegroundColor White
  Push-Location $servicePath
  
  try {
    # 檢查遠端是否可達
    $remoteCheck = git ls-remote origin 2>&1
    if ($LASTEXITCODE -ne 0) {
      Write-Host "  ✗ 無法連接到遠端倉庫" -ForegroundColor Red
      $errorCount++
      Pop-Location
      continue
    }
    
    # 執行 pull
    $output = git pull origin master 2>&1
    if ($LASTEXITCODE -eq 0) {
      if ($output -match "Already up to date") {
        Write-Host "  ✓ 已是最新版本" -ForegroundColor Green
      } else {
        Write-Host "  ✓ 同步成功" -ForegroundColor Green
      }
      $syncCount++
    } else {
      Write-Host "  ✗ 同步失敗：$output" -ForegroundColor Red
      $errorCount++
    }
    
  } catch {
    Write-Host "  ✗ 異常：$_" -ForegroundColor Red
    $errorCount++
  } finally {
    Pop-Location
  }
}

Write-Host "`n========================================" -ForegroundColor Cyan
Write-Host "同步完成" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "成功: $syncCount 個" -ForegroundColor Green
Write-Host "跳過: $skipCount 個" -ForegroundColor Gray
Write-Host "失敗: $errorCount 個" -ForegroundColor Red
Write-Host ""
```

### 使用方法

```powershell
# 基本用法 — 同步所有服務
.\sync-all-repos.ps1

# 跳過 ai-service（如果 Python 環境問題）
.\sync-all-repos.ps1 -SkipAiService

# 詳細輸出
.\sync-all-repos.ps1 -Verbose
```

---

## 方案四：首次複製所有倉庫（新手）

如果本地還沒有這些服務倉庫，需要先複製：

### PowerShell 腳本 - `clone-all-repos.ps1`

```powershell
# 一次性複製所有服務倉庫到本地
# 使用方法：.\clone-all-repos.ps1

param(
  [string]$Owner = "kuoliwei"  # GitHub 帳戶名稱
)

$baseDir = Get-Location
$repos = @(
  "api-gateway",
  "auth-service",
  "user-service",
  "character-service",
  "chat-service",
  "ai-service",
  "persona-nexus-auth",
  "persona-nexus-character",
  "persona-nexus-lobby",
  "persona-nexus-chat"
)

Write-Host "準備複製所有服務倉庫 (Owner: $Owner)...`n" -ForegroundColor Cyan

foreach ($repo in $repos) {
  $repoPath = Join-Path $baseDir $repo
  
  if (Test-Path $repoPath) {
    Write-Host "⊘ $repo - 已存在，跳過" -ForegroundColor Yellow
    continue
  }
  
  Write-Host "→ 正在複製 $repo..." -ForegroundColor White
  $url = "https://github.com/$Owner/$repo.git"
  
  git clone $url $repoPath 2>&1 | Out-Null
  if ($LASTEXITCODE -eq 0) {
    Write-Host "  ✓ 複製成功" -ForegroundColor Green
  } else {
    Write-Host "  ✗ 複製失敗：請檢查網路或倉庫名稱" -ForegroundColor Red
  }
}

Write-Host "`n所有倉庫複製完成！" -ForegroundColor Cyan
Write-Host "下次可執行 .\sync-all-repos.ps1 進行批量更新`n" -ForegroundColor Gray
```

### 執行方式

```powershell
# 使用預設的 Owner (kuoliwei)
.\clone-all-repos.ps1

# 或指定不同的 GitHub 帳戶
.\clone-all-repos.ps1 -Owner yourusername
```

---

## 方案五：與啟動腳本整合

修改 `start-backend-services.bat`，在啟動服務**前**自動同步所有倉庫：

```batch
@echo off
REM 在啟動後端服務之前，先同步所有倉庫

echo 正在同步所有倉庫...
call sync-all-repos.bat

if %ERRORLEVEL% neq 0 (
  echo 部分倉庫同步失敗，繼續啟動服務...
)

echo.
echo 開始啟動後端服務...
REM 以下為原有的服務啟動邏輯
...
```

---

## 常見問題

### Q1：Pull 時出現衝突怎麼辦？

**A：** 腳本會報告失敗，你需要手動進入該資料夾解決衝突：

```powershell
cd c:\Users\liweikuo\persona-nexus-platform\<service-name>

# 查看衝突
git status

# 編輯衝突檔案，手動解決
# 然後：
git add .
git commit -m "resolve conflict"
git pull origin master
```

### Q2：某個服務沒有 `.git` 資料夾怎麼辦？

**A：** 使用 `clone-all-repos.ps1` 複製遺漏的倉庫。

### Q3：能否跳過某些服務？

**A：** 可以。修改腳本中的 `$services` 或 `set SERVICES` 清單，移除不需要的服務名稱。

### Q4：如何檢查某個服務是否已是最新？

**A：** 進入資料夾並執行：
```powershell
cd <service-name>
git fetch origin
git status
```

---

## 最佳實踐

1. **定期同步** — 建議在啟動開發前執行同步腳本
2. **檢查衝突** — 如果有多人開發，先檢查是否有未提交的本地更改
3. **備份重要工作** — 在執行批量操作前，確保重要更改已 commit/push
4. **使用 PowerShell 版本** — 相比 batch，PowerShell 更靈活且錯誤處理更好

---

## 下一步

- 將 `sync-all-repos.ps1` 或 `sync-all-repos.bat` 放在根目錄
- 設置快捷方式或 alias，方便快速執行
- 考慮與 CI/CD 整合（自動同步、自動測試、自動部署）
