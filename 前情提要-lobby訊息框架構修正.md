# 前情提要：persona-nexus-lobby 訊息框架構修正（代辦 A／B）

> **給接手的新聊天室**：這份文件是你需要知道的全部背景。
> 修法**已由使用者拍板（選項 1）**，但**規格尚未撰寫、程式碼尚未動**。
> 你的工作是：先寫規格（openspec），經使用者確認後再改碼。

---

## 一句話任務

修正 `persona-nexus-lobby` 的訊息提示框（`#message-box`）架構問題：

| 代辦 | 問題 | 性質 |
|---|---|---|
| **A** | 訊息框在某些頁面會被掛到 `document.body`，且永不移除、不隨換頁清除 | 結構問題 |
| **B** | 錯誤訊息缺自動消失時間，各呼叫點行為不一致 | 一致性問題 |

**兩者是不同的問題，不能一次修好**（詳見下方〈為什麼 A、B 不能一起修〉）。

---

## 這件事怎麼來的

平台先前完成一輪前端 debug（8 個 bug），之後依四份設計文件稽核，產出代辦 A～G 七項。
稽核結果記錄在平台根目錄 `前端網頁debug_task_checklist.md` 的
「## ✅ 稽核結果（2026-07-28）」章節，該章節開頭有「待勾選清單」可追蹤進度。

七項的處置狀況：

| 代辦 | 內容 | 狀態 |
|---|---|---|
| **A、B** | **lobby 訊息框架構** | **📋 修法已拍板，待寫規格 ← 你的任務** |
| C | Bug 7 鍵盤事件實作取捨 | ✅ 已完成（寫入 lobby spec） |
| D、E | ai-service 後端架構 | 📋 規格已寫好，另一個聊天室執行中 |
| F、G | 微服務 spec 文件補充 | ✅ 已完成 |

**共同成因（使用者自述）**：「當初也是想到什麼功能就叫 AI 做什麼功能，
沒有想到統一架構的問題，所以就各做各的，完全沒有顧慮一致性問題」。
理解這點有助於判斷「該修到哪裡為止」——這不是單一疏漏，是缺少架構規劃的累積結果。

---

## 背景知識：lobby 的架構

lobby 是**單頁應用（SPA）**，唯一的 HTML 是 `index.html`，結構為：

```
<body>
  ├── #sidebar-container   ← 側邊欄，SPA 外殼的一部分，換頁不會被清掉
  └── .main-content > #content-area   ← 換頁時只有這裡被 innerHTML 換掉
```

「子頁」是被 `fetch` 進來塞進 `#content-area` 的 HTML 片段，共四個：

| 子頁樣板 | 性質 | 自帶 `#message-box`？ |
|---|---|---|
| `public/src/home.html` | lobby 自己的內容 | ✅ 有（第 8 行） |
| `public/src/my-character.html` | lobby 自己的內容 | ✅ 有（第 8 行） |
| `public/src/chat.html` | **只有 5 行，內容是 `<iframe>`** | ❌ 無 |
| `public/src/character-edit.html` | **只有 5 行，內容是 `<iframe>`** | ❌ 無 |

⚠️ 後兩者不是 lobby 的頁面，是**外框**——框裡是另外兩個獨立的前端 app
（`/chat`、`/character`），它們有自己的訊息機制。

---

## 問題 A 的完整調查（已查證，非推測）

### 現行程式碼

`src/message-utils.js` 全檔僅 30 行：

```js
function getMessageBox() {
  let msgBox = document.getElementById('message-box');
  if (!msgBox) {
    msgBox = document.createElement('div');
    msgBox.id = 'message-box';
    document.body.appendChild(msgBox);     // ← 問題所在
  }
  msgBox.setAttribute('role', 'status');
  msgBox.setAttribute('aria-live', 'polite');
  return msgBox;
}
```

「先找現有的 `#message-box`，找不到就建一個掛到 body」——且**全檔沒有任何 `remove()`**，
只靠 `display:none` 隱藏。

### 全部 5 個呼叫點（稽核只提到 2 個，實地清點為 5 個）

| # | 呼叫點 | 執行時所在頁面 | 抓到哪個插座 | `autoHideMs` |
|---|---|---|---|---|
| 1 | `conversation-history.js:34`（刪除對話失敗） | **任何頁面**（側邊欄貫穿全站） | 視當下頁面而定 | ❌ 無 |
| 2 | `home.js:81`（無法獲取角色列表） | 首頁 | `home.html:8` 自帶 | ✅ 3000 |
| 3 | `main.js:86`（缺角色 ID） | 首頁（**刻意**先 `loadHomePage()` 才顯示） | `home.html:8` 自帶 | ✅ 4000 |
| 4 | `my-character.js:34`（載入中…） | 我的角色頁 | `my-character.html:8` 自帶 | ❌ 無（由 #5 的 `clearMessage()` 收掉） |
| 5 | `my-character.js:39/42`（清除／載入失敗） | 我的角色頁 | `my-character.html:8` 自帶 | ❌ 無 |

> ⚠️ **行號更正**：稽核原文寫 `conversation-history.js:33`，實際為**第 34 行**
> （33 行是 `console.error`）。

### 唯一真正會誤貼 body 的路徑

`index.html`（SPA 外殼）**沒有** `#message-box`（`grep -c` 為 0），
`public/src/sidebar.html` 也**沒有**（同樣為 0）。因此：

- 在**首頁／我的角色頁**觸發 → 找到子頁自帶插座 → 插進 `#content-area`，換頁時被一起清掉
- 在**聊天室頁／角色編輯頁**觸發 → 找不到插座 → 掛到 `document.body`
  → **換頁不會被清掉、又沒有自動消失，永久殘留**

側邊欄（含刪除對話按鈕）貫穿全站都在，所以**呼叫點 #1 是唯一能觸發後者的路徑**。

### ⚠️ 已被證偽的修法（不要再提）

**「幫 `chat.html`／`character-edit.html` 補插座」無效。**
已讀過 `chat-page.js`、`character-edit.js` 全檔——兩者**都沒有 import `message-utils.js`、
沒有任何 `showMessage()` 呼叫**，只做三件事：抓樣板塞進 `#content-area`、掛 iframe、
推 history 狀態。這兩頁沒有自己的訊息要顯示，補了插座也不會有人用。

### 這個陷阱早就被踩過（稽核漏記）

`src/main.js:83-85` 有一段既有註解：

```js
// 已知路徑但缺少必要的 id 參數：不要靜默轉向，顯示訊息後再回首頁
// （在 loadHomePage() 之後才顯示，這樣抓到的是 home.html 自帶的
// #message-box，不會在 body 上留下第二個重複 id 的訊息框）
```

證明 Bug 6 修復時就發現了這個坑，當時採「調整呼叫順序閃避」的局部解法，
沒有根治、也沒記進 checklist。**這佐證 A 是結構問題**——
每個呼叫點都得自己記得閃避才不會出事。

### CSS 性質（影響修法）

`src/style.css:295-316` 的 `#message-box` **只有** `margin-bottom`／`padding`／
`border-radius`／`font-size`／`text-align`／`display`——
**沒有 `position: fixed`／`absolute`、沒有 `z-index`**。

代表它是**常態排版區塊**（會占用版面空間、跟著內容排版走），
不是浮在畫面上的 toast。這與 Bug 2 的 tooltip（`position: fixed` + `z-index: 1000`）
性質不同，殘留時的視覺表現不如 tooltip 明顯，但生命週期問題是同一型。

---

## 使用者已拍板的修法：選項 1（側邊欄自帶插座）

> 使用者在三個選項中明確選擇「選項 1」。另兩個選項
> （改造 `message-utils.js` 內建清理／只修 B 接受現狀）**不採用**，不要重新提案。

### 核心思路

依《網頁架構設計原則》原則 2.1 的職責劃分——**跨功能的錯誤屬於外殼，功能內的錯誤屬於子頁**。
側邊欄是外殼的一部分，它的錯誤訊息本來就該有一個屬於外殼的歸屬，
而不是「借用當下子頁的插座、借不到就掛 body」。

因此：**在外殼層級提供一個專屬的訊息框**，讓側邊欄的訊息有正當歸屬。
如此一來「不隨換頁清除」不再是缺陷，而是**正確行為**（外殼本來就一直在）。

### ⚠️ 必須解決的關鍵問題：`id` 不能重複

HTML 規範要求 `id` 全文件唯一，而 `document.getElementById()` **只回傳 DOM 中的第一個**。

若外殼插座與子頁插座都叫 `#message-box`，而外殼的側邊欄在 DOM 順序上
**排在 `#content-area` 之前**（見 `index.html`），那麼：

> **所有訊息（包括子頁自己的）都會被塞進外殼那一個**，子頁的插座永遠抓不到。

這會讓情況比現在**更糟**。必須解決，建議方向：

- 兩個插座用**不同的識別名**（如外殼 `#shell-message-box`、子頁維持 `#message-box`）
- `showMessage()` 需要能**指定目標**（新增參數或新增一支專用函式）
- 呼叫點 #1（側邊欄）改用外殼插座；#2～#5（子頁內容）維持用子頁插座

### ⚠️ 版面限制：側邊欄只有 250px，且會收合

`src/style.css:19-30`：`.sidebar` 寬 **250px**，且有收合功能（收合後 **12px**，
見 `.sidebar.collapsed`）。手機版是抽屜式（`.sidebar.open`）。

> ⚠️ **更正（2026-07-28 實地查證）**：本文件原記載收合後 60px 有誤，實際為
> `width: 12px; padding: 0`，且 `.sidebar-top`／`.sidebar-middle`／`.sidebar-bottom`
> 全部 `display: none`。結論不變，只是「不要把訊息框塞進側邊欄」的理由更強。

**直接把訊息框塞進 `sidebar.html` 內部會有問題**：
錯誤訊息通常較長（含後端回傳的錯誤內容），250px 會擠壓變形，收合成 12px 時更是完全無法閱讀。

**建議**（但請自行評估後與使用者確認）：外殼插座放在 `index.html` 的
`.main-content` 或 `#content-area` **之外但仍屬外殼**的位置，而非 `sidebar.html` 內部。
「屬於外殼」指的是**生命週期**（不隨換頁被清掉），不必然是**視覺上位於側邊欄裡**。

若選擇讓它浮動顯示（`position: fixed`），要注意這會**改變既有視覺行為**
（目前是常態排版），屬可觀察變更，須寫進規格並讓使用者知情。

---

## 問題 B：`autoHideMs` 不一致

### 現況

5 個呼叫點只有 2 個傳了自動消失時間，且兩個值還不同（3000／4000），無規則可循。

### 判斷（已分析，供參考）

| 呼叫點 | 是否該補 | 理由 |
|---|---|---|
| **#1 `conversation-history.js:34`** | ✅ **該補** | 一次性動作回饋（刪除失敗），與 #2「無法獲取角色列表」同性質，但一個有一個沒有——**這就是代辦 B 本體** |
| #4 `my-character.js:34`（載入中…） | ❌ 不該補 | 進行中狀態，本來就該持續顯示到載入結束，且成功時已由 `clearMessage()` 收掉 |
| #5 `my-character.js:42`（載入失敗） | ⚠️ 有討論空間 | 載入失敗時該頁清單是空的，此訊息在解釋「為什麼這頁空白」。若 3 秒後消失，使用者會看到無說明的空白頁。**常駐可能是刻意的**，但程式碼無註解佐證 |

### 待使用者決定的兩件事

1. **秒數用多少**？現有 3000／4000 本身就不一致，要不要順便統一？
   （刪除失敗的訊息較長，含錯誤內容，可能需要更長閱讀時間）
2. **#5 要不要動**？建議**不動**，但若不動，應在規格或程式碼註解中寫明為何是例外，
   否則日後稽核會再抓一次。

---

## 為什麼 A、B 不能一起修

這是討論中釐清的重點，**不要以為修完 B 就等於修好 A**：

- **B 補上 `autoHideMs`** → 訊息幾秒後自動隱藏 → **使用者感受到的問題消失**
- **但那只是 `display:none`** → DOM 節點仍留在 body → **A 的結構問題原封不動**

兩者疊加時後果最嚴重：人在聊天室按刪除失敗 → 掛到 body（A）＋ 不會自動消失（B）
→ **錯誤訊息永久卡在畫面上，切到哪一頁都跟著，除非重新整理**。

所以 B 能大幅降低 A 的實際傷害，但不能取代 A。

---

## 這輪已產出的設計文件（修法的依據）

討論過程中，使用者要求把架構原則系統化，已完成兩份文件，**修法時應對照遵循**：

1. **`前端系統設計原則.md`**（平台根目錄）
   B 節新增原則「**DOM 所有權與生命週期**」——正是本次問題的通用化。
   含四條推論準則：就近掛載、共用元件要能指定掛載目標（明講了 `id` 重複問題）、
   外殼級元素必須自動收斂、不把唯一的清除路徑押在會被銷毀的元素上。
   另附「如何檢查違反」的稽核動作：搜 `document.body.appendChild(` 逐一問三個問題。

2. **`網頁架構設計原則.md`**（平台根目錄，**新建**）
   平台層架構原則，把「四個前端如何組合成一個網站」從單一專案的設計原則中獨立出來。
   與本任務直接相關的是：
   - **零、架構基線**：登入頁獨立整頁；大廳是外殼、其餘是子頁（使用者明確要求不可變動）
   - **原則 2.1**：外殼與子頁的職責劃分表（「跨功能的錯誤 → 外殼」正是本修法的依據）
   - **原則 4.1**：共用元件必須有統一契約（含自動消失規則）

⚠️ 該文件〈附錄：現況對照〉還盤點出**四個新的架構落差**（憑證用網址傳遞、
跨倉庫重複檔案 `menuPosition.js`、四套提示元件實作、子頁寫死外殼路由），
**不在本任務範圍**，但若修法時順手發現相關線索，可補充記錄。

---

## 工作方式要求

### 先寫規格，再改碼

`persona-nexus-lobby` 有 openspec（`openspec/specs/lobby-ui/spec.md`）。
依先前建立的工作約定第 4 條：**可觀察的行為改動要先反映在 spec 再改碼**。
Bug 2 修復時就是先改 `lobby-ui/spec.md` 才動 `character-tooltip.js`。

建議流程：
1. 讀完本文件與 checklist 中代辦 A 的「🔍 補充調查」節
2. **實地重讀原始碼確認現況仍與本文件相符**（程式碼可能又變了）
3. 寫 openspec change（proposal／delta spec／tasks），**與使用者確認後**再動程式碼
4. 改完跑 `npm run build` 與 `openspec validate --all`

### 沿用的工作約定

1. **不可以靠推測與猜測，一定要找到證據才能寫**。查不到證據的標記「未證實」
   並寫明還缺什麼，不要升格成結論。
   （本文件中被證偽的「補子頁插座」修法，就是先前憑稽核轉述推論、未讀原始碼的結果。）
2. **列出問題時不要自行做範圍分類去篩選或降權**。
3. **修 bug 不可以違背設計原則與規格**。
4. **驗證要用「跑起來」而不是「讀過了」**。
5. **關鍵節點停下來跟使用者核對**，不擅自啟動服務、不擅自擴大範圍。
6. **不要順便夾帶其他重構或優化**，只修 A、B 本身。

---

## 環境與陷阱

- **lobby 是獨立 git repo**。平台根目錄 `git diff` **看不到** lobby 的改動，
  要用 `git -C persona-nexus-lobby diff`。這點在先前除錯中造成過誤判。
- **lobby 的 `npm run build` 是完整的**（多入口建置涵蓋本次改動），可作為驗證。
  ⚠️ 但 `persona-nexus-character` 的 build **不能當驗證**（`vite.config.js` 未設多頁入口，
  只編譯 `index.html`）——本任務用不到，但若順手改到 character 要注意。
- **要實測需啟動整套服務**：`start-all-services.bat`（**須先開 Docker Desktop**）。
  lobby 在 5175，統一經 Caddy 的 8080 存取；直接開 5175 不會正常運作
  （前端 API 走相對路徑，必須經 Caddy）。
- **測試「刪除對話失敗」不容易**：需要製造後端失敗（例如停掉 chat-service 或 Qdrant），
  才能觸發呼叫點 #1。這條路徑**至今尚未實測驗證過**，
  是本任務最需要親自確認的情境。

---

## 完成後

1. 在 `前端網頁debug_task_checklist.md` 的「待勾選清單」勾選代辦 A、B 並註記完成情形。
2. 若修法過程中發現本文件記載與現況不符，**回頭更正 checklist**，
   不要讓錯誤描述留著（先前已有三項描述被證據推翻的前例）。
3. **尚未 commit**：本輪（2026-07-28）平台根目錄與各子 repo 都有未 commit 的改動，
   commit 時只挑自己這輪的檔案，不要整包 `git add .`。
