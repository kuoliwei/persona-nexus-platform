# 後端專案優化進度

> 依《後端專案優化標準程序.md》（SOP，Phase 0–11）對各後端微服務逐一做「規格驅動的架構優化」。
> 設計準則見《後端系統設計原則.md》（跨服務共用，已建立，不需重寫）。
> 本檔是**進度總覽**，供在別的聊天室接續下一個服務時快速取得上下文。

---

## 已完成（Phase 0–10，Phase 11 收尾尚未執行）

### 1. auth-service
- 產物：`openspec/`（config.yaml + specs/authentication + changes/authentication-foundation + changes/simplify-auth-service）、`mistake.md`、`CLAUDE.md`
- 清理項目：刪除死代碼（`authMiddleware.js`、`jwtHelper.verifyToken()`、`authController.me()`、`users.json`）、`PORT`/`CORS_ORIGIN` 外部化到 env、login 移除誤用的格式驗證、repository 依角色改名（`userApiClient.js`→`userRepository.js`）、錯誤碼改 `ERROR_MAP` 查表
- 驗證：Jest 2 suites / 14 tests 全通過

### 2. user-service
- 產物：同上結構（`openspec/`、`mistake.md`、`CLAUDE.md`）
- 清理項目：刪除壞掉的 `temp-query.js`、授權邏輯抽成 `authorizeSelf` middleware（原本在 `getUserById`/`deleteUser` 重複兩份）、錯誤碼改 `ERROR_MAP` 查表、移除多餘的 `cors()`（已 grep 前端確認無瀏覽器直連）
- 延後不處理：SOLID-DIP（JS 下過度設計）、「東西向流量是否統一經 gateway」跨服務架構議題
- 驗證：15 個手動情境全通過

### 3. character-service
- 產物：同上結構（`openspec/`、`mistake.md`、**新建的 `CLAUDE.md`**——本服務先前沒有這份文件）
- 清理項目：刪除死代碼（`characterRepository.test-run.js`，會打真實 DB）、錯誤碼改共用 `ERROR_MAP` 查表（取代 5 支方法各自的 if/else + create/update 重複的中文訊息字典）、移除遺留除錯 `console.log`（含明顯的 `[DEBUG]` 行；**其餘請求生命週期 log 起初被誤刪，事後對照 chat-service/ai-service 慣例補回並豐富**，見該服務 `CLAUDE.md`「日誌慣例」一節）、移除多餘的 `cors()`（已 grep 前端 `persona-nexus-character` 確認 `api.dev.js` 唯一直連路徑目前是註解狀態、非啟用）
- 額外發現並修正：`characterService.test.js` 的 `listCharacters` 兩處測試呼叫少傳 `requesterId` 參數（真實簽名是三參數），導致 1 個單元測試原本失敗——已修正，非 service 邏輯本身的錯
- 延後不處理：SOLID-DIP（JS 環境下約定俗成，使用者確認不處理）
- 驗證：18 個單元測試全通過 + 20 個手動整合情境（curl）全通過
- **狀態**：程式碼改動未 commit、openspec change 未 archive、Phase 7 備份仍在 scratchpad——Phase 11 尚未執行

### 4. chat-service（最新完成）
- 產物：同上結構（`openspec/`、`mistake.md`、更新既有 `CLAUDE.md`）
- Phase 1 發現：既有 `CLAUDE.md` 嚴重過時（宣稱「沒有 git」「沒有單元測試」，且缺漏訊息刪除/對話刪除/主角人設/AI 生成狀態輪詢等多項已實裝功能），已依實際 git log（12 筆提交）與程式碼重寫
- 稽核找到 10 項候選違反點，使用者逐項核對後確認範圍：
  - **納入處理**：Controller 20 支方法的錯誤碼→HTTP 映射改共用 `ERROR_MAP`；刪除死代碼 `src/app.js.backup`（舊版 app 進入點）；刪除 `prisma.config.ts`（**查證後確認**：Prisma 5.22 CLI 完全不讀取此檔案，該支援為 Prisma 6.x 才引入，非 CLI 必要慣例檔案）；移除多餘 `cors()`（已 grep 前端 `persona-nexus-chat` 確認無直連路徑）；移除斷掉的 `"test": "jest"` script（jest 從未安裝、無任何測試檔案）；移除 `authMiddleware.js` 寫入但無人讀取的死欄位 `req.userId`；統一 `sendMessage`（依 characterId 舊同步端點）在對話不存在時的回應（400→404，與 `sendMessageToConversation` 一致——**本次唯一的可觀察行為變更**，已寫入 MODIFIED delta）
  - **確認不處理／記錄為已知限制**：除錯用 `console.log`（含 `🐛 [DEBUG]` 前綴）暫緩；`creationJobs`/`aiGenerationStatus` 兩個進程內記憶體 Map（服務重啟即遺失建立中/生成中狀態，違反 12-Factor 無狀態原則）僅記錄為已知限制，牽涉是否換成 DB/Redis 的架構決策，留待日後獨立評估；SOLID-DIP 與前三服務判定一致，JS 環境下約定俗成不處理
- 驗證：本服務無任何單元測試框架整合（`package.json` 原本的 `test` script 是斷的，已移除，不在本輪新增），僅以更新過的 `test.http`／curl 做手動整合測試，涵蓋健康檢查、缺 header、角色不存在、`sendMessage`/`sendMessageToConversation` 對話不存在皆回 404、缺欄位、job 未找到、依角色刪除對話找不到角色等情境，全數通過
- **狀態**：程式碼改動未 commit、openspec change 未 archive、Phase 7 備份仍在 scratchpad——Phase 11 尚未執行（使用者已明確表示本次收尾三步驟——commit/archive/清備份——暫不執行）

### 5. ai-service（最新完成，技術棧首次換成 Python/FastAPI）
- 產物：同上結構（`openspec/`、`mistake.md`、新建的 `CLAUDE.md`——本服務先前沒有這份文件）
- Phase 1 發現：`RAG_SETUP.md`/`.env.example` 描述的是已被取代的 `.env` 配置流程（首個 commit 產物，之後 config 機制演進為 `config.json` 但這兩份文件未同步更新）
- 稽核找到 13 項候選違反點，使用者全數確認納入本輪處理（含兩項需裁示方向的項目）：
  - **`GET /api/v1/rag/status` 修復**：`get_system_status()` 原讀取不存在的 `config.RAG_TOP_K`，必定拋 `AttributeError`、回 500；改讀 `config.RAG_CHUNK_SIZE`/`RAG_CHUNK_OVERLAP`
  - **刪除死代碼**：`reply_controller.py`/`reply_service.py`/`conversation_repository.py`（首個 commit 建立後從未再修改、未被任何服務呼叫的完整舊聊天路徑）、`prompt_builder_zh.py`（163 行無 import 的中文版 prompt builder）；實作時額外發現並修正 `src/controllers/__init__.py` 對 `reply_controller` 的殘留 import（若不修正會導致服務啟動即 `ImportError`）
  - **清理未使用依賴**：`requirements.txt` 移除 `langchain`/`langchain-community`/`langchain-core`/`python-dotenv`
  - **`rag_controller.py` 錯誤碼查表化**：7 支方法改用共用 `_raise_for_error()`，統一 503/500 判斷關鍵字集合；刻意保留 `get_rag_context` 原本「一律 500」的行為不併入查表，避免引入規格未涵蓋的行為變更
  - **Config 全面統一為「無預設值，缺鍵拋錯」**（使用者裁示方向）：`config.py` 原本多數欄位帶隱性預設值（如 `OLLAMA_MODEL` 預設 `"llama2"`），與服務其餘部分（`app.py`/`chat_service.py`/`rag_repository.py`）的「缺 key 拋 `ValueError`」風格雙軌並存；本輪全面改用 `_require()` 統一（`QDRANT_API_KEY` 除外，允許 `None`）
  - **CORS 改讀 config**：`app.py` 的 `allow_origins` 從寫死字串改為 `[config.GATEWAY_URL]`
  - **統一錯誤傳遞風格**：`chat_service.py` 原本內部 try/except 吞異常回傳 error dict，與 `rag_service.py` 的「被動報錯」不一致；改為一致的被動報錯，controller 層統一轉換
  - **SOLID-DIP 抽象化**：`RAGService`/`ChatService` 改為建構子注入依賴（預設參數維持既有呼叫端零改動）——與前四服務「JS 環境下約定俗成、不處理」的判定不同，本服務使用者選擇一併處理
  - **移除實驗性註解死碼與 DEBUG log**
  - **確認不處理／記錄為已知限制**：`initialization_jobs`（RAG 初始化進度的進程內記憶體 dict）服務重啟即遺失狀態，與 chat-service 的 `creationJobs`/`aiGenerationStatus` 屬同類已知限制，牽涉是否換成持久化儲存的架構決策，使用者確認本輪不處理
- 驗證：本服務無任何單元測試框架整合（無 pytest/unittest 檔案），全部改動 `py_compile` 通過、grep 確認無殘留引用；手動整合測試 17 項情境全數通過（因本機無 fastapi/ollama/qdrant-client 套件，測試由 Claude 直接以 curl 對使用者已啟動的服務執行，非使用者手動跑），涵蓋健康檢查、RAG 完整生命週期（初始化/狀態輪詢/檢索/主角人設/摘要存取/清理）、`/rag/status` 修復驗證、聊天生成與摘要生成（實際呼叫本機 Ollama）、多種錯誤情境（422/404/400）、CORS header 驗證、確認已刪除的舊端點回 404
- **狀態**：程式碼改動未 commit、openspec change 未 archive、Phase 7 備份仍在 scratchpad——Phase 11 尚未執行（使用者已明確表示本次收尾三步驟暫不執行，與前兩輪做法一致）

---

## 重要環境筆記（下一個服務會用到）

- **openspec CLI**：`@fission-ai/openspec`（GitHub: `Fission-AI/OpenSpec`），已用 `npm install -g @fission-ai/openspec@1.6.0` 全域安裝。⚠️ npm 上同名/相似名稱的套件（`openspec`、`openspec-shipper`、`openspec-buddy` 等）都是無關或來路不明的第三方套件，**不要誤裝**。
- **`.claude/commands/opsx` + `.claude/skills/openspec-*`**：每個服務的 `openspec init --tools claude` 都會自動產生這套 slash command 與 skill，不需手動複製。
- **openspec CLI 常用指令**：
  - `openspec init --tools claude`（服務目錄內）
  - `openspec new change "<name>"`
  - `openspec status --change "<name>" --json`（查 artifact 相依順序）
  - `openspec instructions <artifact> --change "<name>" --json`（拿 template/context/rules）
  - `openspec validate <change-name-or-spec> --strict` 或 `openspec validate --all --strict`
- **foundation change 不需要 `tasks.md`**（純文件記錄，不會被 apply），但 schema 的 `specs` artifact 必須存在（哪怕只是延續 main spec 的 ADDED 快照）才能解鎖 `tasks`。
- **簡單重構型 change**（如 `simplify-*`）即使「行為不變」，若錯誤碼對照表等內容在 spec 中有明文描述，仍建議寫一份 MODIFIED delta（標註清楚「僅實作機制改變、行為不變」），因為 CLI 的 `tasks` artifact 依賴 `specs` artifact 先存在。

---

## 尚未優化的候選服務

目前平台列出的五個服務（auth/user/character/chat/ai-service）皆已完成第一輪優化（Phase 0–10）。
**Phase 11（commit → archive → 清備份）在全部五個服務都尚未執行**，是目前唯一的共同未完成事項，
需要使用者明確指示才會逐一進行。

若之後盤點出新的候選服務（例如 `api-gateway` 或其他尚未列入的微服務），可依《後端專案優化標準
程序.md》的 Phase 0 重新挑選並套用同一套 SOP。

---

## 給下一個聊天室的開場指令範例

> 「請閱讀『後端專案優化進度.md』與『後端專案優化標準程序.md』，幫我對 <服務名> 執行 Phase 11
> 收尾（commit → archive → 清備份）。」

或

> 「請閱讀『後端專案優化進度.md』與『後端專案優化標準程序.md』，接續優化 <新服務名>。」
