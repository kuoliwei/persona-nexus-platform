# 後端專案優化標準程序（SOP）

> **這份文件是什麼**
> 一套**可複製的標準作業程序**，用來對單一後端微服務做「規格驅動的架構優化」。
> 內容整理自對 `auth-service` 做的第一輪優化實作，抽取成不綁特定服務的通用流程，供之後套用到其他微服務。
>
> **搭配文件**
> - 《後端系統設計原則》— 稽核所依據的「應然」標準（跨服務共用，只需建立一次）。
> - 各服務的 `openspec/`、`CLAUDE.md`、`mistake.md` — 本程序的產物。
>
> **核心信念**
> **設計準則定義「對」，稽核找出「錯」，優化把「錯」改成「對」，最後把規格改回與現實一致。**
> 全程以「規格」為單一真相；凡程式碼與規格對不上之處，即為優化點。

---

## ⚡ 難度評估與模式切換（適用所有 Phase）

**規則**：在執行任何 Phase 的實質工作前，**AI 應評估該 Phase 涉及的任務難度**。若判斷難度較高（例如複雜的架構重構、跨服務協調、模糊需求的釐清），**可主動向使用者反應**，提議開啟思考模式（`/plan`）或更換模型（例如 Opus），**並等待使用者回應後再繼續任務**。

**何時觸發難度評估**：
- Phase 5（稽核）：若違反點超過 10 項、涉及多個服務、判斷把握度有多個「低」
- Phase 6（change 設計）：若涉及架構圖重繪、新增外部依賴、改變 API 契約
- Phase 8（實作）：若 task 清單超過 20 項、單一 task 牽動多個檔案、涉及測試框架調整
- Phase 9（驗證）：若需要整合測試、手動測試場景複雜、涉及多個後端服務的聯動

**評估標準**：
- **低難度**（直接進行）：單一服務、局部改動、清晰的 task 清單、已有範本參考
- **中難度**（建議提醒）：涉及 2–3 個服務、架構級調整、新增 3 個以上檔案
- **高難度**（建議切換模式）：跨多個服務、核心架構變更、模糊需求需釐清、需要創意設計

**格式（向使用者反應）**：
> 「這個 Phase [編號] 涉及 [難度原因]。建議開啟思考模式（`/plan`）/更換模型（Opus）來設計更周全的方案。您同意嗎？」

使用者回應「是」或「同意」後繼續；「否」或「先進行」則直接開始。

---

## 階段總覽

```
Phase 0  選定目標服務
Phase 1  摸清現況（讀碼 + 驗證既有文件）
Phase 2  OpenSpec 初始化 + 現況基準線 spec
Phase 3  建立/確認設計準則（跨服務共用，只做一次）
Phase 4  回溯地基文件（記錄現況的 Why 與結構）
Phase 5  稽核（準則 vs 現況 → 違反點）
Phase 6  建立優化 change（改規格，尤其架構圖）
Phase 7  備份
Phase 8  實作（照 tasks 改碼）
Phase 9  驗證（單元 + 手動整合）
Phase 10 回寫規格（讓規格與現實重新對齊）
Phase 11 收尾（commit → archive → 清備份）
```

---

## Phase 0 — 選定目標服務

**目的**：挑一個服務作為優化對象。第一次做時，選最輕量、最單純的服務當示範案例。

**做法**：
- 只統計**自己開發的程式碼**，排除 `node_modules`、Prisma `generated`、`__pycache__`、`venv`。
- 涵蓋範圍：`src/`、`scripts/`，以及**專案根目錄**的 `.js` / `.py`（別只看 `src/`）。
- 除了行數，也看**職責數、路由數、外部依賴**——行數少但有 DB 層的服務，未必比行數多但職責單一的簡單。

**產物**：選定的服務。

---

## Phase 1 — 摸清現況（讀碼 + 驗證既有文件）

**目的**：真正理解服務現在怎麼運作，並且**不輕信舊文件**。

**做法**：
- 逐一讀完該服務所有自開發腳本（含測試檔、設定檔、`.env`）。
- **實際驗證**既有文件（如 `CLAUDE.md`）的每個宣稱：該跑測試就跑（`npx jest`）、該 grep 就 grep。
- 發現文件與現實不符處，**先修正文件**——尤其會誤導接手者的硬錯誤（例如「測試通過」其實掛掉）。

**心法**：文件會過時，程式碼才是現況。動優化之前，先讓「對現況的描述」是對的。

**產物**：一份與現實一致的 `CLAUDE.md`（或等價的服務說明）。

---

## Phase 2 — OpenSpec 初始化 + 現況基準線 spec

**目的**：建立「規格」這個單一真相的載體，並把現況精準記錄成基準線。

**做法**：
- `npx openspec init --tools claude`（在服務目錄內；各服務是獨立 repo，就各自有 openspec）。
- 填 `openspec/config.yaml` 的 context：如實描述現況，**刻意記下遺留代碼與隱藏耦合**（它們是之後的優化靶）。
- 寫 `openspec/specs/<capability>/spec.md`：**as-is 基準線**，包含
  - 「當前架構圖」（之後要在上面畫紅線的靶）；
  - 逐條行為需求（Requirement + WHEN/THEN Scenario），對照原始碼、不美化。
- `npx openspec validate --specs --strict` 確認格式合法。

**產物**：`config.yaml` + 現況基準線 main spec。

---

## Phase 3 — 建立/確認設計準則（跨服務共用，只做一次）

**目的**：先立「應然」的標準，才能拿去量「實然」。

**關鍵觀念（務必分清）**：
- 我們要的是**設計準則**（規範性、與任何服務現況無關），不是「優化準則」。
- 順序是：**先立準則 → 再稽核現況 → 掉出違反點 = 優化點**。反過來從現況歸納準則，會變成合理化現狀。

**做法**：
- 準則必須**有業界文獻依據，不自行杜撰**（可上網查證權威來源）。
- 每條附「**如何檢查違反**」的稽核鉤子。
- 已完成一份《後端系統設計原則》（KISS/DRY/YAGNI/SSOT、SoC/資訊隱藏/高內聚低耦合/SOLID、12-Factor、Design by Contract）。**這份跨服務共用，之後直接沿用，不必重寫。**

**產物**：《後端系統設計原則》（已存在則跳過此階段）。

---

## Phase 4 — 回溯地基文件（記錄現況的 Why 與結構）

**目的**：補上 main spec（只講「做什麼」）沒有的東西——當初的**動機**與**設計結構**。

**做法**：建一個回溯性的 foundation change（如 `<capability>-foundation`），含：
- `proposal.md`：Why（為什麼有這個服務、範圍、刻意不做的事）。
- `design.md`：**只忠實記錄設計結構**——不寫理由、不做評判、不預先標「負債」。記錄「是什麼」，不記錄「為什麼這樣比較好／不好」。
- delta spec（`## ADDED Requirements`）：地基的**凍結來歷快照**。

**心法（重要）**：
- OpenSpec 規定每個 change 至少要有一份 delta spec。
- delta spec = 「這次新增了什麼」的**凍結歷史**；main spec = 「系統現在有什麼」的**活的真相**。只發生過一次變更時兩者內容相同——這是 **provenance（來歷），不是冗餘**。之後演進只改 main spec，凍結的 delta 不動。

**產物**：foundation change（proposal + design + delta spec）。

---

## Phase 5 — 稽核（準則 vs 現況 → 違反點）

**目的**：拿設計準則逐條掃現況，找出不符之處。

**做法**：
- 逐條走過《後端系統設計原則》，對每條問「這個服務有沒有違反？違反在哪個檔案？」。
- 產出稽核記錄（如 `mistake.md`），內容包含：
  - 每條原則的判定（符合／違反／不適用）與**依據檔案**；
  - **把握程度**（高／中／低）——動態語言下 DIP/OCP 之類的違反較鬆，如實標低，不灌水；
  - **符合的也要標出來**，不硬湊違反（例如做對的 SSOT、SoC、無狀態）。
- 最後彙整「確認的違反點」清單（按把握度排序），作為優化的依據。

**⛔ 檢查點（必停）**：寫完 `mistake.md` 後**停下動作，通知使用者**，與使用者**逐項核對稽核內容**（哪些違反成立、哪些先不處理、把握度是否認同），並**確認接下來要納入 task 的範圍**。**未經使用者確認，不得逕自進入 Phase 6。**

**心法**：稽核是「準則 × 現況」的比對產物。判斷（違反/優化）與記錄（現況事實）要**分開**——不要拿「預先標好答案的負債表」自我印證。稽核結果是**建議**，處理範圍由使用者拍板。

**產物**：稽核記錄（`mistake.md`）+ 使用者確認過的處理範圍。

---

## Phase 6 — 建立優化 change（改規格，尤其架構圖）

**目的**：把違反點轉成「改後的規格」，由規格驅動改碼。

**做法**：建一個優化 change（如 `simplify-<service>`），含四件套：
- `proposal.md`：Why（引稽核結果）、What Changes、Impact（**明列唯一的行為變更**）。
- `design.md`：改動後的結構（可畫「變更前/後」對照圖）。
- delta spec：**MODIFIED / REMOVED** 對應的需求，把「優化後的契約」定死。
- `tasks.md`：可打勾的實作待辦，標依賴順序。

**分類每個改動**（決定它在規格上的足跡）：
| 類型 | 例子 | 規格足跡 |
|------|------|---------|
| **行為變更** | 移除 login 的格式驗證 | 需寫 MODIFIED 需求（可觀察行為改了） |
| **行為不變** | 清死代碼、改名、抽對應表、config 外部化 | 無需求層 delta，只清架構圖/文件 |

**心法**：這是「**修改規格來驅動優化**」真正落地之處——先在 spec（尤其架構圖）畫紅線，tasks 隨之生成，再照 tasks 改碼。若 change 範圍長大超出原名（如從 middleware 擴到 controller），**誠實改名**反映真實範圍。

**產物**：優化 change（proposal + design + delta spec + tasks），`validate --strict` 通過。

---

## Phase 7 — 備份

**目的**：動手前留退路。

**做法**：
- 把**所有會被改動或刪除的腳本**複製到一個備份資料夾（保留原目錄結構）。
- 備份位置要**避開測試掃描路徑**（例如別放在會被 jest 掃到的服務目錄內；放專案外或 scratchpad）。

**產物**：帶時間戳的備份資料夾。

---

## Phase 8 — 實作（照 tasks 改碼）

**目的**：依規格與 tasks 動手，不即興發揮。

**做法**：
- **行為變更的 task 先做**（風險較高，先隔離），行為不變的清理後做。
- 只動 tasks 內的檔案，**不碰無關的部分**。
- 連帶更新測試腳本以反映現況（刪過時案例、修錯誤標籤、更新 `test.http`）。
- **每做完一項就回 `tasks.md` 打勾**；過程中臨時追加的工作，補成新 task 再打勾，別讓它變成沒記錄的隱形工作。

**心法**：規格與 tasks 是契約與清單，coding 是去**滿足**它們，不是重新發明。

**產物**：改好的程式碼 + 逐項打勾的 `tasks.md`。

---

## Phase 9 — 驗證（單元 + 手動整合）

**目的**：證明改動正確、且沒破壞既有行為。

**做法**（**跑測試前先徵得同意 / 由使用者決定時機，不擅自開跑**）：

- **步驟 0（測試前必做）：先核對測試腳本是否過時，更新後才測。**
  - 逐一檢查單元測試（`*.test.js`）與 `test.http` 是否還符合現況——是否引用已刪的符號、過時的訊息、錯誤的 mock 路徑、已不存在的路由。
  - **先更新到與現況一致，才開始跑測試**。拿過時的測試去驗證是沒有意義的。
  - **測試項目必須涵蓋全部 spec scenario，而不是只測被改動的部分**——成功路徑、每一種失敗、行為變更前後對照都要有對應案例。只測改動處會漏掉「改動意外弄壞了其他情境」。
- **單元測試**（`npx jest`）：專注**腳本本身**的邏輯。
- **手動 / 整合測試**（`test.http` 或 curl）：測**路由層**行為。改了路由/middleware **務必先重啟服務**新行為才生效（通知使用者，由使用者親自重啟）。
  - 需要真帳號的案例，用帶時間戳的乾淨 email 避免與先前執行衝突。
- `grep` 全 `src/` 確認無殘留對已刪符號的引用。

**注意 test.http 的邊界**：它直接打服務 port，**跳過 gateway、不觸發 CORS**（CORS 只有真瀏覽器才測得到）。適合測服務本身的邏輯與契約，但不等於端到端前端測試。

**產物**：全數通過的測試結果（單元 + 手動）。

---

## Phase 10 — 回寫規格（讓規格與現實重新對齊）

**目的**：把改動同步回文件，讓「規格 = 現實」再次成立。

**做法**：
- 把優化 change 的 delta 同步進 main spec（更新對應需求）。
- 更新 main spec 的**架構圖**與「遺留物」段落（清掉已刪的東西）。
- 更新 `CLAUDE.md`（現況、測試狀態、演進歷史加一筆）。
- 在稽核記錄（`mistake.md`）標記各項**已處理**。
- `validate --all` 全綠。

**心法**：這一步完成，才閉合了整個閉環——回到「規格是單一真相」的起點。

**產物**：與現實一致的 main spec / CLAUDE.md / mistake.md。

---

## Phase 11 — 收尾（commit → archive → 清備份）

**目的**：正式結案。

**做法**：
1. **git commit**：把已驗證的成果定版（各服務是獨立 repo，在該服務目錄內提交）。
2. **archive**（`/opsx:archive`）：歸檔完成的 change。注意 archive 也會同步 spec——若已手動同步，archive 後檢查 main spec 沒被改壞。
3. **清備份**：確認無誤後刪掉 Phase 7 的備份資料夾。

**產物**：已提交、已歸檔的一輪優化。之後即可對下一個微服務重跑 Phase 0–11。

---

## 貫穿全程的原則

1. **規格是單一真相**：凡程式碼與規格對不上，即為優化點（該刪 / 該拆 / 該外部化）。
2. **記錄 ≠ 評判**：忠實記錄現況時只寫事實，判斷留到稽核階段。
3. **準則要有依據**：設計準則引業界文獻，不杜撰；不確定的引用不硬編頁碼。
4. **誠實不灌水**：稽核標把握度、符合的也標；進度打勾據實；測試失敗就說失敗。
5. **先驗證再相信**：舊文件的宣稱一律實跑驗證。
6. **行為變更要顯性**：可觀察的行為改動必須先反映在 spec，再改碼。
7. **不擅自行動**：跑測試、動無關檔案之前，先確認。
8. **動手前先備份**，且備份別污染測試。
9. **關鍵節點停下核對**：稽核完成、動手優化之前，停下與使用者逐項核對違反點與處理範圍。稽核是建議，範圍由使用者拍板。
10. **測試前先更新測試、且涵蓋全部**：跑測試前先把過時的測試腳本更新到符合現況；測試項目要涵蓋所有 scenario，不能只測被改動的部分。
