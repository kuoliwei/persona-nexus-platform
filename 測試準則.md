# 測試準則

> 本文檔規範所有自動化測試和手動測試的執行方式，確保測試結果真實反映生產環境的行為。

## 核心原則

### 1. 始終通過 API Gateway 測試

**必須遵守**：所有測試請求都必須經由 `http://localhost:8000`（api-gateway）進行。

**禁止**：直接訪問後端服務的裸 port（3000/4000/5000/6000）。

**理由**：
- 繞過 gateway 會跳過以下關鍵層：
  - JWT 驗證（auth-service）
  - 請求轉發與路由重寫（pathRewrite）
  - `x-user-id` header 注入
  - `x-internal-request` header 驗證
  - CORS 和其他安全中間件
  
- 測試結果會給出**虛假的通過率**：
  - 某個端點在直接訪問時可能工作，但通過 gateway 時因路由配置錯誤而失敗
  - 授權檢查被繞過，測試不到實際的權限隔離
  - 響應格式轉換被跳過，可能隱藏序列化問題

**檢查清單**：
- [ ] 所有 curl/PowerShell 請求的 URL 都以 `http://localhost:8000` 開頭
- [ ] 包含 `Authorization: Bearer {token}` 和 `x-user-id` header
- [ ] 從 auth-service 的 `/api/auth/login` 或 `/api/auth/register` 獲得的真實 token
- [ ] 如果測試內部路由（`/api/` 非 `/internal/`），必須帶真實用戶 token

---

### 2. 使用真實登入流程取得 Token

**必須遵守**：
1. 使用 `POST /api/auth/register` 註冊測試帳號（或重用已註冊帳號）
2. 使用 `POST /api/auth/login` 登入並取得 token
3. 將 token 保存在變數中供後續請求使用

**禁止**：
- 手工編造或猜測 token（即使知道 JWT 格式）
- 在代碼中硬編碼過期 token
- 使用其他測試工具生成的 token

**範例**：
```bash
# ✓ 正確做法
login_response=$(curl -s -X POST "http://localhost:8000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"pass123"}')

token=$(echo "$login_response" | grep -o '"token":"[^"]*"' | cut -d'"' -f4)
user_id=$(echo "$login_response" | grep -o '"id":"[^"]*"' | cut -d'"' -f4)

# 後續所有請求都使用這個真實 token
curl -X GET "http://localhost:8000/api/characters" \
  -H "Authorization: Bearer $token" \
  -H "x-user-id: $user_id"
```

**為什麼重要**：
- Token 有過期時間（通常幾小時）
- 過期 token 應拒絕（401），測試能驗證此行為
- 登入流程本身也是需要測試的關鍵路徑

---

### 3. 準確驗證響應格式

**必須遵守**：
- 檢查 HTTP 狀態碼（200/201/202/400/401/403/404/409/503 等）
- 驗證響應 body 的結構（是否為 JSON、包含預期欄位）
- 對於錯誤響應，檢查 `error` 和 `message` 欄位的內容

**禁止**：
- 只檢查是否「有回應」，不檢查狀態碼
- 假設特定字段存在而不驗證
- 忽略錯誤消息中的詳細信息

**範例**（以發送訊息為例）：
```bash
response=$(curl -s -w "\n%{http_code}" -X POST "http://localhost:8000/api/conversations/conv_xxx/messages" \
  -H "Authorization: Bearer $token" \
  -H "x-user-id: $user_id" \
  -H "Content-Type: application/json" \
  -d '{"text":"test"}')

http_code=$(echo "$response" | tail -n1)
body=$(echo "$response" | head -n-1)

if [ "$http_code" == "201" ]; then
  status=$(echo "$body" | grep -o '"status":"[^"]*"' | cut -d'"' -f4)
  if [ "$status" == "accepted" ]; then
    echo "✓ 正確：201 + accepted"
  else
    echo "✗ 錯誤：201 但狀態不是 accepted"
  fi
else
  echo "✗ 錯誤：HTTP $http_code（預期 201）"
fi
```

---

### 4. 測試多用戶場景必須用不同帳號

**必須遵守**：
- 若測試跨帳號權限，必須建立至少 2 個不同帳號
- 每個帳號分別登入，獲得各自的 token
- 用不同 token 發送請求，確保 `x-user-id` 確實不同

**禁止**：
- 用同一個 token 但改變 `x-user-id` header 的值（gateway 會檢查 JWT 與 header 的一致性）
- 假設「只要改了 header 就能模擬不同用戶」

**範例**（測試用戶 A 能否訪問用戶 B 的私有角色）：
```bash
# 建立帳號 A
register_a=$(curl -s -X POST "http://localhost:8000/api/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"email":"userA@test.com","password":"pass123"}')
token_a=$(echo "$register_a" | grep -o '"token":"[^"]*"' | cut -d'"' -f4)
user_id_a=$(echo "$register_a" | grep -o '"id":"[^"]*"' | cut -d'"' -f4)

# 建立帳號 B
register_b=$(curl -s -X POST "http://localhost:8000/api/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"email":"userB@test.com","password":"pass123"}')
token_b=$(echo "$register_b" | grep -o '"token":"[^"]*"' | cut -d'"' -f4)
user_id_b=$(echo "$register_b" | grep -o '"id":"[^"]*"' | cut -d'"' -f4)

# 帳號 B 建立私有角色
char_response=$(curl -s -X POST "http://localhost:8000/api/characters" \
  -H "Authorization: Bearer $token_b" \
  -H "x-user-id: $user_id_b" \
  -H "Content-Type: application/json" \
  -d '{"name":"私有角色","visibility":"private",...}')
private_char_id=$(echo "$char_response" | grep -o '"id":"[^"]*"' | cut -d'"' -f4)

# ✓ 用帳號 A 的 token 試圖訪問帳號 B 的私有角色
response_a=$(curl -s -w "\n%{http_code}" -X POST "http://localhost:8000/api/conversations/character/$private_char_id" \
  -H "Authorization: Bearer $token_a" \
  -H "x-user-id: $user_id_a")

http_code=$(echo "$response_a" | tail -n1)
if [ "$http_code" == "403" ]; then
  echo "✓ 正確：帳號 A 被拒絕訪問帳號 B 的私有角色"
else
  echo "✗ 安全漏洞：帳號 A 不應能訪問私有角色（HTTP $http_code）"
fi
```

---

### 5. 非同步操作必須輪詢驗證

**必須遵守**：
對於非同步端點（返回 202/201 accepted），不能假設背景任務已完成，必須輪詢檢查狀態。

**相關端點**：
- `GET /api/conversations/character/{characterId}` — 可能返回 202 preparing
- `GET /api/conversations/{conversationId}/ai-generation-status` — 用於輪詢 AI 生成進度
- `POST /api/conversations/{conversationId}/messages` — 返回 201 accepted，AI 後臺生成

**禁止**：
- 發送訊息後立即查詢訊息列表，假設 AI 回覆已存
- 建立對話後立即使用 conversationId，不確認 status=ready

**輪詢規範**：
- 初始延遲：1-2 秒
- 輪詢間隔：1-2 秒
- 最大次數：60-120 次（對應 1-4 分鐘超時）
- 監聽狀態：
  - `status: "completed"` 或 `status: "ready"` → 成功，停止輪詢
  - `status: "failed"` → 失敗，停止輪詢並報錯
  - `status: "idle"` / `status: "generating"` / `status: "preparing"` → 繼續輪詢

**範例**：
```bash
conversation_id="conv_xxx"
echo "建立對話（可能異步）..."

create_response=$(curl -s -X GET "http://localhost:8000/api/conversations/character/char_yyy" \
  -H "Authorization: Bearer $token" \
  -H "x-user-id: $user_id")

status=$(echo "$create_response" | grep -o '"status":"[^"]*"' | cut -d'"' -f4)
conversation_id=$(echo "$create_response" | grep -o '"conversationId":"[^"]*"' | cut -d'"' -f4)

# 若狀態是 preparing，輪詢至 ready
if [ "$status" == "preparing" ]; then
  echo "對話建立中，開始輪詢..."
  for attempt in {1..60}; do
    sleep 2
    poll_response=$(curl -s -X GET "http://localhost:8000/api/conversations/character/char_yyy" \
      -H "Authorization: Bearer $token" \
      -H "x-user-id: $user_id")
    
    poll_status=$(echo "$poll_response" | grep -o '"status":"[^"]*"' | cut -d'"' -f4)
    poll_id=$(echo "$poll_response" | grep -o '"conversationId":"[^"]*"' | cut -d'"' -f4)
    
    if [ "$poll_status" == "ready" ]; then
      echo "✓ 對話準備完成（第 $attempt 次輪詢）"
      conversation_id=$poll_id
      break
    fi
    
    if [ $((attempt % 10)) -eq 0 ]; then
      echo "  等待中... ($attempt/60)"
    fi
  done
fi

if [ -z "$conversation_id" ]; then
  echo "✗ 對話建立超時"
  exit 1
fi
```

---

### 6. 集成測試與單元測試的邊界

**必須遵守**：
- 本文檔規範的是**集成測試**（通過 API Gateway 測試完整流程）
- 單元測試（測試單個函數或服務）可以更靈活，但必須與集成測試結果一致

**禁止**：
- Mock 掉 gateway 層，直接測試後端服務
- 使用 `x-internal-request: true` 來跳過授權測試（內部請求只供服務間通信，不供前端使用）

**補充說明**：
- 內部路由（`/internal/*`）的測試應該用 `x-internal-request: true`，但只在測試 gateway 轉發層時使用
- 若要測試「直打後端內部路由」，應寫清楚這是在驗證「內部路由本身」而不是「前端使用」

---

### 7. 錯誤場景必須正面測試

**必須遵守**：
- 不僅測試「成功路徑」（200/201/202）
- 也要測試「失敗路徑」（400/401/403/404/409/503）
- 驗證錯誤消息內容和狀態碼匹配

**禁止**：
- 忽略錯誤情況，只在「一切正常」時測試
- 假設「只要返回錯誤就算通過」，不檢查錯誤碼是否正確

**常見錯誤場景清單**：

| 場景 | 預期行為 | 驗證方式 |
|------|---------|--------|
| 過期 token | 401 Unauthorized | 使用過期或偽造 token 發請求 |
| 缺少 token | 401 Unauthorized | 移除 Authorization header |
| 無效 JWT | 401 Unauthorized | 使用無效格式的 token |
| 無權訪問資源 | 403 Forbidden | 用戶 A 的 token 試圖訪問用戶 B 的私有資源 |
| 資源不存在 | 404 Not Found | 使用不存在的 conversationId/characterId |
| 缺少必填欄位 | 400 Bad Request | 發送 `{"text":""}` 給發送訊息端點 |
| 並行操作衝突 | 409 Conflict | AI 生成中試圖發送新訊息 |
| 後端服務不可用 | 503 Service Unavailable | AI Service 或 Qdrant 離線 |

**範例**：
```bash
echo "測試：無效 token 應返回 401"
response=$(curl -s -w "\n%{http_code}" -X GET "http://localhost:8000/api/characters" \
  -H "Authorization: Bearer invalid_token_xyz" \
  -H "x-user-id: user_123")

http_code=$(echo "$response" | tail -n1)
if [ "$http_code" == "401" ]; then
  echo "✓ 正確：401 Unauthorized"
else
  echo "✗ 失敗：預期 401，實際 $http_code"
fi
```

---

### 8. 測試數據隔離與清理

**必須遵守**：
- 每個測試場景使用獨立的測試帳號（避免數據污染）
- 若需清理測試數據，應在測試完成後執行（不在開始前）
- 測試間不應相互依賴（某個測試的輸出不該是另一個測試的輸入）

**禁止**：
- 創建測試帳號後不清理，導致下次測試時帳號已存在
- 在一個測試中建立資源，在另一個測試中刪除（中間任一失敗會導致級聯失敗）

**最佳實踐**：
```bash
# 測試開始時生成唯一的帳號名（避免重名衝突）
timestamp=$(date +%s%N)
test_email="test_${timestamp}@example.com"

# 註冊並登入
register_response=$(curl -s -X POST "http://localhost:8000/api/auth/register" \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"$test_email\",\"password\":\"pass123\"}")

# ... 執行測試 ...

# 測試結果記錄（但不刪除帳號，讓資料累積以備後續分析）
echo "✓ 測試完成（帳號: $test_email）"
```

---

### 9. 測試輸出與結果記錄

**必須遵守**：
- 每個測試都輸出清晰的結果（✓ 通過 / ✗ 失敗）
- 記錄完整的請求與響應（便於事後調試）
- 記錄時間戳與執行環境信息

**禁止**：
- 安靜地執行，沒有任何輸出
- 縮寫或模糊的測試結果（「似乎工作了」）

**日誌模板**：
```bash
echo "=================================================="
echo "測試: 發送訊息與 AI 生成"
echo "時間: $(date +'%Y-%m-%d %H:%M:%S')"
echo "環境: Gateway=$gateway, ConversationId=$conversation_id"
echo "=================================================="
echo ""

echo "[1/3] 發送訊息..."
# ... 發送邏輯 ...
echo "✓ 訊息已發送（狀態: $status）"
echo ""

echo "[2/3] 輪詢 AI 生成狀態..."
# ... 輪詢邏輯 ...
echo "✓ AI 生成完成（第 $poll_count 秒）"
echo ""

echo "[3/3] 驗證訊息已存儲..."
# ... 驗證邏輯 ...
echo "✓ 訊息已存儲（ID: $msg_id）"
echo ""

echo "=================================================="
echo "測試結果: ✓ 通過（3/3）"
echo "=================================================="
```

---

## 檢查清單（執行任何測試前必讀）

- [ ] **URL 檢查**：所有請求都使用 `http://localhost:8000`，未直接訪問後端 port
- [ ] **Token 檢查**：使用登入端點取得的真實 token，非手工編造
- [ ] **Header 檢查**：包含 `Authorization: Bearer {token}` 和 `x-user-id: {userId}`
- [ ] **狀態碼檢查**：驗證 HTTP 狀態碼（不只檢查「有沒有回應」）
- [ ] **多用戶檢查**：跨帳號測試時使用不同帳號的不同 token
- [ ] **非同步檢查**：涉及非同步操作時使用輪詢驗證（不假設立即完成）
- [ ] **錯誤檢查**：同時測試成功和失敗場景
- [ ] **日誌檢查**：輸出清晰的測試結果與時間戳

---

## 相關參考文件

- [chat-service-api-guide.md](chat-service-api-guide.md) — 完整 API 規格
- [測試清單-進階整合測試.md](測試清單-進階整合測試.md) — Section 5 測試詳細步驟
- [微服務架構實作spec.md](微服務架構實作spec.md) — Gateway 路由與授權設計
