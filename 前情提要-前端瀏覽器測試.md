# 前情提要：前端瀏覽器手動測試

> 這份文件是要帶到另一個聊天室接續討論用的。記錄「在做什麼」「為什麼」「已經確認的方向」
> 「接下來要做什麼」「要參考哪些資料」，讓新的聊天室能直接銜接，不用重新從頭問一遍。

---

## 背景：這件事是怎麼冒出來的

四個前端服務（`persona-nexus-auth`／`persona-nexus-character`／`persona-nexus-chat`／
`persona-nexus-lobby`）已經完成《前端專案優化進度.md》記錄的規格驅動架構優化（Phase 0–10），
但每個服務的 Phase 9 都還缺一塊：**真人開瀏覽器手動走查**（跟既有的 API 模擬測試不同，測的是
aria-live、焦點管理、toast 生命週期、SPA 導航、Network/CORS 這些只有真瀏覽器才測得出來的東西）。

這輪工作就是要補上這一塊，由使用者親自在瀏覽器操作、我在旁邊給指示並記錄結果。

## 已經做完的事

### 測試文件的演變（重要，避免找錯檔案）

1. 一開始寫了《測試任務-前端瀏覽器整合測試.md`，把四個服務**分開**列測試項目——但使用者指出
   這樣測試順序有問題（例如聊天室建立前的可及性測項排在還沒建角色之前，根本沒東西可測）。
   **這份舊檔已經刪除**，內容從未被實際使用來記錄測試結果，不用找它。
2. 改寫成《**前端網頁手動測試task.md**》——把四個服務的測項**依照真實操作依賴關係**串成一條線
   （auth 登入 → lobby 基本導航 → character 建角色 → lobby 確認角色出現 → chat 聊天 →
   character 編輯/刪除角色 → lobby 其餘功能 → 跨服務一致性檢查），這是**目前實際在用的主文件**，
   所有測試記錄（打勾進度、發現的問題）都在這裡。

### 目前的測試進度（《前端網頁手動測試task.md》）

- **第一階段（auth：註冊/登入）**：全部完成 ✅
- **第二階段（lobby：大廳基本進入與導航）**：全部完成 ✅
- **第三階段（character：建立角色）**：全部完成 ✅（3.3 有 3 項需要開發者工具/純鍵盤操作驗證的
  項目暫緩，使用者要求先跳過這類項目，之後統一批次處理，不要逐項打斷測試流程）
- **第四階段（lobby：確認新角色出現）**：全部完成 ✅
- **第五階段（chat：開始聊天）**：測到一半，**在 5.6（重啟聊天室）卡住暫停**
  - 5.1～5.5 已完成（初次進入、傳送訊息、刪除訊息、主人公人設彈窗、toast 通知）
  - 5.6 卡住原因：後端卡在 `202 preparing` 狀態一直起不來（跟下面 Bug 4 有關），且過程中
    連續發現好幾個真實 bug，使用者決定暫停測試、先把發現的問題整理成獨立文件
- **第六～八階段（character 編輯/刪除、lobby 其餘功能、跨服務一致性）**：**尚未開始**

### 測試過程中發現的所有問題（已整理成獨立文件）

《**前端網頁debug_task_checklist.md**》彙整了測試中發現的 **4 個真實 bug**（待辦導向的摘要，
完整現象/根因分析在《前端網頁手動測試task.md》文末「發現的新問題」）：

1. **`persona-nexus-character/src/create.js:56`**：建立角色成功後自動跳轉壞掉。
   `LOBBY_APP_URL` 本輪優化改成 `'/'`，但拼接 `` `${LOBBY_APP_URL}/my-characters` `` 在
   `LOBBY_APP_URL==='/'` 時算出 `'//my-characters'`（protocol-relative URL），瀏覽器誤判成
   要連到叫 `my-characters` 的網域，跳轉到 `ERR_NAME_NOT_RESOLVED`。100% 穩定重現。
   `edit.js` 可能有同樣問題，待確認。
2. **`persona-nexus-lobby/src/character-tooltip.js`**：大廳角色卡 hover 出現的簡介 tooltip
   是掛在 `document.body` 的全站單例，只靠 `mouseleave` 把 opacity 設回 0「隱藏」，從未真的
   從 DOM 移除。點卡片進入聊天室（SPA 導航，不整頁重載）時，`mouseleave` 可能沒確實觸發，
   tooltip 殘留疊在聊天室畫面上，直到回大廳對任一卡片完整 hover 一次才消失。
3. **`persona-nexus-chat/src/virtualMessageList.js` 的 `computeRange()`**：處理空陣列
   （`items.length === 0`）有漏洞，會對 `items[0]`（`undefined`）呼叫 `keyOf`，拋出
   `TypeError: Cannot read properties of undefined (reading 'id')`。觸發路徑是
   `chat.js:632` 在 `initializeChat()` 逾時失敗後仍無條件呼叫 `renderMessages()`，此時
   `messages` 是空陣列，例外蓋掉了原本該顯示的「聊天室建立失敗」錯誤訊息。
4. **`ai-service` 與 Qdrant 斷線後無法自動重連**：手動重啟 Qdrant docker 容器後，`ai-service`
   持續回報連線被拒（`WinError 10061`），必須把所有服務（含 Docker）全部關閉、用
   `start-all-services.bat` 從頭啟動一次才能恢復。推測是 `qdrant-client`/`httpx` 的連線池
   快取了失效連線，沒有重試/重建機制，根因尚未深入排查。

**這四個 bug 目前都只是記錄現象，完全沒有修復**（使用者尚未要求修，且測試本身也已暫停）。

## 接下來要做什麼

沒有強制順序，可能的方向（問使用者想先做哪個）：

1. **修復 Bug 1～4**：照《前端網頁debug_task_checklist.md》裡每個 bug 的具體待辦逐項處理，
   修完一項要回《前端網頁手動測試task.md》對應的測試項目重新實測一次（文件裡都有標注要
   回頭測哪一項）。
2. **繼續測試**：如果決定先不修 bug、想先把測試流程走完看還有沒有更多問題，可以從第五階段
   剩下的 5.6/5.7 開始（但目前後端卡住的狀態如果還沒恢復，可能要先重啟一次
   `start-all-services.bat` 讓環境乾淨），接著第六～八階段。
3. **兩者交替**：修一個 bug、重測一次、再修下一個——這樣風險最低，但比較花時間。

## 要參考哪些資料

進新聊天室後，建議依序讀：

1. **《前端網頁手動測試task.md》**（專案根目錄）——測試主文件，第一到第四階段完整記錄、
   第五階段測到一半，文末「發現的新問題」有完整的 4 個 bug 現象/根因分析（比 debug checklist
   詳細）。
2. **《前端網頁debug_task_checklist.md》**（專案根目錄）——4 個 bug 的待辦導向摘要，適合
   準備要動手修的時候用這份，一項一項處理、修完打勾。
3. **既有的 5 份《測試清單-*.md》**（`測試清單-persona-nexus-auth.md` 等 + `測試清單-進階整合
   測試.md`）——這些是**指令模擬 API 呼叫**的測試（PowerShell 直接打 `fetch()`），47+23 案例
   已全部通過，**不是這輪要測的東西，不要跟目前的瀏覽器測試搞混，也不用重跑**。
4. 若要追溯更早的架構優化脈絡（Phase 0–10 怎麼來的、每個服務改了什麼），才需要回頭讀
   《前情提要-前端專案優化.md》《前端專案優化進度.md》《前端系統設計原則.md》，但**這輪瀏覽器
   測試工作本身不需要重讀這幾份**，除非要深入理解某個 bug 是不是優化引入的。

## 這輪學到、下一步要留意的重點

- **使用者堅持測試指示要一次講清楚、不要漏交代前置條件**：例如「請封鎖 Network 分頁的某請求」
  這種指示，必須在使用者需要「送出前先觀察」的操作**之前**先提醒，不能等使用者已經動作完才說
  「你應該要在動作前做 X」——這樣會讓使用者白做一次。
- **不要自己判斷「已登入」「已在某頁」等前提就給下一步指示**：auth 測完登入後使用者已經在大廳，
  但後面 1.5 的邊界測試預設使用者還在登入頁，導致使用者要先手動導航、還一度被要求「登出」
  （但登出功能排在第七階段才測，不該提前用到）。修法是後來在文件裡明確寫「不要用登出按鈕，
  直接網址列手動導航過去」。
- **列出「所有發現的問題」時，不要自行做範圍/前後端分類去篩選或降權**：這輪明確踩到這個地雷
  （見 `feedback_no_unrequested_categorization` 記憶）——使用者要求把所有問題都列進
  debug checklist，我卻自己把 Qdrant/ai-service 那個問題判斷成「非前端 bug」「不在修復範圍
  內」，放進附註區塊。使用者糾正：沒有要求做這種判斷。**下一步遇到類似情況，一律平等列出，
  範圍/優先順序的判斷留給使用者**。
- **每個 bug 都要盡量找到根因、不能只記表面現象**：這輪四個 bug 都有實際讀原始碼定位到具體
  行號和邏輯漏洞（不是「大概是這樣」的猜測），這個習慣要延續。
- **需要開發者工具/純鍵盤操作驗證的測項，使用者要求先跳過、批次處理**：這類項目在
  《前端網頁手動測試task.md》裡有些已標記「需開發者工具檢查，暫緩」或「需開發者工具/鍵盤焦點
  檢查，暫緩」，之後真的要處理這批的話，建議一次性集中測，不要在測其他功能性項目時穿插進來
  打斷節奏。
