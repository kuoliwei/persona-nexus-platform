# 前情提要：前端專案優化

> 這份文件是要帶到另一個聊天室接續討論用的。記錄「在做什麼」「為什麼」「已經確認的方向」
> 「接下來要做什麼」「要參考哪些資料」，讓新的聊天室能直接銜接，不用重新從頭問一遍。

---

## 背景：這件事是怎麼冒出來的

之前依照《後端專案優化標準程序.md》對五個後端微服務（auth/user/character/chat/ai-service）
做過一輪「規格驅動的架構優化」（詳見《後端專案優化進度.md》）。這輪結束後，決定把同一套方法
套用到前端的四個微服務：`persona-nexus-auth`、`persona-nexus-character`、`persona-nexus-chat`、
`persona-nexus-lobby`（都是 Vite + 原生 JavaScript 的多頁應用，無 UI 框架）。

第一步先確認 SOP 骨架能不能直接沿用：結論是**可以**，Phase 0–11 的流程（立準則→稽核→使用者
確認→改規格→改碼→驗證→回寫規格→commit/archive）是通用方法論，只有兩處因應前端技術棧要調整
（Phase 0 的檔案排除清單、Phase 9 的驗證方式），不需要另寫一份完整的前端專用 SOP。

接著建立了跨服務共用的《前端系統設計原則.md》（結構對齊《後端系統設計原則.md》，A 節通用
軟體設計原則共用，B/C/D 節換成前端相關準則），並選定 `persona-nexus-auth`（四個前端裡檔案
最少、功能最單純）當第一個示範服務，完整走完 Phase 0–10（Phase 11 的 commit/archive 使用者
決定暫不執行）。

## 已經做完的事

### 1. persona-nexus-auth

完整過程與細節都記在《前端專案優化進度.md》，這裡只列重點結論：

- 稽核找到 7 項候選違反點 + 1 項真的壞掉的功能性缺陷（單元測試 0/2 全部失敗），使用者核對後
  **全部納入處理**。
- 已完成 Phase 6–10：建立優化 change、備份、實作、驗證（`npx jest` 全過、`npm run build`
  正常、`openspec validate --all --strict` 全綠）、回寫規格。
- **手動瀏覽器實測**（要接完整的 Caddy + gateway + auth-service）**使用者尚未親自驗證**，
  這點如果之後要回頭處理 persona-nexus-auth 的 Phase 11（commit/archive）要記得先做。
- Phase 11（commit → archive → 清備份）**尚未執行**，使用者決定先不做，之後有需要再回來處理。

### 2. persona-nexus-character

完整過程與細節都記在《前端專案優化進度.md》，這裡只列重點結論：

- 稽核找到 7 項候選違反點（YAGNI、DRY、SSOT、WCAG、Bundle Hygiene、環境設定、遺留依賴），
  使用者逐項確認決定後**全部納入處理**。
- 已完成 Phase 0–10：讀碼驗證、建立稽核、設計優化 change、備份、實作改動、驗證無誤、回寫規格。
- **實作內容**：刪除 counter.js、簡化 config-loader.js（只探測可達性）、改用相對路徑常數
  （`LOGIN_APP_URL = '/login/'`、`LOBBY_APP_URL = '/'`）、加 aria-live 可及性、移除 cors 依賴。
- **驗證結果**：`npm run build` 成功（產物 1.32 KB，無 dead code）、程式碼無殘留引用、與 auth/lobby
  模式統一。
- **規格記錄**：建立 openspec 完整結構（config.yaml + main spec + change 四件套）、更新 CLAUDE.md。
- Phase 11（commit/archive）**尚未執行**，使用者決定暫不做。

### 3. persona-nexus-chat

完整過程與細節都記在《前端專案優化進度.md》，這裡只列重點結論：

- 稽核找到 7 項候選違反點（WCAG、API/UI 分離、YAGNI 死代碼、DRY、狀態管理單一入口、SoC、
  錯誤呈現一致性），使用者核對後**全部納入處理**。
- 已完成 Phase 0–10：`api.js` 從死代碼重寫為真正的 API 層、新增 `session.js`、把
  886 行的 `chat.js` 拆成 `toast.js`/`messageMenu.js`/`protagonistModal.js` 三個模組、
  補齊可及性屬性、重啟失敗改用 toast。`npm run build`／`grep` 驗證通過。
- **特殊狀況**：本專案從一開始就是獨立 git repo（有遠端 `origin/main`），不同於
  auth/character；工作目錄原本就有兩筆與本輪無關的既存未 commit 修改，本輪未觸碰。
- Phase 11（commit/archive）**尚未執行**，使用者決定先不做。

### 4. persona-nexus-lobby

完整過程與細節都記在《前端專案優化進度.md》，這裡只列重點結論：

- 稽核找到 6 項候選違反點（DRY、SoC、模組邊界、狀態管理單一入口、WCAG、一致性），使用者核對後
  **全部納入處理**。
- **Phase 1 二次驗證發現的缺漏**：Glob 工具漏抓 `public/src/` 目錄下的 HTML 樣板檔案，導致首輪
  讀碼不完整；`CLAUDE.md` 嚴重過時（宣稱「主內容區暫時空白」「創建角色按鈕直接跳創建頁」等
  皆與現況不符），已修正回一致。**這輪的教訓**：即使 Glob/find 類工具回報「找不到檔案」，
  也不代表目錄真的是空的——工具可能有盲區，關鍵時刻要用 shell 直接列目錄覆核。
- 已完成 Phase 0–10：新增 `message-utils.js`（統一訊息提示+aria-live）、
  `conversation-history.js`（聊天歷史載入/刪除+菜單焦點管理）、`iframe-loader.js`
  （統一 iframe src 組裝）；`api.js` 補齊 token 統一存取介面；移除 `alert()`；補齊 aria-label。
  `npm run build` 成功、`openspec validate --all --strict` 全綠。
- Phase 11（commit/archive）**尚未執行**，使用者決定先不做。

## 接下來要做什麼

四個前端服務（auth、character、chat、lobby）**皆已完成 Phase 0–10**，目前沒有尚未開始的候選服務。

可能的後續方向（沒有強制順序，問使用者想先做哪個）：
1. **手動瀏覽器整合測試**：四個服務都還沒經過使用者親自的瀏覽器實測（需要完整 Caddy +
   gateway + 後端服務環境），這是每個服務 Phase 9 尚未閉環的部分。
2. **Phase 11 收尾**：四個服務都還沒 commit/archive，備份資料夾也都還在。
3. **跨服務的已知限制**：稽核過程中多次發現的跨服務 DRY 問題（`config-loader.js`／
   JWT 解碼邏輯在 auth/character/lobby 各自重複一份），目前策略是刻意不抽共用套件、
   保持各前端獨立，這是已確認的既有決定，非待辦事項。

## 要參考哪些資料

進新聊天室後，**依序**讀這些文件建立完整脈絡：

1. **《前端專案優化進度.md》**（專案根目錄）——目前的進度總覽，persona-nexus-auth 完整的
   Phase 0–10 記錄都在這裡，也記錄了「與後端 SOP 的差異」那兩點調整。
2. **《後端專案優化標準程序.md》**（專案根目錄）——SOP 骨架本體，Phase 0–11 的流程與心法，
   前端沿用這份，只在 Phase 0／Phase 9 套用《前端專案優化進度.md》裡記錄的調整。
3. **《前端系統設計原則.md》**（專案根目錄）——稽核依據的設計準則，已建立完成、跨服務共用，
   **不需要重寫**，直接拿來對照下一個服務的現況即可。
4. **`persona-nexus-auth/` 底下的實際產物，當作下一輪的格式範本**：
   - `openspec/config.yaml`（context 段落怎麼寫）
   - `openspec/specs/auth-ui/spec.md`（現況基準線 spec 的寫法）
   - `openspec/changes/auth-ui-foundation/`（回溯性 foundation change 的寫法）
   - `openspec/changes/simplify-auth-ui/`（優化 change：proposal/design/delta spec/tasks 的寫法）
   - `mistake.md`（稽核記錄的格式，含把握度標示、稽核結論表）
   - `CLAUDE.md`（優化後現況文件應該長什麼樣）

## 從 persona-nexus-auth 學到、下一輪要留意的重點

- **不要只讀碼，要實跑驗證**：Phase 1 曾發現 `persona-nexus-auth/CLAUDE.md` 有兩處嚴重過時
  的錯誤宣稱（「登入沒存 token」「同源部署是未來規劃」），都是因為沒有實際讀 `main.js`／
  `vite.config.js` 就照抄舊文件。Phase 5 稽核時也曾對 Bundle Hygiene 做出錯誤的直覺判斷，
  後來用 `npm run build` 實測才訂正。下一個服務一樣要「該跑測試就跑，該建置就建置」，不能
  只憑讀碼推測。
- **jsdom 測試用 `eval()` 載入原始碼字串的寫法很脆弱**：`persona-nexus-auth` 的
  `main.test.js` 原本用 `eval(fs.readFileSync(...))` 載入 `main.js`，一旦原始碼裡出現
  `import` 陳述式或 top-level `await` 就會整個壞掉。如果其他前端的測試檔也用同樣手法，
  Phase 1 讀碼時要特別檢查這個地雷,並在 Phase 5 老實記錄成"功能性缺陷"而非"涵蓋不足"。
- **DRY／SSOT 類的違反常常是跨服務的**：`config-loader.js` 的重複與新舊模式並存問題橫跨
  三個前端（auth／character／lobby）。單一服務的稽核只能處理「這個服務要不要跟上已驗證的
  新模式」，沒辦法單靠改一個 repo 就消除跨 repo 的重複——這類發現要老實記錄成「已知限制、
  留待該服務自己的稽核輪次」，不要假裝一次就能連帶修好其他 repo。
- **使用者的稽核核對可能會反覆確認**：這輪 `persona-nexus-auth` 的 Phase 5 檢查點，使用者
  一開始像是要排除某一項跨服務項目，後來澄清其實是全部都要處理、只是先提出疑問。逐項核對
  時如果使用者的意圖有一點模糊，直接問清楚範圍界線（尤其是「要不要連動改另一個 repo」這種
  會擴大改動範圍的決定），不要自己猜。
- **Glob/find 類工具回報「找不到檔案」不代表目錄真的是空的**（`persona-nexus-lobby` 二次驗證
  時發現）：Glob 完全漏掉了 `public/src/` 目錄下的 5 個 HTML 樣板檔案，直到用 PowerShell/Bash
  直接列目錄才發現。這導致首輪 Phase 1 讀碼時，稽核所依據的「現況」其實是不完整的（雖然最後
  結論仍然成立）。**下一個服務**：Phase 1 讀完後，最好用一次 shell 的遞迴列目錄（排除
  node_modules/.git/dist）跟 Glob 找到的結果對一下，確認沒有工具盲區漏掉的檔案，尤其是
  Vite 專案常見的 `public/` 目錄。
- **合併重複邏輯時，要先確認「重複」是否其實是「本來就不一致」**：`persona-nexus-lobby` 的
  `home.js` 原本的 `showMessage()` 有 3 秒自動隱藏，`my-character.js` 的沒有——這種情況下把
  兩份重複程式碼合併成一個共用函式時，不能隨便選一種行為當作正確答案套用到全部呼叫端，
  否則會在「單純重構」的包裝下夾帶未聲明的行為變更。做法是讓共用函式支援選填參數，
  由各呼叫端自己決定要不要用，保留原本的差異。
- **稽核完成後，Phase 6 撰寫範例時也可能無意間虛構出程式碼裡不存在的行為**：這輪曾在
  delta spec 舉「刪除對話成功後顯示 success 訊息」當範例，但 grep 後發現程式碼裡從未用過
  `success` 類型、`style.css` 也沒有對應 CSS class——這其實是新增行為而非重構。寫 Phase 6
  文件時舉的每個例子最好都能對應到「這行為現在就存在」或「這是這次明確要新增的行為」兩者之一，
  不要為了讓文件讀起來完整而順手掰出沒有依據的例子。

## ⚠️ 完成一個服務優化後必須回來更新

**每當完成一個前端服務的優化輪次：**

1. **回到本檔（`前情提要-前端專案優化.md`）**
   - 把已完成的服務從「接下來要做什麼」移到「已經做完的事」
   - 記錄該輪的重點改動與狀態（Phase 完成到第幾號、是否還有待辦）
2. **同時更新《前端專案優化進度.md》**
   - 在「已完成」段落新增該服務的詳細摘要
   - 更新「尚未開始的候選服務」列表（移除已做、補充新發現）

**這很重要，因為：**
- 下一個接續者需要快速掌握進度，不需要從頭問
- 跨服務的設計一致性（DRY、SSOT、架構方向）需要被追蹤
- 避免重複發現同樣的問題（例如多個服務都有舊版 config-loader）

---

## 給下一個聊天室的開場指令範例

四個前端服務都已完成 Phase 0–10，接下來是收尾性質的工作，可依情境選用：

> 「請閱讀『前情提要-前端專案優化.md』和『前端專案優化進度.md』，
> 陪我對 <某個服務> 做手動瀏覽器整合測試（Phase 9 剩餘部分）。」

或

> 「請閱讀『前情提要-前端專案優化.md』和『前端專案優化進度.md』，
> 幫我對 <某個服務> 執行 Phase 11（commit → archive → 清備份）。」

若之後又有新的前端服務加入平台，一樣可以用：

> 「請閱讀『前情提要-前端專案優化.md』和『前端專案優化進度.md』，
> 對 <新服務> 套用同一套 SOP。優化完成後記得回來更新這兩份前情提要檔。」
